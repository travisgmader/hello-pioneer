/**
 * Auth screen stub — Sign In / Sign Up.
 * Implemented in 01-auth-PLAN.md (Wave 3).
 *
 * This stub satisfies the Expo Router file-based routing requirement.
 * The full AuthScreen component with email/password, Google OAuth, and Apple
 * Sign-In is built in the auth plan.
 */

import { View, Text } from 'react-native';

export default function AuthScreen() {
  return (
    <View style={{ flex: 1, backgroundColor: '#0A0A0B', alignItems: 'center', justifyContent: 'center' }}>
      <Text style={{ color: '#E5E2E1', fontSize: 16 }}>Sign In</Text>
    </View>
  );
}
