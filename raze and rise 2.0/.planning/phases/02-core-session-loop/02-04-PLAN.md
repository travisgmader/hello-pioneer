---
phase: 02-core-session-loop
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - app/(session)/_layout.tsx
  - app/(session)/index.tsx
  - app/(tabs)/workouts.tsx
  - src/stores/sessionStore.ts
  - src/hooks/useSessionPersistence.ts
  - src/hooks/useSessionData.ts
  - src/services/sessionService.ts
  - src/components/SessionHeader/index.tsx
  - src/components/SetRow/index.tsx
  - src/components/ExerciseCard/index.tsx
  - src/components/AddSetButton/index.tsx
  - tests/unit/sessionPersistence.test.ts
autonomous: true
requirements:
  - WORKOUT-01
  - WORKOUT-02
  - WORKOUT-18
  - DESIGN-02

must_haves:
  truths:
    - "User can tap Start in the Workouts tab and land on a full-screen session route with tab bar hidden (WORKOUT-01, D-01)"
    - "Session is loaded from PowerSync: today's day = split_settings.rotation_pointer → templates → template_exercises (WORKOUT-01)"
    - "Session UUID is generated client-side BEFORE first write and stored in MMKV under active_session_id (DATA-02, WORKOUT-18)"
    - "ExerciseCard list renders via @shopify/flash-list (FlashList v2, mandatory per STATE.md) — one card per exercise"
    - "Each SetRow shows weight input pre-filled from prev session, Go/No-Go buttons, prev performance link with dots, expand chevron stub (WORKOUT-02)"
    - "Tapping Go/No-Go writes the row to PowerSync session_sets immediately via execute() — no batching (RESEARCH.md anti-pattern)"
    - "Tapping Go on a working set calls useRestTimer.start(resolveRestSeconds(...)) and the RestTimerPill appears (D-04)"
    - "Tapping the previous performance link auto-fills the weight input (D-09)"
    - "Session screen activates expo-keep-awake on mount and deactivates on unmount (RESEARCH.md Pattern 6)"
    - "SetRow/ExerciseCard expanded/focused state lives in Zustand keyed by set UUID — NOT in local useState (RESEARCH.md Pitfall 1: FlashList recycling)"
    - "MMKV active_session_id survives app kill and re-mount restores the session (WORKOUT-18)"
    - "tests/unit/sessionPersistence.test.ts turns from RED to GREEN"
  artifacts:
    - path: "app/(session)/_layout.tsx"
      provides: "Full-screen session stack (gestureEnabled: false, headerShown: false)"
    - path: "app/(session)/index.tsx"
      provides: "SessionScreen route"
    - path: "src/stores/sessionStore.ts"
      provides: "Zustand active-session store"
      exports: ["useSessionStore"]
    - path: "src/hooks/useSessionPersistence.ts"
      provides: "MMKV session metadata hook"
      exports: ["useSessionPersistence", "SESSION_KEYS"]
    - path: "src/hooks/useSessionData.ts"
      provides: "PowerSync read for today's template + previous performance"
      exports: ["useSessionData", "usePreviousPerformance"]
    - path: "src/services/sessionService.ts"
      provides: "PowerSync write helpers"
      exports: ["startSession", "commitSet"]
    - path: "src/components/SessionHeader/index.tsx"
      provides: "Day label + elapsed timer + Complete button"
    - path: "src/components/SetRow/index.tsx"
      provides: "Single set row organism"
    - path: "src/components/ExerciseCard/index.tsx"
      provides: "Exercise card with N SetRows + Add set"
    - path: "src/components/AddSetButton/index.tsx"
      provides: "Ghost button to insert a new set"
  key_links:
    - from: "app/(session)/index.tsx"
      to: "src/hooks/useSessionData.ts"
      via: "loads today's template + exercises via PowerSync usePowerSyncQuery"
      pattern: "useSessionData"
    - from: "src/components/SetRow/index.tsx"
      to: "src/stores/sessionStore.ts"
      via: "reads/writes expanded + focused state keyed by set UUID"
      pattern: "useSessionStore"
    - from: "src/components/SetRow/index.tsx"
      to: "src/services/sessionService.ts"
      via: "commitSet on Go/No-Go tap"
      pattern: "commitSet"
    - from: "src/components/SetRow/index.tsx"
      to: "src/hooks/useRestTimer.ts"
      via: "start(restSeconds) on Go tap if not warmup"
      pattern: "start\\("
    - from: "app/(tabs)/workouts.tsx"
      to: "app/(session)/index.tsx"
      via: "router.push('/(session)/')"
      pattern: "router.push.*session"
---

<objective>
Stand up the active-session screen end-to-end — the central vertical slice that satisfies WORKOUT-01, WORKOUT-02, and WORKOUT-18. Build the full-screen route, the FlashList of ExerciseCards, the SetRow organism composing Plan 02 atoms, the Zustand session store, MMKV persistence (turning the WORKOUT-18 test green), PowerSync read hooks for today's template + previous performance, and the immediate-write PowerSync service for set logging. Integrate the RestTimerPill from Plan 03 so completing a working set auto-starts rest.

Purpose: This is the spine of Phase 2. After this plan ships, a user can start a workout, see today's exercises, log weight + go/no-go on every set, see previous-session data, and the rest timer auto-fires per template overrides. Secondary set actions (RPE/warm-up/notes), supersets, body map, and completion live in subsequent plans that bolt onto this scaffold.
Output: Three route/screen files + four hook/service/store files + four new components + WORKOUT-18 test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-core-session-loop/02-CONTEXT.md
@.planning/phases/02-core-session-loop/02-PATTERNS.md
@.planning/phases/02-core-session-loop/02-UI-SPEC.md
@.planning/phases/02-core-session-loop/02-RESEARCH.md
@app/(onboarding)/_layout.tsx
@app/(onboarding)/practice-set.tsx
@app/(tabs)/workouts.tsx
@src/lib/powersync.ts
@src/lib/storage.ts
@src/hooks/useOnboardingState.ts
@src/hooks/useSetResult.ts
@src/hooks/useRestTimer.ts
@src/components/SetResultButton/index.tsx
@src/components/WeightInput/index.tsx
@src/components/PrevPerformanceLink/index.tsx
@src/components/LeftEdgeBar/index.tsx
@src/components/NumericText/index.tsx
@src/components/RestTimerPill/index.tsx
@src/components/Button/index.tsx
@src/components/IconButton/index.tsx
@tests/unit/sessionPersistence.test.ts

<interfaces>
From src/hooks/useSetResult.ts (Plan 02):
- `useSetResult(initial?: SetResult): { result, handleGo, handleNoGo, reset }`

From src/hooks/useRestTimer.ts (Plan 03):
- `useRestTimer(): { remaining, start, cancel, addSeconds }`
- `resolveRestSeconds(templateOverride: number | null | undefined, globalDefault: number | null | undefined): number`

From src/lib/powersync.ts:
- `getPowerSync(): PowerSyncDatabase`

From src/lib/storage.ts:
- `createMMKV({ id })` factory (use unencrypted instance for session metadata — separate from the encrypted Supabase session store)

From PowerSync schema (Phase 1 — confirmed via STATE.md "9-table schema"):
- `sessions(id, user_id, template_id, day_label, started_at, completed_at, notes, is_deleted)`
- `session_sets(id, session_id, exercise_id, exercise_name, set_number, weight_kg, reps_target, result, rpe, is_warmup, notes, logged_at)`
- `templates(id, user_id, day_label, name, ...)`
- `template_exercises(id, template_id, exercise_id, exercise_name, set_count, rep_low, rep_high, exercise_type, default_rest_seconds, superset_group)`
- `split_settings(user_id, rotation_pointer, global_rest_seconds, ...)`

From app/(onboarding)/_layout.tsx (full-screen Stack pattern):
- Stack `screenOptions={{ headerShown: false, gestureEnabled: false }}` (NOTE: gestureEnabled false is required for session per UI-SPEC.md Back/Cancel)

From app/(onboarding)/practice-set.tsx (lines 14–23 imports, line 65 router.replace pattern; lines 62–70 non-blocking try/catch)

From RESEARCH.md Pattern 6 (keep-awake lifecycle):
- `activateKeepAwakeAsync('session-active')` on mount; `deactivateKeepAwake('session-active')` on unmount

From RESEARCH.md Pattern 3 (PowerSync execute pattern for session_sets INSERT OR REPLACE)

From RESEARCH.md Code Examples — "Previous Session Performance Query":
- `usePowerSyncQuery(\`SELECT ss.set_number, ss.weight_kg, ss.result FROM session_sets ss JOIN sessions s ON s.id = ss.session_id WHERE ss.exercise_id = ? AND ss.session_id != ? AND s.completed_at IS NOT NULL ORDER BY s.completed_at DESC LIMIT 10\`, [exerciseId, currentSessionId])`

From UI-SPEC.md:
- SessionScreen layout, ExerciseCard active variant (accent-dim bg + 2px LeftEdgeBar)
- SetRow 56pt height, layout per "Set Logging Flow" interaction pattern
- Tab bar hidden during session per CONTEXT.md D-01 — implemented via the (session) route group sitting OUTSIDE the (tabs) group
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zustand session store + MMKV persistence hook (WORKOUT-18)</name>
  <read_first>
    - src/hooks/useOnboardingState.ts (MMKV read/write pattern for useMMKVString and getStorage)
    - src/lib/storage.ts (createMMKV factory, .remove() not .delete())
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (useSessionPersistence.ts section — exact hook shape)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Runtime State Inventory — MMKV keys)
    - tests/unit/sessionPersistence.test.ts (Wave 0 stub)
  </read_first>
  <behavior>
    - Test: `saveSession(uuid)` writes `active_session_id` (the uuid) and `active_session_started_at` (a valid ISO string) to a `createMMKV({ id: 'active-session' })` instance
    - Test: `clearSession()` calls `.remove('active_session_id')` AND `.remove('active_session_started_at')` (assert `.remove` NOT `.delete` — STATE.md note + storage.ts line 69 + PATTERNS.md line 274)
    - Test: `useSessionPersistence()` returns `{ sessionId, startedAt }` reflecting current MMKV values via useMMKVString
    - Test: Zustand store `useSessionStore` exposes `expandedSetId: string | null`, `focusedSetId: string | null`, `setExpanded(id)`, `setFocused(id)`, and an `exercises: ExerciseState[]` slice with `setSetResult(setId, result)`, `setSetWeight(setId, weightKg)`, `setSetRpe(setId, rpe)`, `setSetWarmup(setId, isWarmup)`
    - Test: setting `expandedSetId` to a new id automatically collapses the previously expanded set (only one expanded per ExerciseCard rule from UI-SPEC.md)
  </behavior>
  <files>src/hooks/useSessionPersistence.ts, src/stores/sessionStore.ts, tests/unit/sessionPersistence.test.ts</files>
  <action>
    1. Convert `it.todo` in tests/unit/sessionPersistence.test.ts to real assertions per `<behavior>`. Mock `react-native-mmkv` with an in-memory Map-backed `createMMKV` factory. For the useMMKVString hook, the test can call the hook indirectly via the imperative API or use `renderHook`.

    2. Create `src/hooks/useSessionPersistence.ts` matching PATTERNS.md exactly. Module-scope `const SESSION_MMKV = createMMKV({ id: 'active-session' })`. Export:
       - `const SESSION_KEYS = { id: 'active_session_id', startedAt: 'active_session_started_at' } as const`
       - `useSessionPersistence()` hook returning `{ sessionId, startedAt, saveSession(id), clearSession() }` (saveSession writes both keys; clearSession `.remove`s both — confirmed via storage.ts line 69 that v4 MMKV uses `.remove()`).

    3. Create `src/stores/sessionStore.ts` using `zustand`. State shape:
       ```
       interface ExerciseState {
         id: string;                              // template_exercises.id
         exerciseId: string;                      // master exercise id
         exerciseName: string;
         setCount: number;
         repLow: number;
         repHigh: number;
         exerciseType: 'standard' | 'bodyweight' | 'run';
         defaultRestSeconds: number | null;
         supersetGroup: number | null;
         sets: SetState[];
       }
       interface SetState {
         id: string;             // generated UUID, client-side
         setNumber: number;
         weightKg: number | null;
         result: SetResult;
         rpe: number | null;
         isWarmup: boolean;
         notes: string | null;
       }
       ```
       Actions:
       - `loadExercises(exercises: ExerciseState[])` — replace the array (called when session loads)
       - `setSetResult(setId, result)` — find and mutate via immer or spread
       - `setSetWeight(setId, weightKg)` — same
       - `setSetRpe(setId, rpe)`, `setSetWarmup(setId, isWarmup)`, `setSetNotes(setId, notes)` — same
       - `setExpanded(setId | null)` — store at root level
       - `setFocused(setId | null)` — store at root level
       - `addSet(exerciseId)` — push new SetState with generated UUID, weight pre-filled from last set
       - `swapExercise(slotIndex, newExercise)` — Plan 08 will wire; export the action stub now

       Reasoning: per RESEARCH.md Pitfall 1, FlashList recycling makes per-row useState unsafe. All cross-render row state lives here, keyed by set UUID.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/sessionPersistence.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useSessionPersistence.ts` exists, exports `useSessionPersistence` + `SESSION_KEYS`
    - `useSessionPersistence` uses `createMMKV({ id: 'active-session' })` and calls `.remove(key)` (NOT `.delete(key)`) in clearSession
    - `src/stores/sessionStore.ts` exists, exports `useSessionStore` via `create<...>(...)`
    - Store has actions: `loadExercises`, `setSetResult`, `setSetWeight`, `setSetRpe`, `setSetWarmup`, `setSetNotes`, `setExpanded`, `setFocused`, `addSet`
    - `tests/unit/sessionPersistence.test.ts` has zero `it.todo` calls and all `it(...)` blocks pass
    - `npm run test:unit -- --run tests/unit/sessionPersistence.test.ts` exits 0
  </acceptance_criteria>
  <done>Zustand store + MMKV persistence ready; WORKOUT-18 unit test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: PowerSync read hooks + write service</name>
  <read_first>
    - src/lib/powersync.ts (getPowerSync singleton)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (sessionService.ts section)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Pattern 3 — single set write; Code Examples — Previous Session Performance Query; Pitfall 4 — do NOT check rowsAffected)
    - .planning/phases/02-core-session-loop/02-CONTEXT.md (Integration Points — Session route receives today's template from PowerSync; Rotation pointer advance must be idempotent)
  </read_first>
  <files>src/hooks/useSessionData.ts, src/services/sessionService.ts</files>
  <action>
    1. `src/hooks/useSessionData.ts` exports:
       - `useTodaysTemplate(userId: string)` — uses PowerSync hook (`usePowerSyncQuery` from `@powersync/react-native`) to: read `split_settings` row for user → derive today's day_label from `rotation_pointer` → query `templates` for that day_label → query `template_exercises` for that template ordered by display_order (or id). Returns `{ template, exercises, loading, error }`. If no template exists for today, returns `template = null` (Plan 08 wires the skip day flow per WORKOUT-16).
       - `usePreviousPerformance(exerciseId: string, currentSessionId: string | null)` — exact query from RESEARCH.md Code Examples. Returns array of `{ set_number, weight_kg, result }` ordered by recency, LIMIT 10. If `currentSessionId` is null (session not yet started), returns the most recent COMPLETED session for that exercise.
       - The `joins.user_id` data path goes through `sessions` table — match the existing schema. If a different query shape is needed (e.g., the schema names differ from RESEARCH.md), prefer the schema as ground truth — read `src/lib/schema.ts` (Phase 1 schema definition) and adjust.

    2. `src/services/sessionService.ts` exports:
       - `async function startSession(opts: { userId, templateId, dayLabel, exercises: ExerciseState[] }): Promise<{ sessionId: string, startedAt: string }>` — generates client-side UUID (use `expo-crypto`'s `randomUUID()` from Phase 1 imports, OR `crypto.randomUUID` if available), writes session metadata to MMKV via `useSessionPersistence` callable (this function takes MMKV writes inline — does NOT use the hook since it's not a hook). The actual sessions table row is NOT inserted yet — Plan 07's completeSession writes it. For Phase 2 working-set persistence (WORKOUT-18), only session_sets rows are written incrementally, and the sessions row is written at completion. (Rationale: session is "in-progress" until commit; the MMKV `active_session_id` + presence of session_sets rows IS the in-progress state.)
       - `async function commitSet(opts: { sessionId, exerciseId, exerciseName, setNumber, weightKg, repsTarget, result, rpe, isWarmup, notes }): Promise<void>` — `INSERT OR REPLACE INTO session_sets ...` exactly as RESEARCH.md Pattern 3. Generate a deterministic set UUID via SHA-1(sessionId|exerciseId|setNumber) if not passed; OR accept a `setId` arg. Use the explicit `setId` arg — simpler and matches the Zustand store's set UUID generation. Update signature: add `setId` as first arg.
       - Wrap in try/catch, log via `console.warn` on failure, do NOT throw (PowerSync local writes shouldn't fail in normal operation; if they do, the local SQLite is hosed and the user will see the issue elsewhere).
       - Do NOT check `rowsAffected` — use `INSERT OR REPLACE` for idempotency (Pitfall 4).
  </action>
  <verify>
    <automated>test -f src/hooks/useSessionData.ts &amp;&amp; test -f src/services/sessionService.ts &amp;&amp; grep -q "usePowerSyncQuery\|getPowerSync" src/hooks/useSessionData.ts &amp;&amp; grep -q "INSERT OR REPLACE INTO session_sets" src/services/sessionService.ts &amp;&amp; grep -q "startSession\|commitSet" src/services/sessionService.ts &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useSessionData.ts` exports `useTodaysTemplate(userId)` and `usePreviousPerformance(exerciseId, currentSessionId)`
    - Both hooks use PowerSync local SQLite (via `usePowerSyncQuery` or `getPowerSync().execute(...)` wrapped in a useEffect+state)
    - `src/services/sessionService.ts` exports `startSession` and `commitSet`
    - `commitSet` uses `INSERT OR REPLACE INTO session_sets` with all 12 columns from RESEARCH.md Pattern 3
    - Service does NOT check `rowsAffected` (Pitfall 4)
    - `startSession` generates client-side UUID and writes to MMKV via the storage primitives (not the hook — service layer cannot call hooks)
  </acceptance_criteria>
  <done>Read + write layer for the session is in place.</done>
</task>

<task type="auto">
  <name>Task 3: Build SessionHeader, AddSetButton, SetRow, ExerciseCard organisms</name>
  <read_first>
    - src/components/OnboardingStepLayout/index.tsx (header region pattern at lines 72–79)
    - src/components/PracticeSetCard/index.tsx (card shell at line 36)
    - src/components/Button/index.tsx (primary + ghost variants)
    - src/components/SetResultButton/index.tsx (Plan 02)
    - src/components/WeightInput/index.tsx (Plan 02)
    - src/components/PrevPerformanceLink/index.tsx (Plan 02)
    - src/components/LeftEdgeBar/index.tsx (Plan 02)
    - src/components/NumericText/index.tsx (Plan 02)
    - src/hooks/useSetResult.ts (Plan 02)
    - src/stores/sessionStore.ts (Task 1)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Component Inventory; Screen Layouts — Session Screen + SetRow Expanded; Copywriting Contract)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (SetRow, ExerciseCard, SessionHeader sections)
  </read_first>
  <files>
    src/components/SessionHeader/index.tsx,
    src/components/AddSetButton/index.tsx,
    src/components/SetRow/index.tsx,
    src/components/ExerciseCard/index.tsx
  </files>
  <action>
    **SessionHeader** (`src/components/SessionHeader/index.tsx`):
    - Props: `{ dayLabel: string, startedAt: string, onComplete: () => void, onOpenNote?: () => void, onOpenBodyMap?: () => void }`
    - Layout: View `px-md pt-sm flex-row items-center justify-between` (h-14)
    - Day label: Text className `text-heading text-fg` (NOTE: Heading style is 24px Noto Serif 700 per UI-SPEC.md — verify Phase 1 tailwind has `text-heading`; if not, inline style with `fontFamily: 'NotoSerif_700Bold', fontSize: 24, lineHeight: 30`)
    - Elapsed timer: NumericText className `text-fg` (size 20px tabular — use inline `style={{ fontSize: 20, lineHeight: 24, fontWeight: '700' }}` if no `text-numeric-20` token exists; verify against tailwind.config.js — Plan 02 only added `numeric` (16) and `numeric-large` (28))
    - **Elapsed timer implementation**: useState tick every 1000ms. Per UI-SPEC.md, SessionHeader is OUTSIDE the FlashList so React state ticks here are safe (do NOT lift this into the parent screen — that would re-render the list). Format `M:SS` for <1h, `H:MM:SS` for >=1h.
    - Complete button: `<Button label="Complete" variant="primary" onPress={onComplete} />` with inline style overriding height to 36pt and minWidth 88pt (UI-SPEC.md exception)
    - All Text uses `allowFontScaling={false}`

    **AddSetButton** (`src/components/AddSetButton/index.tsx`):
    - Props: `{ onPress: () => void }`
    - Renders ghost variant pressable with Plus 16px icon + "Add set" label
    - 44pt height, full-width inside card padding
    - Haptic: `Haptics.selectionAsync()` on press (UI-SPEC.md Haptic Feedback table)
    - className: `flex-row items-center justify-center h-11 rounded-md active:opacity-80` with `<Plus size={16} color="#99907C" />` + Text `text-body text-fg-muted`

    **SetRow** (`src/components/SetRow/index.tsx`):
    - Props:
      ```
      interface SetRowProps {
        setId: string;
        setNumber: number;
        exerciseId: string;
        exerciseName: string;
        sessionId: string;
        repsTarget: string;       // "8-10"
        unit: 'lbs' | 'kg';
        defaultRestSeconds: number | null;
        globalRestSeconds: number;
        previousPerformance: { weightKg: number | null; results: ('go' | 'no-go' | null)[] };
        onExpand: () => void;     // Plan 05 wires the chevron behavior
      }
      ```
    - Read from sessionStore: `weightKg`, `result`, `isWarmup`, `focusedSetId === setId`, `expandedSetId === setId`
    - Write to sessionStore: `setSetWeight`, `setSetResult` (called from useSetResult handlers via wrapper)
    - Composition:
      - 56pt min-height View `flex-row items-center gap-sm`
      - LeftEdgeBar variant=`result === 'go' ? 'accent' : result === 'no-go' ? 'danger' : isWarmup ? 'subtle' : 'none'`
      - Caption "Set N" via Text `text-caption text-fg-muted`
      - WeightInput bound to `weightKg ?? ''` (string conversion), onChangeText writes to store; error prop set when parseFloat fails or value out of [0, 999.9]
      - SetResultButton variant="go" selected={result==='go'} onPress={async () => { useSetResult-like handler that toggles via sessionStore.setSetResult; on transition to 'go' AND !isWarmup, call commitSet() AND useRestTimer.start(resolveRestSeconds(defaultRestSeconds, globalRestSeconds)); on transition to 'no-go' just commitSet }}
      - SetResultButton variant="no-go" similar
      - ExpandChevron (use lucide-react-native ChevronDown/Up 20px, 44×44pt hit area) — onPress={onExpand} — Plan 05 builds the actual expanded form; for Plan 04 the chevron is a stub that toggles `expandedSetId` in the store but the expanded section is empty
      - Below the row: PrevPerformanceLink with weightKg + results from previousPerformance, onAutoFill={() => setSetWeight(setId, previousPerformance.weightKg ?? 0)}
    - When `expandedSetId === setId`, render a placeholder `<View />` below (Plan 05 fills it)
    - PowerSync write: on Go/No-Go tap, call `commitSet({ setId, sessionId, exerciseId, exerciseName, setNumber, weightKg, repsTarget, result, rpe, isWarmup, notes })`. Use the current values from sessionStore at write time.
    - Rest timer trigger: only when `result === 'go'` AND `!isWarmup` per UI-SPEC.md interaction step 5. Useful: the SetRow consumes `useRestTimer()` directly so it can call `start(restSeconds)`.

    **ExerciseCard** (`src/components/ExerciseCard/index.tsx`):
    - Props: `{ exercise: ExerciseState; sessionId: string; unit: 'lbs' | 'kg'; globalRestSeconds: number; isActive: boolean; onSwap: () => void; onExpandSet: (setId: string) => void; }`
    - Card shell: View `bg-bg-elevated rounded-lg p-md border border-border gap-sm` (when `isActive`: `bg-accent-dim border-border-strong` + 2px accent LeftEdgeBar on card itself)
    - Header row: exercise name (Text `text-body font-bold text-fg` when active, `text-body text-fg` when not) + set count caption ("5 sets" / "1 set" — singular handling) + Shuffle IconButton onPress={onSwap}
    - Render each set as `<SetRow ...>` — plain View children, NOT nested FlashList (Pitfall 6)
    - Bottom: AddSetButton onPress={() => useSessionStore.getState().addSet(exercise.id)}
    - Progressive overload hint stub: render an empty placeholder View — Plan 08 fills it
    - Note: ExerciseCard MUST NOT use local useState for any cross-render state. Use `useRecyclingState` from FlashList v2 only for ephemeral UI flags that should reset on recycle (none in Plan 04 — the only candidate is `isActive` which is computed from props).

    All Text uses `allowFontScaling={false}`. All `text-*` className choices match UI-SPEC.md.
  </action>
  <verify>
    <automated>test -f src/components/SessionHeader/index.tsx &amp;&amp; test -f src/components/AddSetButton/index.tsx &amp;&amp; test -f src/components/SetRow/index.tsx &amp;&amp; test -f src/components/ExerciseCard/index.tsx &amp;&amp; grep -q "useSessionStore" src/components/SetRow/index.tsx &amp;&amp; grep -q "commitSet" src/components/SetRow/index.tsx &amp;&amp; grep -q "useRestTimer\|start(" src/components/SetRow/index.tsx &amp;&amp; grep -q "bg-bg-elevated rounded-lg" src/components/ExerciseCard/index.tsx &amp;&amp; grep -q "Shuffle" src/components/ExerciseCard/index.tsx &amp;&amp; grep -q "allowFontScaling={false}" src/components/SessionHeader/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - All four component files exist and export named components
    - SessionHeader has elapsed timer ticking via local useState (NOT lifted) and 36pt-height Complete button
    - SetRow consumes Plan 02 atoms (SetResultButton, WeightInput, PrevPerformanceLink, LeftEdgeBar, NumericText)
    - SetRow reads/writes state ONLY via useSessionStore — zero local useState for cross-render data
    - SetRow calls `commitSet(...)` on Go/No-Go tap
    - SetRow calls `useRestTimer().start(resolveRestSeconds(...))` only when result transitions to 'go' AND !isWarmup
    - ExerciseCard uses `bg-bg-elevated rounded-lg p-md border border-border gap-sm` shell with active-variant override
    - ExerciseCard renders SetRows as plain View children (no nested FlashList)
    - ExerciseCard header includes Shuffle IconButton from lucide-react-native (20px)
    - AddSetButton uses ghost Pressable + Plus 16px icon + "Add set" label + `Haptics.selectionAsync()`
    - All Text elements use `allowFontScaling={false}`
  </acceptance_criteria>
  <done>All four core session components in place, ready to be assembled in the screen.</done>
</task>

<task type="auto">
  <name>Task 4: Build session route + workouts tab entry point</name>
  <read_first>
    - app/(onboarding)/_layout.tsx (Stack layout pattern)
    - app/(onboarding)/practice-set.tsx (full-screen route with safe area)
    - app/(tabs)/workouts.tsx (current placeholder)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Session Screen layout, Back / Cancel Navigation Rules)
    - .planning/phases/02-core-session-loop/02-CONTEXT.md (D-01 — tab bar hidden)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Pattern 6 keep-awake, Pattern 1 FlashList v2)
  </read_first>
  <files>app/(session)/_layout.tsx, app/(session)/index.tsx, app/(tabs)/workouts.tsx</files>
  <action>
    1. **app/(session)/_layout.tsx**:
       ```
       export default function SessionLayout() {
         return (
           <Stack screenOptions={{ headerShown: false, gestureEnabled: false, animation: 'fade' }}>
             <Stack.Screen name="index" />
           </Stack>
         );
       }
       ```
       `gestureEnabled: false` is mandatory per UI-SPEC.md Back/Cancel: "iOS swipe-back gesture is DISABLED". Android hardware back handler is Plan 07's concern.

    2. **app/(session)/index.tsx** — SessionScreen:
       Imports: useEffect, useRef, View, AppState, SafeAreaView (from react-native-safe-area-context), FlashList from `@shopify/flash-list`, FlashListRef type, activateKeepAwakeAsync/deactivateKeepAwake from expo-keep-awake, router from expo-router, useSession, useSessionStore, useSessionPersistence, useTodaysTemplate, useRestTimer, startSession from sessionService, SessionHeader, ExerciseCard, RestTimerPill, initAudioMode from src/lib/audio.

       Lifecycle:
       - On mount: `activateKeepAwakeAsync('session-active')` + `initAudioMode()` (await fire-and-forget)
       - On unmount: `deactivateKeepAwake('session-active')`
       - On mount, read MMKV `active_session_id` via useSessionPersistence:
         - If exists: rehydrate (call useTodaysTemplate, load exercises into store, keep the existing sessionId)
         - Else: call `startSession({ userId, templateId, dayLabel, exercises })` to generate UUID + save to MMKV + loadExercises into store
       - If `useTodaysTemplate` returns `template === null`, route back to /(tabs)/workouts (Plan 08 handles the skip-day flow; Plan 04 just renders nothing and bounces back)

       Render:
       ```
       <SafeAreaView className="flex-1 bg-bg">
         <SessionHeader dayLabel={dayLabel} startedAt={startedAt} onComplete={handleComplete} />
         <FlashList
           ref={listRef}
           data={exercises}
           keyExtractor={(item) => item.id}
           renderItem={({ item, index }) => (
             <ExerciseCard
               exercise={item}
               sessionId={sessionId}
               unit={unit}
               globalRestSeconds={globalRestSeconds}
               isActive={index === currentExerciseIndex}
               onSwap={() => {/* Plan 08 */}}
               onExpandSet={(setId) => useSessionStore.getState().setExpanded(setId)}
             />
           )}
           ItemSeparatorComponent={() => <View className="h-md" />}
           contentContainerStyle={{ paddingHorizontal: 16, paddingTop: 24 }}
         />
         <RestTimerPill
           remaining={remaining}
           totalSeconds={lastStartedRestSeconds}
           onSkip={cancel}
           onAddSeconds={addSeconds}
         />
       </SafeAreaView>
       ```

       Where `currentExerciseIndex` is computed as: index of the first ExerciseState whose `sets.some(s => s.result === null)` is true. Provide that to ExerciseCard via the `isActive` prop.

       `handleComplete` for Plan 04 is a placeholder that routes back to /(tabs)/ — Plan 07 replaces this with the Anubis flow.

       FlashList v2 specifics (RESEARCH.md Pattern 1):
       - Do NOT set `estimatedItemSize` (v2 auto-sizes)
       - Do NOT use CellContainer
       - Use `FlashListRef<ExerciseState>` type for the ref

    3. **app/(tabs)/workouts.tsx** — entry point:
       - Replace placeholder content with a single primary Button "Start workout" that calls `router.push('/(session)/')`. Compose with the existing layout (don't strip greeting copy if present).
       - For Plan 04 we assume a template exists for today; Plan 08 wires the skip/no-template UX.
  </action>
  <verify>
    <automated>test -f "app/(session)/_layout.tsx" &amp;&amp; test -f "app/(session)/index.tsx" &amp;&amp; grep -q "headerShown: false" "app/(session)/_layout.tsx" &amp;&amp; grep -q "gestureEnabled: false" "app/(session)/_layout.tsx" &amp;&amp; grep -q "activateKeepAwakeAsync" "app/(session)/index.tsx" &amp;&amp; grep -q "FlashList" "app/(session)/index.tsx" &amp;&amp; grep -q "from '@shopify/flash-list'" "app/(session)/index.tsx" &amp;&amp; grep -q "RestTimerPill" "app/(session)/index.tsx" &amp;&amp; grep -q "router.push.*session" "app/(tabs)/workouts.tsx" &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `app/(session)/_layout.tsx` exists with `headerShown: false` AND `gestureEnabled: false`
    - `app/(session)/index.tsx` exists as the SessionScreen route
    - SessionScreen imports `FlashList` from `'@shopify/flash-list'` (NOT FlatList)
    - SessionScreen calls `activateKeepAwakeAsync('session-active')` on mount and `deactivateKeepAwake('session-active')` on unmount
    - SessionScreen consumes useSessionData (useTodaysTemplate + usePreviousPerformance via SetRow downstream)
    - SessionScreen renders SessionHeader + FlashList + RestTimerPill in that order
    - SessionScreen does NOT set `estimatedItemSize` on FlashList (v2 auto-sizes — Pattern 1)
    - `app/(tabs)/workouts.tsx` invokes `router.push('/(session)/')` on the Start button press
    - Session route is a sibling of (tabs), not a child — so tab bar is hidden during the session
  </acceptance_criteria>
  <done>Session screen scaffold is live; user can start a session and log sets, rest timer auto-fires on Go.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User auth session → session_sets writes | Each row written must be scoped to authenticated user_id (via PowerSync RLS on join through sessions.user_id) |
| User text input (weight) → PowerSync | WeightInput passes string to sessionStore; SetRow parseFloat + range-check before commitSet |
| FlashList recycling → SetRow visual state | Stale state risk mitigated by Zustand-keyed state (not local useState) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | session_sets cross-user read | mitigate | Verified by Phase 1 RLS: `session_id IN (SELECT id FROM sessions WHERE user_id = auth.uid())`. Plan 04 does not introduce new write paths — all writes route through commitSet which uses the active sessionId tied to the current MMKV-stored session. Confirm in Task 2 that `commitSet` accepts sessionId from MMKV (which is derived from the user's authenticated session via Plan 1 useSession), not from props. |
| T-02-03 | Tampering | WeightInput value | mitigate | SetRow performs `parseFloat` + range check `0 <= w <= 999.9` BEFORE commitSet. Out-of-range sets error prop on WeightInput (HelperText "Enter a weight between 0 and 999." per UI-SPEC.md) and DOES NOT call commitSet for the invalid value. Negative or non-numeric (post-paste) values revert to previous on blur. |
| T-02-04 | Tampering | Session UUID generation | mitigate | UUID generated client-side via `expo-crypto.randomUUID()` (Phase 1 import) — cryptographically secure, idempotent INSERT OR REPLACE means duplicate writes are no-ops. Stored in MMKV until completion. |
</threat_model>

<verification>
After all tasks: `npm run test:unit` exits 0; `tests/unit/sessionPersistence.test.ts` has zero `it.todo`. App boots: route resolution works (verified by TypeScript build via Wave 0 lint + the `expo prebuild --no-install` check). Manual smoke: tap Start in Workouts → land on session route with tab bar gone → FlashList renders today's exercises → tap Go on a set → RestTimerPill appears.
</verification>

<success_criteria>
- (session) route group exists outside (tabs), tab bar hidden during workout (D-01)
- FlashList v2 renders ExerciseCard list (mandated per STATE.md FlashList blocker)
- SetRow + ExerciseCard composed from Plan 02 atoms with Zustand-backed state (Pitfall 1 mitigated)
- commitSet writes session_sets row immediately on each Go/No-Go tap
- Rest timer auto-starts on Go for working sets, uses template override or global default (WORKOUT-07 via resolveRestSeconds)
- Previous performance loaded from PowerSync and displayed via PrevPerformanceLink with tap-to-fill (WORKOUT-02, D-09)
- Session metadata persisted to MMKV; re-mount rehydrates session (WORKOUT-18)
- WORKOUT-18 unit test GREEN
- expo-keep-awake activated on mount, deactivated on unmount
- Workouts tab entry point routes to (session)/index
</success_criteria>

<output>
Create `.planning/phases/02-core-session-loop/02-04-SUMMARY.md` when done
</output>
