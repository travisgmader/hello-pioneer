---
phase: 02-core-session-loop
plan: 08
type: execute
wave: 6
depends_on:
  - 02-04
  - 02-05
  - 02-07
files_modified:
  - src/lib/progressiveOverload.ts
  - src/components/ProgressiveOverloadHint/index.tsx
  - src/components/ExerciseSwapModal/index.tsx
  - src/components/SessionNoteSheet/index.tsx
  - src/components/ExerciseCard/index.tsx
  - src/components/SessionHeader/index.tsx
  - src/components/SkipDayButton/index.tsx
  - src/stores/sessionStore.ts
  - src/hooks/useSessionData.ts
  - app/(tabs)/workouts.tsx
  - app/(session)/index.tsx
  - tests/unit/progressiveOverload.test.ts
autonomous: true
requirements:
  - WORKOUT-09
  - WORKOUT-14
  - WORKOUT-15
  - WORKOUT-16

must_haves:
  truths:
    - "Tapping a Shuffle IconButton in an ExerciseCard opens an ExerciseSwapModal listing all exercises in the curated library (WORKOUT-15)"
    - "Swap modal uses FlashList for the exercise list (per STATE.md FlashList mandate); selecting an exercise replaces the current ExerciseCard for THIS session only (does NOT mutate the template)"
    - "Tapping the StickyNote IconButton in SessionHeader opens a SessionNoteSheet bottom sheet; auto-saves on dismiss to sessions.notes (WORKOUT-09)"
    - "ProgressiveOverloadHint renders at the bottom of an ExerciseCard when all working sets in the PREVIOUS session were 'go' at the current weight (WORKOUT-14)"
    - "Hint text: 'Try +2.5–5 lbs next time.' (per UI-SPEC.md exact copy); dismissible per session"
    - "Workouts tab shows a Skip day button if no template exists for today (WORKOUT-16); tapping advances rotation_pointer via a focused writeTransaction (no session row created)"
    - "tests/unit/progressiveOverload.test.ts turns from RED to GREEN"
  artifacts:
    - path: "src/lib/progressiveOverload.ts"
      provides: "shouldShowOverloadHint predicate"
      exports: ["shouldShowOverloadHint"]
    - path: "src/components/ProgressiveOverloadHint/index.tsx"
      provides: "Inline overload suggestion banner"
      exports: ["ProgressiveOverloadHint"]
    - path: "src/components/ExerciseSwapModal/index.tsx"
      provides: "Bottom-sheet exercise picker with FlashList"
      exports: ["ExerciseSwapModal"]
    - path: "src/components/SessionNoteSheet/index.tsx"
      provides: "Bottom-sheet free-text note editor"
      exports: ["SessionNoteSheet"]
    - path: "src/components/SkipDayButton/index.tsx"
      provides: "Workouts-tab CTA for days with no template (WORKOUT-16)"
      exports: ["SkipDayButton"]
  key_links:
    - from: "src/lib/progressiveOverload.ts"
      to: "WORKOUT-14 acceptance test"
      via: "evaluates previous session's working sets vs current weight"
      pattern: "shouldShowOverloadHint"
    - from: "src/components/ExerciseSwapModal/index.tsx"
      to: "src/stores/sessionStore.ts"
      via: "swapExercise(slotIndex, newExercise) mutates exercises slice"
      pattern: "swapExercise"
    - from: "src/components/SessionNoteSheet/index.tsx"
      to: "src/services/sessionService.ts"
      via: "updateSessionNotes upserts sessions.notes (or stages for completeSession)"
      pattern: "updateSessionNotes"
---

<objective>
Add the four remaining secondary capabilities that polish the active session: session-level free-text notes (WORKOUT-09), progressive overload hint (WORKOUT-14), exercise swap modal (WORKOUT-15), and skip day flow when no template exists (WORKOUT-16). Turn the progressive overload test GREEN.

Purpose: These four features are independently small but interact with multiple parts of the screen — note button goes in SessionHeader, overload hint lives at the bottom of ExerciseCard, swap modal is opened from each card, skip day diverges the Workouts tab flow. Grouping them keeps the scope coherent (all are "session-adjacent UX" rather than core logging mechanics).
Output: One library + four new components + Skip day flow + sessionStore.swapExercise action; WORKOUT-14 unit test GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-session-loop/02-CONTEXT.md
@.planning/phases/02-core-session-loop/02-PATTERNS.md
@.planning/phases/02-core-session-loop/02-UI-SPEC.md
@.planning/phases/02-core-session-loop/02-RESEARCH.md
@app/(tabs)/workouts.tsx
@app/(session)/index.tsx
@src/components/ExerciseCard/index.tsx
@src/components/SessionHeader/index.tsx
@src/components/Button/index.tsx
@src/components/TextInput/index.tsx
@src/components/IconButton/index.tsx
@src/lib/powersync.ts
@src/lib/sessionStats.ts
@src/stores/sessionStore.ts
@src/services/sessionService.ts
@tests/unit/progressiveOverload.test.ts

<interfaces>
From src/lib/sessionStats.ts (Plan 05):
- `computeGoRate(sets)`, `computeWorkingSetCount(sets)` — used by overload heuristic

From PowerSync schema (Phase 1):
- `exercises(id, name, primary_muscle, equipment, instructions, ...)` — the curated library Plan 1 seeded with 36 exercises
- `sessions.notes` — free text column
- `split_settings.rotation_pointer` — incremented on completion OR skip day

From UI-SPEC.md:
- ExerciseSwapModal: bottom sheet, FlashList for results, search field (Phase 1 TextInput), 56pt rows. "Swap applies to this session only" footer.
- SessionNoteSheet: bottom sheet, "Session note" heading, multiline TextInput 6 lines visible, "What's worth remembering about today's session?" body copy, "Felt strong on bench, knee twinge on squats..." placeholder, auto-saves on dismiss
- ProgressiveOverloadHint: caption text "Try +2.5–5 lbs next time." with TrendingUp 14px prefix icon; appears at bottom of ExerciseCard
- Skip day copy: "Skip today" button label per UI-SPEC.md Destructive Actions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: progressiveOverload library (WORKOUT-14)</name>
  <read_first>
    - tests/unit/progressiveOverload.test.ts (Wave 0 stub)
    - src/lib/sessionStats.ts (Plan 05 — SetRowForStats interface)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Phase Requirements WORKOUT-14)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Component Inventory ProgressiveOverloadHint)
  </read_first>
  <behavior>
    - Test: returns true when previous session has >= 1 working set at currentWeight AND all working sets at currentWeight are 'go'
    - Test: returns false when previous session has any 'no-go' working set at currentWeight
    - Test: returns false when previous session has no working sets at currentWeight (changed weight)
    - Test: returns false when previousSets is empty array
    - Test: ignores warm-up sets entirely (is_warmup=true sets do not affect the decision)
    - Test: ignores null-result sets (incomplete sets do not affect the decision)
    - Test: floating-point weight tolerance — `currentWeight = 100.0, previousWorking = 99.99` → counts as same weight (use abs diff < 0.05 threshold) — alternative: caller is expected to pass kg-rounded values, so exact equality is acceptable. Use exact equality and document.
  </behavior>
  <files>src/lib/progressiveOverload.ts, tests/unit/progressiveOverload.test.ts</files>
  <action>
    1. Convert `it.todo` in tests/unit/progressiveOverload.test.ts to real assertions per `<behavior>`.

    2. Create `src/lib/progressiveOverload.ts`:
       ```typescript
       import type { SetRowForStats } from './sessionStats';

       export interface SetRowWithWeight extends SetRowForStats {
         weight_kg: number | null;
       }

       export function shouldShowOverloadHint(
         previousSets: SetRowWithWeight[],
         currentWeightKg: number,
       ): boolean {
         const candidates = previousSets.filter(
           s => !s.is_warmup && s.result !== null && s.weight_kg === currentWeightKg,
         );
         if (candidates.length === 0) return false;
         return candidates.every(s => s.result === 'go');
       }
       ```

       Document: caller must round weights consistently (PowerSync stores kg as REAL; we treat values as already-rounded to typical lifting increments — 1.25 kg minimum). Drift from float arithmetic should not exceed lift precision.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/progressiveOverload.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/progressiveOverload.ts` exists, exports `shouldShowOverloadHint`
    - Filters out warm-up and null-result sets BEFORE evaluation
    - Returns false on empty filtered set (no signal)
    - Returns true only when ALL candidates are 'go'
    - `tests/unit/progressiveOverload.test.ts` has zero `it.todo` and all `it(...)` blocks pass
    - `npm run test:unit -- --run tests/unit/progressiveOverload.test.ts` exits 0
  </acceptance_criteria>
  <done>WORKOUT-14 predicate is pure, unit-tested, ready for UI consumption.</done>
</task>

<task type="auto">
  <name>Task 2: ProgressiveOverloadHint component (WORKOUT-14 UI)</name>
  <read_first>
    - src/components/NumericText/index.tsx (Plan 02)
    - src/components/ExerciseCard/index.tsx (Plan 04 — placeholder for hint)
    - src/lib/progressiveOverload.ts (Task 1)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Component Inventory ProgressiveOverloadHint; Copywriting — Exercise Card)
  </read_first>
  <files>src/components/ProgressiveOverloadHint/index.tsx, src/components/ExerciseCard/index.tsx, src/stores/sessionStore.ts</files>
  <action>
    1. **ProgressiveOverloadHint** (`src/components/ProgressiveOverloadHint/index.tsx`):
       - Props: `{ onDismiss: () => void }`
       - Renders: `<Pressable className="flex-row items-center gap-xs" onPress={onDismiss}>` with `<TrendingUp size={14} color="#F2CA50" />` + `<Text className="text-caption text-fg-muted" allowFontScaling={false}>Try </Text><Text className="text-caption text-accent font-bold" allowFontScaling={false}>+2.5–5 lbs</Text><Text className="text-caption text-fg-muted" allowFontScaling={false}> next time.</Text>`
       - accessibilityRole="button", accessibilityLabel="Progressive overload suggestion. Tap to dismiss."
       - Haptics.selectionAsync() on press

    2. **src/stores/sessionStore.ts**: Add `dismissedOverloadHints: Set<string>` (keyed by exerciseId) and action `dismissOverloadHint(exerciseId)`. Use a plain `Record<string, boolean>` instead of Set if Zustand+immer don't handle Set well.

    3. **src/components/ExerciseCard/index.tsx**: at the bottom of the card (after sets + AddSetButton), conditionally render `<ProgressiveOverloadHint onDismiss={() => dismissOverloadHint(exercise.id)} />` when:
       - `dismissedOverloadHints[exercise.id]` is NOT set
       - AND `shouldShowOverloadHint(previousPerformanceSets, currentWeightKg)` returns true
       - The "current weight" is the weight prefilled in the first non-warmup, non-completed set row (i.e., what the user is about to lift)
       - `previousPerformanceSets` come from `usePreviousPerformance(exerciseId, sessionId)` (Plan 04) — must include `weight_kg` and `is_warmup` (extend the SELECT in useSessionData if needed)

    4. Update `src/hooks/useSessionData.ts` `usePreviousPerformance` to SELECT additionally `is_warmup` so the hint can filter (the existing query already selects `weight_kg` and `result`; just add `is_warmup`).
  </action>
  <verify>
    <automated>test -f src/components/ProgressiveOverloadHint/index.tsx &amp;&amp; grep -q "TrendingUp" src/components/ProgressiveOverloadHint/index.tsx &amp;&amp; grep -q "Try " src/components/ProgressiveOverloadHint/index.tsx &amp;&amp; grep -q "+2.5–5 lbs" src/components/ProgressiveOverloadHint/index.tsx &amp;&amp; grep -q "next time." src/components/ProgressiveOverloadHint/index.tsx &amp;&amp; grep -q "ProgressiveOverloadHint\|shouldShowOverloadHint" src/components/ExerciseCard/index.tsx &amp;&amp; grep -q "is_warmup" src/hooks/useSessionData.ts &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - ProgressiveOverloadHint renders TrendingUp 14px icon + "Try +2.5–5 lbs next time." (exact UI-SPEC.md copy with the em-dash and period)
    - Hint is dismissible per session (sessionStore.dismissedOverloadHints tracks dismissed exerciseIds)
    - ExerciseCard renders the hint conditionally based on `shouldShowOverloadHint(...)` AND not-yet-dismissed
    - usePreviousPerformance SELECT includes `is_warmup` column
    - All Text uses `allowFontScaling={false}`
    - Haptics.selectionAsync() fires on dismiss
  </acceptance_criteria>
  <done>WORKOUT-14 UI shows the overload hint when conditions are met.</done>
</task>

<task type="auto">
  <name>Task 3: ExerciseSwapModal (WORKOUT-15)</name>
  <read_first>
    - src/components/TextInput/index.tsx (Phase 1 — for search field)
    - src/components/Button/index.tsx (ghost variant for Cancel)
    - src/lib/powersync.ts (read exercises table)
    - src/stores/sessionStore.ts (swapExercise action — Plan 04 stub)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Component Inventory ExerciseSwapModal; Copywriting — Exercise Swap Modal; Loading States — Search has no loading state)
  </read_first>
  <files>src/components/ExerciseSwapModal/index.tsx, src/stores/sessionStore.ts</files>
  <action>
    1. **ExerciseSwapModal** (`src/components/ExerciseSwapModal/index.tsx`):
       - Props: `{ visible: boolean; currentExerciseName: string; onSelect: (newExercise: { id: string; name: string; primaryMuscle: string }) => void; onClose: () => void }`
       - Use RN `Modal` (animationType='slide', transparent=true, presentationStyle='overFullScreen') OR a custom Animated.View overlay with bottom-sheet positioning. Recommendation: RN `Modal` with custom shadow — Phase 2 doesn't introduce a new bottom-sheet library.
       - Inside Modal:
         - Backdrop: View `flex-1 bg-black/50` with onPress=onClose for tap-outside dismiss
         - Sheet: View `bg-bg-elevated rounded-t-lg p-md`, style={{ shadowColor: '#000', shadowOpacity: 0.7, shadowRadius: 32, shadowOffset: { width: 0, height: -8 } }}
         - Header row: Text `text-body font-bold text-fg` "Swap exercise" + ghost Button "Cancel" (right-aligned, onPress=onClose) with X 20px icon
         - TextInput placeholder="Search exercises..." (Phase 1 TextInput)
         - FlashList of exercise rows (56pt each: name in `text-body text-fg` + muscle category in `text-caption text-fg-muted`)
         - Footer caption: Text `text-caption text-fg-muted` "Swap applies to this session only." (UI-SPEC.md Copywriting)
         - Empty search results: Text `text-caption text-fg-muted` `No exercises match "${query}".`
       - Data: read exercises table via PowerSync usePowerSyncQuery (`SELECT id, name, primary_muscle FROM exercises ORDER BY primary_muscle, name`). Filter by `query.toLowerCase()` against name OR primary_muscle.
       - On row tap: `Haptics.selectionAsync()`, call `onSelect(exercise)`, dismiss.
       - All Text uses `allowFontScaling={false}`.

    2. **src/stores/sessionStore.ts**: Implement `swapExercise(exerciseSlotIndex: number, newExercise: { id: string; name: string })` action that replaces the ExerciseState at the given index (preserving `sets` array and superset group — but UI-SPEC.md says swap is reversible and per-session, so resetting sets is OK if needed; recommended: KEEP sets array so user keeps any prior weight/result entries against the new exercise. Update `exerciseId` and `exerciseName` only). Document this in the action's JSDoc.

    3. **src/components/ExerciseCard/index.tsx**: Wire the Shuffle IconButton to set local `swapModalOpen` state (this is one of the few legitimate per-card local states; ExerciseCard already has it via FlashList recycling concern — solve via Zustand: add `swapModalForExerciseId: string | null` and `openSwapModal(exerciseId)`/`closeSwapModal()`). Render ExerciseSwapModal at the SessionScreen level, not per card, to avoid one modal per card mounting (modal state lives in store, but the modal itself is rendered once at the root).

    4. **app/(session)/index.tsx**: Render `<ExerciseSwapModal visible={swapModalForExerciseId !== null} currentExerciseName={lookupName(swapModalForExerciseId)} onSelect={(ex) => { swapExercise(index, ex); closeSwapModal(); }} onClose={closeSwapModal} />`.
  </action>
  <verify>
    <automated>test -f src/components/ExerciseSwapModal/index.tsx &amp;&amp; grep -q "Swap exercise" src/components/ExerciseSwapModal/index.tsx &amp;&amp; grep -q "Swap applies to this session only" src/components/ExerciseSwapModal/index.tsx &amp;&amp; grep -q "FlashList" src/components/ExerciseSwapModal/index.tsx &amp;&amp; grep -q "swapExercise" src/stores/sessionStore.ts &amp;&amp; grep -q "ExerciseSwapModal" "app/(session)/index.tsx" &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - ExerciseSwapModal exists and uses `@shopify/flash-list` for the results list (per STATE.md mandate)
    - Modal copy matches UI-SPEC.md: "Swap exercise" heading, "Search exercises..." placeholder, "Swap applies to this session only." footer, empty-state copy
    - Modal dismisses on Cancel button OR backdrop tap OR row selection
    - Selecting a row calls swapExercise via sessionStore — does NOT write to template
    - SessionScreen renders the modal once (state driven by Zustand `swapModalForExerciseId`)
    - All Text uses `allowFontScaling={false}`
    - Haptics.selectionAsync() on row tap
  </acceptance_criteria>
  <done>WORKOUT-15 swap modal is wired.</done>
</task>

<task type="auto">
  <name>Task 4: SessionNoteSheet (WORKOUT-09)</name>
  <read_first>
    - src/components/TextInput/index.tsx (multiline behavior)
    - src/components/IconButton/index.tsx
    - src/components/SessionHeader/index.tsx (Plan 04 — add the note IconButton)
    - src/services/sessionService.ts (Plan 04 — extend with updateSessionNotes)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Component Inventory SessionNoteSheet; Copywriting — Session Note Sheet)
  </read_first>
  <files>src/components/SessionNoteSheet/index.tsx, src/components/SessionHeader/index.tsx, src/services/sessionService.ts, src/stores/sessionStore.ts, app/(session)/index.tsx</files>
  <action>
    1. **SessionNoteSheet** (`src/components/SessionNoteSheet/index.tsx`):
       - Props: `{ visible: boolean; initialValue: string; onSave: (notes: string) => void; onClose: () => void }`
       - RN Modal (same shell pattern as ExerciseSwapModal but smaller — no FlashList)
       - Header: "Session note" Text `text-body font-bold text-fg` + X IconButton (close)
       - Body Text `text-body text-fg-muted` "What's worth remembering about today's session?"
       - Multiline TextInput (Phase 1) with placeholder "Felt strong on bench, knee twinge on squats..." — `multiline={true}`, `numberOfLines={6}`, `textAlignVertical="top"`, bound to local state seeded from `initialValue`
       - **No Save button** — UI-SPEC.md: "auto-saves on dismiss"
       - On close (X tap or backdrop tap or swipe down): call `onSave(currentText)` THEN `onClose()`

    2. **src/services/sessionService.ts** EXTEND: Add `async function updateSessionNotes(sessionId: string, notes: string | null): Promise<void>` that does `INSERT OR REPLACE INTO sessions (id, notes, ...other-fields-or-just-notes-update)` — but since the session row may not exist yet in Phase 2 (completeSession is what writes it on finish), prefer: store the notes in sessionStore + MMKV; pass it as `sessionNotes` arg to `completeSession` at completion time. Implement this way to avoid an orphan `sessions` row mid-workout. JSDoc comment notes this.

    3. **src/stores/sessionStore.ts** EXTEND: add `sessionNotes: string`, `setSessionNotes(notes: string)`, `noteSheetOpen: boolean`, `openNoteSheet()`, `closeNoteSheet()`. Persist sessionNotes to MMKV under key `active_session_notes` via useSessionPersistence (extend Plan 04's persistence hook with a `notes` field — useMMKVString for live binding).

    4. **src/components/SessionHeader/index.tsx** EXTEND: add a StickyNote IconButton (20px lucide) below the day label OR as part of the header row (per UI-SPEC.md "positioned just below the day label"). Wire to `useSessionStore.getState().openNoteSheet()`. accessibilityLabel="Add session note".

    5. **app/(session)/index.tsx**: render `<SessionNoteSheet visible={noteSheetOpen} initialValue={sessionNotes} onSave={setSessionNotes} onClose={closeNoteSheet} />` (sessionNotes is read from sessionStore).
  </action>
  <verify>
    <automated>test -f src/components/SessionNoteSheet/index.tsx &amp;&amp; grep -q "Session note" src/components/SessionNoteSheet/index.tsx &amp;&amp; grep -q "What's worth remembering" src/components/SessionNoteSheet/index.tsx &amp;&amp; grep -q "Felt strong on bench" src/components/SessionNoteSheet/index.tsx &amp;&amp; grep -q "multiline" src/components/SessionNoteSheet/index.tsx &amp;&amp; grep -q "StickyNote" src/components/SessionHeader/index.tsx &amp;&amp; grep -q "openNoteSheet\|sessionNotes" src/stores/sessionStore.ts &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - SessionNoteSheet exists with multiline TextInput auto-save-on-dismiss
    - Copy matches UI-SPEC.md exactly (heading "Session note", body "What's worth remembering about today's session?", placeholder "Felt strong on bench, knee twinge on squats...")
    - SessionHeader has StickyNote IconButton with accessibilityLabel="Add session note"
    - sessionStore exposes sessionNotes string + open/close note sheet state
    - SessionNotes are persisted to MMKV and passed to completeSession at finish (Plan 07 already accepts `sessionNotes` arg — Plan 08 wires the store value through)
    - All Text uses `allowFontScaling={false}`
  </acceptance_criteria>
  <done>WORKOUT-09 session-level note works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 5: Skip day flow (WORKOUT-16)</name>
  <read_first>
    - app/(tabs)/workouts.tsx (Plan 04 — Start workout button)
    - src/hooks/useSessionData.ts (Plan 04 — useTodaysTemplate returns null if no template)
    - src/services/sessionService.ts (extend with skipDay)
    - src/lib/powersync.ts
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Destructive Actions — Skip today: no confirmation, single tap, button label "Skip today")
  </read_first>
  <files>src/components/SkipDayButton/index.tsx, app/(tabs)/workouts.tsx, src/services/sessionService.ts</files>
  <action>
    1. **src/services/sessionService.ts** EXTEND: `async function skipDay(userId: string): Promise<void>` that does `await getPowerSync().execute('UPDATE split_settings SET rotation_pointer = rotation_pointer + 1 WHERE user_id = ?', [userId])`. No session row created. Wrap in try/catch with console.warn (consistent with other PowerSync writes).

    2. **src/components/SkipDayButton/index.tsx**:
       - Props: `{ onSkip: () => void }`
       - Renders ghost Button with label "Skip today" (UI-SPEC.md)
       - No confirmation (UI-SPEC.md Destructive Actions)
       - Haptics.selectionAsync() on press

    3. **app/(tabs)/workouts.tsx**:
       - Use `useTodaysTemplate(userId)` from useSessionData
       - If `template === null && !loading`: render SkipDayButton wired to `() => { await skipDay(userId); /* TanStack Query revalidates the day */ }`
       - If `template !== null`: render the existing "Start workout" Button that routes to `/(session)/`
       - Make sure the loading state shows a Spinner (Phase 1 atom)
  </action>
  <verify>
    <automated>test -f src/components/SkipDayButton/index.tsx &amp;&amp; grep -q "Skip today" src/components/SkipDayButton/index.tsx &amp;&amp; grep -q "skipDay" "app/(tabs)/workouts.tsx" &amp;&amp; grep -q "rotation_pointer = rotation_pointer + 1" src/services/sessionService.ts &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - skipDay service advances rotation_pointer via PowerSync execute (single statement, no transaction needed — single-row update)
    - SkipDayButton renders "Skip today" with no confirmation
    - workouts.tsx branches on template availability: SkipDayButton OR Start workout Button
    - Haptics.selectionAsync() on tap
    - All Text uses `allowFontScaling={false}`
  </acceptance_criteria>
  <done>WORKOUT-16 skip day flow works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text input (session note) → PowerSync sessions.notes | Free text persisted on session commit |
| User selection (swap exercise) → in-memory exercise slot replacement | Does NOT mutate templates table |
| Skip day → rotation_pointer increment | No session row; idempotency depends on user not double-tapping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | swapExercise mutates template | mitigate | swapExercise is a Zustand-only action; never writes to template_exercises table. completeSession writes session_sets rows with the SWAPPED exercise_id, but template stays intact. Code reviewer must verify no PowerSync write to template_exercises happens in this plan. |
| T-02-13 | Tampering | skipDay double-tap advances rotation_pointer twice | accept | UI-SPEC.md: no confirmation. Risk is user double-tap → 2-day skip. Mitigation: disable button for 500ms after tap (use a local ref to dedupe — implement in SkipDayButton). |
| T-02-14 | Information Disclosure | session notes accessible by anyone with device | accept | Workout notes are user-owned data; device is the trust boundary. |
</threat_model>

<verification>
After all tasks: `npm run test:unit -- --run tests/unit/progressiveOverload.test.ts` exits 0. ExerciseSwapModal, SessionNoteSheet, ProgressiveOverloadHint, SkipDayButton all exist. Smoke test: open swap modal from an ExerciseCard, search for an exercise, pick one — exercise name in the card updates.
</verification>

<success_criteria>
- shouldShowOverloadHint unit test GREEN (WORKOUT-14)
- ProgressiveOverloadHint appears conditionally at bottom of ExerciseCard (WORKOUT-14)
- ExerciseSwapModal works as a per-session override (does not mutate template) (WORKOUT-15)
- SessionNoteSheet auto-saves on dismiss; notes flow through to completeSession (WORKOUT-09)
- SkipDayButton advances rotation_pointer without creating a session row (WORKOUT-16)
- All four features match UI-SPEC.md copy and visuals exactly
</success_criteria>

<output>
Create `.planning/phases/02-core-session-loop/02-08-SUMMARY.md` when done
</output>
