---
phase: 02-core-session-loop
plan: 09
type: execute
wave: 7
depends_on:
  - 02-04
  - 02-08
files_modified:
  - src/components/BodyMap/index.tsx
  - src/components/BodyMap/anatomy.tsx
  - src/components/BodyweightOffsetInput/index.tsx
  - src/components/RunExerciseRow/index.tsx
  - src/lib/healthkit.ts
  - app/(session)/body-map.tsx
  - app/(session)/_layout.tsx
  - app/(tabs)/settings.tsx
  - app/(tabs)/workouts.tsx
  - src/components/SetRow/index.tsx
  - src/stores/sessionStore.ts
autonomous: false
requirements:
  - WORKOUT-10
  - WORKOUT-12
  - WORKOUT-13
  - DESIGN-02

must_haves:
  truths:
    - "BodyMap screen renders a tappable SVG anatomy (front/back) using react-native-svg Path elements (WORKOUT-10)"
    - "Tapped muscle regions toggle selected state (accent-dim fill + border-strong outline); selected list shown as Chips below the SVG"
    - "BodyMap pre-session screen is reachable from the Workouts tab BEFORE the session starts; sore muscles stored as JSON in sessions.notes (RESEARCH.md Open Question 2)"
    - "BodyMap is also accessible mid-session via Activity IconButton in SessionHeader"
    - "Bodyweight exercise rows replace WeightInput with BodyweightOffsetInput: weight = latest measurements.weight_kg + user offset (WORKOUT-12)"
    - "Run exercise rows show a distance/time row (NOT WeightInput); GPS pull from Apple Health is the source via HealthKit wrapper (WORKOUT-13)"
    - "Settings has a `useDeviceGpsForRun` toggle (WORKOUT-13 — fallback toggle UI only; implementation deferred)"
    - "Manual verification: tapping muscle highlights it; bodyweight row pulls user weight; run row pulls Apple Health distance on iOS device"
  artifacts:
    - path: "src/components/BodyMap/index.tsx"
      provides: "Anatomy SVG + selection state + chips list"
      exports: ["BodyMap"]
    - path: "src/components/BodyMap/anatomy.tsx"
      provides: "Path data for front/back anatomy"
      exports: ["FrontAnatomy", "BackAnatomy", "MUSCLE_GROUPS"]
    - path: "src/components/BodyweightOffsetInput/index.tsx"
      provides: "Bodyweight + offset input (WORKOUT-12)"
      exports: ["BodyweightOffsetInput"]
    - path: "src/components/RunExerciseRow/index.tsx"
      provides: "Distance/time row for run exercise type (WORKOUT-13)"
      exports: ["RunExerciseRow"]
    - path: "src/lib/healthkit.ts"
      provides: "Apple Health pull stub for distance + heart rate (WORKOUT-13 partial — full impl Phase 5)"
      exports: ["readLastRunDistance"]
    - path: "app/(session)/body-map.tsx"
      provides: "Pre-session body-map route"
  key_links:
    - from: "src/components/BodyMap/index.tsx"
      to: "react-native-svg"
      via: "renders Path + Pressable per muscle group"
      pattern: "from 'react-native-svg'"
    - from: "src/components/BodyweightOffsetInput/index.tsx"
      to: "PowerSync measurements table"
      via: "reads latest weight_kg via usePowerSyncQuery"
      pattern: "measurements"
    - from: "src/components/SetRow/index.tsx"
      to: "src/components/BodyweightOffsetInput/index.tsx"
      via: "renders bodyweight row when exerciseType === 'bodyweight'"
      pattern: "exerciseType === 'bodyweight'"
---

<objective>
Implement the three remaining workout requirements: BodyMap injury flagging (WORKOUT-10), bodyweight exercise type with offset input (WORKOUT-12), and Run exercise type with Apple Health GPS pull (WORKOUT-13 — Settings toggle + iOS-only HealthKit read; device GPS fallback explicitly deferred per CONTEXT.md). These are the "exercise type" variants that diverge from the standard barbell/dumbbell logging UX.

Purpose: Each of these is a specialized SetRow that shares the chrome of the standard row (LeftEdgeBar, Go/No-Go, expand chevron) but replaces the weight input with a different input model. Grouping them keeps the SetRow polymorphism logic in one place. BodyMap is its own dedicated screen, gated before the session screen.
Output: Five new components + HealthKit wrapper + body-map route + Settings toggle + SetRow polymorphism; WORKOUT-10/12/13 covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-core-session-loop/02-CONTEXT.md
@.planning/phases/02-core-session-loop/02-PATTERNS.md
@.planning/phases/02-core-session-loop/02-UI-SPEC.md
@.planning/phases/02-core-session-loop/02-RESEARCH.md
@app/(tabs)/workouts.tsx
@app/(tabs)/settings.tsx
@src/components/Chip/index.tsx
@src/components/Toggle/index.tsx
@src/components/Button/index.tsx
@src/components/IconButton/index.tsx
@src/components/WeightInput/index.tsx
@src/components/NumericText/index.tsx
@src/components/SetRow/index.tsx
@src/components/SessionHeader/index.tsx
@src/lib/powersync.ts
@src/stores/sessionStore.ts

<interfaces>
From PowerSync schema (Phase 1 — 9-table schema per STATE.md):
- `measurements(id, user_id, weight_kg, body_fat_pct, height_cm, ..., logged_at)` — latest row drives bodyweight

From src/components/Chip/index.tsx — selected/unselected pattern for selected-muscles list and toggle visual

From src/components/WeightInput/index.tsx (Plan 02) — composition pattern for inline editable numeric input; reuse for bodyweight offset.

From RESEARCH.md Open Question 2:
- For Phase 2 scope (flag-only, no AI adjustment), store sore-muscles JSON in sessions.notes
- A dedicated column / table can be added in Phase 4 when AI consumes the data

From CONTEXT.md Claude's Discretion:
- "Body map injury UI (muscle group tap targets — standard anatomical front/back view)"
- "WORKOUT-13 (Run exercise GPS) — implement the Settings toggle and Apple Health pull; device GPS fallback is lower priority"

From RESEARCH.md Pitfall 7 + UI-SPEC.md Component Inventory BodyMap:
- Custom SVG via react-native-svg (already installed) — third-party body-highlighter has uncertain New Architecture compatibility
- 32×32pt visible muscle regions, 44×44pt hit area via hitSlop
- Selected: accent-dim fill + border-strong outline

From RESEARCH.md Environment Availability:
- iOS Simulator/Xcode unavailable; HealthKit read is a wrapper-only implementation in Phase 2 (full impl in Phase 5). Pattern: expose `readLastRunDistance()` that returns 0 on Android/non-iOS and a fixture value when running in non-device contexts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BodyMap component + pre-session route (WORKOUT-10)</name>
  <read_first>
    - src/components/Chip/index.tsx (selection pattern for muscle chips and Front/Back toggle)
    - src/components/Toggle/index.tsx (Front/Back toggle)
    - src/components/Button/index.tsx (Start workout CTA)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Screen Layouts — Body Map; Component Inventory BodyMap; Copywriting — Body Map)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (BodyMap section)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Pitfall 7 — custom SVG approach)
  </read_first>
  <files>src/components/BodyMap/anatomy.tsx, src/components/BodyMap/index.tsx, app/(session)/body-map.tsx, app/(session)/_layout.tsx, app/(tabs)/workouts.tsx, src/stores/sessionStore.ts</files>
  <action>
    1. **src/components/BodyMap/anatomy.tsx**:
       - Export `MUSCLE_GROUPS` constant — array of `{ id: string; name: string; side: 'front' | 'back'; pathD: string }` covering at least: chest, left shoulder, right shoulder, left bicep, right bicep, abs, left quad, right quad, left calf, right calf (front); and traps, left lat, right lat, lower back, left glute, right glute, left hamstring, right hamstring (back). PathD strings can be simplified SVG path data — they need to render an identifiable anatomical shape but do not need to be anatomically perfect. Use a viewBox of `0 0 200 400` for consistent sizing. (For a v1 implementation, even simple rounded-rect approximations are acceptable — the tap target is what matters; the visual is a stylized silhouette.)
       - Export `FrontAnatomy` and `BackAnatomy` arrays as the front/back subsets.

    2. **src/components/BodyMap/index.tsx**:
       - Props: `{ selected: string[]; onToggle: (muscleId: string) => void }`
       - State: local `view: 'front' | 'back'`
       - Layout:
         - Toggle (Phase 1) with options Front / Back, bound to view state
         - Svg viewBox="0 0 200 400" width="200" height="400" (responsive — wrap in View with align-items center)
         - For each muscle in current view's array: render `<Path d={muscle.pathD} fill={selected.includes(muscle.id) ? '#F2CA50' : '#1C1B1B'} stroke={selected.includes(muscle.id) ? '#D4AF37' : 'transparent'} strokeWidth="2" />` wrapped in a `<Pressable>` (react-native-svg Path inside Pressable; OR use `onPress` prop directly on Path if supported by SVG implementation — verify via grep `node_modules/react-native-svg/src/elements/Path.tsx onPress`. If onPress not supported, use TouchableWithoutFeedback wrapping the entire SVG and use absolute-positioned invisible Pressables over the muscle regions).
         - Below SVG: "Selected:" caption + horizontal scroll of Chip per selected muscle (Phase 1 Chip with `selected={true}`, tap removes)
       - Haptics.impactAsync(Light) on muscle tap
       - All Text uses `allowFontScaling={false}`

    3. **app/(session)/body-map.tsx** (NEW route):
       - SafeAreaView wrapping
       - Heading "Flag sore muscles" (Noto Serif 24px via heading className)
       - Body "Optional — helps you train smart today."
       - `<BodyMap selected={soreMuscles} onToggle={toggleMuscle} />`
       - Primary "Start workout" Button at bottom — onPress: store soreMuscles to sessionStore, navigate to `/(session)/`
       - sessionStore.soreMuscles is serialized into sessions.notes JSON at completion (extend the notes shape in Plan 05's parseSetNotes? NO — sessions.notes is session-level, not set-level. Use a new MMKV key `active_session_sore_muscles` and pass via completeSession args.)

    4. **app/(session)/_layout.tsx** EXTEND: add `<Stack.Screen name="body-map" />` to the Stack so this route exists.

    5. **app/(tabs)/workouts.tsx** EXTEND: change "Start workout" button to route to `/(session)/body-map` first (per UI-SPEC.md interaction step 1: "User opens the workout → BodyMap (optional, can skip) → SessionScreen"). The body-map screen's "Start workout" button then navigates to `/(session)/`.

    6. **src/stores/sessionStore.ts** EXTEND: add `soreMuscles: string[]`, `toggleMuscle(muscleId: string)`, `setSoreMuscles(ids: string[])`.

    7. ALSO add a body-map IconButton (Activity 20px from lucide-react-native) in `SessionHeader` for mid-session access — when tapped, opens BodyMap as a modal (or routes to the body-map screen with `presentation: 'modal'`). For simplicity, route to body-map and add a `?return=session` query param so the body-map "Start workout" button knows to route back to the session, not start a new one. Simpler: add explicit `mode='pre' | 'mid'` state in body-map.tsx; if `mode='mid'`, the CTA is "Save and resume" and navigates back via `router.back()`.
  </action>
  <verify>
    <automated>test -f src/components/BodyMap/index.tsx &amp;&amp; test -f src/components/BodyMap/anatomy.tsx &amp;&amp; test -f "app/(session)/body-map.tsx" &amp;&amp; grep -q "MUSCLE_GROUPS" src/components/BodyMap/anatomy.tsx &amp;&amp; grep -q "from 'react-native-svg'" src/components/BodyMap/index.tsx &amp;&amp; grep -q "Flag sore muscles" "app/(session)/body-map.tsx" &amp;&amp; grep -q "body-map" "app/(session)/_layout.tsx" &amp;&amp; grep -q "body-map\|router.push" "app/(tabs)/workouts.tsx" &amp;&amp; grep -q "Activity" src/components/SessionHeader/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - BodyMap renders SVG anatomy via react-native-svg Path elements (not third-party body-highlighter — Pitfall 7)
    - MUSCLE_GROUPS exports at least 10 front muscles and 8 back muscles with non-empty pathD strings
    - Selected muscles use accent-dim fill (#F2CA50) and border-strong stroke (#D4AF37)
    - Body-map route exists at `app/(session)/body-map.tsx` and is registered in the session Stack
    - Workouts tab routes through body-map first (UI-SPEC.md interaction step 1)
    - SessionHeader has Activity IconButton for mid-session body-map access
    - sessionStore exposes soreMuscles + toggle/set actions
    - At completion, soreMuscles serialized into sessions.notes JSON (extend completeSession sessionNotes shape to `{ free_text: string, sore_muscles: string[] }`; update parseSetNotes counterpart for session notes if needed — simplest: store as JSON.stringify({ text: notes, soreMuscles })). Document this notes shape extension in the service.
    - Copy matches UI-SPEC.md: "Flag sore muscles", "Optional — helps you train smart today.", "Start workout"
    - All Text uses `allowFontScaling={false}`
  </acceptance_criteria>
  <done>WORKOUT-10 body-map injury flagging works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Bodyweight exercise type — BodyweightOffsetInput (WORKOUT-12)</name>
  <read_first>
    - src/components/WeightInput/index.tsx (Plan 02)
    - src/components/IconButton/index.tsx
    - src/lib/powersync.ts
    - .planning/phases/02-core-session-loop/02-CONTEXT.md (Phase boundary — bodyweight exercise)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Phase Requirements WORKOUT-12)
  </read_first>
  <files>src/components/BodyweightOffsetInput/index.tsx, src/components/SetRow/index.tsx, src/hooks/useSessionData.ts</files>
  <action>
    1. **src/hooks/useSessionData.ts** EXTEND: `useLatestBodyweight(userId: string): { weightKg: number | null; loading: boolean }` — query `SELECT weight_kg FROM measurements WHERE user_id = ? ORDER BY logged_at DESC LIMIT 1`.

    2. **src/components/BodyweightOffsetInput/index.tsx**:
       - Props: `{ bodyweightKg: number | null; offsetKg: number; onOffsetChange: (delta: number) => void }`
       - Layout: horizontal row with
         - NumericText showing bodyweight: `{bodyweightKg ?? '—'} kg`
         - "±" separator
         - MinusCircle IconButton (20px) onPress=() => onOffsetChange(offsetKg - 2.5)
         - NumericText `{offsetKg > 0 ? '+' : ''}${offsetKg} kg`
         - PlusCircle IconButton onPress=() => onOffsetChange(offsetKg + 2.5)
         - "=" separator
         - NumericText `${(bodyweightKg ?? 0) + offsetKg} kg` (total)
       - Haptics.selectionAsync() on each ± tap
       - All Text uses `allowFontScaling={false}`

    3. **src/components/SetRow/index.tsx** EXTEND: branch on `exerciseType`:
       - `'standard'`: render the existing WeightInput
       - `'bodyweight'`: render `<BodyweightOffsetInput bodyweightKg={bodyweight} offsetKg={offset} onOffsetChange={(d) => setSetWeight(setId, (bodyweight ?? 0) + d) /* persist computed total as weight_kg */} />`. The store's `weightKg` field stores the computed total; the offset is a UI-level adjustment that resolves to the final weight before commitSet.
       - `'run'`: defer to Task 3's RunExerciseRow.

       The SetRow consumes `exercise.exerciseType` from ExerciseState (already in Plan 04's store schema).
  </action>
  <verify>
    <automated>test -f src/components/BodyweightOffsetInput/index.tsx &amp;&amp; grep -q "MinusCircle\|PlusCircle" src/components/BodyweightOffsetInput/index.tsx &amp;&amp; grep -q "BodyweightOffsetInput\|exerciseType" src/components/SetRow/index.tsx &amp;&amp; grep -q "useLatestBodyweight\|measurements" src/hooks/useSessionData.ts &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - BodyweightOffsetInput renders bodyweight + offset stepper + total
    - SetRow renders BodyweightOffsetInput when `exerciseType === 'bodyweight'`
    - Offset adjustments persist to PowerSync as the total (bodyweight + offset) — caller does NOT need to know it's a bodyweight exercise after the fact
    - useLatestBodyweight pulls from PowerSync measurements table (LIMIT 1, ORDER BY logged_at DESC)
    - Haptics.selectionAsync() on each ± tap
    - All Text uses `allowFontScaling={false}`
  </acceptance_criteria>
  <done>WORKOUT-12 bodyweight exercise type renders correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Run exercise type + HealthKit pull + Settings toggle (WORKOUT-13)</name>
  <read_first>
    - app/(tabs)/settings.tsx (existing Settings tab)
    - src/components/Toggle/index.tsx
    - src/components/NumericText/index.tsx
    - src/components/SetRow/index.tsx (Task 2's branch logic)
    - .planning/phases/02-core-session-loop/02-CONTEXT.md (D-13 + Claude's Discretion — WORKOUT-13 GPS toggle + Apple Health pull; device GPS fallback deferred)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Phase Requirements WORKOUT-13)
  </read_first>
  <files>src/lib/healthkit.ts, src/components/RunExerciseRow/index.tsx, src/components/SetRow/index.tsx, app/(tabs)/settings.tsx, src/hooks/useSessionData.ts</files>
  <action>
    1. **src/lib/healthkit.ts**:
       Phase 2 stub (full implementation in Phase 5 per RESEARCH.md):
       ```typescript
       import { Platform } from 'react-native';

       export interface RunSample {
         distanceMeters: number;
         durationSeconds: number;
         endedAt: string;  // ISO
       }

       /**
        * Phase 2: Apple Health integration is a wrapper-only stub.
        * Returns null on Android and on iOS when react-native-health is not yet integrated.
        * Phase 5 (WEARABLE-01) wires the actual HealthKit read.
        *
        * The Settings `useDeviceGpsForRun` toggle exists to capture user preference
        * even though the GPS fallback path is deferred (CONTEXT.md Deferred Ideas).
        */
       export async function readLastRunDistance(): Promise<RunSample | null> {
         if (Platform.OS !== 'ios') return null;
         // Phase 5 will replace this with a react-native-health call
         return null;
       }
       ```
       Export `readLastRunDistance` (named).

    2. **src/components/RunExerciseRow/index.tsx**:
       - Props: `{ setId: string; distanceMeters: number | null; durationSeconds: number | null; onPullFromHealth: () => Promise<void> }`
       - Layout: horizontal row with:
         - NumericText `${(distanceMeters / 1000).toFixed(2)} km` (or `mi` if user unit is imperial — for Phase 2 default to km; unit conversion lands in Phase 6)
         - NumericText `${formatDuration(durationSeconds)}` (M:SS or H:MM:SS)
         - Ghost Button "Pull from Apple Health" with Activity 16px icon — onPress=onPullFromHealth
       - If distanceMeters and durationSeconds are null: show muted placeholder text "Tap to pull from Apple Health"
       - All Text uses `allowFontScaling={false}`

    3. **src/components/SetRow/index.tsx** EXTEND: when `exerciseType === 'run'`: render `<RunExerciseRow distanceMeters={...} durationSeconds={...} onPullFromHealth={async () => { const sample = await readLastRunDistance(); if (sample) { setSetDistance(setId, sample.distanceMeters); setSetDuration(setId, sample.durationSeconds); commitSet(...with the new fields...); } else { /* silently no-op per UI-SPEC.md error pattern */ } }} />`. Hide the WeightInput and Go/No-Go buttons (or repurpose Go/No-Go to mark the run as completed/missed — keep them; semantic "go" = completed run).

    4. **src/stores/sessionStore.ts** EXTEND: add `distanceMeters: number | null`, `durationSeconds: number | null` to SetState. Add `setSetDistance(setId, meters)`, `setSetDuration(setId, seconds)` actions.

    5. **src/services/sessionService.ts** EXTEND commitSet to also write `distance_meters` and `duration_seconds` columns to session_sets (if schema supports them; if not, store in the notes JSON as `{ run: { distance, duration } }`). Plan 09 EXTEND completeSession via the notes JSON path to avoid schema changes in Phase 2 — schema additions are explicitly out of scope for Phase 2.

    6. **app/(tabs)/settings.tsx**: add a new section "Workout" with a Toggle "Use device GPS for runs" bound to a new MMKV key `settings.useDeviceGpsForRun` (default false). Use Phase 1 Toggle + Label patterns. The toggle has no effect in Phase 2 (the device-GPS fallback is deferred per CONTEXT.md); the toggle just persists user preference for Phase 5 to consume. Add a caption "Apple Health is the default source. Device GPS fallback coming in a future update." in fg-muted.
  </action>
  <verify>
    <automated>test -f src/lib/healthkit.ts &amp;&amp; test -f src/components/RunExerciseRow/index.tsx &amp;&amp; grep -q "readLastRunDistance" src/lib/healthkit.ts &amp;&amp; grep -q "Platform.OS !== 'ios'" src/lib/healthkit.ts &amp;&amp; grep -q "Pull from Apple Health\|pull from Apple Health" src/components/RunExerciseRow/index.tsx &amp;&amp; grep -q "useDeviceGpsForRun\|Use device GPS" "app/(tabs)/settings.tsx" &amp;&amp; grep -q "RunExerciseRow\|exerciseType === 'run'" src/components/SetRow/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/healthkit.ts exports `readLastRunDistance` (returns null on Android; null stub on iOS until Phase 5)
    - RunExerciseRow component renders distance + duration + "Pull from Apple Health" button
    - SetRow branches to RunExerciseRow when `exerciseType === 'run'`
    - sessionStore SetState has `distanceMeters` and `durationSeconds` fields with corresponding setters
    - Settings tab has "Use device GPS for runs" Toggle with caption explaining Phase 5 deferral
    - commitSet writes distance/duration via notes JSON path (`{ run: { distanceMeters, durationSeconds }, ... }`) — no Phase 2 schema migration
    - All Text uses `allowFontScaling={false}`
  </acceptance_criteria>
  <done>WORKOUT-13 settings toggle + RunExerciseRow + HealthKit wrapper stub all in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual verification of BodyMap + bodyweight + run flows</name>
  <what-built>
    The three exercise-type variants and the body-map screen are visual + interaction features that cannot be unit-tested in vitest's node environment.
  </what-built>
  <how-to-verify>
    1. **Body Map flow:**
       - From Workouts tab, tap Start workout
       - VERIFY: body-map screen appears BEFORE the session
       - Tap several muscle paths — VERIFY: they highlight with accent fill
       - Tap a selected Chip in the Selected list — VERIFY: muscle deselects on the SVG
       - Toggle Front/Back — VERIFY: SVG swaps
       - Tap Start workout — VERIFY: session begins
    2. **Mid-session body-map:**
       - In an active session, tap the Activity IconButton in SessionHeader
       - VERIFY: body-map route opens with prior selections preserved
       - Save/back — VERIFY: returns to active session intact
    3. **Bodyweight exercise:**
       - Configure or seed a template_exercise with `exercise_type='bodyweight'`
       - Start a workout — VERIFY: the bodyweight exercise SetRow shows weight + offset stepper, NOT a WeightInput
       - Tap ± buttons — VERIFY: total updates
    4. **Run exercise:**
       - Configure a template_exercise with `exercise_type='run'`
       - Start a workout — VERIFY: the run SetRow shows distance/duration row with "Pull from Apple Health" button
       - Tap the pull button — VERIFY: on iOS, the call returns null silently (Phase 5 wires the real read); no error UI
    5. **Settings toggle:**
       - Open Settings — VERIFY: "Use device GPS for runs" Toggle present with explanatory caption
       - Toggle on/off — VERIFY: persists across app restart (MMKV)
  </how-to-verify>
  <resume-signal>Type "verified" when all five flows behave correctly, or describe failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple Health → app | Phase 5 wires the real read; Phase 2 stub returns null. No HealthKit permission requested in Phase 2. |
| SVG Pressable touch events → muscle selection | New React Native New Architecture event handling; mitigated by react-native-svg compatibility (Pitfall 7) |
| Bodyweight read from measurements table | RLS scoped to user_id (Phase 1) — no cross-user reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Information Disclosure | sessions.notes JSON includes sore-muscle IDs | accept | Workout metadata is user-owned; muscle IDs are non-PII enum strings |
| T-02-16 | Tampering | Bodyweight offset arithmetic could underflow if user pushes offset extremely negative | mitigate | Cap offset at -bodyweight (can't be lighter than 0); cap at +999 to match WeightInput range. Implement clamp in BodyweightOffsetInput. |
| T-02-17 | Spoofing | HealthKit read on non-iOS returns null — caller treats as no-op | accept | Stub is explicit + tested via Platform.OS check; null return is documented |
| T-02-18 | Elevation of Privilege | react-native-body-highlighter (third-party) — NOT used | mitigate | Plan 09 uses custom react-native-svg approach per Pitfall 7. No third-party body-highlighter dependency added. |
</threat_model>

<verification>
After all tasks: all five new component files exist, body-map route reachable, Settings toggle persists. Manual checkpoint Task 4 verifies on-device behavior.
</verification>

<success_criteria>
- BodyMap pre-session screen works; soreMuscles persist into sessions.notes (WORKOUT-10)
- Mid-session body-map accessible via SessionHeader Activity icon (WORKOUT-10)
- BodyweightOffsetInput replaces WeightInput for bodyweight exercises; pulls from measurements (WORKOUT-12)
- RunExerciseRow renders distance/duration row; Pull from Apple Health wired to HealthKit stub (WORKOUT-13)
- Settings toggle for device GPS fallback persists in MMKV (WORKOUT-13 partial — toggle only, fallback deferred)
- DESIGN-02 dark/data-dense aesthetic maintained across all new components
- All new visuals use Phase 1 + Phase 2 tokens (no raw hex outside declared exceptions)
</success_criteria>

<output>
Create `.planning/phases/02-core-session-loop/02-09-SUMMARY.md` when done
</output>
