---
phase: 02-core-session-loop
plan: 02
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/hooks/useSetResult.ts
  - src/components/SetResultButton/index.tsx
  - src/components/WeightInput/index.tsx
  - src/components/NumericText/index.tsx
  - src/components/LeftEdgeBar/index.tsx
  - src/components/PrevPerformanceLink/index.tsx
  - tailwind.config.js
  - tests/unit/useSetResult.test.ts
autonomous: true
requirements:
  - WORKOUT-03
  - DESIGN-02

must_haves:
  truths:
    - "useSetResult hook cycles state null → 'go' → 'no-go' → null on alternating taps (WORKOUT-03)"
    - "Light haptic fires on every go/no-go tap"
    - "SetResultButton renders the Go and No-Go variants with selected and unselected visuals from PracticeSetCard, extracted into a reusable atom"
    - "WeightInput accepts decimal input (decimal-pad keyboard), 0.0–999.9 range, rejects negatives and >999.9 inline"
    - "NumericText applies fontVariant tabular-nums to every numeric Text in Phase 2"
    - "PrevPerformanceLink renders muted '{weight} {unit} · ●●●○' text and invokes onAutoFill on tap (D-09)"
    - "tailwind.config.js exposes set-go, set-nogo, set-warmup, timer-zero-bg, timer-zero-fg semantic color aliases and numeric/numeric-large typography roles (UI-SPEC.md NEW tokens)"
    - "useSetResult tests (WORKOUT-03) flip from RED to GREEN"
  artifacts:
    - path: "src/hooks/useSetResult.ts"
      provides: "Reusable state machine hook"
      exports: ["useSetResult", "SetResult"]
    - path: "src/components/SetResultButton/index.tsx"
      provides: "Go/No-Go button atom"
      exports: ["SetResultButton"]
    - path: "src/components/WeightInput/index.tsx"
      provides: "Decimal weight input"
      exports: ["WeightInput"]
    - path: "src/components/NumericText/index.tsx"
      provides: "Tabular-nums Text wrapper"
      exports: ["NumericText"]
    - path: "src/components/LeftEdgeBar/index.tsx"
      provides: "2px state-color vertical bar"
      exports: ["LeftEdgeBar"]
    - path: "src/components/PrevPerformanceLink/index.tsx"
      provides: "Tap-to-fill previous performance text"
      exports: ["PrevPerformanceLink"]
    - path: "tailwind.config.js"
      provides: "Phase 2 semantic color and typography tokens"
      contains: "set-go"
  key_links:
    - from: "src/components/SetResultButton/index.tsx"
      to: "src/hooks/useSetResult.ts"
      via: "imports SetResult type"
      pattern: "import.*SetResult.*from.*useSetResult"
    - from: "src/components/WeightInput/index.tsx"
      to: "src/components/TextInput/index.tsx"
      via: "extends focus state pattern from existing TextInput"
      pattern: "useState.*focused"
---

<objective>
Extract the Go/No-Go state machine from PracticeSetCard into a reusable `useSetResult` hook, build the five new Phase 2 atoms (SetResultButton, WeightInput, NumericText, LeftEdgeBar, PrevPerformanceLink), and add Phase 2 design tokens to tailwind.config.js. Turn the WORKOUT-03 test from RED to GREEN.

Purpose: These atoms are the building blocks that Plan 04 (SetRow + ExerciseCard) and Plan 03 (RestTimerPill) consume. Without them, downstream plans cannot construct a session screen that satisfies DESIGN-02's Whoop/Strong density aesthetic.
Output: Six new source files + tailwind token additions + first Phase 2 test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-core-session-loop/02-CONTEXT.md
@.planning/phases/02-core-session-loop/02-PATTERNS.md
@.planning/phases/02-core-session-loop/02-UI-SPEC.md
@src/components/PracticeSetCard/index.tsx
@src/components/TextInput/index.tsx
@src/components/Button/index.tsx
@src/components/Chip/index.tsx
@tailwind.config.js
@tests/unit/useSetResult.test.ts

<interfaces>
Extracted from PracticeSetCard/index.tsx (lines 16, 22–96) — these are the contracts new atoms must replicate:

- State type: `type SetResult = null | 'go' | 'no-go'`
- Handler signature: `() => Promise<void>` (haptic before state update)
- Toggle semantics: tapping the currently-selected variant resets to `null` (`prev === 'go' ? null : 'go'`)
- Haptic pattern: `await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` BEFORE setState
- Visual states for Go button: selected = `bg-accent-dim border-border-strong` + `text-accent font-bold`; unselected = `bg-bg border-border` + `text-fg-muted`
- Visual states for No-Go button: selected = `bg-danger/20 border-danger` + `text-danger font-bold`; unselected = `bg-bg border-border` + `text-fg-muted`
- Accessibility: `accessibilityRole="button"`, `accessibilityLabel="Mark set as go"`, `accessibilityState={{ selected: result === 'go' }}`
- Every Text uses `allowFontScaling={false}`

From TextInput/index.tsx (focus state pattern):
- `const [focused, setFocused] = useState(false)`
- `handleFocus`/`handleBlur` call setFocused + delegate to props.onFocus/onBlur
- `placeholderTextColor="#99907C"` (hex required — RN does not accept NativeWind className on this prop)
- Wrapped in `forwardRef<RNTextInput, ...>`

From Chip/index.tsx (Pressable + haptic pattern):
- `await Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` on press
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract useSetResult hook (WORKOUT-03)</name>
  <read_first>
    - src/components/PracticeSetCard/index.tsx (lines 16, 22–33 — state type and handlers to extract verbatim)
    - tests/unit/useSetResult.test.ts (Wave 0 stub — turn todos into real assertions)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (`useSetResult.ts` section)
  </read_first>
  <behavior>
    - Test: initial result is null when no `initial` arg passed
    - Test: initial result matches the `initial` arg when passed
    - Test: handleGo() transitions null → 'go'
    - Test: handleGo() while result === 'go' transitions to null (toggle off)
    - Test: handleNoGo() transitions null → 'no-go'
    - Test: handleNoGo() while result === 'no-go' transitions to null
    - Test: handleGo() while result === 'no-go' transitions to 'go' (cross-toggle)
    - Test: handleNoGo() while result === 'go' transitions to 'no-go'
    - Test: reset() returns to null from any state
    - Test (mock): Haptics.impactAsync is called with `ImpactFeedbackStyle.Light` on every handleGo/handleNoGo invocation
  </behavior>
  <files>src/hooks/useSetResult.ts, tests/unit/useSetResult.test.ts</files>
  <action>
    1. First convert each `it.todo` in `tests/unit/useSetResult.test.ts` into a real `it` block with the assertions listed in `<behavior>` above. Mock `expo-haptics` via `vi.mock('expo-haptics', () => ({ impactAsync: vi.fn(), ImpactFeedbackStyle: { Light: 'Light' } }))`. Use `@testing-library/react-hooks` or `@testing-library/react` `renderHook` if available; otherwise call the returned functions directly via a thin manual wrapper (the hook is pure state + handlers).
    2. Create `src/hooks/useSetResult.ts` exporting `type SetResult = null | 'go' | 'no-go'` and `function useSetResult(initial: SetResult = null): { result, handleGo, handleNoGo, reset }`. Implementation MUST match PATTERNS.md `useSetResult.ts` section verbatim (the pattern is a lift-and-extract from PracticeSetCard lines 25–33 with `useState` + `useCallback`).
    3. Hook MUST NOT use `useState` initial value derived from external state — accept `initial` parameter so a FlashList-recycled parent can rehydrate from Zustand on item reuse (per RESEARCH.md Pitfall 1).
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/useSetResult.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useSetResult.ts` exists and exports `useSetResult` + `SetResult` type
    - `useSetResult.ts` imports from `expo-haptics` (the haptic IS the spec)
    - `tests/unit/useSetResult.test.ts` contains zero `it.todo` calls and at least 9 `it(...)` blocks
    - `npm run test:unit -- --run tests/unit/useSetResult.test.ts` exits 0 with all tests passing (no skipped, no failed)
    - Hook returns object shape `{ result, handleGo, handleNoGo, reset }` (verified by tests)
  </acceptance_criteria>
  <done>WORKOUT-03 test is GREEN. The state machine is extracted and reusable.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 2 tokens to tailwind.config.js</name>
  <read_first>
    - tailwind.config.js (existing token structure — colors, fontSize, fontFamily, etc.)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Color section — NEW Phase 2 tokens table; Typography section — numeric/numeric-large)
  </read_first>
  <files>tailwind.config.js</files>
  <action>
    Add Phase 2 tokens to `theme.extend`:

    Colors (semantic aliases — same hex as Phase 1 tokens, new semantic names):
    - `set-go: '#F2CA50'` (alias of accent)
    - `set-nogo: '#EF4444'` (alias of danger)
    - `set-warmup: '#5C564B'` (alias of fg-subtle)
    - `timer-zero-bg: '#F2CA50'` (alias of accent)
    - `timer-zero-fg: '#0A0A0B'` (alias of bg)

    Font sizes (NEW typography roles):
    - `numeric: ['16px', { lineHeight: '24px', fontWeight: '700' }]` (Body emphasis + tabular-nums applied at component level via NumericText)
    - `numeric-large: ['28px', { lineHeight: '31px', fontWeight: '700' }]` (timer pill countdown only)

    Do NOT modify any Phase 1 token. Do NOT add a JetBrains Mono fontFamily — UI-SPEC.md explicitly rejects loading a third font asset (System monospace via `fontVariant` is the established pattern).

    `fontVariant: ['tabular-nums']` is NOT a NativeWind utility — it is applied at the NumericText component level via `style` prop (handled in Task 3).
  </action>
  <verify>
    <automated>node -e "const c = require('./tailwind.config.js'); const ext = c.theme?.extend ?? {}; const colors = ext.colors ?? {}; const sizes = ext.fontSize ?? {}; const missing = []; ['set-go','set-nogo','set-warmup','timer-zero-bg','timer-zero-fg'].forEach(k => { if (!colors[k]) missing.push('color:'+k); }); ['numeric','numeric-large'].forEach(k => { if (!sizes[k]) missing.push('size:'+k); }); if (missing.length) { console.error('MISSING:', missing.join(',')); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `theme.extend.colors` contains keys `set-go`, `set-nogo`, `set-warmup`, `timer-zero-bg`, `timer-zero-fg` with the exact hex values from UI-SPEC.md
    - `theme.extend.fontSize` contains keys `numeric` and `numeric-large` with the size + lineHeight + fontWeight tuples specified
    - No Phase 1 token is mutated — `accent`, `danger`, `fg-subtle`, `bg`, `fg` keep their existing values
    - No new fontFamily added
  </acceptance_criteria>
  <done>Token additions match UI-SPEC.md; downstream components can use `text-set-go`, `bg-timer-zero-bg`, `text-numeric-large`, etc.</done>
</task>

<task type="auto">
  <name>Task 3: Create NumericText atom</name>
  <read_first>
    - src/components/PracticeSetCard/index.tsx (lines 40–48 — Text with allowFontScaling=false pattern)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (`NumericText/index.tsx` section)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Typography — tabular-nums policy)
  </read_first>
  <files>src/components/NumericText/index.tsx</files>
  <action>
    Create `NumericText` component that wraps RN `Text` with:
    - `allowFontScaling={false}` (always, Phase 2 policy from UI-SPEC.md)
    - `style={[{ fontVariant: ['tabular-nums'] }, style]}` (the fontVariant array spread MUST be the first style entry so caller-provided style can override fontWeight/color)
    - Accept `className` prop so caller can use NativeWind utilities (`text-numeric`, `text-fg`, `text-fg-muted`, etc.)
    - Forward all other Text props via `{...rest}` spread
    - Export named `NumericText` function component (no default export — follow Phase 1 named-export convention from `Button/index.tsx`)

    Component signature: `interface NumericTextProps extends Omit<TextProps, 'allowFontScaling'> { className?: string }`.

    Per UI-SPEC.md, this component is MANDATORY for every numeric display in Phase 2 (weight, RPE, time, set count). The executor of downstream plans must use NumericText, not inline `<Text>`, for digits.
  </action>
  <verify>
    <automated>test -f src/components/NumericText/index.tsx &amp;&amp; grep -q "fontVariant.*tabular-nums" src/components/NumericText/index.tsx &amp;&amp; grep -q "allowFontScaling={false}" src/components/NumericText/index.tsx &amp;&amp; grep -q "export function NumericText" src/components/NumericText/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/NumericText/index.tsx` exists and exports named `NumericText`
    - Component sets `allowFontScaling={false}` on the underlying Text
    - Component applies `style={[{ fontVariant: ['tabular-nums'] }, ...]}` (verify with grep for `fontVariant`)
    - Component accepts and forwards `className` prop
    - File has no default export
  </acceptance_criteria>
  <done>NumericText atom available for digit display across Phase 2.</done>
</task>

<task type="auto">
  <name>Task 4: Create SetResultButton atom (extract from PracticeSetCard)</name>
  <read_first>
    - src/components/PracticeSetCard/index.tsx (lines 56–96 — the Go and No-Go Pressable blocks to extract)
    - src/hooks/useSetResult.ts (the SetResult type)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (`SetResultButton/index.tsx` section)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Component Inventory — SetResultButton row; Accessibility — VoiceOver labels)
  </read_first>
  <files>src/components/SetResultButton/index.tsx</files>
  <action>
    Create `SetResultButton` component. Props interface:
    ```
    interface SetResultButtonProps {
      variant: 'go' | 'no-go';
      selected: boolean;
      onPress: () => void;
      setNumber?: number; // for accessibilityLabel "Mark set {N} as go"
    }
    ```

    Visual rules (extracted from PracticeSetCard lines 57–95, do NOT vary):
    - Both variants: `flex-1 h-11 rounded-md items-center justify-center border active:opacity-80` base class
    - Go selected: `bg-accent-dim border-border-strong`; Go unselected: `bg-bg border-border`
    - No-go selected: `bg-danger/20 border-danger`; No-go unselected: `bg-bg border-border`
    - Go label: `✓ Go` — Text className `text-accent font-bold` when selected, `text-fg-muted` when unselected
    - No-go label: `✗ No-go` — Text className `text-danger font-bold` when selected, `text-fg-muted` when unselected
    - Every Text uses `allowFontScaling={false}`

    Accessibility:
    - `accessibilityRole="button"`
    - `accessibilityLabel={setNumber ? \`Mark set ${setNumber} as ${variant}\` : \`Mark set as ${variant}\`}` (when variant is `'no-go'`, render as "no-go" string in the label — match UI-SPEC.md exactly)
    - `accessibilityState={{ selected }}`

    Min-width: 56pt (per UI-SPEC.md spacing exception). Express via `style={{ minWidth: 56 }}` since NativeWind cannot infer pt; height 44pt is satisfied by `h-11` (Tailwind 11 × 4px = 44px ≈ 44pt on RN).

    The SetResultButton does NOT call useSetResult internally — that hook is owned by the parent SetRow (per RESEARCH.md Pitfall 1 about FlashList recycling). SetResultButton is presentational + tap handler.
  </action>
  <verify>
    <automated>test -f src/components/SetResultButton/index.tsx &amp;&amp; grep -q "export function SetResultButton" src/components/SetResultButton/index.tsx &amp;&amp; grep -q "bg-accent-dim border-border-strong" src/components/SetResultButton/index.tsx &amp;&amp; grep -q "✓ Go" src/components/SetResultButton/index.tsx &amp;&amp; grep -q "✗ No-go" src/components/SetResultButton/index.tsx &amp;&amp; grep -q "accessibilityState" src/components/SetResultButton/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SetResultButton/index.tsx` exists and exports named `SetResultButton`
    - Props interface accepts `variant`, `selected`, `onPress`, optional `setNumber`
    - Selected Go state uses className `bg-accent-dim border-border-strong` AND Text className `text-accent font-bold`
    - Selected No-go state uses className `bg-danger/20 border-danger` AND Text className `text-danger font-bold`
    - Labels are exactly `✓ Go` and `✗ No-go` (UI-SPEC.md Copywriting Contract)
    - `accessibilityRole="button"` and `accessibilityState={{ selected }}` present
    - `allowFontScaling={false}` on every Text
    - Min-width 56pt enforced via inline style
  </acceptance_criteria>
  <done>SetResultButton atom matches PracticeSetCard visuals exactly; consumable from SetRow in Plan 04.</done>
</task>

<task type="auto">
  <name>Task 5: Create WeightInput atom (decimal input with validation)</name>
  <read_first>
    - src/components/TextInput/index.tsx (lines 38–55 focus state, 89 placeholderTextColor)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (`WeightInput/index.tsx` section)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Component Inventory — WeightInput row; Error Copy — Weight input < 0 or > 999.9)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Security Domain — V5 Input Validation; T-02-03 weight tampering mitigation)
  </read_first>
  <files>src/components/WeightInput/index.tsx</files>
  <action>
    Create `WeightInput` component via `forwardRef<RNTextInput, WeightInputProps>` (match Phase 1 TextInput forwardRef pattern).

    Props:
    ```
    interface WeightInputProps extends Omit<RNTextInputProps, 'keyboardType' | 'allowFontScaling' | 'style'> {
      value: string;
      onChangeText: (v: string) => void;
      error?: boolean;
      disabled?: boolean;
    }
    ```

    Behavior (T-02-03 client-side validation):
    - `keyboardType="decimal-pad"` (D-07 from CONTEXT.md)
    - `allowFontScaling={false}`
    - `placeholderTextColor="#99907C"` (hex — RN does not accept NativeWind className on this prop)
    - `style={{ fontVariant: ['tabular-nums'] }}` (numeric alignment in stacked rows)
    - Focus state via `useState(false)` + `onFocus`/`onBlur` handlers (copy pattern from TextInput/index.tsx lines 38–55)
    - Container className with focus + error states:
      - error: `bg-danger-dim border border-danger`
      - focused: `bg-bg-input border border-border-strong`
      - default: `bg-bg-input border border-border`
      - All states: `px-md h-12 rounded-sm`
    - Min-width 88pt via inline `style={{ minWidth: 88 }}`
    - Disabled state: `opacity-60`, `editable={false}`

    Validation: This component does NOT internally reject input — it accepts any string via `onChangeText` and the parent decides validity. Parent (SetRow) is responsible for `parseFloat` + range check 0.0–999.9 and setting the `error` prop. This separation matches the TextInput precedent. Document this in a JSDoc comment.
  </action>
  <verify>
    <automated>test -f src/components/WeightInput/index.tsx &amp;&amp; grep -q "forwardRef" src/components/WeightInput/index.tsx &amp;&amp; grep -q "keyboardType=\"decimal-pad\"" src/components/WeightInput/index.tsx &amp;&amp; grep -q "placeholderTextColor=\"#99907C\"" src/components/WeightInput/index.tsx &amp;&amp; grep -q "fontVariant.*tabular-nums" src/components/WeightInput/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/WeightInput/index.tsx` exists and exports `WeightInput` via forwardRef
    - Underlying TextInput uses `keyboardType="decimal-pad"`
    - `allowFontScaling={false}` set
    - `placeholderTextColor="#99907C"` set (literal hex string)
    - `style={{ fontVariant: ['tabular-nums'] }}` applied
    - Focus state managed via `useState(false)` + `onFocus`/`onBlur` handlers that also delegate to props
    - Container className conditional on `error`/`focused`/default with the three className strings above
    - Min-width 88pt via inline style
    - JSDoc comment notes parent owns validation (`error` prop controls visual error state)
  </acceptance_criteria>
  <done>WeightInput atom available for SetRow consumption; validation surface is on the parent.</done>
</task>

<task type="auto">
  <name>Task 6: Create LeftEdgeBar + PrevPerformanceLink atoms</name>
  <read_first>
    - src/components/Chip/index.tsx (lines 32–54 — Pressable + selection visual + haptic pattern)
    - src/components/NumericText/index.tsx (created in Task 3)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (`PrevPerformanceLink` section — referenced via `SetRow` row in the table)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (NEW Atoms — LeftEdgeBar, PrevPerformanceLink; Copywriting — Previous performance pattern)
  </read_first>
  <files>src/components/LeftEdgeBar/index.tsx, src/components/PrevPerformanceLink/index.tsx</files>
  <action>
    **LeftEdgeBar:**
    ```
    interface LeftEdgeBarProps { variant: 'accent' | 'danger' | 'subtle' | 'none' }
    ```
    A `View` 2px wide, full-height of its parent (use `className="w-[2px] self-stretch"` + variant-mapped background: `accent` → `bg-set-go`; `danger` → `bg-set-nogo`; `subtle` → `bg-set-warmup`; `none` → `bg-transparent`). Decorative atom — no interactivity, no accessibility role.

    **PrevPerformanceLink:**
    ```
    interface PrevPerformanceLinkProps {
      weightKg: number | null;
      unit: 'lbs' | 'kg';
      results: ('go' | 'no-go' | null)[];   // up to 5 entries, in order
      onAutoFill: () => void;
    }
    ```
    A `Pressable` rendering a single line: `{weight} {unit} · {dots}` where dots are `●` (Unicode 25CF, filled circle) for 'go' and `○` (Unicode 25CB, empty circle) for 'no-go'. Truncate at 5 dots with trailing ellipsis if `results.length > 5`. Use the NumericText component for the weight value; surrounding ` lbs · ` and dots use plain Text with `text-caption text-fg-muted` className and `allowFontScaling={false}`. Color the dots inline via `style={{ color: result === 'go' ? '#F2CA50' : '#EF4444' }}` (hex required — `set-go`/`set-nogo` tokens are NativeWind classNames, not directly addressable on inline Text style fragments). If `weightKg === null`, render `—` placeholder string and DISABLE the Pressable (no onPress). On press, call `onAutoFill()` and fire `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light)` first (match Chip pattern).

    Accessibility (PrevPerformanceLink): `accessibilityRole="button"`, `accessibilityLabel="Previous: ${weight} ${unit}, ${goCount} go, ${noGoCount} no-go. Tap to auto-fill."` per UI-SPEC.md.

    `hitSlop={{ top: 12, bottom: 12, left: 0, right: 0 }}` so the row meets 44pt vertical tap target without padding the visible text.
  </action>
  <verify>
    <automated>test -f src/components/LeftEdgeBar/index.tsx &amp;&amp; test -f src/components/PrevPerformanceLink/index.tsx &amp;&amp; grep -q "w-\[2px\]" src/components/LeftEdgeBar/index.tsx &amp;&amp; grep -q "onAutoFill" src/components/PrevPerformanceLink/index.tsx &amp;&amp; grep -q "Haptics.impactAsync" src/components/PrevPerformanceLink/index.tsx &amp;&amp; grep -q "accessibilityLabel" src/components/PrevPerformanceLink/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/LeftEdgeBar/index.tsx` exports `LeftEdgeBar` with variant prop accepting `'accent' | 'danger' | 'subtle' | 'none'`
    - LeftEdgeBar uses `w-[2px] self-stretch` and the variant-to-background mapping above
    - `src/components/PrevPerformanceLink/index.tsx` exports `PrevPerformanceLink`
    - PrevPerformanceLink renders the `{weight} {unit} · ●●●○` pattern with proper glyphs (● / ○)
    - Truncates dot history beyond 5 entries
    - Disabled (no onPress) when `weightKg === null`
    - Fires `Haptics.impactAsync(Light)` then calls `onAutoFill()` on press
    - `accessibilityRole="button"` and structured accessibilityLabel present
    - `hitSlop` ensures 44pt vertical hit area
  </acceptance_criteria>
  <done>Five Phase 2 atoms (SetResultButton, WeightInput, NumericText, LeftEdgeBar, PrevPerformanceLink) ready for SetRow composition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text input → WeightInput | Decimal weight string crosses into app state |
| Atom props → SetResultButton/PrevPerformanceLink | Parent-provided callbacks invoked on user gesture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | WeightInput accepts arbitrary text | mitigate | WeightInput uses `keyboardType="decimal-pad"` to constrain on-screen keyboard to digits + decimal; parent (SetRow in Plan 04) performs parseFloat + range check `0 <= w <= 999.9`. PowerSync row write is gated by parent validation. |
| T-02-A1 | Spoofing | PrevPerformanceLink invokes onAutoFill | accept | Callback is parent-provided; no cross-user reach. Trust boundary is within app process. |
| T-02-A2 | Information Disclosure | SetResultButton accessibilityLabel exposes set number | accept | Set number is non-sensitive workout metadata; user owns the device and the data. |
</threat_model>

<verification>
After all tasks: `npm run test:unit` exits 0 with the WORKOUT-03 useSetResult test transitioning from todo to passing (count of `it.todo` in useSetResult.test.ts == 0). All six new source files exist. `tailwind.config.js` parses and contains the new tokens.
</verification>

<success_criteria>
- useSetResult hook extracted with full state machine + haptic + reset
- Five atoms (SetResultButton, WeightInput, NumericText, LeftEdgeBar, PrevPerformanceLink) implemented per UI-SPEC.md exactly
- Phase 2 tokens added to tailwind.config.js (5 colors + 2 fontSizes)
- tests/unit/useSetResult.test.ts is GREEN (WORKOUT-03 satisfied at unit level)
- No Phase 1 token mutated
- No new font family loaded
</success_criteria>

<output>
Create `.planning/phases/02-core-session-loop/02-02-SUMMARY.md` when done
</output>
