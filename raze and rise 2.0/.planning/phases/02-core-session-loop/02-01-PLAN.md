---
phase: 02-core-session-loop
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - app.json
  - metro.config.js
  - assets/animations/anubis.json
  - assets/sounds/timer-complete.wav
  - tests/unit/useSetResult.test.ts
  - tests/unit/sessionStats.test.ts
  - tests/unit/useRestTimer.test.ts
  - tests/unit/supersetLogic.test.ts
  - tests/unit/progressiveOverload.test.ts
  - tests/unit/sessionService.test.ts
  - tests/unit/sessionPersistence.test.ts
autonomous: false
requirements: []
user_setup:
  - service: assets
    why: "Source Anubis Lottie JSON + timer-complete WAV audio asset before bundling"
    dashboard_config:
      - task: "Place anubis.json in assets/animations/ (sourced from v1 raze-and-rise.vercel.app conversion, OR commissioned/created via LottieFiles)"
        location: "assets/animations/anubis.json"
      - task: "Place a short (~300ms) timer-complete.wav sound (CC0 from freesound.org acceptable)"
        location: "assets/sounds/timer-complete.wav"

must_haves:
  truths:
    - "Five new packages (@shopify/flash-list, lottie-react-native, expo-audio, expo-notifications, expo-keep-awake) are installed and resolvable"
    - "anubis.json exists at assets/animations/ and contains no `expressions` field (Lottie JS-exec security gate, T-02-02)"
    - "timer-complete.wav exists at assets/sounds/"
    - "Seven Wave 0 test stub files exist and are runnable (red — describe-only or skipped, fail-loud markers)"
    - "Android notification channel registration code is present (channel id `rest-timer`)"
  artifacts:
    - path: "package.json"
      provides: "Five new dependencies present in dependencies block"
      contains: "@shopify/flash-list"
    - path: "assets/animations/anubis.json"
      provides: "Lottie animation asset for AnubisOverlay"
    - path: "assets/sounds/timer-complete.wav"
      provides: "Rest timer zero-tone audio asset"
    - path: "tests/unit/useSetResult.test.ts"
      provides: "Wave 0 stub for WORKOUT-03"
    - path: "tests/unit/sessionStats.test.ts"
      provides: "Wave 0 stub for WORKOUT-05"
    - path: "tests/unit/useRestTimer.test.ts"
      provides: "Wave 0 stub for WORKOUT-06"
    - path: "tests/unit/supersetLogic.test.ts"
      provides: "Wave 0 stub for WORKOUT-11"
    - path: "tests/unit/progressiveOverload.test.ts"
      provides: "Wave 0 stub for WORKOUT-14"
    - path: "tests/unit/sessionService.test.ts"
      provides: "Wave 0 stub for WORKOUT-17"
    - path: "tests/unit/sessionPersistence.test.ts"
      provides: "Wave 0 stub for WORKOUT-18"
  key_links:
    - from: "metro.config.js"
      to: "assets/animations/anubis.json"
      via: "assetExts includes `json`"
      pattern: "assetExts.*json"
    - from: "app.json"
      to: "expo-notifications"
      via: "plugins array includes 'expo-notifications'"
      pattern: "expo-notifications"
---

<objective>
Install Phase 2 native dependencies, source two binary assets (Anubis Lottie + timer tone), register Wave 0 test stub files, and ensure Metro + app.json are configured for the new packages.

Purpose: Wave 0 unblocks every other Phase 2 plan. Wave 1 plans (set logging, rest timer, session screen) all reference packages and assets this plan creates.
Output: Updated dependency manifest, two binary assets in repo, seven failing test stubs that downstream plans must turn green, and metro/app.json updates that make the new modules bundleable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-session-loop/02-CONTEXT.md
@.planning/phases/02-core-session-loop/02-RESEARCH.md
@.planning/phases/02-core-session-loop/02-PATTERNS.md
@.planning/phases/02-core-session-loop/02-UI-SPEC.md
@.planning/phases/02-core-session-loop/02-VALIDATION.md
@package.json
@app.json
@metro.config.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (T-02-SC)</name>
  <what-built>
    RESEARCH.md `## Package Legitimacy Audit` table marks five new packages as `[ASSUMED]` because slopcheck was unavailable at research time. Per planner Package Legitimacy Gate protocol, every `[ASSUMED]` install requires a blocking human checkpoint BEFORE `npx expo install`.
  </what-built>
  <how-to-verify>
    Visit each package's official source and confirm it is the legitimate package (not a typo-squat):
    1. https://www.npmjs.com/package/@shopify/flash-list — verify Shopify-maintained, current version ~2.3.x
    2. https://www.npmjs.com/package/lottie-react-native — verify lottie-react-native org, Apache 2.0
    3. https://docs.expo.dev/versions/latest/sdk/audio/ — verify expo-audio is the SDK 55 audio module
    4. https://docs.expo.dev/versions/latest/sdk/notifications/ — verify expo-notifications is the SDK 55 notifications module
    5. https://docs.expo.dev/versions/latest/sdk/keep-awake/ — verify expo-keep-awake is the SDK 55 keep-awake module
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installation, or "block" to halt with reasoning</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Phase 2 packages via expo install</name>
  <read_first>
    - package.json (to confirm current dependencies and `--legacy-peer-deps` install context from Phase 1)
    - .planning/STATE.md (Decisions section — `--legacy-peer-deps required for all installs due to React 19/ecosystem peer dep gaps`)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Standard Stack table for exact versions)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    Run `npx expo install @shopify/flash-list lottie-react-native expo-audio expo-notifications expo-keep-awake` from the project root. If peer-dep resolution fails (React 19 ecosystem gaps documented in STATE.md), retry with `--legacy-peer-deps`. After install, verify each package appears in package.json `dependencies` block. Do NOT pin versions — let expo install resolve to the SDK 55 compatible version.
  </action>
  <verify>
    <automated>node -e "const p = require('./package.json'); const want = ['@shopify/flash-list','lottie-react-native','expo-audio','expo-notifications','expo-keep-awake']; const missing = want.filter(k => !p.dependencies?.[k]); if (missing.length) { console.error('MISSING:', missing.join(',')); process.exit(1); } else { console.log('OK'); }"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `dependencies` contains all five package names with non-empty version strings
    - `package-lock.json` resolves all five packages without UNMET dependency warnings
    - `node_modules/@shopify/flash-list/package.json` exists on disk
    - `node_modules/lottie-react-native/package.json` exists on disk
    - `node_modules/expo-audio/package.json` exists on disk
    - `node_modules/expo-notifications/package.json` exists on disk
    - `node_modules/expo-keep-awake/package.json` exists on disk
  </acceptance_criteria>
  <done>All five packages installed and importable from project root.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Source binary assets (anubis.json + timer-complete.wav)</name>
  <what-built>
    Two binary assets must exist before downstream plans can bundle them. Claude cannot generate proprietary animation/audio assets; user must place files.
  </what-built>
  <how-to-verify>
    1. Anubis Lottie JSON:
       - Convert the v1 raze-and-rise.vercel.app Anubis animation to Lottie JSON via LottieFiles (https://lottiefiles.com), OR commission one, OR export from After Effects with the Bodymovin plugin.
       - Place the file at `assets/animations/anubis.json` (create the `assets/animations/` directory if missing).
       - The file must NOT contain a top-level or per-layer `"expressions"` field (Lottie's JS-execution feature is a security risk per T-02-02 / RESEARCH.md Pitfall 5).
    2. Timer tone WAV:
       - Source a ~300ms tone from freesound.org (CC0 license) or generate one (e.g., 440 Hz sine, 200ms attack-decay).
       - Place the file at `assets/sounds/timer-complete.wav` (create the `assets/sounds/` directory if missing).
    3. Confirm both files exist by running: `ls assets/animations/anubis.json assets/sounds/timer-complete.wav`
  </how-to-verify>
  <resume-signal>Type "assets placed" when both files are in the repo</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Audit anubis.json for Lottie expressions (T-02-02 mitigation)</name>
  <read_first>
    - assets/animations/anubis.json (the file Task 3 placed)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Pitfall 5: Lottie expressions field)
  </read_first>
  <files>assets/animations/anubis.json</files>
  <action>
    Verify the Anubis Lottie asset does NOT contain a top-level or per-layer `"expressions"` field. Run a grep for the exact JSON key `"expressions"` (case-insensitive). If found, fail loudly — the user must re-export without expressions or strip them via LottieFiles editor. This task does NOT modify the asset; it only audits.
  </action>
  <verify>
    <automated>test ! -f assets/animations/anubis.json && { echo "MISSING: anubis.json not placed by Task 3"; exit 1; }; if grep -i '"expressions"' assets/animations/anubis.json > /dev/null; then echo "FAIL: anubis.json contains expressions field (Lottie JS-exec risk, T-02-02)"; exit 1; else echo "OK: no expressions field"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `assets/animations/anubis.json` exists on disk
    - `grep -i '"expressions"' assets/animations/anubis.json` returns no match (exit code 1 from grep)
    - File parses as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('assets/animations/anubis.json','utf8'))"` exits 0
  </acceptance_criteria>
  <done>Anubis asset cleared of executable expressions and parses as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 5: Configure metro and app.json for new packages</name>
  <read_first>
    - metro.config.js (current Metro config for the project; check assetExts handling)
    - app.json (current Expo config; check plugins array)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Pattern 4 Metro config note + Pattern 2 Android notification channel)
  </read_first>
  <files>metro.config.js, app.json</files>
  <action>
    1. In `metro.config.js`: ensure `config.resolver.assetExts` includes `'json'` (Lottie JSON bundling). If standard Expo bare workflow config already includes it, leave alone. Add `'wav'` to `assetExts` only if NOT already covered.
    2. In `app.json`: add `expo-notifications` to the `plugins` array if not present. Example entry: a plugin config block with `"icon": "./assets/notification-icon.png"` is NOT required for Phase 2 — bare entry `"expo-notifications"` is sufficient.
    3. Do NOT add any iOS `infoPlist` notification permission strings yet — runtime permission request is handled by Wave 1 Plan 03 (useRestTimer).
  </action>
  <verify>
    <automated>node -e "const cfg = require('./app.json'); const plugins = cfg.expo?.plugins ?? []; const has = plugins.some(p => p === 'expo-notifications' || (Array.isArray(p) && p[0] === 'expo-notifications')); if (!has) { console.error('FAIL: expo-notifications missing from app.json plugins'); process.exit(1); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - `app.json` `expo.plugins` array contains `'expo-notifications'` (either as string or as `['expo-notifications', {...}]` tuple)
    - `metro.config.js` does not break: `node -e "require('./metro.config.js')"` exits 0
    - Running `npx expo prebuild --no-install --platform ios` in dry mode (or `npx expo config --type prebuild`) does not throw a plugin resolution error for expo-notifications
  </acceptance_criteria>
  <done>Metro can bundle the new asset types; expo-notifications plugin is registered.</done>
</task>

<task type="auto">
  <name>Task 6: Create seven Wave 0 test stub files (RED gates)</name>
  <read_first>
    - tests/unit/components.task1.test.ts (existing test file — for vitest import/style conventions)
    - vitest.config.ts (test runner config, environment: 'node')
    - .planning/phases/02-core-session-loop/02-VALIDATION.md (Wave 0 Requirements section — exact file paths and behaviors)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Phase Requirements → Test Map)
  </read_first>
  <files>
    tests/unit/useSetResult.test.ts,
    tests/unit/sessionStats.test.ts,
    tests/unit/useRestTimer.test.ts,
    tests/unit/supersetLogic.test.ts,
    tests/unit/progressiveOverload.test.ts,
    tests/unit/sessionService.test.ts,
    tests/unit/sessionPersistence.test.ts
  </files>
  <action>
    Create seven vitest test stub files. Each file MUST contain `describe`/`it.todo` (or `it` + `expect.fail`) blocks naming the specific behavior under test. Use `it.todo(...)` so the suite passes structurally but reports pending behaviors — this is the Nyquist Wave 0 pattern (file exists, behavior described, implementation pending). Structure per file:

    - `useSetResult.test.ts`: describe `useSetResult` — todos: "null → go transition", "go → no-go transition", "no-go → null transition", "tap go while already go resets to null"
    - `sessionStats.test.ts`: describe `computeGoRate` — todos: "warm-up sets excluded from go-rate", "go-rate is (go count) / (working set count) × 100", "empty set list returns 0"
    - `useRestTimer.test.ts`: describe `useRestTimer` — todos: "start() schedules notification with TIME_INTERVAL trigger and stores notificationId", "cancel() cancels the scheduled notification and clears remaining", "AppState active recomputes remaining from MMKV timer_start_epoch"
    - `supersetLogic.test.ts`: describe `supersetRoundComplete` — todos: "round not complete when only A marked", "round not complete when only B marked", "round complete when both A and B marked at same set_number"
    - `progressiveOverload.test.ts`: describe `shouldShowOverloadHint` — todos: "shows hint when previous session completed all working sets at current weight", "hides hint when previous session had any no-go at current weight", "hides hint when no previous session exists"
    - `sessionService.test.ts`: describe `completeSession` — todos: "writeTransaction is called with both sessions upsert and split_settings rotation pointer increment", "uses INSERT OR REPLACE for sessions (idempotent by session UUID)", "does not check rowsAffected (PowerSync JSON view returns 0)"
    - `sessionPersistence.test.ts`: describe `useSessionPersistence` — todos: "saveSession writes active_session_id + active_session_started_at to MMKV", "clearSession removes both keys via .remove() (not .delete())", "returns persisted sessionId on hook subscribe via useMMKVString"

    Each file imports vitest primitives: `import { describe, it, expect } from 'vitest';`. NO implementation imports yet — implementations land in Wave 1/2 plans. Use `it.todo(...)` not `it.skip(...)` so they show as pending in test output.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/useSetResult.test.ts tests/unit/sessionStats.test.ts tests/unit/useRestTimer.test.ts tests/unit/supersetLogic.test.ts tests/unit/progressiveOverload.test.ts tests/unit/sessionService.test.ts tests/unit/sessionPersistence.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All seven files exist at the listed paths
    - `vitest run tests/unit/useSetResult.test.ts` exits 0 with "todo" markers (pending count > 0)
    - Each file imports from `'vitest'` and uses `it.todo(...)` syntax
    - No file contains an unresolved implementation import (Wave 1 plans add those)
    - Combined: total pending/todo count across seven files >= 20 (matches the behavior list above)
  </acceptance_criteria>
  <done>Seven RED test stubs in place — every downstream plan has a target behavior list.</done>
</task>

<task type="auto">
  <name>Task 7: Register Android notification channel at app startup</name>
  <read_first>
    - app/_layout.tsx (current startup sequence — initStorage, powersync.init, etc.)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Pattern 2 — Android channel registration code)
  </read_first>
  <files>app/_layout.tsx</files>
  <action>
    Add a one-time `Notifications.setNotificationChannelAsync('rest-timer', { name: 'Rest Timer', importance: Notifications.AndroidImportance.HIGH, vibrationPattern: [0, 250, 250, 250], sound: null })` call in the startup sequence in `app/_layout.tsx`. Place it AFTER `initStorage()` completes and BEFORE `powersync.init()`. Wrap in a try/catch — channel registration failure must not block app startup (Android-only API, no-op on iOS). Import: `import * as Notifications from 'expo-notifications';`.

    Note: per UI-SPEC.md error pattern, do NOT request permission here — permission request is owned by the session entry point (Wave 1 Plan 04 SessionScreen mount). This task ONLY registers the channel.
  </action>
  <verify>
    <automated>grep -q "setNotificationChannelAsync.*rest-timer" app/_layout.tsx &amp;&amp; grep -q "from 'expo-notifications'" app/_layout.tsx &amp;&amp; echo OK || { echo "MISSING: channel registration or import"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `app/_layout.tsx` imports `expo-notifications` (named or namespace import)
    - `app/_layout.tsx` calls `Notifications.setNotificationChannelAsync('rest-timer', ...)` exactly once
    - Channel config has `importance: AndroidImportance.HIGH` and `sound: null` (expo-audio handles in-app sound)
    - Call is wrapped in try/catch to be no-op-friendly on iOS
    - App still boots: `npm run test:unit` exits 0
  </acceptance_criteria>
  <done>Android notification channel registered at startup; Plan 03 can schedule notifications on Android 8+.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Five new packages downloaded; supply chain risk mitigated by package legitimacy gate (Task 1) |
| User-supplied Lottie asset → bundled app | anubis.json may contain JS expressions; mitigated by Task 4 grep gate |
| OS notification scheduler → device | Local notifications scheduled by app, fired by OS; no cross-user reach |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering (supply chain) | npm install of @shopify/flash-list, lottie-react-native, expo-audio, expo-notifications, expo-keep-awake | mitigate | Blocking human checkpoint Task 1 verifies each package on npmjs.com / docs.expo.dev before install (all five are `[ASSUMED]` per RESEARCH.md Package Legitimacy Audit) |
| T-02-02 | Elevation of Privilege | assets/animations/anubis.json | mitigate | Task 4 greps for `"expressions"` field — if present, build fails; user must re-export without expressions |
| T-02-AS | Information Disclosure | assets/sounds/timer-complete.wav | accept | Audio file is bundled local asset, not user-generated content, no PII |
</threat_model>

<verification>
After all tasks: `npm run test:unit` exits 0 with at least 20 pending/todo markers. `node -e "require('./app.json')"` exits 0. `ls assets/animations/anubis.json assets/sounds/timer-complete.wav` succeeds. `grep -i expressions assets/animations/anubis.json` returns no match.
</verification>

<success_criteria>
- All five Phase 2 packages installed and verified per Task 1 gate
- Anubis JSON asset placed and audited (no expressions field)
- Timer tone WAV asset placed
- Seven Wave 0 test stub files exist with explicit it.todo behavior names
- app.json plugins include expo-notifications
- Android `rest-timer` notification channel registered at app startup
- Test suite remains green (todo markers do not fail the suite)
</success_criteria>

<output>
Create `.planning/phases/02-core-session-loop/02-01-SUMMARY.md` when done
</output>
