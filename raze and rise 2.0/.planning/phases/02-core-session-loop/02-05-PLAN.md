---
phase: 02-core-session-loop
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - src/components/SetRow/index.tsx
  - src/components/SetRow/ExpandedSetForm.tsx
  - src/components/RPEStepper/index.tsx
  - src/components/ExpandChevron/index.tsx
  - src/components/QuickTagChip/index.tsx
  - src/services/sessionService.ts
  - src/stores/sessionStore.ts
  - tests/unit/sessionStats.test.ts
  - src/lib/sessionStats.ts
autonomous: true
requirements:
  - WORKOUT-04
  - WORKOUT-05
  - WORKOUT-08

must_haves:
  truths:
    - "Tapping the expand chevron on a SetRow toggles the row's expanded form (RPE stepper + warm-up toggle + free-text note + quick-tag chips)"
    - "Only one SetRow per ExerciseCard may be expanded at a time (expanding row B collapses row A)"
    - "RPEStepper lets the user pick 1–10; selection writes session_sets.rpe via commitSet (WORKOUT-04)"
    - "Warm-up toggle writes session_sets.is_warmup; warm-up sets show set-warmup LeftEdgeBar variant (WORKOUT-05)"
    - "computeGoRate(sets) excludes warmup sets from both numerator and denominator (WORKOUT-05 — unit test in sessionStats)"
    - "Set free-text note + quick-tag chips (easy/hard/good form/bad form/pain) write to session_sets.notes (WORKOUT-08)"
    - "Quick-tag chip selection state is persisted via JSON-serialized notes field (e.g. `{tags:['easy'],text:'...'}` shape)"
    - "Chevron rotation: 150ms ease-out animation between ChevronDown (collapsed) and ChevronUp (expanded)"
    - "tests/unit/sessionStats.test.ts turns from RED to GREEN"
  artifacts:
    - path: "src/components/RPEStepper/index.tsx"
      provides: "1–10 horizontal selector atom"
      exports: ["RPEStepper"]
    - path: "src/components/ExpandChevron/index.tsx"
      provides: "44×44pt chevron with rotation animation"
      exports: ["ExpandChevron"]
    - path: "src/components/QuickTagChip/index.tsx"
      provides: "Set-note quick-tag chip (uses Phase 1 Chip with specific labels + optional icon)"
      exports: ["QuickTagChip"]
    - path: "src/components/SetRow/ExpandedSetForm.tsx"
      provides: "Inline expanded form below SetRow with RPE, warm-up, note, quick tags"
      exports: ["ExpandedSetForm"]
    - path: "src/lib/sessionStats.ts"
      provides: "Go-rate calculator with warm-up exclusion (WORKOUT-05)"
      exports: ["computeGoRate", "computeWorkingSetCount"]
  key_links:
    - from: "src/lib/sessionStats.ts"
      to: "WORKOUT-05 acceptance test"
      via: "computeGoRate filters out is_warmup before counting"
      pattern: "is_warmup"
    - from: "src/components/SetRow/ExpandedSetForm.tsx"
      to: "src/services/sessionService.ts"
      via: "commitSet on rpe/warmup/notes change"
      pattern: "commitSet"
    - from: "src/components/SetRow/index.tsx"
      to: "src/components/SetRow/ExpandedSetForm.tsx"
      via: "renders when expandedSetId === setId"
      pattern: "ExpandedSetForm"
---

<objective>
Add the secondary set actions (RPE, warm-up flag, free-text note, quick-tag chips) via the expand chevron and inline form, satisfying WORKOUT-04, WORKOUT-05, and WORKOUT-08. Also ship the go-rate calculator that excludes warm-up sets and turn the sessionStats test GREEN.

Purpose: Set logging is incomplete without RPE/warmup/notes — they're the data that differentiate this app from a generic logger. Warm-up exclusion is mathematically load-bearing for go-rate stats that drive WORKOUT-14 (progressive overload) and later progress charts in Phase 3.
Output: Four new atoms + one molecule (ExpandedSetForm) + stats lib + WORKOUT-05 test green; SetRow extended to render the expanded form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-core-session-loop/02-CONTEXT.md
@.planning/phases/02-core-session-loop/02-PATTERNS.md
@.planning/phases/02-core-session-loop/02-UI-SPEC.md
@src/components/Chip/index.tsx
@src/components/TextInput/index.tsx
@src/components/Toggle/index.tsx
@src/components/SetRow/index.tsx
@src/components/IconButton/index.tsx
@src/stores/sessionStore.ts
@src/services/sessionService.ts
@tests/unit/sessionStats.test.ts

<interfaces>
From src/components/Chip/index.tsx (Phase 1 atom):
- Props: `{ label: string, selected: boolean, onPress: () => void, haptics?: boolean }`
- Renders Pressable with `accent-dim`/`bg-elevated` background based on selected
- Already fires Haptics.impactAsync(Light) when haptics prop true

From src/components/Toggle/index.tsx:
- Binary on/off toggle — use directly for the warm-up flag

From src/stores/sessionStore.ts (Plan 04):
- `setSetRpe(setId, rpe)`, `setSetWarmup(setId, isWarmup)`, `setSetNotes(setId, notesJson)` actions
- `expandedSetId: string | null`, `setExpanded(setId | null)` for one-at-a-time expansion

Notes JSON shape (Plan 05 introduces this; Plan 04 left `notes` as string):
```typescript
type SetNotes = { tags: ('easy' | 'hard' | 'good form' | 'bad form' | 'pain')[]; text: string };
```
Stored as JSON.stringify(...) in session_sets.notes. UI parses with try/catch — if parse fails, treat as legacy string in `text` field, `tags = []`.

From src/services/sessionService.ts (Plan 04):
- `commitSet({ setId, sessionId, exerciseId, exerciseName, setNumber, weightKg, repsTarget, result, rpe, isWarmup, notes })` — Plan 05 calls this with the new rpe/warmup/notes values
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement computeGoRate stat helper (WORKOUT-05)</name>
  <read_first>
    - tests/unit/sessionStats.test.ts (Wave 0 stub)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Phase Requirements: WORKOUT-05)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Set state — Warm-up note: "warm-up sets are excluded from go-rate and PR calculations")
  </read_first>
  <behavior>
    - Test: empty array → 0
    - Test: all working sets all 'go' → 100
    - Test: 2 'go', 1 'no-go', 0 warmup → 66.67 (or 67 depending on rounding — use Math.round(x * 100) / 100 for 2 decimals)
    - Test: 3 'go' working + 2 'go' warmup → 100 (warmup excluded from BOTH numerator and denominator)
    - Test: 1 'go' working + 1 'no-go' working + 3 'go' warmup → 50 (only working sets counted)
    - Test: all warmup sets → 0 (no working sets to compute rate against)
    - Test: null result excluded from denominator (incomplete sets don't count)
    - Test: `computeWorkingSetCount(sets)` returns count of sets where `is_warmup === false`
  </behavior>
  <files>src/lib/sessionStats.ts, tests/unit/sessionStats.test.ts</files>
  <action>
    1. Convert `it.todo` in `tests/unit/sessionStats.test.ts` to real assertions per `<behavior>`.

    2. Create `src/lib/sessionStats.ts`:
       ```typescript
       export interface SetRowForStats {
         result: 'go' | 'no-go' | null;
         is_warmup: boolean;
       }

       export function computeWorkingSetCount(sets: SetRowForStats[]): number {
         return sets.filter(s => !s.is_warmup).length;
       }

       export function computeGoRate(sets: SetRowForStats[]): number {
         const working = sets.filter(s => !s.is_warmup && s.result !== null);
         if (working.length === 0) return 0;
         const goCount = working.filter(s => s.result === 'go').length;
         return Math.round((goCount / working.length) * 10000) / 100;
       }
       ```
    3. No PowerSync coupling — pure function over an array.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/sessionStats.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/sessionStats.ts` exists, exports `computeGoRate` and `computeWorkingSetCount`
    - `computeGoRate` excludes warm-up sets from numerator AND denominator (WORKOUT-05)
    - `computeGoRate` excludes null-result sets from denominator
    - Returns 0 for empty array or zero working sets (no divide-by-zero)
    - `tests/unit/sessionStats.test.ts` contains zero `it.todo` and at least 8 `it(...)` blocks
    - `npm run test:unit -- --run tests/unit/sessionStats.test.ts` exits 0
  </acceptance_criteria>
  <done>WORKOUT-05 satisfied at unit level; warm-up exclusion is correct math.</done>
</task>

<task type="auto">
  <name>Task 2: Build ExpandChevron + RPEStepper + QuickTagChip atoms</name>
  <read_first>
    - src/components/Chip/index.tsx (Phase 1 atom; QuickTagChip uses it directly)
    - src/components/IconButton/index.tsx (rotation + hitSlop reference)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (no specific section — derive from UI-SPEC.md)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (NEW Atoms — RPEStepper, ExpandChevron, QuickTagChip; Icon Inventory)
  </read_first>
  <files>src/components/ExpandChevron/index.tsx, src/components/RPEStepper/index.tsx, src/components/QuickTagChip/index.tsx</files>
  <action>
    **ExpandChevron** (`src/components/ExpandChevron/index.tsx`):
    - Props: `{ expanded: boolean; onPress: () => void; accessibilityLabel?: string }`
    - Pressable wrapping a Reanimated.View that rotates the ChevronDown icon 180° to ChevronUp visual via `transform: [{ rotate: \`${spin.value}deg\` }]` with `withTiming(expanded ? 180 : 0, { duration: 150, easing: Easing.out(Easing.ease) })`
    - 44×44pt hit area via `style={{ width: 44, height: 44 }}` + `hitSlop` for extra margin (44pt is already 44pt — hitSlop may be omitted)
    - Visible icon ChevronDown 20px in `#99907C` (fg-muted hex — Lucide color prop doesn't accept className)
    - accessibilityRole="button", accessibilityLabel default `expanded ? 'Hide set options' : 'Show set options'`
    - Haptic on press: `Haptics.selectionAsync()`

    **RPEStepper** (`src/components/RPEStepper/index.tsx`):
    - Props: `{ value: number | null; onChange: (rpe: number) => void }`
    - Renders a flex-row gap-xs of 10 small Pressables (1–10), each 28pt × 32pt
    - Each cell:
      - Selected (value === N): className `bg-accent-dim border border-border-strong rounded-sm` with NumericText `text-accent font-bold`
      - Unselected: className `bg-bg-input border border-border rounded-sm` with NumericText `text-fg-muted`
    - Haptic on tap: `Haptics.selectionAsync()`
    - accessibilityRole="button" per cell, accessibilityLabel `RPE ${N}`, accessibilityState `{ selected: value === N }`
    - All Text uses `allowFontScaling={false}` and the NumericText component

    **QuickTagChip** (`src/components/QuickTagChip/index.tsx`):
    - Props: `{ tag: 'easy' | 'hard' | 'good form' | 'bad form' | 'pain'; selected: boolean; onPress: () => void }`
    - Renders the Phase 1 Chip with `label` mapped to UI-SPEC.md copy exactly (`Easy`, `Hard`, `Good form`, `Bad form`, `Pain`)
    - For the `pain` tag ONLY, prefix the label with a 14px AlertOctagon icon (lucide-react-native, color `#EF4444` danger hex). Compose this via a wrapper Pressable around Chip — OR pass a render-prop to Chip if it supports one; otherwise build a thin wrapper that renders AlertOctagon icon + Chip side by side inside a single Pressable.
    - Forward selected + onPress to Chip
    - haptics=true on Chip
  </action>
  <verify>
    <automated>test -f src/components/ExpandChevron/index.tsx &amp;&amp; test -f src/components/RPEStepper/index.tsx &amp;&amp; test -f src/components/QuickTagChip/index.tsx &amp;&amp; grep -q "ChevronDown\|ChevronUp" src/components/ExpandChevron/index.tsx &amp;&amp; grep -q "withTiming.*150" src/components/ExpandChevron/index.tsx &amp;&amp; grep -q "AlertOctagon" src/components/QuickTagChip/index.tsx &amp;&amp; grep -q "RPE" src/components/RPEStepper/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - ExpandChevron renders Pressable + Reanimated rotation between 0° and 180° over 150ms ease-out
    - ExpandChevron uses ChevronDown icon from lucide-react-native (rotation effects the up-state visually)
    - ExpandChevron has 44×44pt hit area and accessibilityLabel "Show set options" / "Hide set options"
    - RPEStepper renders 10 cells; selected cell has `bg-accent-dim border-border-strong` className
    - RPEStepper fires `Haptics.selectionAsync()` on tap
    - QuickTagChip maps 5 exact labels per UI-SPEC.md Copywriting Contract
    - QuickTagChip with `tag='pain'` includes AlertOctagon 14px icon
    - All Text uses `allowFontScaling={false}`
  </acceptance_criteria>
  <done>Three atoms ready for ExpandedSetForm composition.</done>
</task>

<task type="auto">
  <name>Task 3: Build ExpandedSetForm and wire SetRow chevron</name>
  <read_first>
    - src/components/SetRow/index.tsx (Plan 04 — chevron stub already in place)
    - src/components/Toggle/index.tsx (Phase 1 atom for warm-up)
    - src/components/TextInput/index.tsx (multiline TextInput pattern)
    - src/stores/sessionStore.ts (setSetRpe/setSetWarmup/setSetNotes actions)
    - src/services/sessionService.ts (commitSet signature)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (SetRow — Expanded State diagram + Copywriting)
  </read_first>
  <files>src/components/SetRow/ExpandedSetForm.tsx, src/components/SetRow/index.tsx, src/services/sessionService.ts, src/stores/sessionStore.ts</files>
  <action>
    1. **src/components/SetRow/ExpandedSetForm.tsx** (NEW):
       Props: `{ setId: string; sessionId: string; exerciseId: string; exerciseName: string; setNumber: number; weightKg: number | null; repsTarget: string; result: SetResult }`

       Read from sessionStore: `rpe`, `isWarmup`, `notesJson` (raw notes string). Parse `notesJson` with try/catch into `{ tags: string[], text: string }`; on parse failure, treat the whole string as `text` with empty `tags`.

       Layout (per UI-SPEC.md SetRow Expanded diagram):
       - View `p-lg gap-md` container with `border-t border-border` separator at top
       - Row 1: Text label `text-caption text-fg-muted` "RPE" + RPEStepper bound to rpe and `onChange={(n) => { setSetRpe(setId, n); commitSet(...with updated rpe...); Haptics.selectionAsync(); }}`
       - Row 2: Text label `text-caption text-fg-muted` "Warm-up" + Toggle bound to isWarmup and `onChange={(v) => { setSetWarmup(setId, v); commitSet(...with updated isWarmup...); Haptics.selectionAsync(); }}`
       - Row 3: Text label `text-caption text-fg-muted` "Note" + TextInput multiline (3 lines visible) with placeholder `"Optional free text..."`, bound to text. On blur: serialize `{ tags, text }` to JSON, write via setSetNotes + commitSet.
       - Row 4: Text label `text-caption text-fg-muted` "Quick tags:" + flex-row flex-wrap gap-sm of 5 QuickTagChips. Each tap toggles the tag in the local set + immediately serializes notes JSON + writes via setSetNotes + commitSet. Haptics.selectionAsync() on each.

       All Text uses `allowFontScaling={false}`. Component uses Phase 1 Toggle + Phase 1 TextInput, not RN primitives directly (style consistency).

    2. **src/components/SetRow/index.tsx** (EXTEND from Plan 04):
       - Replace the chevron stub with a real `<ExpandChevron expanded={isExpanded} onPress={() => { setExpanded(isExpanded ? null : setId); }} />`
       - When `expandedSetId === setId`, render `<ExpandedSetForm ... />` below the row
       - Pass the `result` from useSetResult down to ExpandedSetForm (so RPE selection knows whether to send the row)

    3. **src/services/sessionService.ts** (EXTEND):
       - No signature change — `commitSet` already accepts `rpe`, `isWarmup`, `notes`. Verify the existing SQL writes ALL these columns (per Plan 04 Task 2; this task only verifies/extends if columns were stubbed).
       - Add a helper `function serializeSetNotes(tags: string[], text: string): string { return JSON.stringify({ tags, text }); }` and `function parseSetNotes(raw: string | null): { tags: string[]; text: string } { try { const p = JSON.parse(raw ?? '{}'); return { tags: Array.isArray(p.tags) ? p.tags : [], text: typeof p.text === 'string' ? p.text : '' }; } catch { return { tags: [], text: raw ?? '' }; } }` — exported for use by ExpandedSetForm.

    4. **src/stores/sessionStore.ts** (EXTEND):
       - Verify `setSetRpe`, `setSetWarmup`, `setSetNotes` actions exist (added in Plan 04). If `setSetNotes` is missing or types it as something other than `string | null`, fix to accept JSON string.
  </action>
  <verify>
    <automated>test -f src/components/SetRow/ExpandedSetForm.tsx &amp;&amp; grep -q "ExpandedSetForm" src/components/SetRow/index.tsx &amp;&amp; grep -q "RPEStepper" src/components/SetRow/ExpandedSetForm.tsx &amp;&amp; grep -q "Toggle" src/components/SetRow/ExpandedSetForm.tsx &amp;&amp; grep -q "QuickTagChip" src/components/SetRow/ExpandedSetForm.tsx &amp;&amp; grep -q "serializeSetNotes\|parseSetNotes" src/services/sessionService.ts &amp;&amp; grep -q "expandedSetId" src/components/SetRow/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/SetRow/ExpandedSetForm.tsx` exists and renders the four rows per UI-SPEC.md (RPE / Warm-up / Note / Quick tags)
    - ExpandedSetForm uses RPEStepper, Toggle, TextInput, and 5× QuickTagChip
    - Each interactive control calls the corresponding sessionStore action AND commitSet
    - SetRow renders ExpandedSetForm conditionally when `expandedSetId === setId`
    - SetRow chevron now wired to `setExpanded(isExpanded ? null : setId)` (one-at-a-time enforced by store: setting expandedSetId replaces the previous value)
    - sessionService.ts exports `serializeSetNotes` and `parseSetNotes` helpers
    - Notes JSON shape: `{ tags: string[], text: string }`
    - All Text uses `allowFontScaling={false}`
  </acceptance_criteria>
  <done>Secondary set actions (WORKOUT-04, WORKOUT-05, WORKOUT-08) work inline on the SetRow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text input (set notes) → PowerSync notes column | Free text persisted; no rendering as HTML so XSS not applicable |
| JSON.parse of session_sets.notes column | If a malformed row exists (manual DB edit), parse must not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | session_sets.notes JSON parse | mitigate | parseSetNotes wraps JSON.parse in try/catch and falls back to `{ tags: [], text: raw ?? '' }` for legacy or malformed values |
| T-02-06 | Information Disclosure | RPE/note values exposed via accessibilityLabel | accept | Workout metadata is user's own; no cross-user leakage |
| T-02-07 | Tampering | Quick-tag selections enable downstream filtering (e.g. "pain" tag) | accept | Tag values are constrained to the 5-string literal type; no SQL injection vector (parameterized via PowerSync execute) |
</threat_model>

<verification>
After all tasks: `npm run test:unit -- --run tests/unit/sessionStats.test.ts` exits 0. ExpandChevron expands/collapses the form. Tapping RPE/warmup/quick-tag commits to PowerSync (smoke test via dev tools or follow-up plan).
</verification>

<success_criteria>
- ExpandedSetForm renders inline below SetRow with all four rows per UI-SPEC.md
- One-at-a-time expansion enforced
- WORKOUT-04 (RPE), WORKOUT-05 (warm-up), WORKOUT-08 (notes + quick tags) wired end-to-end (UI → store → commitSet → PowerSync)
- computeGoRate correctly excludes warm-up sets (WORKOUT-05 unit test GREEN)
- Notes serialization handles legacy values gracefully
- Three new atoms (ExpandChevron, RPEStepper, QuickTagChip) match UI-SPEC.md visuals
</success_criteria>

<output>
Create `.planning/phases/02-core-session-loop/02-05-SUMMARY.md` when done
</output>
