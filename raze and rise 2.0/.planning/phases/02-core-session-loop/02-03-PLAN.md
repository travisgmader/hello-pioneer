---
phase: 02-core-session-loop
plan: 03
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - src/hooks/useRestTimer.ts
  - src/lib/audio.ts
  - src/components/RestTimerPill/index.tsx
  - src/components/IconButton/index.tsx
  - tests/unit/useRestTimer.test.ts
autonomous: true
requirements:
  - WORKOUT-06
  - WORKOUT-07

must_haves:
  truths:
    - "useRestTimer.start(seconds) schedules an OS notification via Notifications.scheduleNotificationAsync with TIME_INTERVAL trigger (WORKOUT-06)"
    - "useRestTimer.cancel() cancels the scheduled notification AND clears the in-app countdown"
    - "Timer start writes timer_start_epoch + timer_duration_seconds to MMKV so AppState 'active' rehydrates accurate remaining (RESEARCH.md Pitfall 2)"
    - "On AppState change to 'active', remaining is recomputed from MMKV and either resumed or marked completed (no JS setInterval drift)"
    - "Rest timer pill renders countdown in M:SS format, drains a 1s-linear progress bar, and shows accent zero-state for 3 seconds"
    - "At zero: Haptics.notificationAsync(Success) fires, expo-audio plays timer-complete.wav, pill auto-dismisses after 3s"
    - "Notification permission denial is silent — countdown still works in-app (no error banner, RESEARCH.md Pitfall 3)"
    - "Rest seconds resolve from template_exercises.default_rest_seconds if present, else from split_settings.global_rest_seconds (WORKOUT-07)"
    - "useRestTimer tests turn from RED to GREEN"
  artifacts:
    - path: "src/hooks/useRestTimer.ts"
      provides: "Timer hook with schedule/cancel/rehydrate API"
      exports: ["useRestTimer", "resolveRestSeconds"]
    - path: "src/lib/audio.ts"
      provides: "expo-audio player + setAudioModeAsync wrapper"
      exports: ["playTimerCompleteTone", "initAudioMode"]
    - path: "src/components/RestTimerPill/index.tsx"
      provides: "Floating bottom pill with countdown + controls"
      exports: ["RestTimerPill"]
  key_links:
    - from: "src/hooks/useRestTimer.ts"
      to: "expo-notifications"
      via: "scheduleNotificationAsync TIME_INTERVAL trigger"
      pattern: "scheduleNotificationAsync"
    - from: "src/hooks/useRestTimer.ts"
      to: "react-native-mmkv"
      via: "createMMKV({ id: 'rest-timer' })"
      pattern: "createMMKV"
    - from: "src/components/RestTimerPill/index.tsx"
      to: "src/hooks/useRestTimer.ts"
      via: "consumes useRestTimer().remaining"
      pattern: "useRestTimer"
---

<objective>
Build the rest timer subsystem: `useRestTimer` hook that schedules OS-backed local notifications, persists timer state to MMKV for crash/background recovery, plays the timer-complete tone on zero, and the floating `RestTimerPill` overlay component with countdown + Skip + ±30s controls. Resolve rest duration from template overrides or global default (WORKOUT-07).

Purpose: The rest timer is the most-touched UX in a session and the most failure-prone (background lifecycle, notification permissions, audio playback). Isolating it in a dedicated plan keeps the executor focused on the lifecycle correctness. Session screen (Plan 04) consumes `useRestTimer` and renders `RestTimerPill` as a sibling of FlashList.
Output: Three new source files + one extended icon component + WORKOUT-06 test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-core-session-loop/02-CONTEXT.md
@.planning/phases/02-core-session-loop/02-PATTERNS.md
@.planning/phases/02-core-session-loop/02-UI-SPEC.md
@.planning/phases/02-core-session-loop/02-RESEARCH.md
@src/hooks/useSession.ts
@src/lib/storage.ts
@src/components/ProgressBar/index.tsx
@src/components/Button/index.tsx
@src/components/IconButton/index.tsx
@tests/unit/useRestTimer.test.ts

<interfaces>
From src/hooks/useSession.ts (subscription + cleanup pattern at lines 31–54):
- `useEffect` returns `() => subscription.unsubscribe()` cleanup
- The teardown pattern is the load-bearing piece — adapt for AppState listener

From src/lib/storage.ts (MMKV API confirmed in installed v4):
- `createMMKV({ id: 'rest-timer' })` returns an instance
- Instance has `.set(key, value)`, `.getString(key)`, `.remove(key)` (NOT `.delete()` — PATTERNS.md line 274)
- For useRestTimer, create a SEPARATE unencrypted MMKV instance with id `'rest-timer'` (not the encrypted session store)

From src/components/ProgressBar/index.tsx (Reanimated withTiming animated width pattern at lines 1–8, 43–58):
- `useSharedValue(0)`, `useAnimatedStyle(() => ({ width: \`${widthPercent.value}%\` }))`
- `withTiming(target, { duration, easing })` — for the timer use linear 1000ms ticks

From src/components/IconButton/index.tsx (existing Phase 1 atom):
- Already exports IconButton with `variant`, `onPress`, `accessibilityLabel` props
- Phase 2 adds new icon usages (MinusCircle, PlusCircle from lucide-react-native) — no new variants needed if existing IconButton accepts an `icon` slot or `children`

From RESEARCH.md Pattern 2 (the rest timer notification pattern is the load-bearing spec):
- Cancel any prior notification via `cancelScheduledNotificationAsync(notificationIdRef.current)`
- Schedule new notification: `{ content: { title: 'Rest complete', body: 'Back to it.', sound: false }, trigger: { type: Notifications.SchedulableTriggerInputTypes.TIME_INTERVAL, seconds: durationSeconds } }`
- Store returned id in ref
- `sound: false` in notification — `expo-audio` handles in-app sound; OS plays default channel sound when backgrounded

From UI-SPEC.md RestTimerPill section:
- Three visual states: active countdown / zero (3s flash) / hidden
- Active: bg-elevated bg, border-strong border, shadow-glow, NumericText numeric-large "M:SS"
- Zero: bg-timer-zero-bg (accent), no border, shadow-glow-strong, NumericText numeric-large in timer-zero-fg color, "Rest complete" body emphasis caption
- Position: absolute, bottom: safeArea + 16pt, horizontal margin 16pt, height 64pt
- ProgressBar at bottom of pill, 2px height, accent fill on border track, drains over rest duration with `timer-tick` 1000ms linear easing
- ±30s buttons: 36×36pt IconButton with MinusCircle/PlusCircle 20px
- Skip button: ghost variant, "Skip" text in fg-muted, 44pt min
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audio helper (src/lib/audio.ts)</name>
  <read_first>
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Pattern 7: expo-audio for Rest Timer Tone)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Motion / Transitions — timer-tick + zero flash)
  </read_first>
  <files>src/lib/audio.ts</files>
  <action>
    Create `src/lib/audio.ts` exporting:

    - `async function initAudioMode(): Promise<void>` — calls `setAudioModeAsync({ shouldPlayInBackground: false, playsInSilentMode: false, interruptionMode: 'mixWithOthers' })` once. Idempotent (guard with a module-level boolean).
    - `function playTimerCompleteTone(): void` — wraps an internal `useAudioPlayer(require('../../assets/sounds/timer-complete.wav'))` instance and exposes a `seekTo(0)` + `play()` invocation. Because `useAudioPlayer` is a hook, the actual hook usage MUST happen in a small `useTimerCompletePlayer()` hook (also exported), and the imperative `playTimerCompleteTone` is a fallback that the pill calls. Recommended: export only the hook `useTimerCompletePlayer()` returning a `play: () => void` callback so the pill consumes it via `const { play } = useTimerCompletePlayer()`. Document this pattern in JSDoc — expo-audio is hook-based and cannot be called from module top-level.
    - Import: `import { useAudioPlayer, setAudioModeAsync } from 'expo-audio';`

    Wrap setAudioModeAsync in try/catch — audio mode set failure is non-blocking (timer still works without sound per UI-SPEC.md error pattern).
  </action>
  <verify>
    <automated>test -f src/lib/audio.ts &amp;&amp; grep -q "from 'expo-audio'" src/lib/audio.ts &amp;&amp; grep -q "setAudioModeAsync" src/lib/audio.ts &amp;&amp; grep -q "useAudioPlayer" src/lib/audio.ts &amp;&amp; grep -q "useTimerCompletePlayer\|playTimerCompleteTone" src/lib/audio.ts &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/audio.ts` exists
    - Imports `useAudioPlayer` and `setAudioModeAsync` from `'expo-audio'`
    - Exports `initAudioMode` (idempotent async function)
    - Exports `useTimerCompletePlayer` hook returning `{ play: () => void }`
    - `initAudioMode` configures `shouldPlayInBackground: false`, `playsInSilentMode: false`, `interruptionMode: 'mixWithOthers'`
    - Errors in `setAudioModeAsync` are caught and logged (non-blocking)
    - The hook references `assets/sounds/timer-complete.wav` via `require(...)`
  </acceptance_criteria>
  <done>Audio helper ready for RestTimerPill consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement useRestTimer hook with notification + MMKV (WORKOUT-06)</name>
  <read_first>
    - src/hooks/useSession.ts (lines 31–54 — useEffect cleanup pattern)
    - src/lib/storage.ts (lines 44, 69 — createMMKV factory + .remove() API)
    - .planning/phases/02-core-session-loop/02-RESEARCH.md (Pattern 2: Rest Timer Background Notification; Pitfalls 2 + 3)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (useRestTimer.ts section)
    - tests/unit/useRestTimer.test.ts (Wave 0 stub)
  </read_first>
  <behavior>
    - Test: `start(120)` calls `Notifications.scheduleNotificationAsync` exactly once with trigger `{ type: SchedulableTriggerInputTypes.TIME_INTERVAL, seconds: 120 }` and `content.sound: false`
    - Test: `start` while a prior notification is active first calls `cancelScheduledNotificationAsync` with the prior id, then schedules the new one
    - Test: `start(120)` writes `timer_start_epoch` (current Date.now() string) and `timer_duration_seconds` ('120') to a `createMMKV({ id: 'rest-timer' })` instance
    - Test: `cancel()` calls `cancelScheduledNotificationAsync` with the stored id, clears `notificationIdRef.current` to null, clears the interval, and sets `remaining` to null
    - Test (mock AppState): firing an AppState 'active' event recomputes `remaining` as `max(0, duration - (Date.now() - start) / 1000)` from MMKV-persisted values
    - Test (mock AppState): if `remaining <= 0` after rehydration, `remaining` is set to null (timer treated as completed)
    - Test: if `requestPermissionsAsync` returns status !== 'granted', `start()` STILL sets up the in-app countdown (via setInterval driving `remaining`) but SKIPS `scheduleNotificationAsync` — no error thrown (Pitfall 3 silent fallback)
    - Test: `resolveRestSeconds(template_exercise.default_rest_seconds, split_settings.global_rest_seconds)` returns the template override if non-null, else the global default, else 90 (last-resort fallback) — WORKOUT-07
  </behavior>
  <files>src/hooks/useRestTimer.ts, tests/unit/useRestTimer.test.ts</files>
  <action>
    1. Replace `it.todo` in `tests/unit/useRestTimer.test.ts` with real `it` blocks per the behavior list. Mock `expo-notifications`: `vi.mock('expo-notifications', () => ({ scheduleNotificationAsync: vi.fn().mockResolvedValue('mock-id-1'), cancelScheduledNotificationAsync: vi.fn().mockResolvedValue(undefined), requestPermissionsAsync: vi.fn().mockResolvedValue({ status: 'granted' }), SchedulableTriggerInputTypes: { TIME_INTERVAL: 'timeInterval' } }))`. Mock `react-native-mmkv` createMMKV factory returning an in-memory Map-backed mock with `.set`, `.getString`, `.remove`. Mock `react-native` AppState with an emit helper.
    2. Create `src/hooks/useRestTimer.ts` implementing the API in PATTERNS.md. Hook returns `{ remaining: number | null, start: (seconds) => Promise<void>, cancel: () => Promise<void>, addSeconds: (delta: number) => void }`. The `addSeconds` API is for the ±30s buttons — it must cancel and reschedule the OS notification with the new remaining + delta, and update MMKV `timer_duration_seconds`.
    3. Also export `function resolveRestSeconds(templateOverride: number | null | undefined, globalDefault: number | null | undefined): number` — returns `templateOverride ?? globalDefault ?? 90` (WORKOUT-07).
    4. Internal: `notificationIdRef = useRef<string | null>(null)`, `intervalRef = useRef<ReturnType<typeof setInterval> | null>(null)`. The interval ticks `remaining` down by 1 every 1000ms when foregrounded; on backgrounding the interval is paused (cleanup in AppState change handler), and on foregrounding it's recreated from rehydrated remaining.
    5. Permission flow: on first `start()` call, await `Notifications.requestPermissionsAsync()`. If denied, skip schedule but proceed with in-app countdown (silent per UI-SPEC.md). Cache result so subsequent starts don't re-prompt.
  </action>
  <verify>
    <automated>npm run test:unit -- --run tests/unit/useRestTimer.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useRestTimer.ts` exists and exports `useRestTimer`, `resolveRestSeconds`
    - Hook signature: `useRestTimer(): { remaining, start, cancel, addSeconds }`
    - `start` schedules via `Notifications.scheduleNotificationAsync` with TIME_INTERVAL trigger
    - `start` writes `timer_start_epoch` + `timer_duration_seconds` to `createMMKV({ id: 'rest-timer' })`
    - `cancel` calls `cancelScheduledNotificationAsync` and clears state
    - `addSeconds(delta)` reschedules and updates MMKV duration
    - AppState listener registered in `useEffect`, removed in cleanup (matches useSession.ts pattern)
    - `tests/unit/useRestTimer.test.ts` has zero `it.todo` and all `it(...)` blocks pass
    - `npm run test:unit -- --run tests/unit/useRestTimer.test.ts` exits 0
    - Permission denial does not throw — verified by test
  </acceptance_criteria>
  <done>Rest timer hook handles foreground countdown, background notification, app-restart rehydration, and per-template/global rest resolution.</done>
</task>

<task type="auto">
  <name>Task 3: Build RestTimerPill component</name>
  <read_first>
    - src/components/ProgressBar/index.tsx (Reanimated useSharedValue + useAnimatedStyle pattern)
    - src/components/Button/index.tsx (Button variants for Skip)
    - src/components/IconButton/index.tsx (for ±30s buttons)
    - src/lib/audio.ts (created in Task 1 — useTimerCompletePlayer hook)
    - src/hooks/useRestTimer.ts (created in Task 2 — consumes remaining + addSeconds + cancel)
    - .planning/phases/02-core-session-loop/02-UI-SPEC.md (Component Inventory RestTimerPill row; Screen Layouts RestTimerPill — Three Visual States; Copywriting Contract — Rest Timer Pill)
    - .planning/phases/02-core-session-loop/02-PATTERNS.md (RestTimerPill section)
  </read_first>
  <files>src/components/RestTimerPill/index.tsx</files>
  <action>
    Create `RestTimerPill` component.

    Props:
    ```
    interface RestTimerPillProps {
      remaining: number | null;           // null = hidden, 0 = zero state, >0 = active
      totalSeconds: number;                // original duration for progress bar percentage
      onSkip: () => void;
      onAddSeconds: (delta: number) => void;   // pass +30 or -30
    }
    ```

    Render logic:
    - If `remaining === null`: return `null` (unmounted, not a placeholder per UI-SPEC.md State 3)
    - If `remaining === 0`: render zero state (3-second auto-dismiss handled by parent — parent passes `remaining = null` after timer-zero + 3s)
    - Else: render active countdown state

    Active state structure:
    ```
    Animated.View (absolute, bottom: safeAreaBottom + 16, left: 16, right: 16)
      View (className: "h-16 rounded-lg bg-bg-elevated border border-border-strong overflow-hidden", style: { shadowColor: '#F2CA50', shadowOpacity: 0.15, shadowRadius: 20 })
        View (flex-row items-center justify-between px-md)
          IconButton (MinusCircle 20px, onPress=() => onAddSeconds(-30), accessibilityLabel="Remove 30 seconds")
          NumericText (className="text-numeric-large text-fg", "M:SS")
          IconButton (PlusCircle 20px, onPress=() => onAddSeconds(30), accessibilityLabel="Add 30 seconds")
          Pressable (Skip — className "text-fg-muted", min-width 44pt, accessibilityRole="button", accessibilityLabel="Skip rest timer", onPress=onSkip)
        View (absolute, bottom 0, left 0, right 0, h-[2px], bg-border)
          Animated.View (style={drainStyle, height: 2, backgroundColor: '#F2CA50'})
    ```

    Zero state structure:
    ```
    Animated.View (same position)
      View (className: "h-16 rounded-lg overflow-hidden items-center justify-center", style: { backgroundColor: '#F2CA50', shadowColor: '#F2CA50', shadowOpacity: 0.25, shadowRadius: 24 })
        NumericText (className="text-numeric-large", style: { color: '#0A0A0B' }, "0:00")
        Text (className="text-body font-bold", style: { color: '#0A0A0B' }, allowFontScaling=false, "Rest complete")
    ```

    Progress bar drain:
    - `useSharedValue(100)` for widthPercent
    - In `useEffect([remaining, totalSeconds])`: `widthPercent.value = withTiming((remaining / totalSeconds) * 100, { duration: 1000, easing: Easing.linear })`
    - `useAnimatedStyle(() => ({ width: \`${widthPercent.value}%\` }))`
    - `useReducedMotion`: if true, set widthPercent.value directly without withTiming (matches ProgressBar pattern)

    Zero-state side effects (in `useEffect([remaining])` when `remaining === 0`):
    - Fire `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)`
    - Call the `play()` returned by `useTimerCompletePlayer()`
    - These run ONCE per zero transition (track via ref to prevent re-fire on re-render)

    Format `remaining` as `M:SS`: `Math.floor(remaining / 60)`:`String(Math.floor(remaining % 60)).padStart(2, '0')`. At `remaining === 0` show `0:00`.

    Accessibility:
    - Wrapping View has `accessibilityRole="timer"`, `accessibilityLiveRegion="polite"`
    - All Text uses `allowFontScaling={false}`

    Per UI-SPEC.md: pill must NOT use NativeWind for dynamic colors at zero state — use `style={{ backgroundColor: '#F2CA50' }}` for the zero state background (the hex `#F2CA50` is the accent value; NativeWind dynamic className-on-state was rejected in Phase 1 STATE.md for animated/dynamic colors).
  </action>
  <verify>
    <automated>test -f src/components/RestTimerPill/index.tsx &amp;&amp; grep -q "useSharedValue" src/components/RestTimerPill/index.tsx &amp;&amp; grep -q "withTiming.*1000.*linear" src/components/RestTimerPill/index.tsx &amp;&amp; grep -q "MinusCircle\|PlusCircle" src/components/RestTimerPill/index.tsx &amp;&amp; grep -q "accessibilityRole=\"timer\"" src/components/RestTimerPill/index.tsx &amp;&amp; grep -q "Rest complete" src/components/RestTimerPill/index.tsx &amp;&amp; grep -q "Haptics.notificationAsync" src/components/RestTimerPill/index.tsx &amp;&amp; echo OK || { echo "FAIL"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/RestTimerPill/index.tsx` exists, exports `RestTimerPill`
    - Returns `null` when `remaining === null`
    - Renders zero-state visual (accent bg, "Rest complete" body) when `remaining === 0`
    - Renders active countdown otherwise, formatted M:SS via NumericText with `text-numeric-large` className
    - Uses MinusCircle and PlusCircle icons from lucide-react-native at 20px
    - Skip button has `accessibilityLabel="Skip rest timer"` and min-width meets 44pt
    - Animated progress bar uses `withTiming(target, { duration: 1000, easing: Easing.linear })`
    - On transition to remaining===0: fires `Haptics.notificationAsync(NotificationFeedbackType.Success)` exactly once + calls audio play
    - Wrapping View has `accessibilityRole="timer"` + `accessibilityLiveRegion="polite"`
    - All Text uses `allowFontScaling={false}`
    - Floating position via absolute style with safeArea + 16pt bottom offset
  </acceptance_criteria>
  <done>RestTimerPill component implements all three visual states with correct UX, animation, audio, and haptic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → OS notification scheduler | Local notifications scheduled, fire on device, no network |
| User input (skip/±30s) → timer state | Button taps mutate timer ref + MMKV |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-N1 | Denial of Service | scheduleNotificationAsync without permission silently fails | mitigate | useRestTimer requestPermissionsAsync; if denied, fallback to in-app countdown only (no error UI per UI-SPEC.md). Test asserts no-throw on denied permission. |
| T-02-N2 | Tampering | Another local notification scheduler could collide on the same channel | accept | Single notification id tracked via ref; cancel-then-schedule pattern prevents stacked notifications. Cross-app notification collision is OS-level, not in scope. |
| T-02-A3 | Information Disclosure | Notification body "Back to it." shown on lock screen | accept | Generic copy reveals no PII; user-controlled per OS notification settings. |
</threat_model>

<verification>
After all tasks: `npm run test:unit -- --run tests/unit/useRestTimer.test.ts` exits 0. Three new source files exist. `grep -c "it.todo" tests/unit/useRestTimer.test.ts` returns 0.
</verification>

<success_criteria>
- useRestTimer hook schedules + cancels OS notifications with cleanup
- MMKV-backed rehydration on AppState 'active' (Pitfall 2)
- Notification permission denial is silent fallback (Pitfall 3)
- WORKOUT-07 rest seconds resolution helper exported
- RestTimerPill renders three visual states per UI-SPEC.md exactly
- WORKOUT-06 unit test GREEN
- Zero-state side effects (haptic + audio) fire exactly once per transition
</success_criteria>

<output>
Create `.planning/phases/02-core-session-loop/02-03-SUMMARY.md` when done
</output>
