---
phase: 03-templates-programs-progress
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - supabase/migrations/20260610000000_phase3_schema.sql
  - src/lib/schema.ts
  - src/lib/tokens.ts
  - src/lib/chartStyles.ts
  - tailwind.config.js
  - assets/fonts/Manrope-Medium.ttf
  - package.json
  - src/lib/__tests__/badgeRules.test.ts
  - src/lib/__tests__/streak.test.ts
  - src/lib/__tests__/volumeBucket.test.ts
  - src/lib/__tests__/exerciseSearch.test.ts
  - tests/integration/historyEdit.test.ts
autonomous: false
requirements: [TEMPLATE-05, PROGRAM-01, PROGRESS-04, PHOTO-02, GAMIFY-01]
user_setup:
  - service: anthropic
    why: "generate-program Edge Function (plan 03-07) calls Claude"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> API Keys; set via `supabase secrets set ANTHROPIC_API_KEY=...`"
  - service: rapidapi-exercisedb
    why: "one-time exercise media seed script (plan 03-08); local-only, never shipped"
    env_vars:
      - name: RAPIDAPI_KEY
        source: "rapidapi.com ExerciseDB listing -> Subscribe (free tier) -> X-RapidAPI-Key"

must_haves:
  truths:
    - "supabase db push applies the Phase 3 migration without error"
    - "PowerSync schema.ts declares programs, program_weeks, progress_photos, badges, user_stats tables and the added template_exercises.exercise_type + measurements columns"
    - "tailwind.config.js resolves chart-line, chart-bar, chart-measurement, chart-grid, badge-unlocked, badge-locked, badge-locked-border tokens"
    - "victory-native, @shopify/react-native-skia, expo-video, expo-image-picker, expo-image-manipulator, date-fns are installed and a fresh native dev build loads without a missing-native-module error"
    - "npm run test:unit runs the four new placeholder test files (they fail RED until their helpers ship)"
  artifacts:
    - path: "supabase/migrations/20260610000000_phase3_schema.sql"
      provides: "programs, program_weeks, progress_photos, badges, user_stats tables + RLS + columns + storage buckets"
      contains: "CREATE TABLE public.programs"
    - path: "src/lib/schema.ts"
      provides: "PowerSync mirror of all new tables + columns"
      contains: "programs:"
    - path: "src/lib/tokens.ts"
      provides: "JS color/size constants for Victory chart style props"
      exports: ["COLOR_CHART_LINE", "COLOR_CHART_BAR", "COLOR_CHART_MEASUREMENT", "COLOR_CHART_GRID", "FONT_SIZE_CHART_AXIS"]
    - path: "src/lib/chartStyles.ts"
      provides: "Victory tickLabel/axis style objects importing from tokens"
      contains: "tickLabels"
    - path: "assets/fonts/Manrope-Medium.ttf"
      provides: "TTF asset for Victory Native XL useFont"
  key_links:
    - from: "src/lib/schema.ts"
      to: "supabase/migrations/20260610000000_phase3_schema.sql"
      via: "column-for-column mirror"
      pattern: "exercise_type"
    - from: "src/lib/chartStyles.ts"
      to: "src/lib/tokens.ts"
      via: "import"
      pattern: "from '@/lib/tokens'"
---

<objective>
Lay the entire Phase 3 foundation so every downstream feature plan can read/write data, render charts, and run tests: the Supabase migration (new tables + columns + RLS + storage buckets), the matching PowerSync schema.ts mirror, the design-token JS constants + Tailwind tokens for charts, the new native dependencies + native rebuild, the chart-axis TTF font asset, and the failing unit/integration test scaffolds.

Purpose: This is the BLOCKING Wave 0 gate. No feature plan (03-02 .. 03-08) can run until the schema is pushed and the native modules are in the dev client. Schema drift and a missing Skia native module are the two biggest risks in this phase (RESEARCH Pitfalls 1, 2). Resolves RESEARCH Open Questions 1 (template_exercises.exercise_type) and 4 (user_stats for badges).
Output: One migration file, an updated schema mirror, token constants, chart style helpers, a bundled font, six installed packages, a rebuilt dev client, and five failing test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-templates-programs-progress/03-RESEARCH.md
@.planning/phases/03-templates-programs-progress/03-PATTERNS.md
@.planning/phases/03-templates-programs-progress/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase 3 Supabase migration + storage buckets</name>
  <files>supabase/migrations/20260610000000_phase3_schema.sql</files>
  <read_first>
    - supabase/migrations/20260519000000_initial_schema.sql (column conventions: timestamptz, boolean, FK order, indexes)
    - supabase/migrations/20260519000100_rls_policies.sql (cached `(SELECT auth.uid())` RLS pattern to copy verbatim, lines 14-33)
    - supabase/migrations/20260519000200_powersync_setup.sql (publication is FOR ALL TABLES — do NOT edit publication; new tables auto-publish)
    - .planning/phases/03-templates-programs-progress/03-PATTERNS.md (migration analog section, RLS pattern, MIRROR REQUIREMENT)
  </read_first>
  <action>
    Create ONE migration file in FK-dependency order:
    1. Table public.programs (id uuid PK default gen_random_uuid(), user_id uuid FK auth.users on delete cascade, name text, total_weeks int, current_week int default 1, phase int, is_active boolean default false, is_deleted boolean default false, created_at/updated_at timestamptz default now()).
    2. Table public.program_weeks (id uuid PK, program_id uuid FK programs on delete cascade, week_number int, is_deload boolean default false, day_label text, template_id uuid nullable FK templates, position int, created_at timestamptz). One row per (week, day) cell of ProgramWeekGrid; template_id null = rest day.
    3. Table public.progress_photos (id uuid PK, user_id uuid FK auth.users on delete cascade, taken_on text ISO date, storage_path text, created_at timestamptz). Per PATTERNS Pattern 7 / RESEARCH A7.
    4. Table public.badges (id uuid PK, user_id uuid FK auth.users on delete cascade, badge_key text, unlocked_at timestamptz, created_at timestamptz; add a UNIQUE (user_id, badge_key) constraint so ON CONFLICT DO NOTHING is idempotent per RESEARCH Pattern 8).
    5. Table public.user_stats (user_id uuid PK FK auth.users on delete cascade, total_sessions int default 0, lifetime_volume_kg real default 0, first_pr_at timestamptz nullable, updated_at timestamptz) — running aggregates for badge detection, resolving RESEARCH Open Question 4 (GAMIFY-01).
    6. ALTER public.template_exercises ADD COLUMN exercise_type text (nullable; null falls back to library exercise type). Resolves RESEARCH Open Question 1 / Pitfall 3 for TEMPLATE-05.
    7. ALTER public.measurements ADD COLUMN hips_cm real, ADD COLUMN arms_cm real, ADD COLUMN thighs_cm real, ADD COLUMN notes text (PROGRESS-04). measured_at already exists — do NOT re-add.
    8. For EACH of the 5 new tables: ENABLE ROW LEVEL SECURITY and create ALL FOUR CRUD policies ({table}_select_own / insert_own / update_own / delete_own) using the cached `user_id = (SELECT auth.uid())` form from the RLS analog. program_weeks (no direct user_id) is scoped via an EXISTS subquery against the parent programs row's user_id. Mandatory — missing SELECT policy silently drops PowerSync sync (RESEARCH Pitfall 7).
    9. Indexes on user_id / FK columns matching the initial schema convention.
    10. Storage buckets: insert `progress-photos` (private) with storage RLS `(storage.foldername(name))[1] = auth.uid()::text` for all four CRUD ops on storage.objects; insert `exercise-media` (public read, write restricted to service role). Use `insert into storage.buckets ... on conflict do nothing` + `create policy ... on storage.objects` (RESEARCH Pitfall 6).
    Do NOT add new tables to the PowerSync publication — it is FOR ALL TABLES already.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -v '^--' supabase/migrations/20260610000000_phase3_schema.sql | grep -c "CREATE POLICY" | awk '{ if ($1 >= 20) exit 0; else exit 1 }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "CREATE TABLE public\.(programs|program_weeks|progress_photos|badges|user_stats)" supabase/migrations/20260610000000_phase3_schema.sql` returns 5
    - `grep "ADD COLUMN exercise_type" supabase/migrations/20260610000000_phase3_schema.sql` matches
    - `grep -cE "ADD COLUMN (hips_cm|arms_cm|thighs_cm|notes)" supabase/migrations/20260610000000_phase3_schema.sql` returns 4
    - badges table contains a `UNIQUE (user_id, badge_key)` constraint
    - at least 20 `CREATE POLICY` statements (excluding comment lines)
    - file contains `storage.buckets`, `progress-photos`, `exercise-media`, and `(storage.foldername(name))[1] = auth.uid()::text`
    - file does NOT contain `ALTER PUBLICATION`
  </acceptance_criteria>
  <done>Migration file exists with 5 new tables, column additions, 20+ RLS policies, and both storage buckets with folder-scoped RLS.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror schema in PowerSync schema.ts; add tokens, chartStyles, font, Tailwind tokens</name>
  <files>src/lib/schema.ts, src/lib/tokens.ts, src/lib/chartStyles.ts, tailwind.config.js, assets/fonts/Manrope-Medium.ttf</files>
  <read_first>
    - src/lib/schema.ts (current 9-table mirror; PowerSync adds `id` implicitly — do NOT declare id; booleans are column.integer, timestamps are column.text, reals are column.real)
    - tailwind.config.js (existing theme.extend.colors token block to extend)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md ("New Color Tokens — Summary for tailwind.config.js" and the "src/lib/tokens.ts" code blocks — exact hex values)
    - .planning/phases/03-templates-programs-progress/03-PATTERNS.md (MIRROR REQUIREMENT)
  </read_first>
  <action>
    1. In src/lib/schema.ts add five new Table objects to AppSchema — programs, program_weeks, progress_photos, badges, user_stats — mirroring the migration columns COLUMN-FOR-COLUMN (booleans as column.integer, timestamps as column.text, reals as column.real). Do NOT declare id. Add by_user_id / FK indexes per table. Add exercise_type: column.text to templateExercisesTable. Add hips_cm, arms_cm, thighs_cm (column.real) and notes (column.text) to measurementsTable (leave measured_at as-is). Register all five new tables in the new Schema({...}) map.
    2. Create src/lib/tokens.ts exporting the exact constants from the UI-SPEC tokens block: FONT_SIZE_CAPTION = 12, FONT_SIZE_BODY = 16, FONT_SIZE_CHART_AXIS = 12, FONT_FAMILY_CHART = 'Manrope', COLOR_FG_MUTED = '#99907C', COLOR_ACCENT = '#F2CA50', COLOR_BG_ELEVATED = '#141416', COLOR_CHART_LINE = '#F2CA50', COLOR_CHART_BAR = 'rgba(212, 175, 55, 0.35)', COLOR_CHART_MEASUREMENT = '#60A5FA', COLOR_CHART_GRID = 'rgba(92, 86, 75, 0.30)'.
    3. Create src/lib/chartStyles.ts importing from @/lib/tokens; export reusable Victory style objects — a tickLabels style ({ fontSize: FONT_SIZE_CHART_AXIS, fill: COLOR_FG_MUTED, fontFamily: FONT_FAMILY_CHART }) plus axis-line and grid color constants. No inline hex.
    4. In tailwind.config.js under theme.extend.colors add the seven Phase 3 tokens exactly as the UI-SPEC summary block specifies: chart-line, chart-bar, chart-measurement, chart-grid, badge-unlocked, badge-locked, badge-locked-border. Do NOT redefine any existing token.
    5. Add the chart-axis font asset at assets/fonts/Manrope-Medium.ttf (Victory Native XL useFont requires a real bundled TTF; none exists today per PATTERNS "No Analog Found"). Source Manrope Medium TTF from the Manrope Google Fonts release; if a Manrope TTF already exists under node_modules, copy it instead.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && test -f assets/fonts/Manrope-Medium.ttf && grep -q "COLOR_CHART_LINE" src/lib/tokens.ts && grep -q "exercise_type" src/lib/schema.ts && grep -q "programs:" src/lib/schema.ts && grep -q "chart-line" tailwind.config.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/schema.ts contains `programs:`, `program_weeks:`, `progress_photos:`, `badges:`, `user_stats:` registered in the Schema map
    - src/lib/schema.ts templateExercisesTable contains `exercise_type: column.text`
    - src/lib/schema.ts measurementsTable contains `hips_cm`, `arms_cm`, `thighs_cm`, `notes`
    - src/lib/tokens.ts exports COLOR_CHART_LINE, COLOR_CHART_BAR, COLOR_CHART_MEASUREMENT, COLOR_CHART_GRID, FONT_SIZE_CHART_AXIS, FONT_FAMILY_CHART
    - src/lib/chartStyles.ts contains `tickLabels` and imports `from '@/lib/tokens'`
    - tailwind.config.js resolves all seven new tokens; `npx tsc --noEmit` reports no new errors in schema.ts / tokens.ts / chartStyles.ts
    - assets/fonts/Manrope-Medium.ttf exists and is a valid TTF (`file assets/fonts/Manrope-Medium.ttf` reports TrueType)
  </acceptance_criteria>
  <done>schema.ts mirrors the migration, token constants and chart styles exist, the seven Tailwind tokens resolve, and the TTF font is bundled.</done>
</task>

<task type="auto">
  <name>Task 3: Install native deps, scaffold failing tests, then push schema and rebuild dev client</name>
  <files>package.json, src/lib/__tests__/badgeRules.test.ts, src/lib/__tests__/streak.test.ts, src/lib/__tests__/volumeBucket.test.ts, src/lib/__tests__/exerciseSearch.test.ts, tests/integration/historyEdit.test.ts</files>
  <read_first>
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Standard Stack install commands + SDK-55 pin warning + Validation Architecture Wave 0 gaps + Package Legitimacy Audit)
    - tests/unit/sessionStats.test.ts (existing vitest style to match — describe/it/expect, no PowerSync coupling)
    - package.json (test:unit / test:integration scripts; --legacy-peer-deps project flag)
  </read_first>
  <action>
    1. Install native + supporting deps per RESEARCH (SDK-55 pin matters): `npx expo install expo-video expo-image-picker expo-image-manipulator`; then `npm install victory-native @shopify/react-native-skia date-fns --legacy-peer-deps`. Use the package-verification checkpoint (Task 4) BEFORE running these installs.
    2. Create the five test scaffolds as FAILING RED tests that import not-yet-existing pure helpers (the helpers ship in their feature plans):
       - src/lib/__tests__/badgeRules.test.ts — imports `detectNewBadges`/threshold rules from '@/lib/badgeRules' (ships 03-06). Assert: crossing 100 sessions yields 'century'; crossing 30-day streak yields 'on_fire'; first PR yields 'personal_record'; no double-unlock when already owned. Covers GAMIFY-01.
       - src/lib/__tests__/streak.test.ts — imports `computeStreak(dates)` from '@/lib/streak' (ships 03-03). Assert: consecutive ISO weeks increment; a gap week breaks the streak; empty array returns 0. Covers GAMIFY-03.
       - src/lib/__tests__/volumeBucket.test.ts — imports `bucketVolumeByWeek(rows)` from '@/lib/volumeBucket' (ships 03-04). Assert: weight_kg*reps_target summed into startOfISOWeek buckets; warmups already filtered upstream. Covers PROGRESS-03.
       - src/lib/__tests__/exerciseSearch.test.ts — imports `filterExercises(rows, query, muscle, userId)` from '@/lib/exerciseSearch' (ships 03-02). Assert: name LIKE and muscle_group filters; custom exercises only for the owning userId; 'All' returns all. Covers TEMPLATE-02.
       - tests/integration/historyEdit.test.ts — exercises historyService writeTransaction atomicity (ships 03-03). Use the existing tests/integration harness style. Covers HISTORY-02/03.
       Each test MUST reference a real import path so it fails with a module-not-found / undefined error (true RED), not a skipped test.
    3. AFTER Tasks 1+2 are complete and the deps are installed: run `supabase db push` to apply the migration to the remote project (BLOCKING — required by the phase schema-push mandate). Then rebuild the native dev client (`npx expo run:ios` or the EAS dev profile) so Skia / expo-video / expo-image-picker load — they will NOT come through OTA (RESEARCH Pitfall 2).
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && node -e "const p=require('./package.json');const d={...p.dependencies};for(const k of ['victory-native','@shopify/react-native-skia','expo-video','expo-image-picker','expo-image-manipulator','date-fns']){if(!d[k]){console.error('MISSING '+k);process.exit(1)}}console.log('all deps present')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies include victory-native, @shopify/react-native-skia, expo-video, expo-image-picker, expo-image-manipulator, date-fns
    - `npm run test:unit` collects and RUNS badgeRules.test.ts, streak.test.ts, volumeBucket.test.ts, exerciseSearch.test.ts and they FAIL (RED) due to missing helper modules
    - `supabase db push` reports the migration applied with no error (BLOCKING gate)
    - the native dev build completes and the app launches without a "Cannot read property 'Skia' of undefined" or missing-native-module error
  </acceptance_criteria>
  <done>Six packages installed, schema pushed, dev client rebuilt with native modules loading, and five RED test scaffolds in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Verify package legitimacy before install (slopcheck unavailable)</name>
  <what-built>RESEARCH flagged every new package as [ASSUMED] because slopcheck was unavailable. Per the package legitimacy gate, confirm each package name + source repo before Task 3 runs its installs.</what-built>
  <how-to-verify>
    Confirm each package against its registry/source page (RESEARCH Package Legitimacy Audit table):
    1. victory-native — npmjs.com/package/victory-native — source github.com/FormidableLabs/victory-native-xl, MIT, ~250k/wk
    2. @shopify/react-native-skia — npmjs.com/package/@shopify/react-native-skia — github.com/Shopify/react-native-skia
    3. expo-video, expo-image-picker, expo-image-manipulator — all under github.com/expo/expo (first-party)
    4. date-fns — npmjs.com/package/date-fns — github.com/date-fns/date-fns, ~25M/wk
    5. @anthropic-ai/sdk (server-only, used in 03-07) — github.com/anthropics/anthropic-sdk-typescript
    Confirm none are typosquats and the exact names match RESEARCH. T-03-SC in the threat model tracks this.
  </how-to-verify>
  <resume-signal>Type "approved" to allow Task 3 installs, or name any package that looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase Postgres | PowerSync sync upload crosses here; RLS is the only server-side gate |
| client -> Supabase Storage | progress photo binary upload crosses here |
| npm/expo registry -> build | third-party package code enters the native bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | new tables (programs/program_weeks/progress_photos/badges/user_stats) | mitigate | all four CRUD RLS policies in the SAME migration; SELECT policy present so PowerSync sync is not silently dropped (Pitfall 7) |
| T-03-02 | Information Disclosure | progress-photos Storage bucket | mitigate | private bucket + folder-scoped policy `(storage.foldername(name))[1] = auth.uid()::text` (Pitfall 6) |
| T-03-03 | Elevation of Privilege | program_weeks RLS (no direct user_id) | mitigate | EXISTS subquery against parent programs.user_id in every CRUD policy |
| T-03-SC | Tampering | npm/expo installs (victory-native, skia, expo-*, date-fns, @anthropic-ai/sdk) | mitigate | slopcheck + blocking human checkpoint (Task 4) for all [ASSUMED] packages before install |
</threat_model>

<verification>
- `grep -v '^--' supabase/migrations/20260610000000_phase3_schema.sql | grep -c "CREATE POLICY"` >= 20
- `npm run test:unit` runs and the four new lib tests fail RED
- `supabase db push` applies the migration with no error
- native dev build launches with Skia / expo-video / expo-image-picker loaded
- `npx tsc --noEmit` shows no new errors in schema.ts / tokens.ts / chartStyles.ts
</verification>

<success_criteria>
Migration written and pushed; PowerSync mirror reconciled column-for-column; tokens + chartStyles + Tailwind tokens + TTF font in place; six packages installed after legitimacy approval; dev client rebuilt; five RED test scaffolds committed.
</success_criteria>

<output>
Create `.planning/phases/03-templates-programs-progress/03-01-SUMMARY.md` when done.
</output>
