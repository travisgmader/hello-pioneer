---
phase: 03-templates-programs-progress
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - src/components/progress/PhotosSegmentPlaceholder.tsx
  - src/components/ProgressPhotoGallery/index.tsx
  - src/components/ProgressPhotoComparison/index.tsx
  - src/hooks/useProgressPhotos.ts
  - src/services/photoService.ts
autonomous: true
requirements: [PHOTO-01, PHOTO-02, PHOTO-03]

must_haves:
  truths:
    - "User can take or pick a progress photo; it is compressed and uploaded to the private progress-photos Storage bucket and a metadata row is created"
    - "Photos display in a chronological 3-column grid with date captions"
    - "User can select any two photos for a side-by-side before/after comparison"
  artifacts:
    - path: "src/services/photoService.ts"
      provides: "pick/capture -> compress -> Storage upload -> metadata write"
      exports: ["uploadProgressPhoto", "deleteProgressPhoto"]
    - path: "src/components/ProgressPhotoGallery/index.tsx"
      provides: "3-column FlashList grid + upload button (PHOTO-02)"
      contains: "FlashList"
    - path: "src/components/ProgressPhotoComparison/index.tsx"
      provides: "two-date before/after modal (PHOTO-03)"
    - path: "src/hooks/useProgressPhotos.ts"
      provides: "reactive progress_photos read + storage URL resolution"
      exports: ["useProgressPhotos"]
  key_links:
    - from: "src/components/progress/PhotosSegmentPlaceholder.tsx"
      to: "src/components/ProgressPhotoGallery/index.tsx"
      via: "re-export the real Photos segment"
      pattern: "ProgressPhotoGallery"
    - from: "src/services/photoService.ts"
      to: "supabase.storage"
      via: "upload to progress-photos bucket"
      pattern: "progress-photos"
---

<objective>
Build the Photos segment of the Progress tab: capture/pick a photo (expo-image-picker), compress it (expo-image-manipulator), upload to the private progress-photos Storage bucket, write a metadata row, display a chronological grid, and support two-date before/after comparison (PHOTO-01/02/03).

Purpose: Progress photos are a between-session engagement feature and the only binary-Storage path in the phase. The metadata row stays in PowerSync (relational) while the image binary lives in Storage.
Output: A photo service, a reactive photos hook, the gallery + comparison components, and the wired Photos segment (replacing the 03-03 placeholder).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-templates-programs-progress/03-RESEARCH.md
@.planning/phases/03-templates-programs-progress/03-PATTERNS.md
@.planning/phases/03-templates-programs-progress/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: photoService (pick -> compress -> upload -> metadata) + useProgressPhotos hook</name>
  <files>src/services/photoService.ts, src/hooks/useProgressPhotos.ts</files>
  <read_first>
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Pattern 7 progress photo upload exact flow; Storage path progress-photos/{userId}/{iso-date}.jpg; private bucket; Security — image-manipulator re-encodes to strip payloads; Pitfall 6)
    - src/lib/supabase.ts (supabase client singleton for Storage)
    - src/services/sessionService.ts (PowerSync metadata-write half: writeTransaction + Crypto.randomUUID + INSERT OR REPLACE; never check rowsAffected)
    - src/lib/schema.ts (progress_photos table shape from 03-01: id, user_id, taken_on, storage_path, created_at)
  </read_first>
  <action>
    Create src/services/photoService.ts exporting `uploadProgressPhoto({ assetUri, userId })`: run expo-image-manipulator resize to max 1200px + JPEG quality 0.8 (RESEARCH Pattern 7 — re-encode strips arbitrary payloads, security mitigation), build path `${userId}/${isoDate}.jpg`, fetch the manipulated uri to an arrayBuffer, `supabase.storage.from('progress-photos').upload(path, buf, { contentType: 'image/jpeg', upsert: true })`, then INSERT a progress_photos row (id via Crypto.randomUUID, user_id, taken_on = isoDate, storage_path = path) via a PowerSync write (PHOTO-01). Also export `deleteProgressPhoto({ id, storagePath })` (remove the Storage object + delete/soft-delete the metadata row). Create src/hooks/useProgressPhotos.ts: reactive usePowerSyncQuery of progress_photos for the user ordered by taken_on desc, resolving each row's Storage signed/public URL for display (PHOTO-02). Capture (camera vs library) is chosen in the gallery component via an action sheet — photoService accepts the resulting assetUri.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "progress-photos" src/services/photoService.ts && grep -q "ImageManipulator\|manipulate" src/services/photoService.ts && grep -q "useProgressPhotos" src/hooks/useProgressPhotos.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - photoService.ts compresses via expo-image-manipulator (resize 1200px, quality 0.8) BEFORE upload
    - upload targets bucket `progress-photos` with path `{userId}/{iso-date}.jpg` and contentType image/jpeg
    - a progress_photos metadata row is inserted with a Crypto.randomUUID id and the storage_path
    - useProgressPhotos returns rows ordered by taken_on descending with a resolved display URL
    - `npx tsc --noEmit` reports no new errors in the service/hook
  </acceptance_criteria>
  <done>A picked/captured photo compresses, uploads to the private bucket, and creates a chronological metadata row.</done>
</task>

<task type="auto">
  <name>Task 2: ProgressPhotoGallery + ProgressPhotoComparison + wire Photos placeholder</name>
  <files>src/components/ProgressPhotoGallery/index.tsx, src/components/ProgressPhotoComparison/index.tsx, src/components/progress/PhotosSegmentPlaceholder.tsx</files>
  <read_first>
    - app/(tabs)/progress.tsx (how the Photos placeholder is imported — gallery must slot in with no progress.tsx edit)
    - src/components/progress/PhotosSegmentPlaceholder.tsx (placeholder from 03-03 — overwrite to render ProgressPhotoGallery)
    - src/components/ExerciseSwapModal/index.tsx (Modal + FlashList grid shell analog for the comparison modal)
    - src/components/Button/index.tsx + src/components/Spinner/index.tsx (upload button + uploading state)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (ProgressPhotoGallery 3-col 80x80 grid, ProgressPhotoComparison spec, Photos copywriting incl action sheet + delete confirmation, "Progress Photo Upload + Comparison Selection" interaction, accessibility, Icon Inventory Camera/X)
    - src/services/photoService.ts + src/hooks/useProgressPhotos.ts (Task 1)
  </read_first>
  <action>
    Build ProgressPhotoGallery: "Upload photo" primary Button at top -> action sheet "Take photo" / "Choose from library" / "Cancel" -> expo-image-picker launchCameraAsync / launchImageLibraryAsync -> uploadProgressPhoto; show "Uploading..." + Spinner while in flight (button disabled); on error HelperText "Upload failed. Try again." Render a 3-column FlashList grid (numColumns 3) of 80x80 thumbnails with date captions (PHOTO-02). Long-press a thumbnail -> action sheet "Select for comparison" / "Delete photo" / "Cancel" (delete -> Alert.alert "Delete photo?" / "This can't be undone." -> deleteProgressPhoto). "Select for comparison" enters selection mode with the bottom prompt "Now select a second photo to compare." and opens ProgressPhotoComparison on the second pick. Build ProgressPhotoComparison: full-screen modal, two images side-by-side (left "Before" earlier date, right "After" later date), date captions, dismiss X (PHOTO-03). Empty state "No photos yet" / "Tap Upload to add your first progress photo." Then overwrite src/components/progress/PhotosSegmentPlaceholder.tsx to render ProgressPhotoGallery WITHOUT editing app/(tabs)/progress.tsx. Use exact UI-SPEC copy, sizing, tokens, accessibility labels.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "FlashList" src/components/ProgressPhotoGallery/index.tsx && grep -q "launchCamera\|launchImageLibrary\|ImagePicker" src/components/ProgressPhotoGallery/index.tsx && grep -q "ProgressPhotoGallery" src/components/progress/PhotosSegmentPlaceholder.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ProgressPhotoGallery renders a 3-column FlashList grid (no FlatList) of thumbnails with date captions
    - upload action sheet offers Take photo / Choose from library and calls uploadProgressPhoto; uploading shows Spinner + "Uploading..."
    - long-press offers Select for comparison / Delete photo; delete uses the exact Alert.alert copy and calls deleteProgressPhoto
    - selecting two photos opens ProgressPhotoComparison with Before (earlier) left / After (later) right
    - PhotosSegmentPlaceholder.tsx renders ProgressPhotoGallery; app/(tabs)/progress.tsx is NOT modified by this plan
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Photos segment uploads, lists chronologically, and supports two-date comparison.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Supabase Storage | progress photo binary upload crosses here |
| device camera/library -> app | untrusted image bytes enter the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | progress-photos bucket | mitigate | private bucket + folder-scoped storage RLS `(storage.foldername(name))[1] = auth.uid()::text` (created in 03-01); path is `{userId}/...` |
| T-03-12 | Tampering | malformed image upload | mitigate | expo-image-manipulator re-encodes to JPEG (strips arbitrary payloads); explicit contentType image/jpeg |
| T-03-13 | Information Disclosure | display URL resolution | mitigate | private bucket served via signed/scoped URL; never expose other users' folder paths |
</threat_model>

<verification>
- A captured/picked photo uploads to progress-photos/{userId}/{date}.jpg and appears in the grid
- Two-date comparison renders before/after correctly
- `npx tsc --noEmit` no new errors
</verification>

<success_criteria>
PHOTO-01/02/03 satisfied: upload (camera + library) with compression, chronological grid, two-date comparison — all against a private, folder-scoped bucket.
</success_criteria>

<output>
Create `.planning/phases/03-templates-programs-progress/03-05-SUMMARY.md` when done.
</output>
