---
phase: 03-templates-programs-progress
plan: 07
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-06"]
files_modified:
  - supabase/functions/generate-program/index.ts
  - app/(tabs)/split.tsx
  - app/(tabs)/workouts.tsx
  - app/program-builder/[id].tsx
  - app/program-review.tsx
  - src/hooks/usePrograms.ts
  - src/services/programService.ts
  - src/services/sessionService.ts
  - src/lib/programSchema.ts
  - src/lib/deload.ts
  - src/components/ProgramSection/index.tsx
  - src/components/ProgramWeekGrid/index.tsx
  - src/components/AIGenerationSheet/index.tsx
  - src/components/DeloadBanner/index.tsx
  - tests/integration/generateProgram.test.ts
autonomous: true
requirements: [PROGRAM-01, PROGRAM-02, PROGRAM-03, PROGRAM-04, PROGRAM-05, PROGRAM-06, PROGRAM-07]

must_haves:
  truths:
    - "User can create a multi-week program manually (name, weeks, template per week, deload week flag) from the Split tab"
    - "User can generate a multi-week program via Claude (real ungated Edge Function), review it as a week grid, then Accept / Regenerate / Edit"
    - "An accepted active program auto-advances its current week when a session is completed"
    - "After N weeks (default 4) in a phase the Workouts tab surfaces a deload suggestion; a manual Split-settings toggle overrides it; deload reduces suggested weights to ~65%"
  artifacts:
    - path: "supabase/functions/generate-program/index.ts"
      provides: "JWT-validated Claude program generation (D-09, ungated)"
      contains: "auth.getUser"
    - path: "src/services/programService.ts"
      provides: "program save + week advance writeTransaction"
      exports: ["saveProgram", "advanceProgramWeek"]
    - path: "src/lib/programSchema.ts"
      provides: "zod ProgramSchema for Claude output validation"
      exports: ["ProgramSchema"]
    - path: "src/components/DeloadBanner/index.tsx"
      provides: "deload suggestion banner (PROGRAM-05/06)"
  key_links:
    - from: "src/components/AIGenerationSheet/index.tsx"
      to: "supabase.functions"
      via: "invoke generate-program"
      pattern: "generate-program"
    - from: "src/services/sessionService.ts"
      to: "src/services/programService.ts"
      via: "advanceProgramWeek inside completeSession transaction"
      pattern: "advanceProgramWeek"
    - from: "app/(tabs)/workouts.tsx"
      to: "src/components/DeloadBanner/index.tsx"
      via: "render in the DeloadBanner slot below TodayCard"
      pattern: "DeloadBanner"
---

<objective>
Deliver the Programs + deload vertical slice (D-09/10/11): a real ungated Claude Edge Function for AI program generation, the Split-tab Programs section with manual + AI CTAs, the AI generation sheet + week-grid review (Accept/Regenerate/Edit), the manual program builder, program-week auto-advance on session completion, the split phase display (PROGRAM-04), and deload detection + manual override + 65% weight reduction (PROGRAM-05/06/07).

Purpose: Programs turn single templates into periodized multi-week training and are the last major engagement surface of the phase. The Edge Function is built ungated now (D-09) so Phase 4 only adds the premium gate.
Output: The generate-program Edge Function (+ Deno integration test), the Programs UI (Split tab section, generation sheet, review + manual builder, week grid), program service + zod schema + hook, week-advance wired into completeSession, and the DeloadBanner + deload logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-templates-programs-progress/03-RESEARCH.md
@.planning/phases/03-templates-programs-progress/03-PATTERNS.md
@.planning/phases/03-templates-programs-progress/03-UI-SPEC.md
@.planning/phases/03-templates-programs-progress/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: generate-program Edge Function + zod ProgramSchema + Deno integration test</name>
  <files>supabase/functions/generate-program/index.ts, src/lib/programSchema.ts, tests/integration/generateProgram.test.ts</files>
  <read_first>
    - supabase/functions/migrate-v1-user/index.ts (JWT-validate skeleton lines 117-152, try/catch envelope, env-read of secrets — copy verbatim)
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Pattern 6 Edge Function; Program JSON schema; Pitfall 5 prose-around-JSON -> zod-validate + 422; Open Question 2 SDK vs raw fetch; A3 model id verify; A4 npm-in-Deno; Security V5 clamp weeks/max_tokens, prompt-injection -> treat output as untrusted)
    - .planning/phases/03-templates-programs-progress/03-PATTERNS.md (generate-program analog section; client invocation supabase.functions.invoke)
    - src/lib/supabase.ts (functions.invoke from the client)
  </read_first>
  <action>
    Create src/lib/programSchema.ts exporting a zod `ProgramSchema` matching RESEARCH Pattern 6 ({ name, weeks: [{ weekNumber, isDeload, days: [{ dayLabel, templateName|null }] }] }) — shared shape the client review grid expects. Create supabase/functions/generate-program/index.ts (D-09 / PROGRAM-02): copy the migrate-v1-user JWT-validation skeleton (anon client + auth.getUser, 401 on missing/invalid), then call Claude with goal/currentLifts/weeks inputs. Per RESEARCH Open Question 2 use raw `fetch` to api.anthropic.com/v1/messages with prompt-enforced JSON as the robust baseline (SDK as optional enhancement if npm: imports cleanly). Treat Claude output as untrusted: JSON.parse then zod-validate against ProgramSchema; return 422 on validation failure (Pitfall 5). Clamp `weeks` to a sane max (e.g. 1..16) and bound max_tokens (Security V5 / DoS). Read ANTHROPIC_API_KEY from Deno.env (never client). Leave a clearly-commented insertion point `// Phase 4: if (!isPremium) return 402` (D-09) — do NOT add the gate now. Add tests/integration/generateProgram.test.ts: a Deno/integration test asserting the function returns a schema-valid program for a valid JWT and 401 without auth and 422 on malformed model output (mock the Claude call).
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "auth.getUser" supabase/functions/generate-program/index.ts && grep -q "ProgramSchema" supabase/functions/generate-program/index.ts src/lib/programSchema.ts && grep -q "402" supabase/functions/generate-program/index.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - generate-program validates the JWT via anon client + auth.getUser and returns 401 when missing/invalid
    - the function zod-validates Claude output against ProgramSchema and returns 422 on failure
    - `weeks` is clamped and ANTHROPIC_API_KEY is read from Deno.env only (not present in any client file)
    - a commented `// Phase 4: if (!isPremium) return 402` insertion point exists; no premium gate is active (D-09)
    - tests/integration/generateProgram.test.ts asserts valid-JWT success, 401 no-auth, and 422 malformed-output
  </acceptance_criteria>
  <done>The Edge Function generates and zod-validates a structured program for authenticated users, ungated, with a Phase-4 gate seam.</done>
</task>

<task type="auto">
  <name>Task 2: programService + week advance in completeSession + usePrograms + deload logic</name>
  <files>src/services/programService.ts, src/services/sessionService.ts, src/hooks/usePrograms.ts, src/lib/deload.ts</files>
  <read_first>
    - src/services/sessionService.ts (completeSession writeTransaction — add advanceProgramWeek + deload-suggestion check in the SAME transaction/flow; skipDay single-statement counter analog)
    - src/services/templateService.ts (saveTemplate writeTransaction pattern from 03-02 to mirror for saveProgram)
    - src/hooks/useSessionData.ts (split_settings read: phase, weeks_in_phase, deload_active already exist)
    - src/components/ProgressiveOverloadHint/index.tsx (Phase 2 — extend to apply a 0.65 multiplier when deload active, PROGRAM-07)
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (PROGRAM-03 current_week increment; PROGRAM-05 weeks_in_phase + MMKV deload_suggested; PROGRAM-06 split_settings.deload_active; PROGRAM-07 0.65 multiplier)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md ("Deload Suggestion Trigger + Toggle" interaction: MMKV deload_suggested/deload_acknowledged, deload_active, 65%)
  </read_first>
  <action>
    Create src/services/programService.ts: `saveProgram` (writeTransaction upserting a programs row + replacing program_weeks rows keyed by client UUID — PROGRAM-01; manual + accepted-AI both use this) and `advanceProgramWeek(programId)` (UPDATE programs SET current_week = current_week + 1 — PROGRAM-03). In src/services/sessionService.ts completeSession: INSIDE the existing writeTransaction (which 03-06 already extended), if an active program exists, call the advanceProgramWeek UPDATE so the week pointer co-commits with the session; also evaluate the deload-suggestion rule (weeks_in_phase >= N default 4) and write MMKV `deload_suggested = true` when crossed (UI hint, not a training record — PROGRAM-05). Create src/lib/deload.ts: pure helpers `shouldSuggestDeload(weeksInPhase, n=4)` and `applyDeloadMultiplier(weightKg)` returning 0.65*weight (PROGRAM-07); extend ProgressiveOverloadHint to use applyDeloadMultiplier when split_settings.deload_active. Create src/hooks/usePrograms.ts: reactive read of the user's programs + program_weeks (active program, current week) plus a `setManualDeload(active)` writer toggling split_settings.deload_active + MMKV (PROGRAM-06).
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "advanceProgramWeek" src/services/programService.ts src/services/sessionService.ts && grep -q "current_week" src/services/programService.ts && grep -q "0.65\|applyDeloadMultiplier" src/lib/deload.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - programService exports saveProgram (writeTransaction upserting programs + program_weeks) and advanceProgramWeek (current_week + 1)
    - completeSession calls the program week advance INSIDE its writeTransaction when an active program exists (co-commit)
    - src/lib/deload.ts exports shouldSuggestDeload (default n=4) and applyDeloadMultiplier (0.65) and is pure (no PowerSync import)
    - ProgressiveOverloadHint applies the 0.65 multiplier when split_settings.deload_active
    - usePrograms exposes setManualDeload toggling split_settings.deload_active (PROGRAM-06)
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Programs save, the active program advances on completion, and deload detection + manual override + 65% reduction work.</done>
</task>

<task type="auto">
  <name>Task 3: Split-tab Programs section + AI sheet + review + manual builder + week grid + DeloadBanner</name>
  <files>app/(tabs)/split.tsx, app/(tabs)/workouts.tsx, app/program-builder/[id].tsx, app/program-review.tsx, src/components/ProgramSection/index.tsx, src/components/ProgramWeekGrid/index.tsx, src/components/AIGenerationSheet/index.tsx, src/components/DeloadBanner/index.tsx</files>
  <read_first>
    - app/(tabs)/split.tsx (existing split type + phase display to keep; add Programs section below — PROGRAM-04 already shows phase)
    - app/(tabs)/workouts.tsx (the DeloadBanner slot below TodayCard left by 03-02 — render DeloadBanner there now)
    - app/(session)/index.tsx (full-screen route shell for program-builder + program-review)
    - src/components/Button/index.tsx + src/components/Toggle/index.tsx + src/components/HelperText/index.tsx (CTAs, manual deload toggle, error copy)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (ProgramSection/ProgramWeekGrid/AIGenerationSheet/AIProgramReviewScreen/DeloadBanner specs, Split Tab + Programs layout, AI Program Generation Flow interaction, Programs + AI Generation Sheet + AI Program Review + Deload Banner copywriting, Icon Inventory Sparkles/ListPlus/AlertTriangle)
    - src/services/programService.ts + src/hooks/usePrograms.ts + src/lib/programSchema.ts (Tasks 1-2)
    - src/lib/supabase.ts (functions.invoke('generate-program'))
  </read_first>
  <action>
    Add ProgramSection to app/(tabs)/split.tsx (D-10): below the existing split type + phase (PROGRAM-04) display, a "Programs" section — no-program state = "Generate with AI" (Sparkles, primary) + "Create manually" (ListPlus, secondary) CTAs; has-program state = active program summary + compact ProgramWeekGrid (active variant, current week accent-dim) + Edit/Change. Also add the manual-deload Toggle to the Split tab (PROGRAM-06) wired to usePrograms.setManualDeload. Build AIGenerationSheet (bottom sheet: goal / benchmark lifts / duration fields; "Generate program" -> supabase.functions.invoke('generate-program') -> on success push program-review with the returned program; loading "Claude is building your program..."; error HelperText "Program generation failed. Try again."). Build app/program-review.tsx (D-11): ProgramWeekGrid (review variant) + sticky bottom bar with full-width Accept program (saveProgram -> active, navigate back to Split) / Regenerate program (re-invoke same inputs) / Edit manually (push program-builder pre-filled). Build app/program-builder/[id].tsx (manual create/edit per PROGRAM-01: name, weeks, template-per-week, deload-week flag; saveProgram). Build ProgramWeekGrid (per-week rows, day cells with day name + workout name; deload week shows the "Deload week" inline label). Build DeloadBanner (auto + manual variants, AlertTriangle accent, Override ghost button -> Alert.alert per UI-SPEC copy; dismiss X persists deload_acknowledged to MMKV) and render it in the workouts.tsx slot below TodayCard (auto when MMKV deload_suggested && !acknowledged, manual when split_settings.deload_active — PROGRAM-05/06). Use exact UI-SPEC copy/tokens/sizing/haptics.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "generate-program" src/components/AIGenerationSheet/index.tsx && grep -q "ProgramSection" app/\(tabs\)/split.tsx && grep -q "DeloadBanner" app/\(tabs\)/workouts.tsx && grep -q "saveProgram" app/program-review.tsx app/program-builder/\[id\].tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Split tab renders ProgramSection (Generate with AI + Create manually CTAs) below the split type + phase display and a manual-deload Toggle wired to setManualDeload
    - AIGenerationSheet invokes `generate-program` and on success pushes app/program-review.tsx with the returned program; error shows the exact HelperText copy
    - program-review Accept calls saveProgram and returns to Split; Regenerate re-invokes; Edit pushes program-builder pre-filled
    - program-builder supports name/weeks/template-per-week/deload-week-flag and calls saveProgram (PROGRAM-01)
    - DeloadBanner renders in the workouts.tsx slot below TodayCard with auto + manual variants and the exact Override copy
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Manual + AI program building, review/accept/regenerate/edit, the phase display, and the deload banner all work end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> generate-program Edge Function | user goal/lifts inputs cross to the server and into a Claude prompt |
| Edge Function -> Anthropic API | server-only Claude call; API key never client-side |
| Claude output -> client | model output is untrusted until zod-validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Information Disclosure | ANTHROPIC_API_KEY | mitigate | key only in Edge Function Deno.env (set via supabase secrets); never in any client file (AI-01) |
| T-03-18 | Tampering | prompt injection via goal/lifts | mitigate | treat Claude output as untrusted — JSON.parse + zod-validate ProgramSchema, never eval/execute; 422 on failure (Pitfall 5) |
| T-03-19 | Denial of Service | unbounded AI cost (ungated in Phase 3) | mitigate | clamp `weeks` (1..16) + bound max_tokens; JWT required (no anonymous calls); Phase 4 adds premium gate + rate limit at the marked seam |
| T-03-20 | Spoofing | unauthenticated program generation | mitigate | auth.getUser JWT validation before any Claude call (401 otherwise) |
</threat_model>

<verification>
- `grep auth.getUser supabase/functions/generate-program/index.ts` present; tests/integration/generateProgram.test.ts passes (401/422/success)
- Generate with AI -> review grid -> Accept makes the program active in the Split tab
- Completing a session with an active program increments current_week
- Deload banner appears after N weeks; manual toggle overrides; suggested weights drop to 65%
- `npx tsc --noEmit` no new errors
</verification>

<success_criteria>
PROGRAM-01..07 satisfied: manual + AI program building (real ungated Edge Function), review flow, auto week-advance, retained phase system, deload detection + manual override + 65% reduction.
</success_criteria>

<output>
Create `.planning/phases/03-templates-programs-progress/03-07-SUMMARY.md` when done.
</output>
