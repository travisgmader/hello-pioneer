---
phase: 03-templates-programs-progress
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - src/components/progress/ChartsSegmentPlaceholder.tsx
  - src/components/ChartScreen/index.tsx
  - src/components/WeightProgressionChart/index.tsx
  - src/components/VolumeChart/index.tsx
  - src/components/MeasurementChart/index.tsx
  - src/components/ExercisePicker/index.tsx
  - src/components/DateRangeControl/index.tsx
  - src/hooks/useChartData.ts
  - src/hooks/useMeasurements.ts
  - src/hooks/useSessionData.ts
  - src/lib/volumeBucket.ts
  - src/lib/__tests__/volumeBucket.test.ts
autonomous: true
requirements: [PROGRESS-02, PROGRESS-03, PROGRESS-04]

must_haves:
  truths:
    - "Charts segment shows a weight-progression line chart for a picked exercise, filterable by date range (30d/90d/1y/All)"
    - "Charts segment shows a volume bar chart (sets x reps x weight bucketed per ISO week)"
    - "A measurement history chart (weight / body fat toggle) renders below the exercise charts"
    - "Editing a session in history (03-03) is reflected in these charts immediately on return"
  artifacts:
    - path: "src/components/ChartScreen/index.tsx"
      provides: "Charts segment content (picker + range + 3 charts)"
      contains: "ExercisePicker"
    - path: "src/lib/volumeBucket.ts"
      provides: "pure bucketVolumeByWeek (unit-tested)"
      exports: ["bucketVolumeByWeek"]
    - path: "src/hooks/useChartData.ts"
      provides: "weight-progression + volume aggregation reads"
      exports: ["useWeightProgression", "useVolumeByWeek"]
    - path: "src/components/WeightProgressionChart/index.tsx"
      provides: "Victory Native XL line chart"
      contains: "CartesianChart"
  key_links:
    - from: "src/components/progress/ChartsSegmentPlaceholder.tsx"
      to: "src/components/ChartScreen/index.tsx"
      via: "re-export the real Charts segment"
      pattern: "ChartScreen"
    - from: "src/components/WeightProgressionChart/index.tsx"
      to: "src/lib/tokens.ts"
      via: "chart colors via tokens (no inline hex)"
      pattern: "COLOR_CHART_LINE"
    - from: "src/components/VolumeChart/index.tsx"
      to: "src/lib/volumeBucket.ts"
      via: "bucketVolumeByWeek"
      pattern: "bucketVolumeByWeek"
---

<objective>
Build the Charts segment of the Progress tab (D-07): an ExercisePicker + DateRangeControl driving a Victory Native XL weight-progression line chart and a volume bar chart, plus a separate measurement-history chart (weight / body fat) below — all reading locally so history edits reflect immediately.

Purpose: Charts are the analytical payoff of all logged data and the first use of Victory Native XL + Skia in the app. The pure volume-bucketing helper turns the last RED test GREEN, and the measured_at carry-forward bug is fixed here where measurements are first read.
Output: The Charts segment content (replacing the 03-03 placeholder), three chart components, picker + range controls, two data hooks, a measurements hook, and the pure volume-bucket helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-templates-programs-progress/03-RESEARCH.md
@.planning/phases/03-templates-programs-progress/03-PATTERNS.md
@.planning/phases/03-templates-programs-progress/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure volume-bucket helper + useChartData + useMeasurements hooks (fix measured_at bug)</name>
  <files>src/lib/volumeBucket.ts, src/hooks/useChartData.ts, src/hooks/useMeasurements.ts, src/hooks/useSessionData.ts, src/lib/__tests__/volumeBucket.test.ts</files>
  <read_first>
    - src/hooks/useSessionData.ts (usePowerSyncQuery reads; usePreviousPerformance aggregation analog; the CARRY-FORWARD BUG — measurements order column is measured_at NOT logged_at; useLatestBodyweight currently queries logged_at and must be fixed — PATTERNS line 179)
    - src/lib/sessionStats.ts (pure-helper structure to mirror)
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Code Example 3 volume-per-ISO-week; Pitfall 1 measured_at fix; Patterns 1-2 data shape — yKeys must be NUMERIC, dates pre-converted to epoch ms)
    - src/lib/__tests__/volumeBucket.test.ts (RED test from 03-01 — make GREEN)
  </read_first>
  <behavior>
    - bucketVolumeByWeek(rows): rows are {weight_kg, reps_target, completed_at}; volume = weight_kg * reps_target; sum into startOfISOWeek buckets; return [{x: weekIndex, y: totalVolume}] ascending; warmups already excluded by the caller's SQL (is_warmup = 0).
  </behavior>
  <action>
    Create src/lib/volumeBucket.ts exporting pure `bucketVolumeByWeek` using date-fns startOfISOWeek (PROGRESS-03; RESEARCH Code Example 3); make the RED volumeBucket test GREEN. Create src/hooks/useChartData.ts exporting `useWeightProgression(exerciseId, range)` (sessions JOIN session_sets for the exercise, max weight_kg per session, mapped to {x: epochMs, y: maxWeight} — PROGRESS-02) and `useVolumeByWeek(exerciseId, range)` (raw non-warmup working sets in range -> bucketVolumeByWeek). Map ranges 30d/90d/1y/All to a sinceIso lower bound. Create src/hooks/useMeasurements.ts: reactive read of the measurements table ordered by `measured_at` (NOT logged_at) returning weight and body-fat series (PROGRESS-04), and FIX the existing useLatestBodyweight query inside useSessionData.ts to order by measured_at (RESEARCH Pitfall 1 carry-forward bug). All chart hooks return NUMERIC x/y (dates as epoch ms) so Victory yKeys never receive strings.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && npx vitest run src/lib/__tests__/volumeBucket.test.ts && ! grep -q "logged_at" src/hooks/useMeasurements.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/volumeBucket.test.ts` exits 0 (GREEN)
    - src/lib/volumeBucket.ts exports `bucketVolumeByWeek`, imports date-fns, and has no '@powersync/react-native' import
    - useMeasurements.ts orders by `measured_at` and contains no `logged_at`
    - the useLatestBodyweight query in useSessionData.ts orders the measurements read by measured_at (no `logged_at` against measurements)
    - useChartData exports useWeightProgression + useVolumeByWeek returning numeric x (epoch ms) and numeric y
  </acceptance_criteria>
  <done>Volume bucketing is GREEN, chart hooks return numeric series, and the measured_at latent bug is fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Victory Native XL chart components + ExercisePicker + DateRangeControl</name>
  <files>src/components/WeightProgressionChart/index.tsx, src/components/VolumeChart/index.tsx, src/components/MeasurementChart/index.tsx, src/components/ExercisePicker/index.tsx, src/components/DateRangeControl/index.tsx</files>
  <read_first>
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Pattern 1 line chart with useFont guard; Pattern 2 bar chart needs chartBounds; Anti-Patterns — guard render until useFont resolves, never pass string x to yKeys; verify Line/Bar props against installed victory-native@41 types A1)
    - src/lib/tokens.ts + src/lib/chartStyles.ts (chart colors + axis styles — import these, NO inline hex)
    - assets/fonts/Manrope-Medium.ttf (the useFont asset from 03-01)
    - src/components/Chip/index.tsx (DateRangeControl chips + ExercisePicker selected pattern)
    - src/components/Toggle/index.tsx (MeasurementChart weight/body-fat toggle)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (WeightProgressionChart/VolumeChart/MeasurementChart/ExercisePicker/DateRangeControl specs, chart titles + empty-state copy, 200pt min height, chart accessibilityLabel requirements, Reduce Motion animate=false)
  </read_first>
  <action>
    Build WeightProgressionChart and VolumeChart using victory-native CartesianChart with Line / Bar respectively (RESEARCH Patterns 1-2). Load the axis font via `useFont(require('@/assets/fonts/Manrope-Medium.ttf'), FONT_SIZE_CHART_AXIS)`; guard rendering with a Spinner/empty state until font is non-null. Style stroke/fill from tokens (COLOR_CHART_LINE for the line, COLOR_CHART_BAR for bars) and axes from chartStyles — no inline hex. Bar reads chartBounds from the render-prop context. Pass `animate={false}` when isReducedMotionEnabled. Each chart sets the UI-SPEC accessibilityLabel. Build MeasurementChart (chart-measurement blue line, Toggle weight/body-fat, own DateRangeControl state, empty state "No measurements recorded. Add one in Settings."). Build ExercisePicker (collapsed trigger + dropdown of exercises the user has logged; combobox a11y) and DateRangeControl (4 Chips 30d/90d/1y/All, default 90d, selected = accent-dim + border-strong). Confirm the exact Line/Bar prop names against the installed victory-native@41 types before finalizing (RESEARCH A1).
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "CartesianChart" src/components/WeightProgressionChart/index.tsx && grep -q "useFont" src/components/WeightProgressionChart/index.tsx && grep -q "COLOR_CHART_LINE" src/components/WeightProgressionChart/index.tsx && grep -q "chartBounds" src/components/VolumeChart/index.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - WeightProgressionChart and VolumeChart import `CartesianChart` from 'victory-native' and guard on `useFont` returning null
    - chart colors come from src/lib/tokens.ts (COLOR_CHART_LINE / COLOR_CHART_BAR / COLOR_CHART_MEASUREMENT) — no raw hex in chart style props
    - VolumeChart Bar uses `chartBounds` from the render-prop context
    - each chart sets an `accessibilityLabel`; charts pass `animate={false}` under reduced motion
    - DateRangeControl renders 30d/90d/1y/All with 90d default; MeasurementChart toggles weight/body-fat
    - `npx tsc --noEmit` reports no new errors in the chart components
  </acceptance_criteria>
  <done>Three Victory Native XL charts render from numeric series with token-driven styling, font-guarded, and accessible.</done>
</task>

<task type="auto">
  <name>Task 3: ChartScreen segment assembly + wire into Progress Charts placeholder</name>
  <files>src/components/ChartScreen/index.tsx, src/components/progress/ChartsSegmentPlaceholder.tsx</files>
  <read_first>
    - app/(tabs)/progress.tsx (how the Charts placeholder is imported — ChartScreen must slot in with no progress.tsx edit)
    - src/components/progress/ChartsSegmentPlaceholder.tsx (the placeholder created in 03-03 — overwrite to render ChartScreen)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (Charts Segment layout: ExercisePicker -> DateRangeControl -> Weight progression -> Volume -> Divider -> Measurements; ScrollView not FlashList; empty/loading/error states + exact titles)
    - src/components/WeightProgressionChart, VolumeChart, MeasurementChart, ExercisePicker, DateRangeControl (Task 2)
    - src/hooks/useChartData.ts + src/hooks/useMeasurements.ts (Task 1)
  </read_first>
  <action>
    Build src/components/ChartScreen/index.tsx as the Charts segment content (D-07): a ScrollView (NOT FlashList — fixed layout) with ExercisePicker at top, DateRangeControl, then "Weight progression" line chart, "Volume (sets x reps x weight / week)" bar chart, a Divider, and the "Measurements" MeasurementChart section. Wire picker + range into useWeightProgression / useVolumeByWeek. Empty states per UI-SPEC: no exercise selected -> "Select an exercise above to see your progress."; exercise selected but no data -> "No data for {exerciseName} yet. Log a set to start tracking." Then overwrite src/components/progress/ChartsSegmentPlaceholder.tsx to render ChartScreen so the Progress Charts segment shows the real charts WITHOUT editing app/(tabs)/progress.tsx (progress.tsx stays owned by 03-03).
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "ChartScreen" src/components/progress/ChartsSegmentPlaceholder.tsx && grep -q "ExercisePicker" src/components/ChartScreen/index.tsx && ! grep -q "FlashList" src/components/ChartScreen/index.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ChartScreen/index.tsx composes ExercisePicker, DateRangeControl, WeightProgressionChart, VolumeChart, Divider, MeasurementChart in a ScrollView (no FlashList)
    - chart titles match the UI-SPEC exactly ("Weight progression", "Volume (sets x reps x weight / week)", "Measurements")
    - empty-state copy matches the UI-SPEC exactly
    - ChartsSegmentPlaceholder.tsx renders ChartScreen and app/(tabs)/progress.tsx is NOT modified by this plan
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>The Progress Charts segment shows weight progression, volume, and measurement charts with picker + range controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PowerSync local SQLite | chart data is read locally; no network call during render |
| npm -> bundle | victory-native + Skia native code (legitimacy approved in 03-01 Task 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial of Service | chart render on large history | accept | Skia canvas render of locally-queried, range-bounded data; DateRangeControl bounds row counts; no per-render network |
| T-03-11 | Information Disclosure | chart data reads | accept | usePowerSyncQuery reads are RLS-scoped to the user's own synced rows; no cross-user query path |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/volumeBucket.test.ts` GREEN
- Charts segment renders 3 charts; picking an exercise + changing range updates the line/bar charts
- Editing a session weight in history (03-03) then returning to Charts shows the updated value
- measured_at bug fixed (no logged_at against measurements anywhere)
- `npx tsc --noEmit` no new errors
</verification>

<success_criteria>
PROGRESS-02/03/04 satisfied: weight progression line chart (range-filterable), volume bar chart (ISO-week buckets), and measurement history chart, all reactive to history edits.
</success_criteria>

<output>
Create `.planning/phases/03-templates-programs-progress/03-04-SUMMARY.md` when done.
</output>
