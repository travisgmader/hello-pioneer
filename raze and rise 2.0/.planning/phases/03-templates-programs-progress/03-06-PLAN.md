---
phase: 03-templates-programs-progress
plan: 06
type: execute
wave: 2
depends_on: ["03-01", "03-03"]
files_modified:
  - src/lib/badgeRules.ts
  - src/services/sessionService.ts
  - src/hooks/useBadges.ts
  - src/components/progress/AchievementsSegmentPlaceholder.tsx
  - src/components/BadgeGrid/index.tsx
  - src/components/BadgeCard/index.tsx
  - src/components/BadgeLockIcon/index.tsx
  - src/components/BadgeUnlockToast/index.tsx
  - app/(session)/index.tsx
  - app/(tabs)/index.tsx
  - src/lib/__tests__/badgeRules.test.ts
autonomous: true
requirements: [GAMIFY-01, GAMIFY-03]

must_haves:
  truths:
    - "Completing a session detects newly-crossed milestones (first/10/50/100 workouts, 30-day streak, first PR, 10k lbs) against cheap running counters and inserts a badge row idempotently"
    - "Achievements segment shows the StreakCounter + a 3-column badge grid with unlocked badges first and locked badges visually distinct"
    - "After the Anubis animation completes, if a milestone was hit, a one-time BadgeUnlockToast appears on the Dashboard"
  artifacts:
    - path: "src/lib/badgeRules.ts"
      provides: "pure milestone threshold rules + detectNewBadges (unit-tested)"
      exports: ["detectNewBadges", "BADGE_RULES"]
    - path: "src/hooks/useBadges.ts"
      provides: "reactive badges read + unlocked/locked merge with badge catalog"
      exports: ["useBadges"]
    - path: "src/components/BadgeUnlockToast/index.tsx"
      provides: "post-Anubis one-time toast (D-13)"
    - path: "src/components/BadgeGrid/index.tsx"
      provides: "Achievements segment content"
      contains: "BadgeCard"
  key_links:
    - from: "src/services/sessionService.ts"
      to: "src/lib/badgeRules.ts"
      via: "detectNewBadges inside completeSession writeTransaction"
      pattern: "detectNewBadges"
    - from: "app/(tabs)/index.tsx"
      to: "src/components/BadgeUnlockToast/index.tsx"
      via: "mount on newBadge nav param after Anubis"
      pattern: "newBadge"
    - from: "src/components/progress/AchievementsSegmentPlaceholder.tsx"
      to: "src/components/BadgeGrid/index.tsx"
      via: "re-export real Achievements segment"
      pattern: "BadgeGrid"
---

<objective>
Build gamification (D-12 scope: badges GAMIFY-01 + streak GAMIFY-03 only): a pure milestone-rules module, badge detection wired into the existing completeSession writeTransaction against cheap running counters (user_stats + streak), the Achievements segment (StreakCounter + badge grid), and the one-time post-Anubis BadgeUnlockToast (D-13).

Purpose: Badges reward real milestones and close the gamification requirements for this phase. Detection MUST be O(1)/O(log n) against running counters — never a full-history scan per session (RESEARCH Pattern 8).
Output: badgeRules (turns RED test GREEN), a modified completeSession (badge detection + user_stats increment + program-week-advance hook left for 03-07), the badges hook, the Achievements segment, four badge components, and the toast wired on the Dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-templates-programs-progress/03-RESEARCH.md
@.planning/phases/03-templates-programs-progress/03-PATTERNS.md
@.planning/phases/03-templates-programs-progress/03-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure badgeRules + detection wired into completeSession + useBadges</name>
  <files>src/lib/badgeRules.ts, src/services/sessionService.ts, src/hooks/useBadges.ts, src/lib/__tests__/badgeRules.test.ts</files>
  <read_first>
    - src/lib/sessionStats.ts (pure-helper structure to mirror)
    - src/services/sessionService.ts (completeSession writeTransaction lines 278-299 — add user_stats increment + badge detection INSIDE the same transaction; never check rowsAffected)
    - src/lib/streak.ts (computeStreak from 03-03 — reuse for the 30-day-streak rule input)
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Pattern 8 badge detection against counters, ON CONFLICT DO NOTHING idempotency, A4/user_stats; avoid full-history scan)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (Badge Names + Unlock Copy table — badge keys/names/copy; toast copy)
    - src/lib/schema.ts (badges + user_stats tables from 03-01)
    - src/lib/__tests__/badgeRules.test.ts (RED test from 03-01 — make GREEN)
  </read_first>
  <behavior>
    - detectNewBadges(counters, ownedKeys): counters = { totalSessions, streakWeeksOrDays, lifetimeVolumeKg, firstPrJustHit }; returns the badge keys whose thresholds are newly crossed AND not already in ownedKeys. Thresholds: first_reps (1 session), ten_down (10), half_century (50), century (100), on_fire (30-day streak), personal_record (first PR), ten_thousand (10000 lbs lifetime). No double-unlock.
  </behavior>
  <action>
    Create src/lib/badgeRules.ts: a `BADGE_RULES` catalog (key, name, threshold predicate) matching the UI-SPEC Badge Names table, and pure `detectNewBadges(counters, ownedKeys)` (GAMIFY-01). Make the RED badgeRules test GREEN. In src/services/sessionService.ts completeSession: INSIDE the existing writeTransaction, increment user_stats (total_sessions + lifetime_volume_kg from the just-completed working sets; set first_pr_at if a PR was hit), read the current owned badge keys + computed counters, call detectNewBadges, and `INSERT ... ON CONFLICT (user_id, badge_key) DO NOTHING` each newly-unlocked badge with unlocked_at = now (RESEARCH Pattern 8 — idempotent, no full scan). Return the first newly-unlocked badge key from completeSession so the session screen can pass it as a nav param. Create src/hooks/useBadges.ts: reactive read of the user's badges merged against BADGE_RULES to yield unlocked (with date) + locked entries for the grid, plus the streak via useStreak for GAMIFY-03 display. Keep streak (date-only) and live go-rate as recompute-on-read; cache totals in user_stats.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && npx vitest run src/lib/__tests__/badgeRules.test.ts && grep -q "detectNewBadges" src/services/sessionService.ts && grep -q "ON CONFLICT" src/services/sessionService.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/badgeRules.test.ts` exits 0 (GREEN)
    - src/lib/badgeRules.ts exports `detectNewBadges` + `BADGE_RULES` with no '@powersync/react-native' import
    - completeSession calls detectNewBadges and inserts badges with `ON CONFLICT (user_id, badge_key) DO NOTHING` INSIDE the writeTransaction, and increments user_stats in the same transaction
    - completeSession does NOT scan full history (no COUNT over session_sets per call beyond the just-completed session's sets)
    - useBadges returns unlocked + locked entries merged from BADGE_RULES
  </acceptance_criteria>
  <done>Milestone rules are GREEN; completeSession unlocks badges idempotently against counters and returns the first new badge key.</done>
</task>

<task type="auto">
  <name>Task 2: Achievements segment (BadgeGrid/BadgeCard/BadgeLockIcon/StreakCounter) + wire placeholder</name>
  <files>src/components/BadgeGrid/index.tsx, src/components/BadgeCard/index.tsx, src/components/BadgeLockIcon/index.tsx, src/components/progress/AchievementsSegmentPlaceholder.tsx</files>
  <read_first>
    - app/(tabs)/progress.tsx (how the Achievements placeholder is imported — BadgeGrid must slot in with no progress.tsx edit)
    - src/components/progress/AchievementsSegmentPlaceholder.tsx (placeholder from 03-03 — overwrite to render BadgeGrid)
    - src/components/StreakCounter/index.tsx (built in 03-03 — reuse at top of BadgeGrid)
    - src/components/Chip/index.tsx (selected/locked visual token patterns)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (BadgeGrid/BadgeCard/BadgeLockIcon specs, badge-unlocked/badge-locked/badge-locked-border tokens, Achievements layout, badge names + accessibility labels, Lock icon 14)
    - src/hooks/useBadges.ts (Task 1)
  </read_first>
  <action>
    Build BadgeGrid (Achievements segment content): StreakCounter pinned at top (reuse from 03-03), then a 3-column grid of BadgeCard sorted unlocked-first (newest unlock top-left), locked last. BadgeCard: 72x72 tap target, unlocked = badge-unlocked (accent) fill + border-strong + name caption + unlock-date caption; locked = badge-locked fill + opacity-50 + badge-locked-border + Lock 14 overlay; pressing an unlocked badge opens a small detail sheet (name, description, "Unlocked {Month D, YYYY}"). BadgeLockIcon: 48x48 SVG wrapper rendering each badge's custom SVG glyph (per UI-SPEC Badge Names table) in locked vs unlocked fill. Accessibility: unlocked button "{name}, unlocked {date}. Tap for details."; locked "{name}, locked". Overwrite src/components/progress/AchievementsSegmentPlaceholder.tsx to render BadgeGrid WITHOUT editing app/(tabs)/progress.tsx. Use exact UI-SPEC copy/tokens/sizing.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "BadgeCard" src/components/BadgeGrid/index.tsx && grep -q "badge-locked\|badge-unlocked" src/components/BadgeCard/index.tsx && grep -q "BadgeGrid" src/components/progress/AchievementsSegmentPlaceholder.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - BadgeGrid renders StreakCounter then a 3-column grid of BadgeCard with unlocked sorted first
    - BadgeCard locked state uses badge-locked + badge-locked-border + Lock overlay + opacity-50; unlocked uses badge-unlocked + border-strong
    - badge names + unlock-date copy match the UI-SPEC Badge Names table exactly
    - AchievementsSegmentPlaceholder.tsx renders BadgeGrid; app/(tabs)/progress.tsx is NOT modified by this plan
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Achievements segment shows the streak and a sorted, locked/unlocked-distinct badge grid.</done>
</task>

<task type="auto">
  <name>Task 3: BadgeUnlockToast + post-Anubis nav handoff (D-13)</name>
  <files>src/components/BadgeUnlockToast/index.tsx, app/(session)/index.tsx, app/(tabs)/index.tsx</files>
  <read_first>
    - app/(session)/index.tsx (completeSession call ~line 222 + AnubisOverlay.onFadeOutComplete -> router.replace('/(tabs)/') ~lines 401-407 — pass the returned newBadge key as a nav param on the replace)
    - src/components/AnubisOverlay/index.tsx (timed overlay -> navigation handoff analog for the toast timing)
    - src/components/LeftEdgeBar/index.tsx (accent left-edge bar variant reused on the toast)
    - app/(tabs)/index.tsx (Dashboard — mount BadgeUnlockToast when newBadge param present; also surface the GAMIFY-03 streak flame here if not already)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (BadgeUnlockToast spec, Motion BadgeUnlockToast sequence + toast-hold 2500ms + slow 300ms, "Badge Unlock Toast Timing" interaction, badge unlock copy table, Reduce Motion policy, alert accessibilityLiveRegion assertive)
  </read_first>
  <action>
    Build src/components/BadgeUnlockToast (4px accent LeftEdgeBar + 32x32 badge SVG + heading Body-emphasis + body Caption fg-muted; Reanimated translateY+opacity; slide up over slow 300ms, hold toast-hold 2500ms, fade+slide down 300ms, unmount; instant under reduced motion; accessibilityRole alert + accessibilityLiveRegion assertive; one per session). Heading/body copy come from the UI-SPEC Badge unlock copy table keyed by badge key. In app/(session)/index.tsx, capture the badge key returned by completeSession and pass it as a `newBadge` param on `router.replace('/(tabs)/')` in AnubisOverlay.onFadeOutComplete (D-13 sequence — toast fires AFTER Anubis, never during the Lottie). In app/(tabs)/index.tsx, read the newBadge param on mount; if present, mount BadgeUnlockToast then clear the param; only the first badge shows if multiple (queue, not stack). Ensure the GAMIFY-03 weekly streak is visible on the Dashboard (Flame + count) per the discretion call.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "newBadge" app/\(session\)/index.tsx && grep -q "newBadge" app/\(tabs\)/index.tsx && grep -q "BadgeUnlockToast" app/\(tabs\)/index.tsx && grep -q "LeftEdgeBar\|toast-hold\|2500" src/components/BadgeUnlockToast/index.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - completeSession's returned badge key is passed as the `newBadge` param on router.replace('/(tabs)/') in AnubisOverlay.onFadeOutComplete
    - app/(tabs)/index.tsx mounts BadgeUnlockToast only when newBadge is present and clears the param after; max one toast
    - BadgeUnlockToast holds 2500ms then exits over 300ms; instant under reduced motion; accessibilityRole alert
    - the Dashboard shows the weekly streak (Flame + count) for GAMIFY-03
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>A milestone hit on completion shows exactly one post-Anubis toast on the Dashboard; the streak is visible there too.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PowerSync local SQLite -> Supabase | badge + user_stats writes sync up; RLS gates per user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | badge double-unlock / duplicate rows | mitigate | UNIQUE(user_id, badge_key) + INSERT ON CONFLICT DO NOTHING; detection inside the atomic completeSession transaction |
| T-03-15 | Denial of Service | badge detection cost per session | mitigate | O(1)/O(log n) against user_stats counters + cheap streak; no full session_sets scan (RESEARCH Pattern 8) |
| T-03-16 | Spoofing | badge ownership | mitigate | user_id from authenticated useSession; RLS scopes badges to the owner on sync |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/badgeRules.test.ts` GREEN
- Completing the 1st session unlocks first_reps and shows one toast after Anubis
- Achievements segment shows unlocked first, locked distinct; re-completing does not duplicate a badge
- `npx tsc --noEmit` no new errors
</verification>

<success_criteria>
GAMIFY-01 + GAMIFY-03 satisfied: real-milestone badges detected idempotently at completion, an Achievements grid, a post-Anubis unlock toast, and the weekly streak on Dashboard + Achievements.
</success_criteria>

<output>
Create `.planning/phases/03-templates-programs-progress/03-06-SUMMARY.md` when done.
</output>
