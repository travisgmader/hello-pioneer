---
phase: 03-templates-programs-progress
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - app/(tabs)/workouts.tsx
  - app/template-builder/[id].tsx
  - app/custom-exercise.tsx
  - src/hooks/useTemplates.ts
  - src/hooks/useExerciseLibrary.ts
  - src/services/templateService.ts
  - src/lib/exerciseSearch.ts
  - src/components/TodayCard/index.tsx
  - src/components/TemplateCard/index.tsx
  - src/components/TodayBadge/index.tsx
  - src/components/ExerciseSearchBar/index.tsx
  - src/components/MuscleGroupFilterRow/index.tsx
  - src/components/ExerciseResultRow/index.tsx
  - src/store/templateBuilderStore.ts
  - src/lib/__tests__/exerciseSearch.test.ts
autonomous: true
requirements: [TEMPLATE-01, TEMPLATE-02, TEMPLATE-03, TEMPLATE-05, TEMPLATE-06, TEMPLATE-07]

must_haves:
  truths:
    - "User can search the exercise library by name and muscle group inside the template builder and the results filter live"
    - "User can create a new template (day label + exercises with sets, rep range, rest override, exercise type, superset pairing) and it appears in the Workouts tab list"
    - "User can edit an existing template; the edit does not alter completed sessions"
    - "User can delete a template via the overflow menu; the active session is cleared if it matched"
    - "User can create a custom exercise scoped to their account and it appears in search results"
    - "Workouts tab shows TodayCard with Start workout at top, then the FlashList of template cards with today's distinguished"
  artifacts:
    - path: "app/template-builder/[id].tsx"
      provides: "full-screen template builder route (new | uuid)"
      contains: "useLocalSearchParams"
    - path: "src/services/templateService.ts"
      provides: "writeTransaction template save + soft delete"
      exports: ["saveTemplate", "deleteTemplate"]
    - path: "src/lib/exerciseSearch.ts"
      provides: "pure filterExercises predicate (unit-tested)"
      exports: ["filterExercises"]
    - path: "src/components/TodayCard/index.tsx"
      provides: "Today's workout card + Start button (D-02)"
  key_links:
    - from: "app/template-builder/[id].tsx"
      to: "src/services/templateService.ts"
      via: "saveTemplate writeTransaction"
      pattern: "saveTemplate"
    - from: "app/(tabs)/workouts.tsx"
      to: "src/hooks/useTemplates.ts"
      via: "reactive PowerSync read"
      pattern: "useTemplates"
    - from: "src/components/ExerciseResultRow/index.tsx"
      to: "src/lib/exerciseSearch.ts"
      via: "filterExercises"
      pattern: "filterExercises"
---

<objective>
Deliver the complete template-management vertical slice (per D-01..D-04): restructure the Workouts tab to TodayCard + template FlashList, build the full-screen template builder with inline exercise search (name + muscle group filters), per-exercise set/rep/rest/type/superset config reusing Phase 2 components, custom-exercise creation, template edit, and soft delete.

Purpose: Templates are the engagement surface and the prerequisite for sessions, programs, and charts. This slice makes a user able to build and manage workouts entirely from the Workouts tab.
Output: A working Workouts tab, a template-builder route, a custom-exercise route, two hooks, a service, a pure search helper (turns the RED exerciseSearch test GREEN), six components, and a builder store.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-templates-programs-progress/03-PATTERNS.md
@.planning/phases/03-templates-programs-progress/03-UI-SPEC.md
@.planning/phases/03-templates-programs-progress/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure exercise search helper + useExerciseLibrary hook + templateService</name>
  <files>src/lib/exerciseSearch.ts, src/hooks/useExerciseLibrary.ts, src/services/templateService.ts, src/lib/__tests__/exerciseSearch.test.ts</files>
  <read_first>
    - src/components/ExerciseSwapModal/index.tsx (exercise search analog — note the latent bug: it selects primary_muscle but the column is muscle_group; query muscle_group)
    - src/hooks/useSessionData.ts (usePowerSyncQuery typed reactive read; JOIN-with-library pattern lines 117-135; loading derivation lines 198-205)
    - src/services/sessionService.ts (writeTransaction + INSERT OR REPLACE + Crypto.randomUUID + never check rowsAffected; lines 145, 188-217, 278-299)
    - src/lib/__tests__/exerciseSearch.test.ts (the RED test from 03-01 — make it GREEN)
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Code Example 2; Pattern 4 template save)
  </read_first>
  <behavior>
    - filterExercises(rows, query, muscle, userId): name LIKE (case-insensitive) on query; muscle_group equals muscle unless muscle === 'All'; includes a row only if is_custom === 0 OR created_by === userId; empty query returns all in-scope rows.
    - saveTemplate: one writeTransaction upserts the templates row (INSERT OR REPLACE keyed by client UUID) then replaces all template_exercises rows (delete-by-template_id then insert, or INSERT OR REPLACE keyed by stable template_exercises.id), persisting exercise_type, sets, rep_low, rep_high, default_rest_seconds, superset_group, position.
    - deleteTemplate: soft delete (is_deleted = 1); if the deleted template id matches the active session's template in MMKV, clear the active session keys (TEMPLATE-07).
  </behavior>
  <action>
    Create src/lib/exerciseSearch.ts exporting a pure `filterExercises` (no PowerSync coupling) implementing the behavior above (TEMPLATE-02, TEMPLATE-03). Create src/hooks/useExerciseLibrary.ts: usePowerSyncQuery against `exercises` selecting id, name, muscle_group, type, exercisedb_video_id, is_custom, created_by (use SQL-side filter from RESEARCH Code Example 2, falling back to filterExercises for client refinement), plus a `createCustomExercise({name, muscleGroup, userId})` writer that INSERTs an exercises row with is_custom=1, created_by=userId, Crypto.randomUUID id (TEMPLATE-03). Create src/services/templateService.ts with `saveTemplate` (per D-01 / TEMPLATE-01/05/06) and `deleteTemplate` (TEMPLATE-07) following the sessionService writeTransaction pattern exactly; never read rowsAffected. Update the RED exerciseSearch test only if its import path/signature needs alignment — do not weaken assertions.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && npx vitest run src/lib/__tests__/exerciseSearch.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/exerciseSearch.test.ts` exits 0 (GREEN)
    - src/lib/exerciseSearch.ts exports `filterExercises` and has no import from '@powersync/react-native'
    - src/services/templateService.ts exports `saveTemplate` and `deleteTemplate`, uses `writeTransaction`, and contains no `rowsAffected` read
    - useExerciseLibrary.ts queries `muscle_group` (NOT primary_muscle) and scopes custom exercises with `is_custom = 0 OR created_by = ?`
    - createCustomExercise inserts with `is_custom` = 1 and `created_by` = the passed userId
  </acceptance_criteria>
  <done>Search helper is GREEN, library hook reads/creates exercises correctly, and templateService saves/soft-deletes via one atomic transaction.</done>
</task>

<task type="auto">
  <name>Task 2: Template builder route + builder store + search components + custom-exercise route</name>
  <files>app/template-builder/[id].tsx, app/custom-exercise.tsx, src/store/templateBuilderStore.ts, src/components/ExerciseSearchBar/index.tsx, src/components/MuscleGroupFilterRow/index.tsx, src/components/ExerciseResultRow/index.tsx</files>
  <read_first>
    - app/(session)/index.tsx (full-screen route shell, one-time init guard lines 124-167, FlashList v2 body lines 380-389, no estimatedItemSize)
    - src/components/ExerciseCard/index.tsx + src/components/SetRow/index.tsx + src/components/SetRow/ExpandedSetForm.tsx (reuse for per-exercise set/rep/rest/type config — TEMPLATE-05)
    - src/components/SupersetPair/index.tsx (reuse for superset pairing in builder)
    - src/components/Chip/index.tsx (muscle group filter chips, selected state bg-accent-dim border-border-strong)
    - src/store/sessionStore.ts (Zustand store keyed by row UUID — FlashList recycling constraint; mirror for builder state)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (Template Builder layout States 1 & 2, ExerciseSearchBar/MuscleGroupFilterRow/ExerciseResultRow specs, Interaction Patterns "Exercise Search Flow" + "Set/Rep/Rest Config", Copywriting Contract Template Builder section + Icon Inventory)
    - src/lib/exerciseSearch.ts (filterExercises from Task 1)
  </read_first>
  <action>
    Create app/template-builder/[id].tsx as a full-screen route (D-03) following the session screen shell: header (back ChevronLeft IconButton + inline template-name TextInput placeholder "Template name" + "Save template" Button), then ExerciseSearchBar, MuscleGroupFilterRow, and the conditional swap between the template exercise FlashList (search inactive) and the exercise results FlashList (search active) per UI-SPEC State 1/State 2. `id === 'new'` seeds an empty builder; a uuid hydrates the existing template via useTemplates (one-time init guard, no re-hydrate). On Save call saveTemplate. Drive per-row selection/expanded state from src/store/templateBuilderStore.ts (Zustand keyed by row UUID — NEVER local useState in FlashList rows). Reuse ExerciseCard/SetRow/ExpandedSetForm for set config, adapting per the UI-SPEC "Set/Rep/Rest Config" pattern: WeightInput becomes Reps-low/Reps-high range, a Rest (sec) input, and a Standard/Bodyweight/Run exercise-type selector writing template_exercises.exercise_type (TEMPLATE-05). Reuse SupersetPair for pairing (long-press "Pair as superset" per UI-SPEC). Create the three search components (ExerciseSearchBar, MuscleGroupFilterRow with chips All/Chest/Back/Legs/Shoulders/Arms/Core, ExerciseResultRow with Plus->Check add/remove and 48x48 thumbnail placeholder) using exact UI-SPEC copy, sizing, tokens, haptics, and accessibility labels. ExerciseResultRow's thumbnail is a placeholder slot (the real ExerciseVideoPlayer is wired in 03-08) — render the static Play-on-bg-input fallback now. Create app/custom-exercise.tsx (heading "New exercise", name + muscle group inputs, "Save exercise") calling createCustomExercise; reachable from a "Create custom exercise" affordance in the search results empty state (TEMPLATE-03). Debounce search input 200ms.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && npx tsc --noEmit 2>&1 | grep -E "template-builder|custom-exercise|ExerciseSearchBar|MuscleGroupFilterRow|ExerciseResultRow|templateBuilderStore" | head -1 || echo "no type errors in new files"</automated>
  </verify>
  <acceptance_criteria>
    - app/template-builder/[id].tsx exists, calls `useLocalSearchParams`, branches on id === 'new' vs uuid, and calls `saveTemplate` on save
    - exercise-type selector writes `exercise_type` into the saved template_exercises rows (Standard/Bodyweight/Run)
    - ExerciseResultRow toggles Plus -> Check on add/remove and uses `Haptics`
    - MuscleGroupFilterRow renders exactly the 7 chips All/Chest/Back/Legs/Shoulders/Arms/Core with single-select
    - no FlashList row component uses local `useState` for selection/expanded state (driven from templateBuilderStore)
    - app/custom-exercise.tsx calls `createCustomExercise`
    - `npx tsc --noEmit` reports no new errors in the new files
  </acceptance_criteria>
  <done>The builder route saves/edits templates with full set config and superset pairing; search works; custom exercises can be created.</done>
</task>

<task type="auto">
  <name>Task 3: Workouts tab rewrite + useTemplates + TodayCard/TemplateCard/TodayBadge</name>
  <files>app/(tabs)/workouts.tsx, src/hooks/useTemplates.ts, src/components/TodayCard/index.tsx, src/components/TemplateCard/index.tsx, src/components/TodayBadge/index.tsx</files>
  <read_first>
    - app/(tabs)/workouts.tsx (current auth + useTodaysTemplate wiring + router.push('/(session)/body-map') start flow — keep these)
    - src/hooks/useSessionData.ts (useTodaysTemplate / usePowerSyncQuery patterns to base useTemplates on)
    - src/components/TemplateBuilder/index.tsx (TemplateDayCard analog for TemplateCard / TodayCard composition)
    - src/components/Button/index.tsx (primary/ghost/secondary variants)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (Workouts Tab layout, TodayCard/TemplateCard/TodayBadge specs, Workouts Tab copywriting + empty state, Icon Inventory MoreVertical/Plus)
    - src/services/templateService.ts (deleteTemplate for the overflow Delete action)
  </read_first>
  <action>
    Create src/hooks/useTemplates.ts: a reactive usePowerSyncQuery listing the user's non-deleted templates with exercise counts (JOIN/aggregate template_exercises), plus a helper to resolve today's template from split rotation (reuse useTodaysTemplate logic). Rewrite app/(tabs)/workouts.tsx (D-02): screen heading "Workouts" + "+" IconButton (Plus 24, navigates to template-builder/new), TodayCard at top (variants scheduled / rest-day / in-progress per UI-SPEC; Start workout keeps the existing router.push('/(session)/body-map') flow), then a FlashList of TemplateCard (MANDATORY FlashList per STATE.md). Leave a slot below TodayCard where DeloadBanner mounts in 03-07 (do not build DeloadBanner here; render nothing in that slot for now — it is owned by 03-07). Build TodayCard, TemplateCard (today variant = accent-dim tint + border-strong + TodayBadge pill; overflow MoreVertical opens an action sheet Edit/Duplicate/Delete with the exact Delete confirmation copy "Delete template?" / "Your workout history using this template is preserved." calling deleteTemplate), and TodayBadge (accent bg, bg text, "Today"). Tapping a TemplateCard body navigates to template-builder/[id] in edit mode. Use exact UI-SPEC copy, sizing (TemplateCard 64pt, TodayCard radius lg / padding lg), tokens, haptics, and accessibility labels. Empty state: "No templates yet" / "Tap + to build your first template."
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "FlashList" app/\(tabs\)/workouts.tsx && grep -q "useTemplates" app/\(tabs\)/workouts.tsx && grep -q "Today" src/components/TodayBadge/index.tsx && npx tsc --noEmit 2>&1 | grep -E "workouts.tsx|TodayCard|TemplateCard|useTemplates" | head -1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/workouts.tsx renders TodayCard then a `FlashList` of TemplateCard and contains no `FlatList`
    - the "+" header IconButton navigates to `/template-builder/new`
    - tapping a TemplateCard body navigates to `/template-builder/[id]` with the template id
    - the overflow Delete action calls `deleteTemplate` after an `Alert.alert` using the exact UI-SPEC copy
    - today's template card uses the `today` variant (accent-dim + border-strong + TodayBadge)
    - empty-state copy matches the UI-SPEC exactly
    - `npx tsc --noEmit` reports no new errors in the modified files
  </acceptance_criteria>
  <done>The Workouts tab shows Today's card and a live template list; templates can be opened to edit, duplicated, and deleted with history preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PowerSync local SQLite -> Supabase | template + custom-exercise writes sync up; RLS gates per user_id / created_by |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | templateService writes | mitigate | INSERT OR REPLACE keyed by client UUID; user_id from authenticated useSession only; RLS enforces ownership on upload |
| T-03-05 | Information Disclosure | custom exercises | mitigate | createCustomExercise sets created_by = authenticated userId; library query scopes `is_custom = 0 OR created_by = ?` so users never see others' custom exercises |
| T-03-06 | Tampering | TEMPLATE-06 history isolation | accept | session_sets snapshot exercise_name at log time; template edits write independent rows — completed sessions are structurally unaffected |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/exerciseSearch.test.ts` GREEN
- Workouts tab uses FlashList (no FlatList) and shows TodayCard + template list
- A new template created in the builder appears in the Workouts list with correct exercise count
- Editing a template does not change a previously-completed session's rows
- Deleting a template soft-deletes and clears a matching active session
- `npx tsc --noEmit` no new errors
</verification>

<success_criteria>
TEMPLATE-01/02/03/05/06/07 satisfied: searchable library (name + muscle), create/edit/delete templates with full set config + superset pairing + exercise type, custom exercises scoped to the account, history isolation preserved.
</success_criteria>

<output>
Create `.planning/phases/03-templates-programs-progress/03-02-SUMMARY.md` when done.
</output>
