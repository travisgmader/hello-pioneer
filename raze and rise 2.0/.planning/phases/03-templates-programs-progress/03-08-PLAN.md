---
phase: 03-templates-programs-progress
plan: 08
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - scripts/seed-exercises.ts
  - src/lib/exerciseMedia.ts
  - src/components/ExerciseVideoPlayer/index.tsx
  - src/components/ExerciseResultRow/index.tsx
autonomous: false
requirements: [TEMPLATE-04]

must_haves:
  truths:
    - "A build-time seed script fetches ExerciseDB media (<=10 req/day budget), uploads it to the exercise-media Storage bucket, and updates exercises rows with the storage object path"
    - "The exercise library shows a demonstration video/GIF per exercise, served from Supabase Storage (never ExerciseDB at runtime)"
    - "When no media exists, the exercise row falls back to a static muscle-group illustration (no network call)"
  artifacts:
    - path: "scripts/seed-exercises.ts"
      provides: "one-time ExerciseDB -> Storage + exercises.exercisedb_video_id seed"
      contains: "exercise-media"
    - path: "src/lib/exerciseMedia.ts"
      provides: "resolve Storage URL from exercisedb_video_id (cache-first)"
      exports: ["resolveExerciseMediaUrl"]
    - path: "src/components/ExerciseVideoPlayer/index.tsx"
      provides: "expo-video / GIF Image player with loading + error fallback"
  key_links:
    - from: "src/components/ExerciseResultRow/index.tsx"
      to: "src/components/ExerciseVideoPlayer/index.tsx"
      via: "thumbnail slot rendering"
      pattern: "ExerciseVideoPlayer"
    - from: "src/components/ExerciseVideoPlayer/index.tsx"
      to: "src/lib/exerciseMedia.ts"
      via: "resolveExerciseMediaUrl"
      pattern: "resolveExerciseMediaUrl"
---

<objective>
Deliver exercise demonstration media (TEMPLATE-04) under the mandatory cache-first strategy: a one-time build-time seed script that pulls ExerciseDB media into the exercise-media Storage bucket and records the object path on each exercises row, a Storage-URL resolver, an ExerciseVideoPlayer (GIF/video with a static fallback), and wiring it into the template-builder ExerciseResultRow thumbnail slot left by 03-02.

Purpose: ExerciseDB's free tier is 10 req/day — calling it at runtime would break the app instantly (STATE.md blocker). All media must be seeded once and served from Storage. RESEARCH Open Question 3 / A2: ExerciseDB likely returns GIF (not MP4), so plan GIF via Image as the primary path with expo-video for any true video.
Output: The seed script, a media-URL resolver, the player component, and the ExerciseResultRow thumbnail wired to it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-templates-programs-progress/03-RESEARCH.md
@.planning/phases/03-templates-programs-progress/03-PATTERNS.md
@.planning/phases/03-templates-programs-progress/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: seed-exercises script + exerciseMedia resolver</name>
  <files>scripts/seed-exercises.ts, src/lib/exerciseMedia.ts</files>
  <read_first>
    - supabase/functions/migrate-v1-user/index.ts (admin/service-role client + upsert loop analog for the seed script)
    - src/lib/supabase.ts (Storage public-URL build pattern for exercise-media)
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Pattern 5 cache-first seed; Anti-Pattern never call ExerciseDB at runtime; A2/A8 ExerciseDB returns gifUrl GIF + licensing todo; Open Question 3; Environment Availability — RAPIDAPI_KEY local-only, fallback to existing 36 exercises)
    - src/lib/schema.ts (exercises.exercisedb_video_id column already exists)
  </read_first>
  <action>
    Create scripts/seed-exercises.ts (run manually, NOT shipped in the app bundle): using a Supabase service-role client (key + RAPIDAPI_KEY from local env, never committed), fetch ExerciseDB exercise media within the <=10 req/day budget, upload each media file to the `exercise-media` Storage bucket as `{slug}.gif` (or `.mp4` if the source is video), and UPDATE the matching exercises row's `exercisedb_video_id` with the Storage object path. Idempotent (upsert; skip already-seeded). If RAPIDAPI_KEY is absent, the script logs and exits cleanly — the app keeps working with the existing 36 exercises and the static fallback (Environment Availability). Create src/lib/exerciseMedia.ts exporting `resolveExerciseMediaUrl(exercisedbVideoId)`: returns the Supabase Storage public URL for the object path, or null when empty (so the player shows the static fallback). NEVER construct an ExerciseDB CDN URL at runtime (cache-first mandate).
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "exercise-media" scripts/seed-exercises.ts && grep -q "resolveExerciseMediaUrl" src/lib/exerciseMedia.ts && ! grep -qi "rapidapi\|exercisedb.p" src/lib/exerciseMedia.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - scripts/seed-exercises.ts uploads to the `exercise-media` bucket and UPDATEs exercises.exercisedb_video_id with the storage object path
    - the script exits cleanly (no crash) when RAPIDAPI_KEY is absent
    - src/lib/exerciseMedia.ts exports resolveExerciseMediaUrl returning a Storage URL or null
    - neither exerciseMedia.ts nor any app/src file references a RapidAPI/ExerciseDB CDN host at runtime (cache-first)
  </acceptance_criteria>
  <done>Media seeds once into Storage with object paths recorded; the resolver yields Storage URLs (or null) without any runtime ExerciseDB call.</done>
</task>

<task type="auto">
  <name>Task 2: ExerciseVideoPlayer + wire into ExerciseResultRow thumbnail</name>
  <files>src/components/ExerciseVideoPlayer/index.tsx, src/components/ExerciseResultRow/index.tsx</files>
  <read_first>
    - src/components/ExerciseResultRow/index.tsx (the 48x48 thumbnail slot placeholder built in 03-02 — replace the static placeholder with ExerciseVideoPlayer thumbnail variant)
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Pattern 5 expo-video useVideoPlayer/VideoView; Open Question 3 GIF-via-Image primary path; A2)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (ExerciseVideoPlayer spec: thumbnail (muted loop) + fullscreen variants, loading/playing/paused/error states, static muscle-illustration error fallback, Icon Inventory Play/Pause; Registry Safety expo-video Storage-only)
    - src/lib/exerciseMedia.ts (resolveExerciseMediaUrl from Task 1)
    - src/components/Spinner/index.tsx (loading overlay)
  </read_first>
  <action>
    Build src/components/ExerciseVideoPlayer: resolve the media URL via resolveExerciseMediaUrl; if the object is a GIF render via `Image` (RN), if it is a video use expo-video useVideoPlayer + VideoView (muted, looping in `thumbnail` variant; user-controlled in `fullscreen`) — GIF is the primary expected path per RESEARCH A2/Open Question 3. States: loading = Spinner over bg-input; error or null URL = a static muscle-group illustration (SVG asset, NO network call); paused = Play overlay. Then update src/components/ExerciseResultRow/index.tsx to render the ExerciseVideoPlayer `thumbnail` variant (48x48) in place of the static placeholder slot. Media is served ONLY from Supabase Storage URLs (never ExerciseDB CDN) per the cache-first mandate and Registry Safety note.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "resolveExerciseMediaUrl" src/components/ExerciseVideoPlayer/index.tsx && grep -q "ExerciseVideoPlayer" src/components/ExerciseResultRow/index.tsx && grep -qE "expo-video|VideoView|Image" src/components/ExerciseVideoPlayer/index.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ExerciseVideoPlayer resolves media via resolveExerciseMediaUrl and renders GIF via Image or video via expo-video VideoView
    - a null/error URL renders the static muscle-group illustration with NO network call
    - ExerciseResultRow renders the ExerciseVideoPlayer thumbnail (48x48) variant
    - no runtime reference to an ExerciseDB/RapidAPI host anywhere under src/ or app/
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Exercise rows show a Storage-served demo GIF/video with a static fallback; ExerciseDB is never called at runtime.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Confirm ExerciseDB data licensing + RAPIDAPI key before seeding</name>
  <what-built>The seed script can pull ExerciseDB media into your Storage bucket. STATE.md flags ExerciseDB data licensing as an open todo (A8) and the RAPIDAPI_KEY is not yet obtained (Environment Availability).</what-built>
  <how-to-verify>
    1. Confirm the ExerciseDB (RapidAPI) free-tier terms permit caching/bundling media into your own Supabase Storage (A8 — legal, not technical). If unclear or not permitted, do NOT run the seed; ship with the existing 36 exercises + the static fallback (TEMPLATE-02 still works, TEMPLATE-04 degrades gracefully).
    2. If permitted: obtain RAPIDAPI_KEY (rapidapi.com ExerciseDB -> Subscribe free tier) and set it in your local env, then run `node scripts/seed-exercises.ts` once (respecting the 10 req/day budget).
    3. Verify a few exercises now show a demo GIF/video in the template builder search results.
  </how-to-verify>
  <resume-signal>Type "seeded" once media is verified, or "skip-seed" to ship with the static fallback.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExerciseDB CDN -> seed script -> Storage | third-party media enters Storage at build time only |
| Storage -> app | demo media served to the client from the first-party bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Denial of Service | ExerciseDB 10 req/day cap | mitigate | seed once at build time; runtime resolves Storage URLs only; never call ExerciseDB live (STATE.md blocker) |
| T-03-22 | Information Disclosure | RAPIDAPI_KEY | mitigate | key is local-only env for the seed script, never committed or shipped in the bundle |
| T-03-23 | Tampering | exercise-media bucket writes | mitigate | bucket is public-read but write-restricted to the service role (created in 03-01); seed uses service-role client |
</threat_model>

<verification>
- Seed script uploads to exercise-media and records object paths; resolveExerciseMediaUrl yields Storage URLs
- Exercise rows show a demo GIF/video or the static fallback; no runtime ExerciseDB call exists
- `npx tsc --noEmit` no new errors
</verification>

<success_criteria>
TEMPLATE-04 satisfied: per-exercise demonstration media served cache-first from Supabase Storage, with a static fallback and zero runtime ExerciseDB calls (gated on licensing confirmation).
</success_criteria>

<output>
Create `.planning/phases/03-templates-programs-progress/03-08-SUMMARY.md` when done.
</output>
