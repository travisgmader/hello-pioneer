---
phase: 03-templates-programs-progress
plan: 03
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - app/(tabs)/progress.tsx
  - app/history/[sessionId].tsx
  - src/hooks/useStreak.ts
  - src/hooks/useProgressStats.ts
  - src/lib/streak.ts
  - src/services/historyService.ts
  - src/components/SegmentTabs/index.tsx
  - src/components/SegmentIndicator/index.tsx
  - src/components/StatsRow/index.tsx
  - src/components/SessionHistoryRow/index.tsx
  - src/components/StreakCounter/index.tsx
  - src/components/HistoryEditView/index.tsx
  - src/components/progress/ChartsSegmentPlaceholder.tsx
  - src/components/progress/PhotosSegmentPlaceholder.tsx
  - src/components/progress/AchievementsSegmentPlaceholder.tsx
  - src/lib/__tests__/streak.test.ts
  - tests/integration/historyEdit.test.ts
autonomous: true
requirements: [PROGRESS-01, PROGRESS-05, HISTORY-01, HISTORY-02, HISTORY-03, GAMIFY-03]

must_haves:
  truths:
    - "Progress tab shows a 4-segment control (Overview | Charts | Photos | Achievements) with a spring-animated indicator"
    - "Overview shows total workouts, weekly streak, go-rate %, and lifetime PR count plus a scrollable recent-sessions list"
    - "Tapping a recent session opens a full-screen edit view; saving commits edits atomically and charts reflect them immediately"
    - "Weekly streak (consecutive ISO weeks with >=1 completed session) computes correctly and breaks on a gap"
  artifacts:
    - path: "app/(tabs)/progress.tsx"
      provides: "SegmentTabs host rendering all 4 segments"
      contains: "SegmentTabs"
    - path: "src/lib/streak.ts"
      provides: "pure computeStreak(dates) (unit-tested)"
      exports: ["computeStreak"]
    - path: "src/services/historyService.ts"
      provides: "atomic multi-table session edit"
      exports: ["saveSessionEdits", "deleteSession"]
    - path: "app/history/[sessionId].tsx"
      provides: "full-screen history edit route (D-08)"
      contains: "useLocalSearchParams"
  key_links:
    - from: "app/(tabs)/progress.tsx"
      to: "src/hooks/useProgressStats.ts"
      via: "reactive stats read"
      pattern: "useProgressStats"
    - from: "src/components/SessionHistoryRow/index.tsx"
      to: "app/history/[sessionId].tsx"
      via: "router.push on tap"
      pattern: "/history/"
    - from: "app/history/[sessionId].tsx"
      to: "src/services/historyService.ts"
      via: "saveSessionEdits writeTransaction"
      pattern: "saveSessionEdits"
---

<objective>
Build the Progress tab shell and its Overview segment plus the full history-editing flow: the 4-segment SegmentTabs host (D-05), the StatsRow + recent-sessions list (D-06, PROGRESS-01/05), the weekly streak (GAMIFY-03), and the full-screen HistoryEditView reusing Phase 2 session components with atomic save (D-08, HISTORY-01/02/03).

Purpose: Overview is the default Progress segment and the entry point to history editing. Building the segment host here (owning progress.tsx) lets the Charts/Photos/Achievements plans (03-04/05/06) flesh out their segment components without touching progress.tsx — clean parallel file ownership.
Output: The Progress host with all 4 segments wired (Overview real; the other three rendered via placeholder components that 03-04/05/06 replace), two stats hooks, a pure streak helper (turns RED test GREEN), the history edit service + route + view (turns RED integration test GREEN), and five components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-templates-programs-progress/03-PATTERNS.md
@.planning/phases/03-templates-programs-progress/03-UI-SPEC.md
@.planning/phases/03-templates-programs-progress/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure streak helper + stats/streak hooks + historyService (atomic edit)</name>
  <files>src/lib/streak.ts, src/hooks/useStreak.ts, src/hooks/useProgressStats.ts, src/services/historyService.ts, src/lib/__tests__/streak.test.ts, tests/integration/historyEdit.test.ts</files>
  <read_first>
    - src/lib/sessionStats.ts (computeGoRate — REUSE for the Overview go-rate stat; pure-function structure to mirror in streak.ts)
    - src/hooks/useSessionData.ts (usePowerSyncQuery aggregates + loading derivation)
    - src/services/sessionService.ts (completeSession writeTransaction pattern — copy verbatim for historyService; never check rowsAffected)
    - .planning/phases/03-templates-programs-progress/03-RESEARCH.md (Code Example 3/4/5; Pattern 3 atomic history edit)
    - src/lib/__tests__/streak.test.ts + tests/integration/historyEdit.test.ts (the RED tests from 03-01 — make GREEN)
  </read_first>
  <behavior>
    - computeStreak(dates): map distinct completed_at dates to startOfISOWeek; walk back from the current ISO week incrementing while each prior week has >=1 session; break on the first gap; empty -> 0; current week with a session -> at least 1.
    - saveSessionEdits: one writeTransaction — UPDATE sessions.notes, INSERT OR REPLACE each edited session_set, DELETE removed session_sets by id; never read rowsAffected (HISTORY-02).
    - deleteSession: soft delete sessions.is_deleted = 1 (DATA-02 soft-delete-only).
  </behavior>
  <action>
    Create src/lib/streak.ts exporting pure `computeStreak(dates: string[])` using date-fns (startOfISOWeek, differenceInCalendarISOWeeks) per RESEARCH Code Example 5 (GAMIFY-03). Create src/hooks/useStreak.ts (queries distinct sessions.completed_at, delegates to computeStreak). Create src/hooks/useProgressStats.ts: reactive reads for total workouts (COUNT completed, not deleted), lifetime go-rate (reuse computeGoRate over non-warmup completed session_sets), lifetime PR count (PROGRESS-01), and the recent-sessions list (sessions JOIN session_sets, date + day_label + per-session go-rate, ordered desc — PROGRESS-05). Create src/services/historyService.ts with `saveSessionEdits` (RESEARCH Pattern 3 verbatim) and `deleteSession`. Make the streak unit test and the historyEdit integration test GREEN without weakening assertions.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && npx vitest run src/lib/__tests__/streak.test.ts tests/integration/historyEdit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/streak.test.ts tests/integration/historyEdit.test.ts` exits 0 (both GREEN)
    - src/lib/streak.ts exports `computeStreak` and imports from 'date-fns', not '@powersync/react-native'
    - src/services/historyService.ts exports `saveSessionEdits` + `deleteSession`, uses a single `writeTransaction`, reads no `rowsAffected`, and deleteSession sets is_deleted = 1 (no hard delete of the session row)
    - useProgressStats reuses `computeGoRate` from sessionStats (no reimplementation)
  </acceptance_criteria>
  <done>Streak math and atomic history edit are GREEN; the Overview stats hooks read total/streak/go-rate/PRs and recent sessions.</done>
</task>

<task type="auto">
  <name>Task 2: SegmentTabs host on progress.tsx + Overview segment (StatsRow, recent sessions, StreakCounter)</name>
  <files>app/(tabs)/progress.tsx, src/components/SegmentTabs/index.tsx, src/components/SegmentIndicator/index.tsx, src/components/StatsRow/index.tsx, src/components/SessionHistoryRow/index.tsx, src/components/StreakCounter/index.tsx, src/components/progress/ChartsSegmentPlaceholder.tsx, src/components/progress/PhotosSegmentPlaceholder.tsx, src/components/progress/AchievementsSegmentPlaceholder.tsx</files>
  <read_first>
    - app/(tabs)/progress.tsx (current placeholder to rebuild)
    - src/components/Chip/index.tsx (selected/unselected pattern reused by SegmentTabs label states)
    - src/components/NumericText/index.tsx (numeric-large 28px tabular-nums for StatsRow numbers — carry from Phase 2)
    - app/(session)/index.tsx (FlashList host for the recent-sessions list)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (Progress Tab Overview layout, SegmentTabs/SegmentIndicator/StatsRow/SessionHistoryRow/StreakCounter specs, Motion segment-slide spring stiffness 300 damping 30, Overview copywriting, accessibility tablist/tab/summary labels, Reduce Motion policy)
    - src/hooks/useProgressStats.ts + src/hooks/useStreak.ts (Task 1)
  </read_first>
  <action>
    Rewrite app/(tabs)/progress.tsx as the segment host (D-05): screen heading "Progress" (Noto Serif 24) + SegmentTabs (Overview | Charts | Photos | Achievements) + per-segment content swap (instant content swap; only the indicator animates). Maintain independent scroll offset per segment. Build SegmentTabs (equal-width 44pt tabs, active label accent Body-emphasis, inactive fg-muted caption, accessibilityRole tablist/tab + accessibilityState.selected, Haptics.selectionAsync on tap) and SegmentIndicator (2px accent bar, Reanimated useSharedValue + withSpring stiffness 300 damping 30; instant when isReducedMotionEnabled). Build the Overview segment (D-06): StatsRow (4 columns Workouts/Streak/Go Rate/PRs, NumericText numeric-large, Streak column appends Flame 16 accent, loading = grey bars no shimmer, accessibilityRole summary) over useProgressStats + useStreak, then a FlashList of SessionHistoryRow (date + day label + "{N}% go" chip + ChevronRight; tap -> router.push to /history/[sessionId]). StreakCounter (Flame 20 accent + NumericText + "{N} day streak", broken state "0 days — start a new streak"). Render the Charts/Photos/Achievements segments via the three placeholder components in src/components/progress/ — each renders the UI-SPEC empty-state copy for its segment ("Select an exercise above to see your progress." / "No photos yet" + body / Achievements StreakCounter + "No badges yet"-style). These placeholders are real empty states, and 03-04/05/06 replace each placeholder's import with the full segment component WITHOUT editing progress.tsx (progress.tsx imports from a stable path each plan overwrites). Use exact UI-SPEC copy, tokens, sizing, haptics.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "SegmentTabs" app/\(tabs\)/progress.tsx && grep -q "FlashList" src/components/SessionHistoryRow/index.tsx app/\(tabs\)/progress.tsx && npx tsc --noEmit 2>&1 | grep -E "progress.tsx|SegmentTabs|StatsRow|SessionHistoryRow" | head -1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/progress.tsx renders SegmentTabs with exactly the 4 labels Overview/Charts/Photos/Achievements and swaps content per active segment
    - SegmentTabs sets accessibilityRole "tablist"/"tab" and accessibilityState selected; SegmentIndicator uses withSpring and respects reduced motion
    - Overview StatsRow renders 4 NumericText numbers with labels Workouts/Streak/Go Rate/PRs
    - the recent sessions list is a FlashList of SessionHistoryRow; tapping a row calls router.push to `/history/` + sessionId
    - the three placeholder segment components render their exact UI-SPEC empty-state copy
    - `npx tsc --noEmit` reports no new errors in modified files
  </acceptance_criteria>
  <done>Progress host renders all 4 segments; Overview shows live stats, streak, and a tappable recent-sessions list.</done>
</task>

<task type="auto">
  <name>Task 3: History edit route + HistoryEditView (reuse Phase 2 session components)</name>
  <files>app/history/[sessionId].tsx, src/components/HistoryEditView/index.tsx</files>
  <read_first>
    - app/(session)/index.tsx (full-screen route shell, header, FlashList body — adapt: no rest timer, no Complete button)
    - src/components/ExerciseCard/index.tsx + src/components/SetRow/index.tsx + src/components/SetRow/ExpandedSetForm.tsx (REUSE exactly — WeightInput editable, SetResultButton toggle, RPE/warmup/notes editable)
    - src/hooks/useSessionData.ts (read a completed session's exercises + sets by sessionId)
    - src/services/historyService.ts (saveSessionEdits + deleteSession from Task 1)
    - .planning/phases/03-templates-programs-progress/03-UI-SPEC.md (HistoryEditView spec, History Edit Workflow interaction, History Edit View copywriting, accessibility)
  </read_first>
  <action>
    Create app/history/[sessionId].tsx (full-screen route per D-08) hosting src/components/HistoryEditView. Read the session (heading "{Month D, YYYY}") and its exercises/sets via useSessionData by sessionId; render the Phase 2 ExerciseCard + SetRow + ExpandedSetForm in editable mode (no RestTimerPill, no Complete Workout button) — WeightInput editable, SetResultButton toggleable, RPE/warm-up/notes editable, plus an "Add exercise" ghost Button. Header "Save session" Button calls saveSessionEdits (writeTransaction) — on success show the Spinner briefly then dismiss (HISTORY-03: PowerSync reactive reads make charts reflect edits automatically — no manual invalidation). Back with unsaved edits triggers Alert.alert "Discard changes?" / "Your edits will be lost." Overflow MoreVertical -> "Delete session" -> Alert.alert "Delete this session?" / "This can't be undone." calling deleteSession. Drive per-row state from the existing session store keyed by set UUID (FlashList recycling constraint) — do NOT add local useState per row. Use exact UI-SPEC copy, tokens, sizing.
  </action>
  <verify>
    <automated>cd "/Users/travismader/Desktop/Pioneer/raze and rise 2.0" && grep -q "useLocalSearchParams" app/history/\[sessionId\].tsx && grep -q "saveSessionEdits" src/components/HistoryEditView/index.tsx app/history/\[sessionId\].tsx && npx tsc --noEmit 2>&1 | grep -E "history|HistoryEditView" | head -1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - app/history/[sessionId].tsx reads sessionId via useLocalSearchParams and renders HistoryEditView
    - HistoryEditView reuses Phase 2 ExerciseCard/SetRow/ExpandedSetForm (imported, not reimplemented) and renders NO rest timer and NO Complete Workout button
    - "Save session" calls saveSessionEdits; delete calls deleteSession after the exact Alert.alert copy
    - no per-row local useState for selection/expanded (driven from the session store)
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>A completed session can be fully edited (weights, results, RPE, warm-up, notes, add/remove exercises) and saved atomically, or deleted; edits flow to charts automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> PowerSync local SQLite -> Supabase | history edits + soft deletes sync up; RLS gates per user_id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | historyService multi-table edit | mitigate | single writeTransaction (all-or-nothing); INSERT OR REPLACE keyed by set UUID; partial writes impossible |
| T-03-08 | Tampering | session deletion | mitigate | soft delete (is_deleted=1) only — no hard delete; DATA-02 immutability/soft-delete contract preserved |
| T-03-09 | Information Disclosure | recent sessions / stats reads | accept | usePowerSyncQuery reads are already RLS-scoped to the synced (own) rows; no cross-user query path |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/streak.test.ts tests/integration/historyEdit.test.ts` GREEN
- Progress tab shows 4 segments with animated indicator; Overview shows stats + streak + recent sessions
- Tapping a session opens the edit view; editing a weight and saving updates the value (and would be reflected in the Charts segment once 03-04 ships)
- `npx tsc --noEmit` no new errors
</verification>

<success_criteria>
PROGRESS-01/05, HISTORY-01/02/03, GAMIFY-03 satisfied: Overview stats + recent sessions, full atomic history editing reflected immediately, and a correct weekly streak.
</success_criteria>

<output>
Create `.planning/phases/03-templates-programs-progress/03-03-SUMMARY.md` when done.
</output>
