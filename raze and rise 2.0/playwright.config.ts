/**
 * Playwright configuration — Web E2E placeholder
 * Full configuration in Phase 6 (Polish & Platform).
 * Playwright is not installed yet — this is a minimal placeholder.
 */

// Minimal placeholder config — Playwright installed in Phase 6
const config = {
  use: {
    baseURL: 'http://localhost:8081',
  },
  testMatch: [] as string[],
};

export default config;
