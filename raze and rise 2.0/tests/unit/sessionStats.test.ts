import { describe, it } from 'vitest';

describe('computeGoRate', () => {
  it.todo('warm-up sets excluded from go-rate');
  it.todo('go-rate is (go count) / (working set count) × 100');
  it.todo('empty set list returns 0');
});
