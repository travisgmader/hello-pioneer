import { describe, it } from 'vitest';

describe('shouldShowOverloadHint', () => {
  it.todo('shows hint when previous session completed all working sets at current weight');
  it.todo('hides hint when previous session had any no-go at current weight');
  it.todo('hides hint when no previous session exists');
});
