---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - kinetic-power-web/components/Footer.tsx
  - kinetic-power-web/app/layout.tsx
  - kinetic-power-web/e2e/footer.spec.ts
  - kinetic-power-web/e2e/smoke.spec.ts
autonomous: false
requirements: [GLOB-03, GLOB-04, GLOB-05]
user_setup: []

must_haves:
  truths:
    - "Footer renders on every page (Home, Amenities, Membership, Schedule, Contact)"
    - "On desktop the footer shows three columns: brand+tagline, navigation links, address+hours+social icons"
    - "On mobile the footer collapses to a single column with brand → nav → contact stacked"
    - "Footer navigation links navigate to the correct routes"
    - "Footer has no CTA button (per D-15)"
    - "All five routes render with background-color rgb(19,19,19) and a footer at the bottom"
    - "Lighthouse mobile performance on the deployed Vercel URL meets the under-3-second load target"
  artifacts:
    - path: "kinetic-power-web/components/Footer.tsx"
      provides: "Static footer Server Component with 3-col desktop / 1-col mobile layout"
      min_lines: 80
    - path: "kinetic-power-web/e2e/footer.spec.ts"
      provides: "E2E tests for GLOB-03 (footer present on all 5 routes)"
    - path: "kinetic-power-web/e2e/smoke.spec.ts"
      provides: "E2E tests for GLOB-04 (dark background) + GLOB-05 (all routes 200)"
  key_links:
    - from: "kinetic-power-web/app/layout.tsx"
      to: "kinetic-power-web/components/Footer.tsx"
      via: "import { Footer } from '@/components/Footer' rendered after {children}"
      pattern: "<Footer"
    - from: "GitHub push"
      to: "Vercel production deployment"
      via: "GitHub→Vercel integration (D-03)"
      pattern: "auto-deploy on push to main"
---

# Plan 03 — Footer + Final Phase Validation (vertical slice)

<objective>
Add the static footer as a complete vertical slice and finish phase validation: build `components/Footer.tsx` (Server Component, 3-col desktop / 1-col mobile per D-12), mount it in `app/layout.tsx`, add Playwright `footer.spec.ts` (covers GLOB-03) and `smoke.spec.ts` (covers GLOB-04 + GLOB-05), then push to GitHub and run the Lighthouse mobile check against the live Vercel preview URL.

Purpose: Close out GLOB-03 with the footer, lock down GLOB-04 (dark background) and GLOB-05 (sub-3s mobile load) with automated tests + manual Lighthouse check. After this plan, Phase 1 is fully verified.

Output: Working footer on all 5 routes, full Playwright suite green, Lighthouse mobile performance verified on the deployed URL, phase ready for `/gsd:verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@DESIGN.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@kinetic-power-web/app/layout.tsx
@kinetic-power-web/components/TopNav.tsx
@kinetic-power-web/playwright.config.ts

<interfaces>
<!-- Tailwind utilities available after Plan 01 (DO NOT redeclare): -->
<!-- bg-surface-container (footer bg), border-outline-variant, -->
<!-- text-on-surface, text-on-surface-variant, font-anton, font-barlow-condensed, -->
<!-- font-hanken-grotesk, text-headline-lg, text-body-md, text-label-sm, -->
<!-- tracking-label, --spacing-margin-mobile, --spacing-margin-desktop, -->
<!-- --spacing-container-max, --spacing-gutter, --spacing-lg, --spacing-md, --spacing-base -->

NAV_LINKS array (same as TopNav, declare at module level in Footer.tsx):
[
  { label: 'Home', href: '/' },
  { label: 'Amenities', href: '/amenities' },
  { label: 'Membership', href: '/membership' },
  { label: 'Schedule', href: '/schedule' },
  { label: 'Contact', href: '/contact' },
]

Placeholder content (UI-SPEC + CONTEXT D-14):
- Brand tagline: "Train without limits."
- Address: "4820 Industrial Parkway, Suite 110, Riverside, CA 92501"
- Hours: "Mon–Fri: 5am–10pm" and "Sat–Sun: 6am–8pm"
- Social handles (all platforms): "@kineticpowergym"
- Social link hrefs: https://instagram.com/kineticpowergym, https://facebook.com/kineticpowergym, https://tiktok.com/@kineticpowergym

Inline SVG icon components (D-13 — no icon font libraries):
- InstagramIcon, FacebookIcon, TikTokIcon — all 20×20 viewBox 0 0 24 24, `currentColor` stroke/fill, `aria-hidden="true"`

Social link aria-labels: "Instagram", "Facebook", "TikTok"
Footer nav landmark: `<nav aria-label="Footer navigation">`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write footer + smoke RED tests, then build Footer (GREEN)</name>
  <files>kinetic-power-web/e2e/footer.spec.ts, kinetic-power-web/e2e/smoke.spec.ts, kinetic-power-web/components/Footer.tsx, kinetic-power-web/app/layout.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (Footer.tsx + e2e/footer.spec.ts + e2e/smoke.spec.ts patterns)
    - .planning/phases/01-foundation/01-RESEARCH.md (Footer is not in standard patterns — relies on PATTERNS.md)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Footer Component Specifications, Copywriting Contract)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-12, D-13, D-14, D-15)
    - kinetic-power-web/app/layout.tsx (current state — read before editing)
  </read_first>
  <behavior>
    Footer behavior (GREEN):
    - Renders on every route (verified by iterating all 5 routes in footer.spec.ts)
    - Contains a `<nav aria-label="Footer navigation">` landmark with 5 links pointing to /, /amenities, /membership, /schedule, /contact
    - Contains social links labeled "Instagram", "Facebook", "TikTok" (aria-label)
    - Contains the placeholder address string `4820 Industrial Parkway` somewhere
    - Has no CTA button (no `<a>` or `<button>` with text "Book a Session" or "Sign Up" inside the footer)

    Smoke behavior (GREEN):
    - All 5 routes return 200
    - `<body>` background-color computes to `rgb(19, 19, 19)` on all 5 routes
    - Page title or body doesn't contain "404" or "Error"
  </behavior>
  <action>
    Three sub-steps in this task — RED tests, then implementation, then GREEN.

    1. Create `kinetic-power-web/e2e/footer.spec.ts` per `01-PATTERNS.md` "e2e/footer.spec.ts" section. Iterate over all 5 routes and assert: (a) the `nav` with accessible name `Footer navigation` is visible, (b) an aria-label="Instagram" link is visible, (c) the page body contains the substring `4820 Industrial Parkway`, (d) the footer does NOT contain a link with accessible name `Book a Session` (D-15 enforcement). Use `page.getByRole('contentinfo')` or `page.locator('footer')` to scope assertion (c) and (d) to the footer.

    2. Create `kinetic-power-web/e2e/smoke.spec.ts` per `01-PATTERNS.md` "e2e/smoke.spec.ts" section. Iterate over the 5 routes, navigate to each, assert title does NOT match `/Error|404/`, and assert `getComputedStyle(document.body).backgroundColor === 'rgb(19, 19, 19)'`.

    3. Run `cd kinetic-power-web && npx playwright test e2e/footer.spec.ts e2e/smoke.spec.ts --reporter=list` — expect failures (no Footer yet → footer assertions fail; smoke background test may pass if Plan 01 set body bg correctly, but footer route-level assertions fail). This is RED.

    4. Create `kinetic-power-web/components/Footer.tsx` per `01-PATTERNS.md` lines 397–508 with these named identifiers/conventions:
       - Server Component (NO `'use client'` — D-13 inline SVG runs server-side)
       - Imports: `import Link from 'next/link'`
       - Inline SVG icon components defined in the same file: `InstagramIcon`, `FacebookIcon`, `TikTokIcon` — each 20×20 viewBox 0 0 24 24, `currentColor`, `aria-hidden="true"`
       - Module-level `NAV_LINKS` const (same 5 entries as TopNav)
       - Exported function `Footer()` (named export)
       - Shell: `<footer className="bg-surface-container border-t border-outline-variant mt-auto">` containing `<div className="max-w-[var(--spacing-container-max)] mx-auto px-[var(--spacing-margin-mobile)] md:px-[var(--spacing-margin-desktop)] py-[var(--spacing-lg)]">`
       - Grid: `<div className="grid grid-cols-1 md:grid-cols-3 gap-[var(--spacing-gutter)]">`
       - Col 1: `<div>` with brand `<p className="font-anton text-on-surface uppercase text-headline-lg tracking-headline">Kinetic Power</p>` and tagline `<p className="font-hanken-grotesk text-on-surface-variant text-body-md mt-[var(--spacing-base)]">Train without limits.</p>` (UI-SPEC Copywriting)
       - Col 2: `<nav aria-label="Footer navigation">` with `<ul className="flex flex-col gap-[var(--spacing-base)]">` mapping NAV_LINKS to `<li><Link href={link.href} className="font-barlow-condensed text-on-surface uppercase text-label-sm tracking-label hover:text-on-surface-variant transition-colors duration-150">{link.label}</Link></li>`. Above the `<ul>`, render a heading `<p className="font-barlow-condensed text-on-surface uppercase text-label-sm tracking-label mb-[var(--spacing-md)]">NAVIGATE</p>` (UI-SPEC Footer Component Specifications).
       - Col 3: `<div className="flex flex-col gap-[var(--spacing-md)]">` containing:
         - Heading `<p className="font-barlow-condensed text-on-surface uppercase text-label-sm tracking-label">FIND US</p>`
         - `<address className="font-hanken-grotesk text-on-surface-variant text-body-md not-italic"><p>4820 Industrial Parkway, Suite 110</p><p>Riverside, CA 92501</p></address>`
         - `<div className="font-hanken-grotesk text-on-surface-variant text-body-md"><p>Mon–Fri: 5am–10pm</p><p>Sat–Sun: 6am–8pm</p></div>`
         - Social icon row `<div className="flex gap-[var(--spacing-md)]">` with three `<a>` elements, one per icon, each with `aria-label="Instagram"` / `aria-label="Facebook"` / `aria-label="TikTok"`, classes `text-on-surface hover:text-on-surface-variant transition-colors duration-150`, and `href` pointing to the placeholder URLs in the `<interfaces>` block. Each contains the corresponding icon component and a `<span className="sr-only">` with the platform name (UI-SPEC Accessibility).
       - Bottom strip: `<div className="border-t border-outline-variant mt-[var(--spacing-lg)] pt-[var(--spacing-md)]"><p className="font-barlow-condensed text-on-surface-variant uppercase text-label-sm tracking-label">© {new Date().getFullYear()} Kinetic Power. All rights reserved.</p></div>`

       Per D-15 there is NO `<button>` or `<Link>` with text `Book a Session` in this file. Per D-13 social icons are inline SVG only — no `react-icons` or any icon font library imported.

    5. Edit `kinetic-power-web/app/layout.tsx` to import `Footer` from `@/components/Footer` and render `<Footer />` inside `<body>` AFTER `{children}` and AFTER `<TopNav />`. Order in body must be: `<TopNav />` → `{children}` → `<Footer />`. Do NOT add `'use client'`.

    6. Edit `kinetic-power-web/app/layout.tsx` body to ensure `min-h-screen flex flex-col` (or equivalent) so the footer sits at the bottom when content is short. Specifically: body classes should include `bg-background text-on-surface min-h-screen flex flex-col`, and `{children}` should be wrapped in `<div className="flex-1">{children}</div>` (or `<main>` if you prefer — but each page already has its own `<main>`, so use a `<div className="flex-1">` here to avoid nested `<main>` landmarks).

    7. Run `cd kinetic-power-web && npm run build` — must exit 0.

    8. Run `cd kinetic-power-web && npx playwright test --reporter=list` (full suite: nav.spec.ts + footer.spec.ts + smoke.spec.ts) — ALL tests must now pass GREEN.
  </action>
  <verify>
    <automated>cd kinetic-power-web && test -f components/Footer.tsx && ! grep -q "'use client'" components/Footer.tsx && ! grep -q "react-icons" package.json && grep -q "Footer navigation" components/Footer.tsx && grep -q "4820 Industrial Parkway" components/Footer.tsx && grep -q "Footer" app/layout.tsx && ! grep -q "Book a Session" components/Footer.tsx && npm run build 2>&1 | tail -10 && npx playwright test --reporter=list 2>&1 | tee /tmp/all-tests.log && ! grep -q " failed" /tmp/all-tests.log && echo "ALL_GREEN_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `kinetic-power-web/components/Footer.tsx` exists, does NOT contain `'use client'` (Server Component)
    - `kinetic-power-web/components/Footer.tsx` exports a named function `Footer`
    - `kinetic-power-web/components/Footer.tsx` contains the literal strings `Footer navigation`, `Train without limits.`, `4820 Industrial Parkway`, `Mon–Fri: 5am–10pm`, `@kineticpowergym` URLs (or the placeholder set declared in the `<interfaces>` block)
    - `kinetic-power-web/components/Footer.tsx` declares three inline SVG icon components (`InstagramIcon`, `FacebookIcon`, `TikTokIcon`) — no imports from `react-icons`, `lucide-react`, `@heroicons/react`, or any icon library
    - `kinetic-power-web/components/Footer.tsx` does NOT contain the string `Book a Session` (D-15 enforcement)
    - `kinetic-power-web/components/Footer.tsx` declares the same 5-entry NAV_LINKS array as TopNav
    - `kinetic-power-web/app/layout.tsx` imports `Footer` from `@/components/Footer` and renders `<Footer />` AFTER `{children}` (verified by line position in file)
    - `kinetic-power-web/app/layout.tsx` body element classes include `bg-background`, `text-on-surface`, `min-h-screen`, and `flex flex-col`
    - `kinetic-power-web/app/layout.tsx` does NOT contain `'use client'`
    - `kinetic-power-web/e2e/footer.spec.ts` exists and tests all 5 routes
    - `kinetic-power-web/e2e/smoke.spec.ts` exists and tests background-color and HTTP 200 for all 5 routes
    - `cd kinetic-power-web && npm run build` exits 0
    - `cd kinetic-power-web && npx playwright test --reporter=list` exits 0 with ALL tests passing (nav.spec.ts + footer.spec.ts + smoke.spec.ts)
  </acceptance_criteria>
  <done>
    Footer renders on all 5 routes, full Playwright suite GREEN, layout.tsx renders TopNav → children → Footer in correct order, no Pitfall violations, D-15 enforced (no CTA in footer), D-13 enforced (inline SVG only).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit, push, and deploy to Vercel</name>
  <files>kinetic-power-web (git state only)</files>
  <read_first>
    - .planning/phases/01-foundation/01-01-SUMMARY.md (production URL recorded by Plan 01)
    - kinetic-power-web/.git (state — to confirm origin is set)
  </read_first>
  <action>
    Commit the work and push to the `main` branch so GitHub→Vercel auto-deploys (D-03).

    1. From `kinetic-power-web/`: `git add -A`
    2. `git commit -m "feat(01): topnav + footer + e2e suite — phase 01 complete"` (or split into multiple commits if Plan 02 didn't push — choose whichever produces a clean log; minimum: one commit containing all of Plans 02 + 03 work if not previously pushed)
    3. `git push origin main`
    4. Wait ~60 seconds, then check the Vercel deployment status: `vercel list` or open the Vercel dashboard. Confirm the latest deployment is `Ready`.
    5. Record the production URL (already from Plan 01 SUMMARY; reconfirm here).

    If `git push` fails because the remote is not yet connected (e.g. Plan 01 Task 4 left it as a dashboard task), the next human-verify checkpoint will surface this — do not fabricate a workaround.
  </action>
  <verify>
    <automated>cd kinetic-power-web && git status --porcelain | wc -l | tr -d ' ' | xargs -I{} test {} = "0" && git log -1 --pretty=%s | grep -q "feat" && git rev-parse --abbrev-ref --symbolic-full-name @{u} 2>&1 | grep -q "origin/main" && echo "PUSHED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cd kinetic-power-web && git status --porcelain` returns empty (working tree clean — everything committed)
    - `cd kinetic-power-web && git log origin/main..HEAD --oneline` returns empty (local matches remote)
    - The latest commit message contains `feat` or `chore` prefix and references Phase 01 work (`feat(01):` or similar)
    - Vercel dashboard shows a deployment with status `Ready` whose source commit matches the latest local commit hash (verified in the next human-verify task)
  </acceptance_criteria>
  <done>
    Code pushed to `main`, Vercel auto-deploy triggered, all artifacts of Phase 01 are now live on the production URL.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 01 complete: TopNav + Footer + 5 route shells deployed to Vercel via GitHub auto-deploy. Full Playwright suite (nav.spec.ts + footer.spec.ts + smoke.spec.ts) is GREEN. The site now has all 4 phase requirements satisfied: GLOB-01 (responsive nav with mobile hamburger), GLOB-03 (footer on every page), GLOB-04 (all UI follows DESIGN.md tokens), GLOB-05 (deployed to Vercel — performance to be verified by Lighthouse below).
  </what-built>
  <how-to-verify>
    Final phase verification — this is the GLOB-05 Lighthouse check + a complete walkthrough on the deployed URL.

    1. Open the production Vercel URL (recorded in `.planning/phases/01-foundation/01-01-SUMMARY.md`) in **Chrome on desktop**.
    2. Click each of the 5 nav links — confirm each route loads, the TopNav remains fixed at the top, and the Footer is visible at the bottom.
    3. Resize the browser to <768px width.
    4. Confirm the hamburger appears; tap it; confirm the overlay opens with all 5 large Anton uppercase links + red pinned CTA. Tap a link; confirm the overlay closes and the route changes.
    5. Open Chrome DevTools → Lighthouse → set category to "Performance", device to "Mobile", and click "Analyze page load".
    6. Wait for the report. Confirm:
       - Performance score: ideally ≥ 90 (≥ 75 acceptable for free tier first deploy)
       - Largest Contentful Paint (LCP): < 2.5 seconds (this is the formal under-3-second GLOB-05 test)
       - Cumulative Layout Shift (CLS): < 0.1
    7. Open the **same URL on your actual phone** over cellular. Confirm the page loads in under 3 seconds subjectively.
    8. Open DevTools → Network tab → filter "fonts" → reload. Confirm font files load from the Vercel domain (not `fonts.googleapis.com`).

    If LCP is over 2.5s on the Lighthouse mobile run, type `failed: lcp=<value>` so we can open a gap-closure plan. Otherwise type `approved <production-url>` to mark Phase 01 complete.
  </how-to-verify>
  <resume-signal>Type "approved &lt;production-url&gt;" or "failed: lcp=&lt;value&gt;"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static built assets → public Vercel CDN | Phase output is served to anonymous users globally |
| social icon links → third-party domains | Footer links to instagram.com, facebook.com, tiktok.com (untrusted destinations from the user's perspective) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Outbound social links could be reverse-tabnabbed | mitigate | All three social `<a>` elements should include `target="_blank" rel="noopener noreferrer"` — add these attributes when building the footer (UI-SPEC implies external links; safe default for outbound). |
| T-03-02 | Information disclosure | Inline SVG icons rendered server-side leak no client state — but `aria-label` text is public | accept | Public marketing site. No PII or secret in icon labels (just `Instagram`/`Facebook`/`TikTok`). |
| T-03-03 | Spoofing | Footer placeholder address content (D-14) is realistic-looking but fake — could mislead visitors before real content lands | accept | Phase 1 ships preview/internal URL only; not yet linked from any public source. Gym owner replaces address before public launch. PROJECT.md notes content dependency. |
| T-03-04 | Denial of service | Lighthouse mobile under-3-second target failure | mitigate | All shell pages are statically generated (no data fetching), fonts are self-hosted via next/font, no large media assets in Phase 1. Manual Lighthouse run in the final human-verify task confirms the target; gap closure planned if it fails. |
| T-03-SC | Tampering | No new package installs in this plan (uses existing motion + @playwright/test from prior plans) | accept | No new supply chain surface introduced. |

ASVS L1 applicability: V14 Configuration (no secrets), V5 Input Validation (no inputs in Phase 1). Phase 1 has minimal security surface — confirmed across all three plans.
</threat_model>

<verification>
1. `cd kinetic-power-web && npm run build` exits 0
2. `cd kinetic-power-web && npx playwright test` exits 0 with ALL three spec files green
3. `git status --porcelain` returns empty after Task 2
4. Vercel deployment for latest commit is `Ready`
5. Manual Lighthouse mobile run on deployed URL shows LCP < 2.5s (final human-verify task)
</verification>

<success_criteria>
- Footer visible on every route (`/`, `/amenities`, `/membership`, `/schedule`, `/contact`) on both desktop (3-col) and mobile (1-col stacked)
- Footer has NO `Book a Session` CTA (D-15) and uses inline SVG icons (D-13)
- All Playwright tests pass (nav.spec.ts + footer.spec.ts + smoke.spec.ts)
- Code pushed to `main` and deployed to Vercel — production URL is `Ready`
- Lighthouse mobile LCP < 2.5s on the deployed URL (GLOB-05 satisfied)
- All four Phase 01 requirements satisfied: GLOB-01, GLOB-03, GLOB-04, GLOB-05
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done. Include:
- The Lighthouse mobile Performance score and LCP value
- The Vercel production URL (final)
- Confirmation that all requirements GLOB-01, GLOB-03, GLOB-04, GLOB-05 are satisfied
- Test run counts (e.g. "12 passed, 0 failed in 18s")

After this summary, Phase 01 is ready for `/gsd:verify-work 01` (or `/gsd:execute-phase 02` if no gap closure needed).
</output>
