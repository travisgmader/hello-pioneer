---
phase: 1
slug: foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-27
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Foundation.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.
> All decisions pre-populated from DESIGN.md, CONTEXT.md, and RESEARCH.md — no user questions required.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none — custom Tailwind v4 @theme | CONTEXT.md D-04 |
| Preset | not applicable | CONTEXT.md D-04 |
| Component library | none — bespoke components only | DESIGN.md |
| Icon library | Inline SVG components (no icon font or library) | CONTEXT.md D-13 |
| Fonts | Anton (400), Barlow Condensed (700), Hanken Grotesk (400) via next/font/google | DESIGN.md + RESEARCH.md Pattern 2 |

**Rationale for no shadcn:** DESIGN.md defines a bespoke Modern Brutalism system with 40+ named color tokens, custom type scale, and strict component rules (hard-geometric radius, no soft shadows, red-only CTA). shadcn presets would conflict with this contract. CONTEXT.md D-04 mandates a single-source-of-truth @theme block as the design system mechanism.

---

## Spacing Scale

Source: DESIGN.md Spacing section. All values are multiples of 4px.

| Token | CSS Variable | Value | Usage in Phase 1 |
|-------|-------------|-------|-----------------|
| xs | `--spacing-xs` | 4px | Icon gaps, inline tight padding |
| base | `--spacing-base` | 8px | Internal component padding (e.g. icon buttons) |
| sm | `--spacing-sm` | 12px | Compact element spacing inside nav links |
| md | `--spacing-md` | 24px | Nav height padding, footer column internal gaps |
| lg | `--spacing-lg` | 48px | Footer section vertical padding |
| xl | `--spacing-xl` | 80px | Page shell vertical padding |
| gutter | `--spacing-gutter` | 24px | Column gap in footer 3-col grid |
| margin-mobile | `--spacing-margin-mobile` | 16px | Page horizontal padding on mobile |
| margin-desktop | `--spacing-margin-desktop` | 40px | Page horizontal padding on desktop |
| container-max | `--spacing-container-max` | 1280px | Max-width constraint on all content containers |

**Exceptions:**
- Touch targets for hamburger button and mobile nav close (X) button: minimum 44x44px per WCAG 2.5.5. Achieved via `p-2` (8px) padding on a 24px icon = 40px. Add `min-h-[44px] min-w-[44px]` to both buttons explicitly.
- Nav bar fixed height: 64px (8px baseline grid × 8). Implemented as `h-16` in Tailwind.
- Mobile nav overlay links: 32px gap between stacked nav items (`gap-8`).

---

## Typography

Source: DESIGN.md Type Scale section. CONTEXT.md D-06 mandates all sizes in @theme.

### Font Families

| Family | Variable | Role |
|--------|----------|------|
| Anton | `--font-anton` | All display headings. Always uppercase. Single weight (400). |
| Barlow Condensed | `--font-barlow-condensed` | Sub-headers, labels, nav links, button text. Weight 700 only. |
| Hanken Grotesk | `--font-hanken-grotesk` | Body text, footer utility text. Weight 400 only. |

### Font Weights

Exactly 2 weights are used across the entire design system:

| Weight | Usage |
|--------|-------|
| 400 | Anton (all display/heading), Hanken Grotesk (all body/utility text) |
| 700 | Barlow Condensed (all nav links, labels, button text, section headings) |

### Type Scale (Phase 1 active styles — exactly 4 sizes)

| Role | Token | Font | Size (mobile) | Size (desktop) | Weight | Line Height | Letter Spacing |
|------|-------|------|--------------|----------------|--------|-------------|----------------|
| Display | `display-xl` | Anton | 64px | 96px (md:text-[6rem]) | 400 | 100% (1.0) | — |
| Headline | `headline-lg` | Anton | 32px | 48px (md:text-5xl) | 400 | 110% (1.1) | 0.02em |
| Body | `body-md` | Hanken Grotesk | 16px | 16px | 400 | 150% (1.5) | — |
| Label | `label-sm` | Barlow Condensed | 12px | 12px | 700 | 100% (1.0) | 0.1em |

**Phase 1 active contexts:**
- TopNav logo/wordmark: Anton uppercase, `headline-lg` size (32px mobile / 48px desktop) — scaled down via responsive usage, not a separate token
- TopNav desktop links: Barlow Condensed 700, uppercase, `label-sm` (12px), `tracking-label` (0.1em)
- Mobile overlay nav links: Anton uppercase, `headline-lg` (32px mobile). Large, hard-left, full-bleed
- Footer brand name: Anton uppercase, `headline-lg`
- Footer tagline: Hanken Grotesk 400, `body-md` (16px)
- Footer navigation links: Barlow Condensed 700, `label-sm` (12px), uppercase, `tracking-label` (0.1em)
- Footer address/hours text: Hanken Grotesk 400, `body-md` (16px)
- Mobile CTA button text (both nav and overlay): Barlow Condensed 700, `label-sm` uppercase with `tracking-label`

**Rules (from DESIGN.md):**
- Anton text must always be uppercase — never sentence case or mixed case
- Hard-left text alignment on all elements except hero display (Phase 2)
- Italics only on action words to imply speed; use sparingly
- `body-lg` (18px) does not exist in Phase 1 — all body/utility text uses `body-md` (16px)
- `title-md` (24px) does not exist in Phase 1 — any element previously described at 24px uses `headline-lg` at application-level sizing or `label-sm` with letter-spacing

---

## Color

Source: DESIGN.md Colors section.

### 60/30/10 Split

| Role | Token | Hex | Coverage |
|------|-------|-----|---------|
| Dominant (60%) | `background` / `surface` | `#131313` | Page background, nav background, mobile overlay background |
| Secondary (30%) | `surface-container-low` / `surface-container-high` | `#1b1b1b` / `#2a2a2a` | Nav bar background (`#1b1b1b`), footer background (`#1f1f1f`), footer column dividers |
| Accent (10%) | `primary-container` | `#e60000` | See reserved-for list below |
| Destructive | `error-container` | `#93000a` | Destructive actions only — none in Phase 1 |

### Accent Reserved For (exhaustive list for Phase 1)

Red (`primary-container: #e60000`) is applied exclusively to:
1. "Book a Session" Primary button background — desktop nav
2. "Book a Session" Primary button background — mobile overlay (pinned CTA)
3. Active navigation link indicator (current route underline or left border)
4. Focus ring on interactive elements (nav links, hamburger button) — `outline: 2px solid #e60000`

Red is NOT used for: decorative dividers, headings, icons (unless active), backgrounds, or any purpose outside this list.

### Full Color Token Map (Phase 1 @theme)

All tokens must be declared verbatim per CONTEXT.md D-05:

| Token | Hex | Phase 1 Usage |
|-------|-----|--------------|
| `background` | `#131313` | `<body>` bg, mobile overlay bg |
| `surface` | `#131313` | Synonymous with background |
| `surface-container-low` | `#1b1b1b` | TopNav bar bg |
| `surface-container` | `#1f1f1f` | Footer bg |
| `surface-container-high` | `#2a2a2a` | Hover state on nav links (subtle) |
| `surface-container-highest` | `#353535` | Not used in Phase 1 |
| `on-background` | `#e2e2e2` | Page text default |
| `on-surface` | `#e2e2e2` | Nav links, footer text, logo |
| `on-surface-variant` | `#e9bcb5` | Muted/secondary text (footer tagline optional) |
| `primary` | `#ffb4a8` | Not used in Phase 1 (reserved for progress, active states) |
| `primary-container` | `#e60000` | Primary button background |
| `on-primary-container` | `#fff7f5` | Primary button text |
| `outline` | `#b08781` | Not used in Phase 1 |
| `outline-variant` | `#5f3f3a` | Nav bar bottom border |
| `error` | `#ffb4ab` | Phase 1: no errors |
| `error-container` | `#93000a` | Phase 1: no destructive actions |
| `secondary` | `#c6c6c7` | Footer subtext or secondary nav link color |
| `secondary-container` | `#454747` | Not used in Phase 1 |

**Elevation rule (from DESIGN.md):**
- No soft drop shadows anywhere
- Surfaces defined by 1px solid borders: `border-b border-outline-variant` on nav, `border-t border-outline-variant` on footer
- Active/focus: 1px–2px solid Primary Red (`#e60000`)

---

## Component Specifications

### TopNav

**Shell:** Fixed, `top-0 inset-x-0 z-50`. Height 64px. Background `surface-container-low` (`#1b1b1b`). Bottom border: `1px solid outline-variant` (`#5f3f3a`).

**Desktop layout (≥ 768px / `md:` breakpoint):**
- Left: Kinetic Power wordmark — Anton uppercase, `on-surface` (`#e2e2e2`), `headline-lg` size
- Right cluster: nav links + Primary button
  - Nav links: Barlow Condensed 700, uppercase, `on-surface`, `tracking-label` (0.1em), `label-sm` (12px). Hover: color transitions to `on-surface-variant` (`#e9bcb5`) — 150ms ease
  - "Book a Session" button: Barlow Condensed 700 uppercase, `primary-container` bg (`#e60000`), `on-primary-container` text (`#fff7f5`), 4px radius, px-5 py-2. No shadow.
- Arrangement: logo far-left, nav links + CTA right-aligned (Claude discretion — right cluster reads most cleanly for a gym brand that wants logo dominance)

**Mobile layout (< 768px):**
- Logo left, hamburger right
- Hamburger: three horizontal bars, 24×24px SVG, `on-surface` stroke, `strokeWidth="2"`. Minimum tap area: `min-h-[44px] min-w-[44px]`
- No nav links visible on mobile

**Mobile overlay (activated by hamburger):**
- Full-viewport dark overlay: `fixed inset-0 z-50 bg-background` (`#131313`)
- Animation: Motion `AnimatePresence` — `initial={{ opacity: 0, y: 20 }}` → `animate={{ opacity: 1, y: 0 }}` → `exit={{ opacity: 0, y: 20 }}`. Duration: 200ms.
- X close button: top-right, 24×24px SVG cross icon, `min-h-[44px] min-w-[44px]`
- Nav links: Anton uppercase, `text-4xl` (36px), `on-surface`, hard-left, `gap-8` between items. Each link closes overlay on tap.
- Pinned CTA: "Book a Session" — full-width, `mt-auto`, same Primary button style as desktop. Barlow Condensed 700 uppercase, `primary-container` bg. Links to `/schedule`.
- Close behavior: X button OR backdrop tap closes overlay

### Footer

**Shell:** Static Server Component. Background `surface-container` (`#1f1f1f`). Top border: `1px solid outline-variant` (`#5f3f3a`).

**Desktop layout (≥ 768px):** 3-column grid, `gap-[var(--spacing-gutter)]` (24px).
- **Col 1:** Brand column — "KINETIC POWER" in Anton uppercase `headline-lg` + tagline in Hanken Grotesk 400 `body-md`
- **Col 2:** Navigation — heading "NAVIGATE" in Barlow Condensed 700 `label-sm` uppercase. Links: Home, Amenities, Membership, Schedule, Contact — Barlow Condensed 700 `label-sm` uppercase. Hover: `on-surface-variant` (`#e9bcb5`)
- **Col 3:** Contact column — address, hours, social icons. Hanken Grotesk 400 `body-md`. Social icons: Instagram, Facebook, TikTok as inline SVG, `on-surface`, 20×20px, `gap-4` horizontal. Hover: `on-surface-variant`.

**Mobile layout (< 768px):** Single column stacked. Same order: brand → nav → contact.

**Placeholder content (CONTEXT.md D-14):**
- Address: "4820 Industrial Parkway, Suite 110, Riverside, CA 92501"
- Hours: "Mon–Fri: 5am – 10pm / Sat–Sun: 6am – 8pm"
- Social handles: @kineticpowergym (all platforms, placeholder)
- Tagline: "Train without limits."

**Note:** No CTA button in footer (CONTEXT.md D-15). Navigation, contact info, and social links only.

### Page Shell Layouts (all 5 routes)

Each shell page (`/`, `/amenities`, `/membership`, `/schedule`, `/contact`) renders:
- `<main className="min-h-screen pt-16">` — pt-16 clears the fixed 64px nav
- Page title in Anton uppercase, `headline-lg` or `display-xl`, `on-surface`
- No other content in Phase 1

---

## Interaction States

| Element | Resting | Hover | Focus | Active |
|---------|---------|-------|-------|--------|
| Desktop nav link | `on-surface` (`#e2e2e2`) | `on-surface-variant` (`#e9bcb5`), 150ms ease | 2px outline `primary-container` (`#e60000`) | `on-surface-variant` |
| Primary button | `primary-container` bg, `on-primary-container` text | Brightness reduce 10% (or `inverse-primary` bg `#c00000`) | 2px outline `primary-container` offset 2px | Pressed: scale 0.97 |
| Hamburger / X button | `on-surface` | `on-surface-variant` | 2px outline `primary-container` | — |
| Footer nav link | `on-surface` | `on-surface-variant`, 150ms ease | 2px outline `primary-container` | — |
| Social icons | `on-surface` | `on-surface-variant` | 2px outline `primary-container` | — |

**Animation contract:**
- Mobile nav overlay: Motion `AnimatePresence`, 200ms, `easeOut`, y: 20→0 + opacity: 0→1
- All hover transitions: 150ms ease (CSS `transition-colors duration-150`)
- No other animations in Phase 1

---

## Copywriting Contract

Phase 1 contains nav and footer only — minimal copywriting surface.

| Element | Copy | Source |
|---------|------|--------|
| Primary CTA (nav + overlay) | "Book a Session" | CONTEXT.md D-09, D-11 |
| Nav logo/wordmark | "KINETIC POWER" | Brand name, Anton uppercase |
| Mobile overlay close label (sr-only) | "Close menu" | WCAG aria-label |
| Mobile overlay open label (sr-only) | "Open menu" | WCAG aria-label |
| Footer brand tagline | "Train without limits." | Placeholder — Claude discretion |
| Footer nav heading | "NAVIGATE" | Barlow Condensed label, uppercase |
| Footer contact heading | "FIND US" | Barlow Condensed label, uppercase |
| Footer address | "4820 Industrial Parkway, Suite 110, Riverside, CA 92501" | Placeholder per D-14 |
| Footer hours | "Mon–Fri: 5am–10pm | Sat–Sun: 6am–8pm" | Placeholder per D-14 |
| Page shell — Home | "HOME" | Anton uppercase, shell placeholder |
| Page shell — Amenities | "AMENITIES" | Anton uppercase, shell placeholder |
| Page shell — Membership | "MEMBERSHIP" | Anton uppercase, shell placeholder |
| Page shell — Schedule | "SCHEDULE" | Anton uppercase, shell placeholder |
| Page shell — Contact | "CONTACT" | Anton uppercase, shell placeholder |

**Empty state:** Not applicable in Phase 1 (no data-driven UI).

**Error state:** Not applicable in Phase 1 (no forms or dynamic data).

**Destructive actions:** None in Phase 1.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized | not applicable |
| Third-party | none | not applicable |

No component registry in use. All components are hand-built from DESIGN.md spec. Registry safety gate: not required.

---

## @theme Token Reference

The following must be declared verbatim in `app/globals.css` per CONTEXT.md D-04/D-05:

```css
@import "tailwindcss";

@theme {
  /* COLORS */
  --color-background: #131313;
  --color-surface: #131313;
  --color-surface-dim: #131313;
  --color-surface-bright: #393939;
  --color-on-background: #e2e2e2;
  --color-on-surface: #e2e2e2;
  --color-on-surface-variant: #e9bcb5;

  --color-surface-container-lowest: #0e0e0e;
  --color-surface-container-low: #1b1b1b;
  --color-surface-container: #1f1f1f;
  --color-surface-container-high: #2a2a2a;
  --color-surface-container-highest: #353535;
  --color-surface-variant: #353535;

  --color-primary: #ffb4a8;
  --color-on-primary: #690000;
  --color-primary-container: #e60000;
  --color-on-primary-container: #fff7f5;
  --color-inverse-primary: #c00000;
  --color-surface-tint: #ffb4a8;
  --color-primary-fixed: #ffdad4;
  --color-primary-fixed-dim: #ffb4a8;
  --color-on-primary-fixed: #410000;
  --color-on-primary-fixed-variant: #930100;

  --color-secondary: #c6c6c7;
  --color-on-secondary: #2f3131;
  --color-secondary-container: #454747;
  --color-on-secondary-container: #b4b5b5;
  --color-secondary-fixed: #e2e2e2;
  --color-secondary-fixed-dim: #c6c6c7;
  --color-on-secondary-fixed: #1a1c1c;
  --color-on-secondary-fixed-variant: #454747;

  --color-tertiary: #c7c6c6;
  --color-on-tertiary: #303031;
  --color-tertiary-container: #727272;
  --color-on-tertiary-container: #faf8f8;
  --color-tertiary-fixed: #e3e2e2;
  --color-tertiary-fixed-dim: #c7c6c6;
  --color-on-tertiary-fixed: #1b1c1c;
  --color-on-tertiary-fixed-variant: #464747;

  --color-outline: #b08781;
  --color-outline-variant: #5f3f3a;
  --color-error: #ffb4ab;
  --color-on-error: #690005;
  --color-error-container: #93000a;
  --color-on-error-container: #ffdad6;

  --color-inverse-surface: #e2e2e2;
  --color-inverse-on-surface: #303030;

  /* TYPOGRAPHY: Font families (resolved via @theme inline from next/font CSS vars) */
  @theme inline {
    --font-anton: var(--font-anton-loaded);
    --font-barlow-condensed: var(--font-barlow-condensed-loaded);
    --font-hanken-grotesk: var(--font-hanken-grotesk-loaded);
  }

  /* TYPOGRAPHY: Type scale — 4 sizes, mobile-first defaults, scale at md: */
  --text-display-xl: 4rem;            /* 64px mobile */
  --text-display-xl--line-height: 1;
  --text-headline-lg: 2rem;           /* 32px mobile */
  --text-headline-lg--line-height: 1.1;
  --text-body-md: 1rem;               /* 16px */
  --text-body-md--line-height: 1.5;
  --text-label-sm: 0.75rem;           /* 12px */
  --text-label-sm--line-height: 1;

  /* TYPOGRAPHY: Letter spacing */
  --tracking-headline: 0.02em;
  --tracking-label: 0.1em;

  /* SPACING */
  --spacing-xs: 0.25rem;             /* 4px */
  --spacing-base: 0.5rem;            /* 8px */
  --spacing-sm: 0.75rem;             /* 12px */
  --spacing-md: 1.5rem;              /* 24px */
  --spacing-lg: 3rem;                /* 48px */
  --spacing-xl: 5rem;                /* 80px */
  --spacing-gutter: 1.5rem;          /* 24px */
  --spacing-margin-mobile: 1rem;     /* 16px */
  --spacing-margin-desktop: 2.5rem;  /* 40px */
  --spacing-container-max: 80rem;    /* 1280px */

  /* BORDER RADIUS */
  --radius-sm: 0.125rem;             /* 2px */
  --radius-DEFAULT: 0.25rem;         /* 4px */
  --radius-md: 0.375rem;             /* 6px */
  --radius-lg: 0.5rem;               /* 8px */
  --radius-xl: 0.75rem;              /* 12px */
  --radius-full: 9999px;
}
```

**Desktop type overrides (applied inline in component, not as separate tokens — per CONTEXT.md D-07):**
- `display-xl` at `md:`: `md:text-[6rem]` (96px)
- `headline-lg` at `md:`: `md:text-5xl` (48px)

**Removed from @theme (not used in Phase 1):**
- `--text-title-md` (1.5rem / 24px) — removed; nav logo uses `headline-lg`, section labels use `label-sm`
- `--text-body-lg` (1.125rem / 18px) — removed; all body/utility text uses `body-md` (16px)
- `--tracking-title` (0.05em) — removed; only `tracking-headline` (0.02em) and `tracking-label` (0.1em) are used

---

## Accessibility Requirements

| Requirement | Standard | Implementation |
|------------|----------|---------------|
| Nav links keyboard navigable | WCAG 2.1 AA | Focus ring: 2px solid `#e60000` offset 2px |
| Hamburger has aria-label | WCAG 4.1.2 | `aria-label="Open menu"` |
| Close button has aria-label | WCAG 4.1.2 | `aria-label="Close menu"` |
| Mobile overlay traps focus | WCAG 2.1 AA | Use `aria-modal="true"` on overlay div |
| Touch targets ≥ 44px | WCAG 2.5.5 | `min-h-[44px] min-w-[44px]` on hamburger and X buttons |
| Color contrast body text | WCAG AA (4.5:1) | `#e2e2e2` on `#131313` = ~14.4:1 — passes |
| Color contrast CTA button | WCAG AA (4.5:1) | `#fff7f5` on `#e60000` = ~4.6:1 — passes |
| Social icon links have sr-only text | WCAG 2.4.4 | `<span className="sr-only">Instagram</span>` inside each icon link |

---

## Do / Don't (Phase 1 enforcement)

| Do | Don't |
|----|-------|
| Use `bg-background` (`#131313`) for all page/overlay backgrounds | Use any white or light surface |
| Use Anton uppercase for all logo and heading text | Use Anton in sentence case |
| Use `primary-container` (`#e60000`) only for the "Book a Session" button and focus rings | Use red on decorative borders or headings |
| Use `border-outline-variant` for all structural dividers (nav bottom, footer top) | Use soft drop shadows |
| Define surfaces with `surface-container-low` / `surface-container` tonal depth | Layer multiple reds |
| Hard-left text alignment in footer columns | Center footer column text |
| `motion/react` import path (not `framer-motion`) | Import from `framer-motion` |
| Inline SVG for social icons | Use react-icons or icon font libraries |
| 4px border-radius on Primary button | Use more than 4px radius on buttons |
| Use weight 400 for all Anton and Hanken Grotesk text | Use Hanken Grotesk 500 — collapsed to 400 |
| Use weight 700 for all Barlow Condensed text | Use Barlow Condensed 600 — collapsed to 700 |
| Use exactly 4 type sizes: display-xl, headline-lg, body-md, label-sm | Use title-md (24px) or body-lg (18px) — removed |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
