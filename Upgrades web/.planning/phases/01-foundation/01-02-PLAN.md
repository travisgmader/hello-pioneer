---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - kinetic-power-web/package.json
  - kinetic-power-web/components/TopNav.tsx
  - kinetic-power-web/app/layout.tsx
  - kinetic-power-web/playwright.config.ts
  - kinetic-power-web/e2e/nav.spec.ts
autonomous: false
requirements: [GLOB-01]
user_setup: []

must_haves:
  truths:
    - "TopNav renders on every page with the Kinetic Power wordmark on the left"
    - "On desktop (≥768px) all five nav links and a red 'Book a Session' CTA are visible in the bar"
    - "On mobile (<768px) only a hamburger button is visible in the bar — nav links and CTA are hidden"
    - "Tapping the hamburger opens a full-viewport dark overlay with five large Anton uppercase nav links and a pinned red CTA at the bottom"
    - "The overlay closes when the user taps the X button OR the backdrop"
    - "The overlay open/close animation is a 200ms fade + 20px upward slide via Motion"
    - "Clicking a nav link in the overlay closes the overlay AND navigates to the route"
  artifacts:
    - path: "kinetic-power-web/components/TopNav.tsx"
      provides: "Top navigation with desktop bar + mobile hamburger + Motion overlay"
      contains: "'use client'"
      min_lines: 80
    - path: "kinetic-power-web/playwright.config.ts"
      provides: "Playwright E2E test runner configured for chromium + mobile-chrome"
      contains: "baseURL"
    - path: "kinetic-power-web/e2e/nav.spec.ts"
      provides: "E2E tests for GLOB-01 (desktop nav + mobile hamburger toggle + overlay)"
  key_links:
    - from: "kinetic-power-web/app/layout.tsx"
      to: "kinetic-power-web/components/TopNav.tsx"
      via: "import { TopNav } from '@/components/TopNav' rendered above {children}"
      pattern: "<TopNav"
    - from: "kinetic-power-web/components/TopNav.tsx"
      to: "motion package"
      via: "import { motion, AnimatePresence } from 'motion/react'"
      pattern: "from 'motion/react'"
---

# Plan 02 — TopNav with Mobile Overlay (vertical slice)

<objective>
Add the top navigation as a complete vertical slice: install Motion, build `components/TopNav.tsx` with the full-screen mobile overlay, render it in `app/layout.tsx` so it appears on all five routes, and add a Playwright test suite covering desktop layout + mobile hamburger toggle + overlay open/close behavior.

Purpose: GLOB-01 satisfied end-to-end. After this plan a real user can land on any route, see the TopNav, tap the hamburger on mobile, navigate via the overlay, and close it — all with the Motion fade+slide animation called out in D-10.

Output: Working nav on all 5 routes (desktop and mobile) + automated Playwright coverage proving the interaction loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@DESIGN.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@kinetic-power-web/app/layout.tsx
@kinetic-power-web/app/globals.css

<interfaces>
<!-- Tailwind utilities available after Plan 01 (DO NOT redeclare): -->
<!-- bg-background, bg-surface-container-low, bg-primary-container, -->
<!-- text-on-surface, text-on-surface-variant, text-on-primary-container, -->
<!-- border-outline-variant, font-anton, font-barlow-condensed, font-hanken-grotesk, -->
<!-- text-display-xl, text-headline-lg, text-body-md, text-label-sm, -->
<!-- tracking-headline, tracking-label -->

NAV_LINKS array (declare as module-level const in TopNav.tsx):
[
  { label: 'Home', href: '/' },
  { label: 'Amenities', href: '/amenities' },
  { label: 'Membership', href: '/membership' },
  { label: 'Schedule', href: '/schedule' },
  { label: 'Contact', href: '/contact' },
]

Motion import (per Pitfall 3):
import { motion, AnimatePresence } from 'motion/react'

Animation contract (D-10):
initial: { opacity: 0, y: 20 }
animate: { opacity: 1, y: 0 }
exit:    { opacity: 0, y: 20 }
transition: { duration: 0.2 }

aria labels:
- Hamburger button: aria-label="Open menu"
- X close button:   aria-label="Close menu"
- Overlay div:      aria-modal="true", role="dialog"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Playwright, write failing nav E2E tests (Wave 0 + RED)</name>
  <files>kinetic-power-web/package.json, kinetic-power-web/playwright.config.ts, kinetic-power-web/e2e/nav.spec.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (playwright.config.ts and e2e/nav.spec.ts patterns)
    - .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements section)
    - .planning/phases/01-foundation/01-UI-SPEC.md (TopNav component spec, Interaction States)
    - kinetic-power-web/package.json (current state — read before modifying)
  </read_first>
  <behavior>
    - Test: desktop nav renders the Kinetic Power wordmark
    - Test: desktop nav renders the "Book a Session" CTA link
    - Test: desktop nav renders all 5 nav link labels (Home, Amenities, Membership, Schedule, Contact)
    - Test: mobile viewport (375x812) shows hamburger button (`aria-label="Open menu"`) and HIDES the desktop nav links
    - Test: tapping the hamburger opens a dialog with `aria-modal="true"` and the X close button (`aria-label="Close menu"`) becomes visible
    - Test: tapping X closes the overlay (hamburger button returns to visible)
    - Test: tapping a nav link inside the overlay navigates to that route AND closes the overlay
    - All tests run against `http://localhost:3000` via the `webServer` config (Plan 01's dev server)
  </behavior>
  <action>
    From `kinetic-power-web/`: install Playwright as a dev dependency and download chromium browser.

    1. Run `npm install -D @playwright/test` from `kinetic-power-web/`. Run `npx playwright install chromium` (downloads browser binary).

    2. Create `kinetic-power-web/playwright.config.ts` per the pattern in `01-PATTERNS.md`. Use `testDir: './e2e'`, `baseURL: process.env['PLAYWRIGHT_BASE_URL'] ?? 'http://localhost:3000'`, two projects (`chromium` with `devices['Desktop Chrome']` and `mobile-chrome` with `devices['Pixel 5']`), and a `webServer` block that runs `npm run dev` on port 3000 unless `PLAYWRIGHT_BASE_URL` is set. `fullyParallel: true`, `forbidOnly: !!process.env['CI']`, `retries: process.env['CI'] ? 2 : 0`, `reporter: 'list'`.

    3. Create `kinetic-power-web/e2e/nav.spec.ts` with the RED tests per `01-PATTERNS.md` "e2e/nav.spec.ts" section. Adapt locator strategy to use `page.getByRole('button', { name: 'Open menu' })`, `page.getByRole('button', { name: 'Close menu' })`, `page.getByRole('link', { name: 'Book a Session' })`. Include the navigation-from-overlay test: click `Open menu` → click overlay link `Amenities` → assert `page.url()` ends with `/amenities` AND `Open menu` button is again visible (overlay closed).

    4. Add an npm script to `package.json`: `"test:e2e": "playwright test"`.

    5. Run `npx playwright test e2e/nav.spec.ts --reporter=list` — expect tests to FAIL because TopNav does not exist yet. This is the RED step in the TDD cycle; do not proceed to Task 2 until the failures are confirmed as "expected" (assertion failures, not config errors).

    Do NOT install `motion` in this task — that happens in Task 2 alongside the component creation.
  </action>
  <verify>
    <automated>cd kinetic-power-web && test -f playwright.config.ts && test -f e2e/nav.spec.ts && grep -q "@playwright/test" package.json && grep -q "Open menu" e2e/nav.spec.ts && grep -q "Close menu" e2e/nav.spec.ts && grep -q "Book a Session" e2e/nav.spec.ts && (npx playwright test e2e/nav.spec.ts --reporter=list 2>&1 | grep -qE "(failed|0 passed)") && echo "RED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `kinetic-power-web/playwright.config.ts` exists and exports a config with `testDir: './e2e'` and `baseURL` resolving to `'http://localhost:3000'` when `PLAYWRIGHT_BASE_URL` is unset
    - `kinetic-power-web/playwright.config.ts` declares at least two projects, with one named `mobile-chrome` using `devices['Pixel 5']`
    - `kinetic-power-web/e2e/nav.spec.ts` exists and contains tests for: desktop wordmark, desktop CTA, desktop link labels, mobile hamburger visible, overlay opens, overlay closes via X, navigation-from-overlay closes the overlay
    - `kinetic-power-web/package.json` has `@playwright/test` under `devDependencies` and a `test:e2e` script
    - Running `cd kinetic-power-web && npx playwright test e2e/nav.spec.ts --reporter=list` produces failing tests (RED state — TopNav does not exist yet)
  </acceptance_criteria>
  <done>
    Playwright installed, config exists with both desktop and mobile projects, nav.spec.ts written and FAILING because the implementation is intentionally not yet there.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build TopNav with mobile overlay, integrate into layout (GREEN)</name>
  <files>kinetic-power-web/package.json, kinetic-power-web/components/TopNav.tsx, kinetic-power-web/app/layout.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-PATTERNS.md (TopNav.tsx full pattern)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 + Pitfalls 3 + 5)
    - .planning/phases/01-foundation/01-UI-SPEC.md (TopNav Component Specifications, Interaction States)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08 through D-11)
    - kinetic-power-web/app/layout.tsx (current state — read before editing)
    - kinetic-power-web/e2e/nav.spec.ts (the RED tests this task must turn GREEN)
  </read_first>
  <behavior>
    - Component is a client component (`'use client'` at top of file)
    - Uses `useState<boolean>` for the overlay open/closed state
    - Renders `<header className="fixed top-0 inset-x-0 z-50 ...">` with `bg-surface-container-low` and `border-b border-outline-variant`
    - Inner container: `max-w-[var(--spacing-container-max)] mx-auto px-[var(--spacing-margin-mobile)] md:px-[var(--spacing-margin-desktop)] h-16 flex items-center justify-between`
    - Desktop nav links list hidden below `md:` breakpoint (`hidden md:flex`)
    - Hamburger button hidden above `md:` (`md:hidden`)
    - Overlay rendered via `<AnimatePresence>` wrapping a conditionally-rendered `<motion.div>` (D-10 animation params)
    - Backdrop click closes overlay (`onClick={e => if (e.target === e.currentTarget) setOpen(false)}`)
    - Overlay nav link click closes overlay AND navigates (via Next.js `<Link>` `onClick={() => setOpen(false)}`)
    - Pinned overlay CTA links to `/schedule` and closes overlay on tap
    - Hamburger and X buttons have `min-h-[44px] min-w-[44px]` (WCAG 2.5.5 per UI-SPEC)
    - Overlay div has `role="dialog"` and `aria-modal="true"`
  </behavior>
  <action>
    Install Motion and create the TopNav component, then mount it in the root layout.

    1. From `kinetic-power-web/`: `npm install motion` (NOT `framer-motion` — per Pitfall 3; the package name is `motion`).

    2. Create `kinetic-power-web/components/TopNav.tsx` following the full pattern in `01-PATTERNS.md` lines 261–391 with these named identifiers/conventions:
       - First line: `'use client'`
       - Imports: `import { useState } from 'react'`, `import { motion, AnimatePresence } from 'motion/react'`, `import Link from 'next/link'`
       - Module-level const `NAV_LINKS` with exactly the 5 entries shown in the `<interfaces>` block
       - Exported function `TopNav()` (named export — `export function TopNav()`)
       - State: `const [open, setOpen] = useState(false)`
       - Header shell: `<header className="fixed top-0 inset-x-0 z-50 bg-surface-container-low border-b border-outline-variant">`
       - Logo `<Link href="/">` with `font-anton text-on-surface uppercase` — text "Kinetic Power" (matches D-04 wordmark casing handled by `uppercase` utility)
       - Desktop nav: `<nav className="hidden md:flex items-center gap-8">` mapping NAV_LINKS to `<Link>` with classes `font-barlow-condensed text-on-surface uppercase tracking-label text-label-sm hover:text-on-surface-variant transition-colors duration-150`
       - Desktop CTA: `<Link href="/schedule" className="hidden md:inline-flex items-center px-5 py-2 bg-primary-container text-on-primary-container font-barlow-condensed uppercase rounded text-label-sm tracking-label">Book a Session</Link>` (text exactly "Book a Session" — UI-SPEC Copywriting Contract)
       - Hamburger button: `md:hidden text-on-surface p-2 min-h-[44px] min-w-[44px]`, `aria-label="Open menu"`, contains the three-line 24×24 SVG (per UI-SPEC)
       - Overlay: `<AnimatePresence>` wrapping `{open && <motion.div initial={{opacity:0,y:20}} animate={{opacity:1,y:0}} exit={{opacity:0,y:20}} transition={{duration:0.2}} role="dialog" aria-modal="true" className="fixed inset-0 z-50 bg-background flex flex-col px-[var(--spacing-margin-mobile)] pt-16 pb-8" onClick={e => { if (e.target === e.currentTarget) setOpen(false) }}>`
       - Overlay X button: `absolute top-4 right-4 text-on-surface p-2 min-h-[44px] min-w-[44px]`, `aria-label="Close menu"`, X-cross SVG
       - Overlay nav: `<nav className="flex flex-col gap-8 mt-8">` mapping NAV_LINKS to `<Link>` with classes `font-anton text-on-surface text-4xl uppercase` and `onClick={() => setOpen(false)}`
       - Overlay pinned CTA: `<Link href="/schedule" className="block w-full text-center py-4 bg-primary-container text-on-primary-container font-barlow-condensed uppercase text-label-sm tracking-label rounded" onClick={() => setOpen(false)}>Book a Session</Link>` wrapped in `<div className="mt-auto">`

    3. Edit `kinetic-power-web/app/layout.tsx` to import `TopNav` from `@/components/TopNav` and render `<TopNav />` inside `<body>` BEFORE `{children}`. Do NOT add `'use client'` to layout.tsx (Pitfall 5). Do NOT render `<Footer />` yet (Plan 03 owns that edit).

    4. Run `cd kinetic-power-web && npm run build` — must exit 0.

    5. Run `cd kinetic-power-web && npx playwright test e2e/nav.spec.ts --reporter=list` — all tests must now pass (GREEN).

    Per Pitfall 3 the import MUST be from `'motion/react'`, NEVER `'framer-motion'`. Per Pitfall 5 `'use client'` belongs ONLY in `TopNav.tsx`, never in `app/layout.tsx`. Per D-09 mobile overlay links are Anton uppercase, large, hard-left aligned. Per D-10 the animation is fade-in opacity 0→1 with y translate 20→0 over 200ms via Motion.
  </action>
  <verify>
    <automated>cd kinetic-power-web && grep -q "'use client'" components/TopNav.tsx && grep -q "from 'motion/react'" components/TopNav.tsx && ! grep -q "from 'framer-motion'" components/TopNav.tsx && ! grep -q "'use client'" app/layout.tsx && grep -q "TopNav" app/layout.tsx && grep -q "motion" package.json && npm run build 2>&1 | tail -10 && (npx playwright test e2e/nav.spec.ts --reporter=list 2>&1 | tee /tmp/nav-test.log | grep -qE "passed.*failed.*0|[0-9]+ passed") && ! grep -q "failed" /tmp/nav-test.log && echo "GREEN_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `kinetic-power-web/components/TopNav.tsx` exists with first non-comment line `'use client'`
    - `kinetic-power-web/components/TopNav.tsx` imports `motion` and `AnimatePresence` from the literal string `'motion/react'` (NOT `'framer-motion'`)
    - `kinetic-power-web/components/TopNav.tsx` exports a named function `TopNav`
    - `kinetic-power-web/components/TopNav.tsx` declares a module-level const `NAV_LINKS` with 5 entries including `{ label: 'Home', href: '/' }`, `{ label: 'Amenities', href: '/amenities' }`, `{ label: 'Membership', href: '/membership' }`, `{ label: 'Schedule', href: '/schedule' }`, `{ label: 'Contact', href: '/contact' }`
    - The hamburger button has `aria-label="Open menu"` and the X close button has `aria-label="Close menu"`
    - The mobile overlay `<motion.div>` has `role="dialog"` and `aria-modal="true"`
    - The motion animation props use exactly `initial={{ opacity: 0, y: 20 }}`, `animate={{ opacity: 1, y: 0 }}`, `exit={{ opacity: 0, y: 20 }}`, `transition={{ duration: 0.2 }}` (verbatim per D-10)
    - `kinetic-power-web/app/layout.tsx` imports `TopNav` from `@/components/TopNav` and renders `<TopNav />` inside `<body>`
    - `kinetic-power-web/app/layout.tsx` does NOT contain `'use client'`
    - `kinetic-power-web/package.json` lists `motion` under `dependencies` (NOT `framer-motion`)
    - `cd kinetic-power-web && npm run build` exits 0
    - `cd kinetic-power-web && npx playwright test e2e/nav.spec.ts --reporter=list` exits 0 with all tests passing (GREEN)
  </acceptance_criteria>
  <done>
    TopNav renders on all 5 routes, desktop bar + mobile hamburger + Motion overlay all work, all RED tests are now GREEN, no Pitfall violations (motion/react import, no use-client on layout, no framer-motion package).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    `TopNav` component with desktop bar (logo left, nav links right, red "Book a Session" CTA far right) and mobile overlay (hamburger → full-screen dark overlay with large Anton uppercase links + X close button + pinned red CTA at bottom + Motion fade+slide animation). Mounted in `app/layout.tsx` so it appears on all five routes. Playwright suite green.
  </what-built>
  <how-to-verify>
    Claude will have already run the Playwright suite and gotten all green. You should manually verify the visual + interaction quality:

    1. Start the dev server if not running: `cd kinetic-power-web && npm run dev`
    2. Open `http://localhost:3000/` in a desktop browser at full width (≥1024px)
       - Confirm: "KINETIC POWER" wordmark in Anton uppercase on the left
       - Confirm: 5 nav links (Home, Amenities, Membership, Schedule, Contact) in the right cluster, Barlow Condensed uppercase
       - Confirm: red "Book a Session" button on the far right
       - Confirm: hover on a nav link transitions the color (150ms)
    3. Shrink the browser to <768px width (or DevTools mobile view, e.g. iPhone 12 / 390px)
       - Confirm: nav links and CTA disappear; only logo + hamburger remain
    4. Tap the hamburger
       - Confirm: full-screen dark overlay (`#131313`) fades in with a subtle upward slide (~200ms)
       - Confirm: X button visible top-right
       - Confirm: 5 large Anton uppercase nav links, hard-left aligned
       - Confirm: red "Book a Session" button pinned at the bottom of the overlay
    5. Tap the X button → overlay fades out and slides down. Confirm the hamburger is back.
    6. Tap the hamburger again → tap any nav link inside the overlay → confirm the overlay closes AND the URL changes to the chosen route AND that route's title (e.g. AMENITIES) is visible
    7. Tap the hamburger → tap on the empty dark space ABOVE the X (the backdrop, but not on the X itself) — confirm overlay closes
    8. Tap the "Book a Session" red CTA in the desktop bar → confirm URL is now `/schedule`

    Type `approved` or describe issues.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client browser JS → DOM | TopNav runs in the browser (`'use client'` boundary) — first introduction of client-side code in this project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm install of `motion` package | mitigate | Per RESEARCH.md Package Legitimacy Audit, `motion` is the renamed `framer-motion` package from Framer, 8+ years old, 8M+/wk downloads, source repo `github.com/framer/motion`. Listed as `[ASSUMED]` legitimate; no `[SUS]`/`[SLOP]` flags. Phase 1 install acceptable without a separate human-verify checkpoint per RESEARCH.md guidance. |
| T-02-02 | Tampering | npm install of `@playwright/test` (devDependency) | mitigate | `@playwright/test` is an official Microsoft package (github.com/microsoft/playwright), 1M+/wk downloads, well-established. Dev-only — never ships to the production browser bundle. |
| T-02-03 | Spoofing | Mobile overlay nav links lacking valid `<Link>` semantics could redirect users to unexpected routes | mitigate | All overlay links use Next.js `<Link>` with `href` from a frozen module-level NAV_LINKS const — no dynamic URL construction, no user input touches the href values. |
| T-02-04 | Information disclosure | Overlay `aria-modal="true"` failure → screen reader users could escape the modal and interact with hidden content | accept | UI-SPEC requires aria-modal; we set it correctly. Full focus-trap implementation deferred (out of scope for Phase 1 — would require an additional library or hand-rolled focus management). Acceptable risk for a marketing site MVP; revisit if accessibility audit raises it. |
| T-02-SC | Tampering | npm/pnpm install at task time | mitigate | All packages added in this plan (`motion`, `@playwright/test`) are from major orgs with high download counts per RESEARCH.md Package Legitimacy Audit. No `[SUS]` or `[SLOP]` packages introduced. |

ASVS L1 applicability: V4 Access Control (public marketing site — no protected routes), V5 Input Validation (no inputs in this plan), V14 Configuration — no secrets introduced.
</threat_model>

<verification>
1. `cd kinetic-power-web && npm run build` exits 0
2. `cd kinetic-power-web && npx playwright test e2e/nav.spec.ts` exits 0 with all tests passing
3. Manual verification of overlay animation, link behavior, and responsive breakpoint (the human-verify checkpoint)
</verification>

<success_criteria>
- TopNav visible on every route (`/`, `/amenities`, `/membership`, `/schedule`, `/contact`) on both desktop and mobile viewports
- Desktop shows logo + 5 nav links + red "Book a Session" CTA
- Mobile (<768px) shows logo + hamburger; tap opens full-screen dark overlay with Motion fade+slide
- Overlay closes on X tap OR backdrop tap OR nav link tap (with navigation)
- All nav.spec.ts tests pass in Playwright
- No `framer-motion` package, no `'use client'` on layout.tsx (Pitfalls 3 + 5 cleared)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done. Include the test run output (passing count) and a note that the next push to `main` will deploy the nav to Vercel.
</output>
