---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kinetic-power-web/package.json
  - kinetic-power-web/tsconfig.json
  - kinetic-power-web/next.config.ts
  - kinetic-power-web/postcss.config.mjs
  - kinetic-power-web/.gitignore
  - kinetic-power-web/app/globals.css
  - kinetic-power-web/app/layout.tsx
  - kinetic-power-web/app/page.tsx
  - kinetic-power-web/app/amenities/page.tsx
  - kinetic-power-web/app/membership/page.tsx
  - kinetic-power-web/app/schedule/page.tsx
  - kinetic-power-web/app/contact/page.tsx
  - kinetic-power-web/lib/fonts.ts
autonomous: false
requirements: [GLOB-04, GLOB-05]
user_setup:
  - service: github
    why: "Repo creation under developer's personal account (D-01) — needed for Vercel auto-deploy integration"
    env_vars: []
    dashboard_config:
      - task: "Authenticate gh CLI (if not already): gh auth login"
        location: "Terminal — one-time setup"
  - service: vercel
    why: "Project creation for GitHub auto-deploy integration (D-02, D-03)"
    env_vars: []
    dashboard_config:
      - task: "Sign in to vercel.com with the developer's account, then connect the kinetic-power-web GitHub repo via 'Add New Project'"
        location: "vercel.com dashboard"

must_haves:
  truths:
    - "Visiting the Vercel preview/production URL loads a dark (#131313) page"
    - "All five routes (/, /amenities, /membership, /schedule, /contact) return HTTP 200"
    - "Anton, Barlow Condensed, and Hanken Grotesk fonts are self-hosted and applied to text"
    - "Each shell page displays its uppercase Anton title (HOME, AMENITIES, MEMBERSHIP, SCHEDULE, CONTACT)"
    - "Pushing to the main branch on GitHub triggers an auto-deploy on Vercel"
  artifacts:
    - path: "kinetic-power-web/package.json"
      provides: "Next.js 16 + Tailwind v4 + TypeScript scaffold dependency manifest"
      contains: "next, react, react-dom, tailwindcss, @tailwindcss/postcss, typescript"
    - path: "kinetic-power-web/app/globals.css"
      provides: "Tailwind v4 @theme block with all DESIGN.md tokens"
      contains: "@import \"tailwindcss\"; @theme {"
      min_lines: 80
    - path: "kinetic-power-web/lib/fonts.ts"
      provides: "next/font/google loader for Anton, Barlow_Condensed, Hanken_Grotesk"
      exports: ["anton", "barlowCondensed", "hankenGrotesk"]
    - path: "kinetic-power-web/app/layout.tsx"
      provides: "Root layout with font CSS variable injection on <html>"
      contains: "className={`${anton.variable} ${barlowCondensed.variable} ${hankenGrotesk.variable}`}"
    - path: "kinetic-power-web/app/page.tsx"
      provides: "/ route shell"
    - path: "kinetic-power-web/app/amenities/page.tsx"
      provides: "/amenities route shell"
    - path: "kinetic-power-web/app/membership/page.tsx"
      provides: "/membership route shell"
    - path: "kinetic-power-web/app/schedule/page.tsx"
      provides: "/schedule route shell"
    - path: "kinetic-power-web/app/contact/page.tsx"
      provides: "/contact route shell"
  key_links:
    - from: "kinetic-power-web/app/layout.tsx"
      to: "kinetic-power-web/lib/fonts.ts"
      via: "import { anton, barlowCondensed, hankenGrotesk } from '@/lib/fonts'"
      pattern: "import.*from.*@/lib/fonts"
    - from: "kinetic-power-web/app/globals.css"
      to: "next/font/google CSS variables"
      via: "@theme inline mapping --font-anton-loaded → --font-anton"
      pattern: "@theme inline"
    - from: "GitHub kinetic-power-web repo main branch"
      to: "Vercel deployment"
      via: "GitHub integration auto-deploy (D-03)"
      pattern: "Vercel detects Next.js on push"
---

# Plan 01 — Walking Skeleton: Scaffold, Tokens, Routes, Deploy

## Phase Goal

**As a** prospective gym member or stakeholder, **I want to** load the Kinetic Power site from a public URL and see all five planned routes render with the dark brand background and elite typography, **so that** I have visible proof the project is live on the internet and the design system foundation is in place.

<objective>
Create the Next.js + Tailwind v4 + TypeScript project, wire ALL DESIGN.md tokens into a single @theme block, load three Google Fonts via next/font, scaffold five route shells, and deploy via GitHub → Vercel auto-deploy integration. This is the Walking Skeleton — the thinnest end-to-end working stack.

Purpose: Lock in framework, styling system, typography, deployment pipeline, and routing in one vertical slice so Plans 02 and 03 can add nav and footer onto a working live URL.

Output: A deployed Vercel preview URL that renders 5 dark-themed Anton-titled shell pages. GitHub repo connected. No TopNav, no Footer (Plans 02 and 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@DESIGN.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/SKELETON.md

<interfaces>
<!-- Token names that this plan MUST produce in globals.css (verbatim per D-05). -->
<!-- Downstream plans 02 and 03 will consume these via Tailwind utility classes. -->

Tailwind v4 generated utilities expected after this plan ships:
- `bg-background` (resolves to `#131313`)
- `bg-surface-container-low` (resolves to `#1b1b1b`) — TopNav background (Plan 02)
- `bg-surface-container` (resolves to `#1f1f1f`) — Footer background (Plan 03)
- `bg-primary-container` (resolves to `#e60000`) — Primary CTA button
- `text-on-surface` (resolves to `#e2e2e2`)
- `text-on-surface-variant` (resolves to `#e9bcb5`)
- `text-on-primary-container` (resolves to `#fff7f5`)
- `border-outline-variant` (resolves to `#5f3f3a`)
- `text-display-xl` (64px), `text-headline-lg` (32px), `text-body-md` (16px), `text-label-sm` (12px)
- `tracking-headline` (0.02em), `tracking-label` (0.1em)
- `font-anton`, `font-barlow-condensed`, `font-hanken-grotesk`

CSS variable names that `lib/fonts.ts` MUST export via next/font `variable:` option:
- `--font-anton-loaded`
- `--font-barlow-condensed-loaded`
- `--font-hanken-grotesk-loaded`

These names are referenced by `@theme inline { --font-anton: var(--font-anton-loaded); ... }` inside `globals.css`. The `-loaded` suffix is deliberate to avoid colliding with Tailwind v4's `--font-*` namespace.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold Next.js project, wire Tailwind v4 tokens + fonts + 5 route shells</name>
  <files>kinetic-power-web/package.json, kinetic-power-web/tsconfig.json, kinetic-power-web/next.config.ts, kinetic-power-web/postcss.config.mjs, kinetic-power-web/.gitignore, kinetic-power-web/app/globals.css, kinetic-power-web/app/layout.tsx, kinetic-power-web/app/page.tsx, kinetic-power-web/app/amenities/page.tsx, kinetic-power-web/app/membership/page.tsx, kinetic-power-web/app/schedule/page.tsx, kinetic-power-web/app/contact/page.tsx, kinetic-power-web/lib/fonts.ts</files>
  <read_first>
    - DESIGN.md (full file — token names, type scale, color palette)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 globals.css, Pattern 2 fonts.ts, Pitfalls 1–7)
    - .planning/phases/01-foundation/01-PATTERNS.md (file-by-file analog mappings)
    - .planning/phases/01-foundation/01-UI-SPEC.md (@theme Token Reference section — verbatim source for globals.css)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 through D-07 — token decisions)
    - CLAUDE.md (Recommended Stack table — version targets)
  </read_first>
  <action>
    Run `npx create-next-app@latest kinetic-power-web --yes --use-npm` from the working directory `/Users/travismader/Desktop/Pioneer/Upgrades web`. After scaffold:

    1. Confirm scaffolded versions in `kinetic-power-web/package.json`. Expected: `next` ^16.x, `react` ^19.x, `tailwindcss` ^4.x, `@tailwindcss/postcss` ^4.x. If `tailwindcss` is on v3, upgrade with `npm install tailwindcss@latest @tailwindcss/postcss@latest postcss@latest`.

    2. Delete `kinetic-power-web/tailwind.config.ts` (or `.js`) if the scaffold generated one (Pitfall 4). Tailwind v4 is CSS-first only.

    3. Verify `kinetic-power-web/postcss.config.mjs` exports `{ plugins: { "@tailwindcss/postcss": {} } }`. Overwrite to this exact value if different.

    4. Create `kinetic-power-web/lib/fonts.ts` that imports `Anton`, `Barlow_Condensed`, `Hanken_Grotesk` from `next/font/google`. Export named consts `anton` (weight `'400'`, `variable: '--font-anton-loaded'`), `barlowCondensed` (weight `'700'`, `variable: '--font-barlow-condensed-loaded'`), `hankenGrotesk` (weight `'400'`, `variable: '--font-hanken-grotesk-loaded'`). All three: `subsets: ['latin']`, `display: 'swap'`. Per D-04/UI-SPEC, only weights actually used in Phase 1 should be loaded — 400 for Anton/Hanken, 700 for Barlow Condensed.

    5. Overwrite `kinetic-power-web/app/globals.css` with the complete `@theme` block from `01-UI-SPEC.md` section "@theme Token Reference". This includes: `@import "tailwindcss";`, the full color token map (38 color variables verbatim per D-05), the four type scale tokens (`--text-display-xl`, `--text-headline-lg`, `--text-body-md`, `--text-label-sm`) with their `--line-height` companions, letter spacing (`--tracking-headline`, `--tracking-label`), all 10 spacing tokens, 6 border-radius tokens, AND the `@theme inline` block mapping `--font-anton: var(--font-anton-loaded)` and the other two font variables. Token names must match DESIGN.md verbatim per D-05.

    6. Overwrite `kinetic-power-web/app/layout.tsx` to import the three font consts from `@/lib/fonts`, import `./globals.css`, and render `<html lang="en" className={\`${anton.variable} ${barlowCondensed.variable} ${hankenGrotesk.variable}\`}><body className="bg-background text-on-surface min-h-screen">{children}</body></html>`. Add `export const metadata: Metadata = { title: 'Kinetic Power', description: 'Elite training facility.' }`. Do NOT add `'use client'` (Pitfall 5). Do NOT render `<TopNav />` or `<Footer />` yet — those are Plans 02 and 03 and will edit this file.

    7. Overwrite `kinetic-power-web/app/page.tsx` (Home) to a Server Component exporting default `HomePage()` that returns `<main className="min-h-screen pt-16"><h1 className="font-anton uppercase text-display-xl md:text-[6rem] text-on-surface">HOME</h1></main>`.

    8. Create `kinetic-power-web/app/amenities/page.tsx`, `kinetic-power-web/app/membership/page.tsx`, `kinetic-power-web/app/schedule/page.tsx`, `kinetic-power-web/app/contact/page.tsx` using the same shell pattern as Home — Server Component, `<main className="min-h-screen pt-16">`, h1 in Anton uppercase showing `AMENITIES`, `MEMBERSHIP`, `SCHEDULE`, `CONTACT` respectively.

    9. Confirm `.gitignore` (scaffolded) includes `node_modules`, `.next`, `.env*.local`, `.vercel`. Add any missing entries.

    10. Run `cd kinetic-power-web && npm run build` and confirm exit 0. Resolve any TypeScript or Tailwind compile errors before proceeding to the verify step.

    Per D-07 do NOT define `--text-display-xl-mobile` or any `-mobile` token name. Per D-04 ALL DESIGN.md tokens go into @theme — colors, spacing, type scale, radius — single source of truth. Per D-05 token names verbatim — no aliases.
  </action>
  <verify>
    <automated>cd kinetic-power-web && npm run build 2>&1 | tail -30 ; test $? -eq 0 && grep -q 'bg-background' app/globals.css && grep -q '@import "tailwindcss"' app/globals.css && grep -q "Hanken_Grotesk" lib/fonts.ts && grep -q "anton.variable" app/layout.tsx && test -f app/page.tsx && test -f app/amenities/page.tsx && test -f app/membership/page.tsx && test -f app/schedule/page.tsx && test -f app/contact/page.tsx && echo "VERIFY_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `cd kinetic-power-web && npm run build` exits 0 with no TypeScript or Tailwind errors
    - `kinetic-power-web/package.json` contains `"next": "^16"` (or any 16.x range) and `"tailwindcss": "^4"` (or any 4.x range)
    - `kinetic-power-web/tailwind.config.ts` and `kinetic-power-web/tailwind.config.js` both do NOT exist
    - `kinetic-power-web/postcss.config.mjs` exports an object with `plugins["@tailwindcss/postcss"]` defined
    - `kinetic-power-web/app/globals.css` contains the literal string `@import "tailwindcss"` on its first non-comment line
    - `kinetic-power-web/app/globals.css` contains `--color-background: #131313`, `--color-primary-container: #e60000`, `--color-surface-container-low: #1b1b1b`, `--color-on-surface: #e2e2e2`, `--color-outline-variant: #5f3f3a`, `--text-display-xl: 4rem`, `--text-headline-lg: 2rem`, `--text-body-md: 1rem`, `--text-label-sm: 0.75rem`, `--tracking-headline: 0.02em`, `--tracking-label: 0.1em`, `--spacing-margin-mobile: 1rem`, `--spacing-margin-desktop: 2.5rem`, `--spacing-container-max: 80rem` (verbatim)
    - `kinetic-power-web/app/globals.css` contains an `@theme inline` block mapping `--font-anton: var(--font-anton-loaded)`, `--font-barlow-condensed: var(--font-barlow-condensed-loaded)`, `--font-hanken-grotesk: var(--font-hanken-grotesk-loaded)`
    - `kinetic-power-web/app/globals.css` does NOT contain any token name ending in `-mobile` (D-07 enforcement)
    - `kinetic-power-web/lib/fonts.ts` imports `Anton`, `Barlow_Condensed`, `Hanken_Grotesk` from `next/font/google`
    - `kinetic-power-web/lib/fonts.ts` exports `anton`, `barlowCondensed`, `hankenGrotesk` and each has `variable: '--font-{slug}-loaded'`
    - `kinetic-power-web/app/layout.tsx` does NOT contain `'use client'` anywhere
    - `kinetic-power-web/app/layout.tsx` references `anton.variable`, `barlowCondensed.variable`, `hankenGrotesk.variable` on the `<html>` element
    - `kinetic-power-web/app/layout.tsx` `<body>` has classes `bg-background` and `text-on-surface`
    - All five route files exist: `app/page.tsx`, `app/amenities/page.tsx`, `app/membership/page.tsx`, `app/schedule/page.tsx`, `app/contact/page.tsx`
    - Each route file contains `font-anton uppercase` and `text-display-xl` on its h1
    - `cd kinetic-power-web && npm run dev` (background) followed by `curl -sI http://localhost:3000/` returns `200 OK` and the same for `/amenities`, `/membership`, `/schedule`, `/contact`
  </acceptance_criteria>
  <done>
    Build succeeds, 5 route shells render with dark background and uppercase Anton title, no `tailwind.config.*` exists, all DESIGN.md tokens declared verbatim in `@theme`, fonts load via `next/font` with the `-loaded` CSS variable convention.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Next.js 16 project scaffolded under `kinetic-power-web/`, Tailwind v4 with the complete DESIGN.md `@theme` token block in `app/globals.css`, three Google Fonts (Anton, Barlow Condensed, Hanken Grotesk) loaded via `next/font` and exposed as CSS variables, root layout with font injection on `<html>`, and five route shells (`/`, `/amenities`, `/membership`, `/schedule`, `/contact`) each rendering an uppercase Anton title on a `#131313` background. No TopNav, no Footer — Plans 02 and 03.
  </what-built>
  <how-to-verify>
    Claude will start the dev server (`cd kinetic-power-web && npm run dev`) and pause here. You should:

    1. Open `http://localhost:3000/` in a browser
    2. Confirm the page background is dark (close to black, `#131313`)
    3. Confirm the word `HOME` is visible, in a tall condensed uppercase typeface (Anton)
    4. Visit `http://localhost:3000/amenities` — confirm `AMENITIES` heading appears, same styling
    5. Visit `http://localhost:3000/membership`, `http://localhost:3000/schedule`, `http://localhost:3000/contact` — confirm each shows its title
    6. Open DevTools → Network tab → reload → filter "fonts" — confirm font files are served from the same origin (e.g. `localhost:3000/_next/static/media/...`) and NOT from `fonts.googleapis.com` or `fonts.gstatic.com`
    7. Open DevTools → Elements → inspect `<body>` → confirm computed `background-color` is `rgb(19, 19, 19)`

    Type `approved` to continue OR describe any visual / behavioral issues you find.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create GitHub repo and wire Vercel auto-deploy</name>
  <files>kinetic-power-web/.git, kinetic-power-web/README.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01, D-02, D-03 — repo + Vercel decisions)
    - .planning/phases/01-foundation/01-RESEARCH.md (Environment Availability section — confirms gh and vercel CLI present)
  </read_first>
  <action>
    Per D-01 create GitHub repo `kinetic-power-web` under the developer's personal account. Per D-03 connect via GitHub→Vercel integration so push-to-main auto-deploys (do NOT use `vercel --prod` manual deploys).

    1. Create a minimal `kinetic-power-web/README.md` containing a single H1 `# Kinetic Power Website` and a one-line description. This file gives the repo a landing page on GitHub.

    2. Inside `kinetic-power-web/`, initialize git: `git init`, `git add -A`, `git commit -m "feat(01): walking skeleton — scaffold, tokens, route shells"`. Set the default branch to `main` if it isn't already: `git branch -M main`.

    3. Create the GitHub repo and push using gh CLI from inside `kinetic-power-web/`:
       `gh repo create kinetic-power-web --public --source=. --remote=origin --push`
       (If the developer prefers a private repo they can re-run with `--private` — this is Claude's discretion area but public is the default for marketing sites with no secrets.)
       If `gh repo create` fails with auth, the human-verify checkpoint at the next step will surface it; do not silently retry.

    4. Connect the repo to Vercel. Try this order:
       a. Attempt `vercel link --yes --project kinetic-power-web` from inside `kinetic-power-web/`. If it succeeds, run `vercel git connect` to wire the GitHub integration (so future pushes auto-deploy).
       b. If either CLI step requires interactive auth that cannot complete from a non-TTY shell, leave the project to be connected via the next human-verify checkpoint where the user opens vercel.com and clicks "Add New Project → Import from GitHub → kinetic-power-web".

    5. Run `git push origin main` once more (no-op if already up to date). The first push after Vercel integration triggers the first deploy.

    Do not commit any `.env*` files. Do not configure any environment variables — Phase 1 has none (RESEARCH Security Domain).
  </action>
  <verify>
    <automated>cd kinetic-power-web && git remote get-url origin 2>&1 | grep -q "github.com" && git log -1 --pretty=%s | grep -q "feat" && test -f README.md && echo "VERIFY_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `kinetic-power-web/.git` directory exists
    - `cd kinetic-power-web && git remote get-url origin` outputs a URL containing `github.com` and ending in `kinetic-power-web` or `kinetic-power-web.git`
    - `cd kinetic-power-web && git log --oneline | wc -l` is at least 1
    - The remote repository is reachable: `gh repo view kinetic-power-web --json url --jq .url` returns a github.com URL
    - `kinetic-power-web/README.md` exists and contains the string `Kinetic Power`
    - No file matching `.env*.local`, `.env`, or any secret pattern is staged or committed (check with `git log --all --diff-filter=A --name-only | grep -E '\\.env'` returning empty)
  </acceptance_criteria>
  <done>
    GitHub repo `kinetic-power-web` exists under developer's account, code is pushed to `main`, and Vercel integration is either fully connected via CLI or staged for one-click connection in the next human-verify step. No secrets committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    GitHub repo `kinetic-power-web` created and code pushed. Vercel CLI either succeeded in linking the project, OR the link must be completed via the vercel.com dashboard. After dashboard connection, the first auto-deploy triggers on the existing `main` branch.
  </what-built>
  <how-to-verify>
    Claude has finished what it can automate. You should:

    1. Open `https://github.com/<your-username>/kinetic-power-web` in a browser
    2. Confirm the repo exists, default branch is `main`, and `app/`, `lib/`, `package.json` are visible at the root
    3. Open `https://vercel.com/dashboard` in a browser
    4. If the project `kinetic-power-web` does NOT appear: click "Add New… → Project → Import from GitHub" and select the `kinetic-power-web` repo. Accept all detected defaults (Next.js framework, root directory `./`, build command `next build`). Click Deploy.
    5. If the project DOES already appear (CLI linked it): open the project → Deployments tab and confirm a deployment is running or successful
    6. Wait for the deployment to finish (status: Ready). Copy the Production URL (e.g. `https://kinetic-power-web.vercel.app` or a hashed preview URL).
    7. Open the production URL in your phone browser (or Chrome DevTools mobile emulation) and confirm:
       - Page background is dark `#131313`
       - The word `HOME` is visible in Anton uppercase
       - Navigating to `/amenities`, `/membership`, `/schedule`, `/contact` each render their title
       - The page loads in under 3 seconds on a mobile connection (subjective — formal Lighthouse check happens after Plan 03)
    8. Confirm push-to-main auto-deploy works: make a trivial commit (`echo "" >> README.md && git commit -am "test: trigger auto-deploy" && git push`), watch Vercel dashboard, confirm a new deployment starts within ~30 seconds. (Optional but recommended — this is the GLOB-05 deploy pipeline contract.)

    Type the production URL when approving (`approved <url>`) so it gets recorded in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved &lt;production-url&gt;" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → public GitHub repo | Source code transitions from private filesystem to a public hosted repository |
| GitHub → Vercel build environment | Build pipeline runs against pushed code with no human in the loop |
| Vercel CDN → public internet | Built static assets served to anonymous users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | `.env*` files accidentally committed to public GitHub repo | mitigate | `create-next-app` scaffolds `.gitignore` with `.env*.local`; verify in Task 1 acceptance and Task 3 acceptance (no `.env*` in git log). No environment variables are configured in Phase 1 — nothing sensitive exists to leak. |
| T-01-02 | Tampering | npm package installs at `npm install` time | mitigate | All packages installed are scaffolded by `create-next-app` (next, react, tailwindcss, @tailwindcss/postcss, postcss, typescript) — Tailwind Labs, Vercel, Facebook, Microsoft sources. Per RESEARCH.md Package Legitimacy Audit, all packages are `[ASSUMED]` legitimate due to major-org provenance and 5M+/wk download counts. No new packages are added in Plan 01 beyond the scaffold. Motion is added in Plan 02. |
| T-01-03 | Information disclosure | Public GitHub repo exposing source | accept | Frontend marketing site has no secrets, no auth, no backend, no PII. Source disclosure is expected (open development). README.md is intentionally minimal. |
| T-01-04 | Denial of service | Vercel free tier bandwidth abuse | accept | Free tier rate limits and Vercel platform controls handle this. No mitigation needed at Phase 1. |
| T-01-SC | Tampering | npm install at scaffold time | accept | All packages in scaffold are from major orgs (Vercel, Tailwind Labs, Facebook, Microsoft). slopcheck was unavailable per RESEARCH.md but legitimacy is not in doubt — no `[SUS]` or `[SLOP]` packages. The plan adds zero new packages beyond the scaffold. |

ASVS L1 categories applicable in Phase 1: V14 Configuration (no secrets in git). All others (V2 Auth, V3 Session, V4 Access, V5 Input, V6 Crypto) are not applicable — there is no auth, no input, no sessions, no crypto in this phase.
</threat_model>

<verification>
1. `cd kinetic-power-web && npm run build` exits 0
2. All 5 routes return 200 when `npm run dev` is running (or on the deployed URL)
3. Browser inspection confirms `background-color: rgb(19, 19, 19)` on `<body>` and self-hosted font files in the network tab
4. GitHub repo `kinetic-power-web` exists, push to `main` triggers a Vercel auto-deploy (verified in human-verify Task 4)
</verification>

<success_criteria>
- A deployed Vercel URL exists and is reachable from the public internet
- That URL renders five distinct routes, each with the dark `#131313` background and an Anton uppercase title
- `cd kinetic-power-web && npm run build` succeeds locally with zero errors
- `app/globals.css` contains the complete DESIGN.md token map per D-04 / D-05 / D-06 / D-07
- `app/layout.tsx` is a Server Component (no `'use client'`) and injects three font CSS variables on `<html>`
- GitHub → Vercel integration is in place per D-03 (push-to-main auto-deploys)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done. Include the production URL in the summary so Plans 02 and 03 (and the eventual Lighthouse check) can reference it.
</output>
