---
phase: 02-members-onboarding-billing
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - src/components/members/CoppaConsentSheet.tsx
  - src/components/members/CoppaConsentSheet.module.css
  - src/components/members/PremiumGateSheet.tsx
  - src/components/members/PremiumGateSheet.module.css
  - src/components/members/AddEditMemberSheet.tsx
  - src/components/members/MemberCard.tsx
  - src/routes/members.tsx
autonomous: false
requirements: [ONBD-04, ONBD-05, MEMB-01]
user_setup: []

must_haves:
  truths:
    - "Checking 'Under 13' and saving routes through CoppaConsentSheet; the child member is NOT written until the consent checkbox is confirmed"
    - "On consent, the child member is written with coppa_consented_at set to the confirmation timestamp"
    - "A free-tier parent (trial expired, no subscription) sees a locked Invite action with a 'Premium' badge; tapping it opens PremiumGateSheet instead of InviteSheet"
    - "During an active trial, the Invite action is unlocked (useTier returns premium)"
    - "The PremiumGateSheet Upgrade CTA shows a placeholder toast (no navigation — Phase 7 wires billing)"
  artifacts:
    - path: "src/components/members/CoppaConsentSheet.tsx"
      provides: "COPPA single-checkbox consent gate"
      min_lines: 25
    - path: "src/components/members/PremiumGateSheet.tsx"
      provides: "free-tier invite block sheet"
      min_lines: 20
  key_links:
    - from: "src/components/members/AddEditMemberSheet.tsx"
      to: "CoppaConsentSheet + coppa_consented_at"
      via: "Under-13 save gate"
      pattern: "coppa_consented_at"
    - from: "src/components/members/MemberCard.tsx"
      to: "PremiumGateSheet vs InviteSheet"
      via: "useTier tier check"
      pattern: "useTier"
---

<objective>
Close Phase 2 with the compliance + gating slice: the COPPA consent gate that blocks any under-13 child record until a parent confirms consent (writing coppa_consented_at), and the premium gate that locks invitations for free-tier families and routes them to PremiumGateSheet instead of InviteSheet. This makes invitations the phase's first real premium gate (D-16) and satisfies the COPPA success criterion (no child data before parental consent).

Purpose: ONBD-04 (COPPA) and ONBD-05 (tier gating) are the last two success criteria. The COPPA legal interpretation is the phase's highest-risk assumption (RESEARCH A3) and gets a human checkpoint.
Output: CoppaConsentSheet + PremiumGateSheet, wired into AddEditMemberSheet/MemberCard/members; CoppaConsentSheet + premium-gate stubs green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-members-onboarding-billing/02-RESEARCH.md
@.planning/phases/02-members-onboarding-billing/02-PATTERNS.md
@.planning/phases/02-members-onboarding-billing/02-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CoppaConsentSheet + Under-13 save gate (no child data before consent)</name>
  <read_first>
    - src/components/members/AddEditMemberSheet.tsx (the Under-13 toggle + handleSave stub from Plan 03; useUpsertMember call site)
    - src/components/members/InviteSheet.tsx (vaul Drawer usage pattern to mirror)
    - src/data/useUpsertMember.ts (accepts coppa_consented_at field — Plan 02)
    - src/data/types.ts (members.coppa_consented_at after Plan 01 regen)
    - .planning/phases/02-members-onboarding-billing/02-UI-SPEC.md (§Copywriting COPPA consent sheet — verbatim title/body/checkbox/confirm/cancel/validation)
    - .planning/phases/02-members-onboarding-billing/02-RESEARCH.md (Pattern 5 COPPA gate + handleSave; D-13/D-14/D-15; A3 highest-risk assumption)
    - .planning/phases/02-members-onboarding-billing/02-PATTERNS.md (§CoppaConsentSheet, §AddEditMemberSheet COPPA gate)
    - tests/integration/... and src/components/members/CoppaConsentSheet.test.tsx (Plan 01 stub)
  </read_first>
  <behavior>
    - With "Under 13" checked, tapping Save does NOT call useUpsertMember; it opens CoppaConsentSheet.
    - CoppaConsentSheet Confirm is disabled / shows "Check the box to confirm consent." until the checkbox is checked.
    - On confirm, the member is written once with coppa_consented_at = new Date().toISOString().
    - With "Under 13" unchecked, Save writes immediately with coppa_consented_at null.
  </behavior>
  <action>
    Create src/components/members/CoppaConsentSheet.tsx (+ .module.css) per D-14 + 02-UI-SPEC.md: a vaul Drawer (open/close, no snap points) with title "Parental consent", the body summary verbatim ("For a child under 13, we store only their name, emoji, color, chore completions, and calendar events — nothing else, and nothing leaves your family."), a single checkbox with the consent label verbatim, primary "Confirm and add", secondary "Back", and the validation "Check the box to confirm consent." when unchecked. Props: `open`, `onConfirm`, `onCancel`. Confirm only fires onConfirm when checked.
    Edit src/components/members/AddEditMemberSheet.tsx to implement the gate per Pattern 5: in handleSave, `if (isUnder13 && !coppaConfirmed) { setShowCoppaSheet(true); return; }`; CoppaConsentSheet.onConfirm sets coppaConfirmed true and calls upsertMember with `coppa_consented_at: isUnder13 ? new Date().toISOString() : null`; non-under-13 path passes coppa_consented_at null. The under-13 child member row must NOT be inserted before onConfirm fires (no child data before consent — the success criterion).
  </action>
  <verify>
    <automated>npx vitest run src/components/members/CoppaConsentSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/members/CoppaConsentSheet.test.tsx` passes (consent gate blocks confirm until checked; member written only after confirm)
    - CoppaConsentSheet renders the body summary and consent checkbox label verbatim from UI-SPEC
    - AddEditMemberSheet handleSave returns early (no upsert) when isUnder13 && !coppaConfirmed
    - On confirm, useUpsertMember is called with `coppa_consented_at` set to an ISO timestamp
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Under-13 child records cannot be written without parental consent; coppa_consented_at stamped on confirm.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PremiumGateSheet + free-tier invite gating via useTier</name>
  <read_first>
    - src/components/OfflineBanner.tsx (state-driven conditional display + role attribute — analog for PremiumGateSheet)
    - src/components/members/MemberCard.tsx (onInvite slot; un-linked card invite affordance from Plans 03/04)
    - src/components/members/InviteSheet.tsx (the sheet to bypass when free-tier)
    - src/routes/members.tsx (hosts the invite vs gate sheet state)
    - src/data/useTier.ts (tier / trialActive — Plan 02)
    - .planning/phases/02-members-onboarding-billing/02-UI-SPEC.md (§Copywriting Premium gate — verbatim badge/title/body/Upgrade/toast/trial counter; §Color "Premium" lock uses --text-muted neutral)
    - .planning/phases/02-members-onboarding-billing/02-RESEARCH.md (D-16/D-17/D-18; Open Q4 Upgrade → toast, no navigation)
    - .planning/phases/02-members-onboarding-billing/02-PATTERNS.md (§PremiumGateSheet)
    - tests/e2e/premium-gate.spec.ts (Plan 01 stub)
  </read_first>
  <behavior>
    - When useTier().tier === 'free', the member-card Invite action renders locked (lock glyph + "Premium" badge) and tapping it opens PremiumGateSheet, NOT InviteSheet.
    - When useTier().tier === 'premium' (incl. active trial), the Invite action is unlocked and opens InviteSheet.
    - PremiumGateSheet Upgrade CTA shows the placeholder toast and does not navigate.
  </behavior>
  <action>
    Create src/components/members/PremiumGateSheet.tsx (+ .module.css) per D-17 + 02-UI-SPEC.md: vaul Drawer (open/close, no snap points) titled "Premium required", body verbatim ("Inviting family members is a Premium feature. Your free trial includes it — and you can upgrade anytime."), primary "Upgrade" CTA that shows the placeholder toast verbatim ("Upgrades arrive at launch — for now, your trial covers this.") with NO navigation (route doesn't exist — Open Q4). Props `open`, `onClose`.
    Edit src/components/members/MemberCard.tsx: render the Invite affordance on un-linked cards with tier awareness — when useTier().tier === 'free', show the lock glyph (inline SVG) + "Premium" badge (--text-muted, neutral) and route onInvite to open PremiumGateSheet; when premium, route onInvite to InviteSheet. Use an inline SVG lock glyph (no icon library).
    Edit src/routes/members.tsx: host both InviteSheet and PremiumGateSheet state; the onInvite handler branches on useTier().tier. Keep the trial counter in the header (Plan 03) consistent with useTier().trialDaysLeft.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/premium-gate.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/e2e/premium-gate.spec.ts` passes (free-tier invite shows PremiumGateSheet; trial-active shows InviteSheet)
    - src/components/members/PremiumGateSheet.tsx renders title/body/Upgrade verbatim; Upgrade shows a toast, no navigation
    - MemberCard branches on `useTier().tier` to choose PremiumGateSheet vs InviteSheet
    - Locked Invite shows a "Premium" badge via --text-muted (no accent color)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Invitations are gated: free-tier → PremiumGateSheet, trial/premium → InviteSheet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: COPPA consent flow + premium gate — legal-assumption verification</name>
  <read_first>
    - .planning/phases/02-members-onboarding-billing/02-RESEARCH.md (Pattern 5 + Assumptions Log A3 — "email-plus" consent sufficiency for a closed family app is the highest-risk assumption; 2026 FTC amendment)
    - .planning/phases/02-members-onboarding-billing/02-UI-SPEC.md (COPPA + Premium gate copy contracts)
  </read_first>
  <what-built>
    The full COPPA consent gate (Under-13 toggle → CoppaConsentSheet → single-checkbox consent → coppa_consented_at timestamp, with NO child row written before consent) and the premium invite gate (free-tier → PremiumGateSheet with placeholder Upgrade toast; trial/premium → InviteSheet). The COPPA legal interpretation (single-checkbox "email-plus" consent for a closed internal-use family app, RESEARCH A3) is the highest-risk assumption in the phase and needs explicit human sign-off.
  </what-built>
  <how-to-verify>
    1. In /members → Add member, check "This child is under 13", fill name, tap "Save member": confirm the CoppaConsentSheet appears and that NO member is added in the list yet.
    2. Confirm without checking the box: confirm "Check the box to confirm consent." blocks it.
    3. Check the consent box, tap "Confirm and add": confirm the child appears and (in Supabase Studio) the row has coppa_consented_at set.
    4. Add a non-under-13 member: confirm it saves immediately with coppa_consented_at null.
    5. Simulate free tier (set trial_ends_at in the past + subscription_status null in Studio): on an un-linked member card the Invite action shows the lock + "Premium" badge and opens PremiumGateSheet; tap Upgrade → confirm the placeholder toast (no navigation).
    6. Restore an active trial: confirm Invite opens the real InviteSheet.
    7. LEGAL SIGN-OFF: confirm the single-checkbox consent copy + coppa_consented_at timestamp is acceptable for the closed-family MVP (RESEARCH A3); defer the 2026 written-security-program / retention obligations to Phase 7 Compliance.
  </how-to-verify>
  <action>
    Finish tests/e2e/premium-gate.spec.ts (driven in Task 2) and run it; then pause for human verification of the COPPA gate behavior and the A3 legal sign-off. This checkpoint is not auto-approvable.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/premium-gate.spec.ts && npx vitest run src/components/members/CoppaConsentSheet.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Both automated specs green
    - Human confirms: child row not written before consent; coppa_consented_at set on confirm; non-under-13 saves with null
    - Human confirms: free-tier → PremiumGateSheet + placeholder toast; trial → InviteSheet
    - Human signs off on the A3 COPPA "email-plus" interpretation for the closed-family MVP (or files a gap for Phase 7)
  </acceptance_criteria>
  <resume-signal>Type "approved" (with legal sign-off) or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent → child member record | Child PII must not be stored before consent (COPPA) |
| client tier check → invite action | Premium gating decided client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Information Disclosure | child data before consent | mitigate | AddEditMemberSheet handleSave returns early for under-13 until coppaConfirmed; useUpsertMember is never called before CoppaConsentSheet confirm; coppa_consented_at stamped at confirm (Task 1) |
| T-02-18 | Tampering | premium gate bypass | accept | The gate is UX; the authoritative billing state (subscription_status) is service_role-only (Plan 01 T-02-01). A user bypassing the client gate can open InviteSheet but the invite-member function and RPCs still enforce parent-of-family authorization. No data boundary is crossed. |
| T-02-19 | Repudiation | COPPA consent record | mitigate | coppa_consented_at timestamp on the members row is the durable consent record (D-14); audit columns (updated_by) capture which parent confirmed |
</threat_model>

<verification>
- `npx vitest run src/components/members/CoppaConsentSheet.test.tsx` green
- `npx playwright test tests/e2e/premium-gate.spec.ts` green
- `npm run typecheck` exits 0
- Full suite `npm test && npm run test:e2e` green before phase verify
</verification>

<success_criteria>
- Under-13 child records gated behind CoppaConsentSheet; coppa_consented_at stamped on consent; no child data before consent
- Free-tier invitations route to PremiumGateSheet; trial/premium route to InviteSheet
- Upgrade CTA is a placeholder toast (no navigation)
- A3 COPPA legal assumption signed off or escalated
</success_criteria>

<output>
Create `.planning/phases/02-members-onboarding-billing/02-05-SUMMARY.md` when done
</output>
