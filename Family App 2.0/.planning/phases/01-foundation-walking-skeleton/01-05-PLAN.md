---
phase: 01-foundation-walking-skeleton
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04b
files_modified:
  - src/lib/trialEnd.ts
  - src/routes/onboarding/create-family.tsx
  - src/routes/onboarding/create-family.module.css
  - src/routes/router.tsx
  - supabase/functions/stripe-create-customer/index.ts
  - supabase/functions/stripe-create-customer/deno.json
  - supabase/functions/stripe-webhook/index.ts
  - supabase/functions/stripe-webhook/deno.json
  - tests/unit/luxon-trial.test.ts
autonomous: false
requirements:
  - ONBD-01
  - ARCH-10
  - ARCH-13
user_setup:
  - service: stripe
    why: "Stripe Customer is created server-side when a family is created (D-05). A test-mode account, secret key, and webhook signing secret are required before this plan can be verified."
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe Dashboard -> Developers -> API keys -> Secret key (test mode)"
      - name: STRIPE_WEBHOOK_SIGNING_SECRET
        source: "Stripe Dashboard -> Developers -> Webhooks -> [your endpoint] -> Signing secret"
      - name: STRIPE_WEBHOOK_FAMILY_SHARED_SECRET
        source: "Self-generated random 32+ character string (e.g. openssl rand -base64 32). Stored in Supabase Vault and used as the Authorization Bearer header on the families INSERT database webhook."
    dashboard_config:
      - task: "Create a Stripe account in test mode (no real card required)"
        location: "https://dashboard.stripe.com/register"
      - task: "Configure Stripe Webhook endpoint pointing at <SUPABASE_URL>/functions/v1/stripe-webhook listening for customer.subscription.updated, customer.subscription.deleted, customer.subscription.trial_will_end"
        location: "https://dashboard.stripe.com/test/webhooks"
      - task: "Set Edge Function secrets: supabase secrets set STRIPE_SECRET_KEY=... STRIPE_WEBHOOK_SIGNING_SECRET=... STRIPE_WEBHOOK_FAMILY_SHARED_SECRET=..."
        location: "Local CLI"
      - task: "Configure Supabase Database Webhook: Dashboard -> Database -> Webhooks -> New hook. Name stripe-create-customer-on-family-insert. Table families. Events INSERT. URL <SUPABASE_URL>/functions/v1/stripe-create-customer. Headers Authorization: Bearer <STRIPE_WEBHOOK_FAMILY_SHARED_SECRET>. Enable retries (3 attempts)."
        location: "Supabase Dashboard -> Database -> Webhooks"

must_haves:
  truths:
    - "Visiting /onboarding/create-family renders a single-screen wizard with a family-name input (placeholder The Mader Family), an emoji picker chip row, helper text, and a primary CTA Create my family"
    - "Submitting the wizard with a name + emoji INSERTs three rows in this order via the user's anon JWT (no service-role bypass): families FIRST, members SECOND (using `members_insert_bootstrap` RLS policy from Plan 02), family_settings THIRD (using `family_settings_insert_bootstrap`, with trial_ends_at 7 days out via Luxon + browser timezone, theme defaulted to OS preference). The three-step wizard completes successfully without RLS chicken-and-egg failure."
    - "After successful submit, the route navigates to /dashboard and RequireFamily passes because useCurrentFamily now returns a row"
    - "Within ~3 seconds of family creation, the Supabase database webhook fires the stripe-create-customer Edge Function which creates a Stripe Customer (idempotent via family-<id>-create-customer key) and UPDATEs families.stripe_customer_id"
    - "Posting a signed customer.subscription.updated event to the stripe-webhook Edge Function (via stripe CLI or dashboard Send test webhook) UPDATEs family_settings.stripe_subscription_status to the event's status"
    - "Posting an unsigned event to the stripe-webhook endpoint returns 400 signature verification failed"
    - "Posting to stripe-create-customer without the shared-secret Authorization header returns 401 unauthorized"
    - "computeTrialEnd(DateTime.now()) returns a DateTime exactly 7 days later in the same zone — DST-safe via Luxon"
  artifacts:
    - path: "src/lib/trialEnd.ts"
      provides: "computeTrialEnd helper isolated for unit testability (ARCH-13)"
      contains: "plus({ days: 7 })"
    - path: "src/routes/onboarding/create-family.tsx"
      provides: "Single-screen family creation wizard satisfying ONBD-01"
      contains: "Create my family"
    - path: "supabase/functions/stripe-create-customer/index.ts"
      provides: "Edge Function creating Stripe Customer on families INSERT webhook (D-05)"
      contains: "idempotencyKey"
    - path: "supabase/functions/stripe-webhook/index.ts"
      provides: "Edge Function verifying Stripe webhook signatures and updating family_settings (D-07)"
      contains: "constructEventAsync"
  key_links:
    - from: "src/routes/onboarding/create-family.tsx"
      to: "supabase.from('families').insert and family_settings.insert and members.insert"
      via: "Three sequential inserts inside a TanStack useMutation"
      pattern: "from\\('families'\\)\\.insert"
    - from: "supabase/functions/stripe-create-customer/index.ts"
      to: "stripe.customers.create"
      via: "Stripe Node SDK invoked from Deno"
      pattern: "stripe\\.customers\\.create"
    - from: "supabase/functions/stripe-webhook/index.ts"
      to: "stripe.webhooks.constructEventAsync"
      via: "signature verification before any state mutation"
      pattern: "constructEventAsync"
---

<objective>
Close the Walking Skeleton end-to-end loop: a new authenticated user lands on the Family Creation Wizard (because RequireFamily redirects), fills in a family name + emoji, submits, and the route transitions to /dashboard. Server-side, the families INSERT triggers a Supabase database webhook that calls the stripe-create-customer Edge Function which creates a Stripe Customer and writes the customer_id back. A separate Edge Function endpoint handles incoming Stripe webhooks (signature-verified) and updates family_settings.stripe_subscription_status.

Purpose: This plan turns the skeleton into a functioning skeleton — without it, the app loops between Login and the empty CreateFamily placeholder. ONBD-01, ARCH-10, and ARCH-13 land here.

Output: A working /onboarding/create-family route, two deployed Edge Functions, and a fully wired DB webhook so the next sign-in by a brand-new family member proceeds: Login -> OAuth -> Allowlist pass -> CreateFamily wizard -> Dashboard -> Stripe Customer created in background.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-walking-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-walking-skeleton/01-PATTERNS.md
@.planning/phases/01-foundation-walking-skeleton/01-UI-SPEC.md
@.planning/phases/01-foundation-walking-skeleton/01-04a-SUMMARY.md
@.planning/phases/01-foundation-walking-skeleton/01-04b-SUMMARY.md
@CLAUDE.md
@src/data/supabase.ts
@src/data/useCurrentFamily.ts
@src/data/types.ts
@src/lib/newId.ts
@src/auth/RequireFamily.tsx
@src/routes/router.tsx

<interfaces>
Established by this plan:

src/lib/trialEnd.ts exports computeTrialEnd(now: DateTime): DateTime returning now.plus({ days: 7 }).

src/routes/onboarding/create-family.tsx default export, mounted by router.tsx at /onboarding/create-family.

supabase/functions/stripe-create-customer/index.ts: HTTP POST handler. Expects Authorization: Bearer <STRIPE_WEBHOOK_FAMILY_SHARED_SECRET>. Body is the Supabase database webhook payload { type: 'INSERT', table: 'families', record: {...} }. Returns 200 with { ok: true, customer_id } on success, 401 on missing/bad auth, 500 on Stripe error.

supabase/functions/stripe-webhook/index.ts: HTTP POST handler. Expects stripe-signature header. Body is the raw Stripe event JSON. Returns 200 on success or accept-and-ignore, 400 on signature failure.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5.1: computeTrialEnd helper + matching unit test (ARCH-13)</name>
  <files>src/lib/trialEnd.ts, tests/unit/luxon-trial.test.ts</files>
  <read_first>.planning/phases/01-foundation-walking-skeleton/01-RESEARCH.md (ARCH-13 row in Phase Requirements table + Date/Time discussion in §State of the Art), .planning/phases/01-foundation-walking-skeleton/01-CONTEXT.md (D-06 trial_ends_at column), CLAUDE.md (Luxon recommendation row)</read_first>
  <behavior>
    - computeTrialEnd(DateTime.fromISO('2026-05-19T12:00:00', { zone: 'America/Chicago' })) returns a DateTime equivalent to DateTime.fromISO('2026-05-26T12:00:00', { zone: 'America/Chicago' })
    - computeTrialEnd preserves the input timezone (returned DateTime.zoneName equals input zoneName)
    - DST-safe: computeTrialEnd around March 14 2026 (spring forward) returns a result 7 calendar days later, NOT 7×24 hours
    - Module exports a default helper-only file; no React, no Supabase imports
  </behavior>
  <action>
src/lib/trialEnd.ts: import DateTime from luxon. Export function computeTrialEnd(now: DateTime): DateTime returning now.plus({ days: 7 }). Add a JSDoc explaining that Luxon's days arithmetic is DST-safe (calendar-day addition, not 7*24h) per ARCH-13. No additional logic — keep this trivial so the unit test is the spec.

tests/unit/luxon-trial.test.ts: replace the Plan 01 RED stub with three GREEN tests:
1. it('returns DateTime exactly 7 calendar days later'): input DateTime.fromISO('2026-05-19T12:00:00', { zone: 'America/Chicago' }); expect output.toISO() to equal DateTime.fromISO('2026-05-26T12:00:00', { zone: 'America/Chicago' }).toISO().
2. it('is DST-safe across spring forward'): input DateTime.fromISO('2026-03-08T12:00:00', { zone: 'America/Chicago' }); expect output.day to equal 15 AND output.hour to equal 12 (NOT 13).
3. it('preserves timezone'): input has zone Europe/Berlin; expect output.zoneName equals 'Europe/Berlin'.
  </action>
  <verify>
    <automated>npm test -- tests/unit/luxon-trial.test.ts && grep -c "plus({ days: 7 })" src/lib/trialEnd.ts | grep -E "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/trialEnd.ts exports a named function computeTrialEnd(now: DateTime): DateTime
    - src/lib/trialEnd.ts contains the literal substring "plus({ days: 7 })"
    - src/lib/trialEnd.ts imports DateTime from luxon
    - tests/unit/luxon-trial.test.ts contains 3 it() blocks
    - npm test -- tests/unit/luxon-trial.test.ts exits 0 with 3 tests passing
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>ARCH-13 (Luxon for all date/time math) has its first usage in the codebase, isolated for unit testability. Plan 05 task 5.2 imports this helper for the wizard submit handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.2: Family Creation Wizard route (ONBD-01)</name>
  <files>src/routes/onboarding/create-family.tsx, src/routes/onboarding/create-family.module.css, src/routes/router.tsx</files>
  <read_first>.planning/phases/01-foundation-walking-skeleton/01-RESEARCH.md (Pattern 10 step 1 — family creation client-side), .planning/phases/01-foundation-walking-skeleton/01-UI-SPEC.md (Copywriting Contract rows for Wizard + Interaction Contracts row for Family Creation Wizard), src/data/supabase.ts, src/data/useCurrentFamily.ts, src/lib/newId.ts, src/lib/trialEnd.ts, src/data/types.ts, src/routes/router.tsx (the CreateFamily Stub placeholder retained by 01-04a/01-04b must be replaced)</read_first>
  <behavior>
    - Visiting /onboarding/create-family while authenticated and without a family renders a card layout mirroring the Login card geometry (max-width 380px, padding 48px 40px, gap 12px, var(--shadow), var(--radius)) per UI-SPEC
    - The card contains: h1 "Name your family" (UI-SPEC Heading 20px), label "What should we call your family?", text input with placeholder "The Mader Family" and value/onChange bound to a useState string, label "Pick a family emoji", an emoji chip row (8 default chips: 🏠 🌳 🌟 🌈 🏡 🦊 🐝 🌻) with one selected at a time, helper text "This is just for you — you can change it later in Settings.", and a primary submit button "Create my family"
    - Submitting calls a TanStack useMutation that runs three INSERTs in this exact order (required by the bootstrap RLS policies added in Plan 02): (1) families with id=newId(), name=input.name, emoji=input.emoji, created_by=user.id — succeeds because `families_insert` policy is `with check (true)`; (2) members with family_id=familyId, auth_user_id=user.id, email=user.email!.toLowerCase(), name from user.user_metadata?.full_name fallback to email local-part, role='parent' — succeeds because `members_insert_bootstrap` policy permits an authenticated user with NO existing member rows to insert their first row; (3) family_settings with family_id=familyId, timezone=Intl.DateTimeFormat().resolvedOptions().timeZone, theme=osDefault(), trial_ends_at=computeTrialEnd(DateTime.now().setZone(browserTz)).toISO() — succeeds because `family_settings_insert_bootstrap` policy permits insert when the user is the family's `created_by`. NO service-role bypass is needed; all three inserts run under the user's anon JWT.
    - On success, the mutation onSuccess calls queryClient.invalidateQueries({ queryKey: ['current-family'] }) and then navigate('/dashboard', { replace: true })
    - On error, the wizard renders an error pill (same style as Login error) with the error message; submit button re-enables for retry
    - The submitting parent's family_id is now in members, so on subsequent visits useCurrentFamily resolves successfully and RequireFamily passes
    - src/routes/router.tsx is updated to import the real CreateFamily component and remove the 01-04a/01-04b Stub placeholder
  </behavior>
  <action>
src/routes/onboarding/create-family.tsx per RESEARCH.md Pattern 10 step 1 and UI-SPEC Copywriting Contract:

Functional default-export CreateFamily. Imports: useState from react, useMutation, useQueryClient from @tanstack/react-query, useNavigate from react-router, DateTime from luxon, supabase from ../../data/supabase, newId from ../../lib/newId, computeTrialEnd from ../../lib/trialEnd.

Inside: const [name, setName] = useState(''); const [emoji, setEmoji] = useState('🏠'); const navigate = useNavigate(); const qc = useQueryClient(). Define the emoji chip array as const EMOJI_CHIPS = ['🏠', '🌳', '🌟', '🌈', '🏡', '🦊', '🐝', '🌻'] as const.

Define a function osDefault inline (or reuse the one from ThemeProvider via a shared export): returns 'midnight' if window.matchMedia('(prefers-color-scheme: dark)').matches else 'lavender'.

Define const createFamily = useMutation. The mutationFn body (in this exact order — required by the bootstrap RLS policies from Plan 02):

1. Resolve user: `const { data: { user } } = await supabase.auth.getUser();` and throw `new Error('Not authenticated')` if user is null.
2. Generate family id and trial: `const familyId = newId(); const browserTz = Intl.DateTimeFormat().resolvedOptions().timeZone; const trialEndsAt = computeTrialEnd(DateTime.now().setZone(browserTz)).toISO();`.
3. INSERT 1: families. `const { error: e1 } = await supabase.from('families').insert({ id: familyId, name: name.trim(), emoji, created_by: user.id }); if (e1) throw e1;`. Succeeds via `families_insert` (with check true).
4. INSERT 2: members (the submitting user as parent). `const fallbackName = user.email!.split('@')[0]!; const { error: e2 } = await supabase.from('members').insert({ family_id: familyId, auth_user_id: user.id, email: user.email!.toLowerCase(), name: (user.user_metadata?.full_name as string | undefined) ?? fallbackName, role: 'parent' }); if (e2) throw e2;`. Succeeds via `members_insert_bootstrap` (the user has no prior member rows).
5. INSERT 3: family_settings. `const { error: e3 } = await supabase.from('family_settings').insert({ family_id: familyId, timezone: browserTz, theme: osDefault(), trial_ends_at: trialEndsAt }); if (e3) throw e3;`. Succeeds via `family_settings_insert_bootstrap` (user is the family's created_by).
6. Return familyId.

onSuccess: `qc.invalidateQueries({ queryKey: ['current-family'] });` then `navigate('/dashboard', { replace: true });`.

Importantly: members MUST be inserted BEFORE family_settings because `private.current_family_id()` reads from the members table; the bootstrap policies remove the chicken-and-egg, but the order documented here is what the source code MUST follow. Reversing steps 4 and 5 would attempt to insert family_settings without a row tying the user to the family, and would silently insert zero rows (RLS check failure surfaces as `error: null, data: null` from PostgREST — see Pitfall about silent RLS failures).

Render: div class styles.wrapper > div class styles.card > h1 styles.title "Name your family" > label styles.label "What should we call your family?" > input styles.input type text value name onChange e=>setName(e.target.value) placeholder "The Mader Family" required minLength 1 maxLength 80 > label styles.label "Pick a family emoji" > div styles.emojiRow with EMOJI_CHIPS mapped to button styles.emojiChip data-active={emoji===chip} onClick to setEmoji(chip) > p styles.helper "This is just for you — you can change it later in Settings." > when createFamily.error render p styles.error with the error.message > button styles.primary type submit disabled={createFamily.isPending || !name.trim()} onClick to createFamily.mutate. The button label is "Create my family" when idle and "Setting up your family…" (UI-SPEC Wizard success transition copy, ellipsis with single character) when isPending.

src/routes/onboarding/create-family.module.css: Port .wrapper and .card layout from login.module.css (same max-width 380px, padding 48px 40px, gap 12px). Add .title (UI-SPEC Heading 20px, font-weight 700, color var(--text)), .label (UI-SPEC Label 14px, font-weight 500, color var(--text-muted)), .input (padding 12px, border 2px solid var(--border), border-radius var(--radius-sm), font-size 16px, min-height 44px; :focus outline var(--lavender)), .emojiRow (display flex flex-wrap wrap gap 8px), .emojiChip (padding 8px 12px, border 2px solid var(--border), border-radius var(--radius-sm), background var(--card-bg), font-size 24px, min-height 44px, cursor pointer; data-active=true: border-color var(--lavender), background var(--lavender-light)), .helper (font-size 13px, color var(--text-muted)), .error (same style as login.module.css .error: pink-light background, pink-dark text, padding 8px 12px, border-radius var(--radius-sm), width 100%), .primary (background var(--lavender), color white, padding 12px 20px, border-radius var(--radius-sm), font-size 15px, font-weight 600, min-height 44px, cursor pointer; :disabled opacity 0.6 cursor not-allowed; :hover:not(:disabled) box-shadow var(--shadow)). All colors via var(--token).

src/routes/router.tsx UPDATE: 01-04a defined a shared `const Stub = () => null` and 01-04b removed it for the layout/tab routes but RETAINED it as the `onboarding/create-family` element. This plan: add `import CreateFamily from './onboarding/create-family';` to the imports block and change the `onboarding/create-family` route's `element: <Stub />` to `element: <CreateFamily />`. If 01-04b's update already removed the `Stub` const entirely, just add the CreateFamily import and use it; if `Stub` is still defined for the onboarding route, the import + element swap is sufficient. Verify the route declaration `{ path: 'onboarding/create-family', element: <CreateFamily />, errorElement: <RouteErrorFallback /> }` is in place and the TODO comment about Plan 05 adding CreateFamily is removed.
  </action>
  <verify>
    <automated>grep -c "Create my family" src/routes/onboarding/create-family.tsx | grep -E "^[1-9]" && grep -c "from\('families'\)\.insert" src/routes/onboarding/create-family.tsx | grep -E "^[1-9]" && grep -c "from\('family_settings'\)\.insert" src/routes/onboarding/create-family.tsx | grep -E "^[1-9]" && grep -c "from\('members'\)\.insert" src/routes/onboarding/create-family.tsx | grep -E "^[1-9]" && grep -c "computeTrialEnd" src/routes/onboarding/create-family.tsx | grep -E "^[1-9]" && grep -c "from './onboarding/create-family'" src/routes/router.tsx | grep -E "^[1-9]" && python3 -c "import re; lines=[ln for ln in open('src/routes/onboarding/create-family.tsx').readlines() if '.insert' in ln]; tables=[re.search(r\"from\\('(\\w+)'\\)\\.insert\", ln).group(1) for ln in lines if re.search(r\"from\\('(\\w+)'\\)\\.insert\", ln)]; print('order:', tables); assert tables == ['families','members','family_settings'], f'Wrong INSERT order: {tables}'" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/onboarding/create-family.tsx contains the literal strings "Name your family", "What should we call your family?", "The Mader Family", "Pick a family emoji", "This is just for you — you can change it later in Settings.", "Create my family"
    - src/routes/onboarding/create-family.tsx contains three .insert calls in source order: families FIRST, then members, then family_settings (verified by grep order: `grep -n "\.insert" src/routes/onboarding/create-family.tsx | head -3` must show families on the first matched line, members on the second, family_settings on the third)
    - src/routes/onboarding/create-family.tsx imports computeTrialEnd from ../../lib/trialEnd
    - src/routes/onboarding/create-family.tsx imports newId from ../../lib/newId
    - src/routes/onboarding/create-family.tsx uses Intl.DateTimeFormat().resolvedOptions().timeZone
    - src/routes/router.tsx imports CreateFamily from './onboarding/create-family' and the placeholder `Stub` reference for the onboarding/create-family route is replaced by the real import
    - src/routes/onboarding/create-family.module.css contains no raw hex colors
    - npm run build exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The Family Creation Wizard is functional. A new authenticated user with no family can create one and lands on /dashboard. The family persists in Supabase under RLS.</done>
</task>

<task type="auto">
  <name>Task 5.3: Stripe Edge Functions (create-customer + webhook)</name>
  <files>supabase/functions/stripe-create-customer/index.ts, supabase/functions/stripe-create-customer/deno.json, supabase/functions/stripe-webhook/index.ts, supabase/functions/stripe-webhook/deno.json</files>
  <read_first>.planning/phases/01-foundation-walking-skeleton/01-RESEARCH.md (Pattern 10 step 3 + Pattern 11 + Pitfall 5), .planning/phases/01-foundation-walking-skeleton/01-CONTEXT.md (D-05 + D-06 + D-07), CLAUDE.md (Stripe row in Recommended Stack), supabase/config.toml (verify_jwt entries from Plan 02)</read_first>
  <behavior>
    - stripe-create-customer Edge Function: deployed via supabase functions deploy stripe-create-customer. Accepts POST. Reads Authorization header and rejects if not equal to "Bearer ${STRIPE_WEBHOOK_FAMILY_SHARED_SECRET}". Parses JSON body and checks type === 'INSERT' && table === 'families'. Reads record.id, record.name. Calls stripe.customers.create({ name, metadata: { family_id } }, { idempotencyKey: `family-${id}-create-customer` }). Updates families.stripe_customer_id via service role client. Returns 200 { ok: true, customer_id }.
    - stripe-webhook Edge Function: deployed via supabase functions deploy stripe-webhook. Accepts POST. Reads stripe-signature header. Calls stripe.webhooks.constructEventAsync(body, sig, STRIPE_WEBHOOK_SIGNING_SECRET). Handles customer.subscription.updated and customer.subscription.deleted via a two-step query: (1) SELECT id FROM families WHERE stripe_customer_id = customerId using the service-role client; (2) if found, UPDATE family_settings SET stripe_subscription_status = sub.status, updated_at = now WHERE family_id = result.id. Logs customer.subscription.trial_will_end. Accept-and-ignore any other event with 200.
    - Both functions use Deno runtime via npm: specifier for stripe (stripe@^22) and jsr:@supabase/supabase-js@2.
  </behavior>
  <action>
supabase/functions/stripe-create-customer/index.ts per RESEARCH.md Pattern 10 step 3 with the version pin correction noted in Assumptions A8 (use stripe@^22 not @^17):

Imports: import Stripe from "npm:stripe@^22"; import { createClient } from "jsr:@supabase/supabase-js@2". Define const stripe = new Stripe(Deno.env.get('STRIPE_SECRET_KEY')!). Define const WEBHOOK_SECRET = Deno.env.get('STRIPE_WEBHOOK_FAMILY_SHARED_SECRET')!.

Deno.serve(async (req) => { /* 1. Verify shared secret */ const auth = req.headers.get('authorization'); if (auth !== `Bearer ${WEBHOOK_SECRET}`) return new Response('unauthorized', { status: 401 }); /* 2. Parse Supabase DB webhook payload */ const payload = await req.json(); if (payload.type !== 'INSERT' || payload.table !== 'families') return new Response('ignored', { status: 200 }); const family = payload.record; /* 3. Create Stripe Customer with idempotency key */ const customer = await stripe.customers.create({ name: family.name, metadata: { family_id: family.id } }, { idempotencyKey: `family-${family.id}-create-customer` }); /* 4. Write back stripe_customer_id via service role client */ const admin = createClient(Deno.env.get('SUPABASE_URL')!, Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!); const { error } = await admin.from('families').update({ stripe_customer_id: customer.id }).eq('id', family.id); if (error) return new Response(error.message, { status: 500 }); return new Response(JSON.stringify({ ok: true, customer_id: customer.id }), { headers: { 'content-type': 'application/json' } }); }).

supabase/functions/stripe-create-customer/deno.json: { "imports": {} } — empty placeholder; npm: and jsr: specifiers are used inline so no deps need to be declared here. Confirm supabase/config.toml [functions.stripe-create-customer] has verify_jwt = false (added in Plan 02).

supabase/functions/stripe-webhook/index.ts per RESEARCH.md Pattern 11 with stripe@^22:

Imports: same as above. Deno.serve(async (req) => { const sig = req.headers.get('stripe-signature'); if (!sig) return new Response('missing signature', { status: 400 }); const body = await req.text(); let event: Stripe.Event; try { event = await stripe.webhooks.constructEventAsync(body, sig, SIGNING_SECRET); } catch (e) { return new Response(`signature verification failed: ${e}`, { status: 400 }); } const admin = createClient(Deno.env.get('SUPABASE_URL')!, Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!); switch (event.type) { case 'customer.subscription.updated': case 'customer.subscription.deleted': { const sub = event.data.object as Stripe.Subscription; const customerId = typeof sub.customer === 'string' ? sub.customer : sub.customer.id; /* Two-step: stripe_customer_id lives on families, not family_settings. */ /* Step 1: Resolve family id via the service-role client. */ const { data: fam } = await admin.from('families').select('id').eq('stripe_customer_id', customerId).single(); /* Step 2: Update family_settings by family_id if the family was found. */ if (fam) { await admin.from('family_settings').update({ stripe_subscription_status: sub.status, updated_at: new Date().toISOString() }).eq('family_id', fam.id); } break; } case 'customer.subscription.trial_will_end': console.log('trial_will_end', event.data.object); break; default: break; /* accept-and-ignore */ } return new Response('ok', { status: 200 }); }).

supabase/functions/stripe-webhook/deno.json: same as create-customer's deno.json.

Run `supabase functions deploy stripe-create-customer` and `supabase functions deploy stripe-webhook` (requires `SUPABASE_ACCESS_TOKEN` from Plan 02). If deploy succeeds, capture the function URLs in the SUMMARY.

DO NOT handle invoice.* or payment_intent.* events — those require a payment method which Phase 1 does not collect. Per RESEARCH.md researcher recommendation.
  </action>
  <verify>
    <automated>test -f supabase/functions/stripe-create-customer/index.ts && test -f supabase/functions/stripe-webhook/index.ts && grep -c "idempotencyKey" supabase/functions/stripe-create-customer/index.ts | grep -E "^[1-9]" && grep -c "constructEventAsync" supabase/functions/stripe-webhook/index.ts | grep -E "^[1-9]" && grep -c "STRIPE_WEBHOOK_FAMILY_SHARED_SECRET" supabase/functions/stripe-create-customer/index.ts | grep -E "^[1-9]" && supabase functions list 2>&1 | grep -c "stripe-create-customer" | grep -E "^[1-9]" && supabase functions list 2>&1 | grep -c "stripe-webhook" | grep -E "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/functions/stripe-create-customer/index.ts contains the literal string `npm:stripe@^22`
    - supabase/functions/stripe-create-customer/index.ts contains `Bearer ${WEBHOOK_SECRET}` (the shared secret check)
    - supabase/functions/stripe-create-customer/index.ts contains `idempotencyKey: \`family-${family.id}-create-customer\``
    - supabase/functions/stripe-create-customer/index.ts contains `payload.type !== 'INSERT'` (the webhook payload validation)
    - supabase/functions/stripe-webhook/index.ts contains constructEventAsync
    - supabase/functions/stripe-webhook/index.ts handles customer.subscription.updated AND customer.subscription.deleted AND customer.subscription.trial_will_end
    - supabase/functions/stripe-webhook/index.ts uses a two-step update: first queries families WHERE stripe_customer_id matches (to obtain family id), then updates family_settings WHERE family_id matches — does NOT filter family_settings directly by stripe_customer_id
    - supabase/functions/stripe-webhook/index.ts does NOT handle invoice.* events
    - `supabase functions list` includes both function names as deployed
  </acceptance_criteria>
  <done>Two Edge Functions are deployed: stripe-create-customer is reachable from the Supabase DB webhook, stripe-webhook is reachable from Stripe. Both reject invalid auth. The Family Creation Wizard's INSERT into families now triggers customer creation in the background.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5.4: End-to-end walking-skeleton smoke test</name>
  <what-built>
    - src/lib/trialEnd.ts (Luxon DST-safe trial computation)
    - /onboarding/create-family wizard
    - Two deployed Edge Functions (stripe-create-customer, stripe-webhook)
    - Database webhook + Stripe webhook fully wired
  </what-built>
  <how-to-verify>
1. Confirm Plan 02 task 2.2 was approved and Plan 03 task 3.4 was approved.
2. Confirm Stripe secrets are set via `supabase secrets list` — STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SIGNING_SECRET, STRIPE_WEBHOOK_FAMILY_SHARED_SECRET all present.
3. Confirm the Supabase database webhook exists: Supabase Dashboard -> Database -> Webhooks -> stripe-create-customer-on-family-insert points at https://<ref>.supabase.co/functions/v1/stripe-create-customer with Authorization: Bearer <shared secret> header. Enable retries (3 attempts).
4. Confirm the Stripe webhook exists: Stripe Dashboard test mode -> Developers -> Webhooks -> endpoint URL https://<ref>.supabase.co/functions/v1/stripe-webhook listening for customer.subscription.updated, customer.subscription.deleted, customer.subscription.trial_will_end.
5. Manual round-trip:
   a. Delete any test family rows you have created in the Supabase Dashboard (so RequireFamily redirects to the wizard).
   b. Run `npm run dev`, visit http://localhost:5173/. The requireAuthLoader runs, sees a session (left over from Plan 03), checks allowlist, passes. RequireFamily resolves to null because there is no member row for this user. The browser is redirected to /onboarding/create-family.
   c. Confirm the wizard renders the UI-SPEC card: title "Name your family", placeholder "The Mader Family", emoji chips, helper text, "Create my family" button.
   d. Type a family name, pick an emoji, click "Create my family". Watch the network panel: three POSTs in this order — /rest/v1/families (201), then /rest/v1/members (201), then /rest/v1/family_settings (201). Browser navigates to /dashboard. The full app shell renders (TopNav with family name, BottomNav with six tabs, Dashboard placeholder content). If any of the three POSTs returns 400/401/403 or completes with an empty body, the bootstrap RLS policies from Plan 02 are missing or misconfigured.
   e. Wait ~3 seconds, then check the Supabase Dashboard -> Database -> Table editor -> families table. The new row's `stripe_customer_id` column should be populated. If null, check Edge Function logs (Supabase Dashboard -> Edge Functions -> stripe-create-customer -> Logs).
   f. Check Stripe Dashboard -> Customers (test mode). A Customer with name matching the family and metadata.family_id matching the row id should appear.
6. Stripe webhook test:
   a. Stripe Dashboard -> Developers -> Webhooks -> stripe-webhook endpoint -> "Send test webhook" -> select customer.subscription.updated. Send it.
   b. Confirm the Edge Function logs show "ok" and family_settings.stripe_subscription_status is updated.
7. Negative tests:
   a. From a terminal: `curl -X POST https://<ref>.supabase.co/functions/v1/stripe-create-customer -H 'Content-Type: application/json' -d '{}'` — expect 401 unauthorized.
   b. `curl -X POST https://<ref>.supabase.co/functions/v1/stripe-webhook -H 'Content-Type: application/json' -d '{}'` — expect 400 missing signature.

Type approved if all checks pass.
  </how-to-verify>
  <files>n/a (verification-only)</files>
  <action>Halt automated execution and wait for the human to perform the end-to-end smoke test in <how-to-verify>. The human MUST verify: secrets set, database webhook configured, Stripe webhook endpoint configured, wizard renders and submits successfully, families.stripe_customer_id populates within ~3 seconds, Stripe Customer appears in Stripe Dashboard, Send test webhook for customer.subscription.updated updates family_settings.stripe_subscription_status, negative tests (no auth header, no signature) return the expected error codes. Then type approved.</action>
  <verify>
    <automated>Wait for human approval. Stripe webhook signature verification cannot be exercised reliably from a unit test; this is a manual smoke gate.</automated>
  </verify>
  <done>Human typed approved. Walking Skeleton core loop verified.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Supabase REST (the 3 INSERTs in the wizard) | RLS gates each row; client-generated UUID. |
| Supabase DB webhook -> stripe-create-customer Edge Function | Shared-secret Bearer token; no Supabase JWT. |
| Stripe -> stripe-webhook Edge Function | Stripe-signed request; HMAC verified. |
| Edge Function -> Stripe API | Server-side STRIPE_SECRET_KEY; never exposed to browser. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Stripe secret key leak via VITE_* | mitigate | STRIPE_SECRET_KEY lives ONLY in Supabase Edge Function secrets (set via supabase secrets set). Never referenced as import.meta.env.VITE_* anywhere in the repo. Verified by grep "VITE_STRIPE" returning nothing. |
| T-05-02 | Spoofing | Unauthorized caller hits stripe-create-customer | mitigate | The function rejects without `Authorization: Bearer <STRIPE_WEBHOOK_FAMILY_SHARED_SECRET>`. The shared secret is stored in Supabase Vault and only the Supabase DB webhook knows it. Returns 401 on missing/bad auth. |
| T-05-03 | Tampering | Stripe webhook replay | mitigate | stripe.webhooks.constructEventAsync enforces signature verification with the configurable replay tolerance window (Stripe default 5 minutes). Returns 400 on signature failure. |
| T-05-04 | Tampering | Spoofed Stripe webhook (no signing) | mitigate | The function reads stripe-signature header and rejects with 400 if missing. constructEventAsync verifies the HMAC against STRIPE_WEBHOOK_SIGNING_SECRET. |
| T-05-05 | Tampering | Duplicate Stripe Customer creation on webhook retry | mitigate | idempotencyKey: family-<id>-create-customer ensures Stripe deduplicates within the 24-hour idempotency window. Safe to retry the webhook. |
| T-05-06 | Information Disclosure | Cross-tenant Stripe writeback | mitigate | The stripe-create-customer function uses the service role key to UPDATE families WHERE id = <family.id> received in the webhook payload. The payload is shared-secret authenticated so only the Supabase webhook can have sourced it. RLS does not apply (service role bypasses). |
| T-05-07 | Denial of Service | Edge Function cold start timeout | accept (mitigated) | RESEARCH.md Pitfall 5 acknowledged. Mitigations: idempotencyKey makes retries safe, webhook retries enabled (3 attempts, exp backoff in Task 5.4 checkpoint). Long-term retry-row enqueue deferred to Phase 2 unless this actually fires. |
| T-05-08 | Tampering | Wizard submits a family name with embedded HTML/script | mitigate | React's default JSX escaping treats the name as text in TopNav and elsewhere. Supabase stores it as text. No eval, no innerHTML anywhere in Phase 1. |
| T-05-SC | Tampering | npm:stripe inside Edge Function | mitigate | Stripe is the official Stripe Node SDK from github.com/stripe/stripe-node, verified via RESEARCH.md §Package Legitimacy Audit. Pinned to ^22 (assumption A8 corrected). Loaded via Deno's npm: specifier — Stripe's SDK is well-supported on Deno. |
</threat_model>

<verification>
1. npm test passes tests/unit/luxon-trial.test.ts.
2. npx tsc --noEmit exits 0.
3. npm run build exits 0.
4. `supabase functions list` shows both functions deployed.
5. End-to-end smoke (Task 5.4): a new family is created via the wizard, Stripe Customer appears in Stripe Dashboard within 3 seconds, families.stripe_customer_id is populated.
6. Negative tests: curl POST without auth -> 401 (create-customer); without signature -> 400 (webhook).
7. Stripe Send test webhook for customer.subscription.updated updates family_settings.stripe_subscription_status.
</verification>

<success_criteria>
- ONBD-01 ships: a parent can create a family space (name + emoji). The three-INSERT wizard (families → members → family_settings) completes via the bootstrap RLS policies from Plan 02 using only the user's anon JWT — no service-role bypass.
- ARCH-10 ships: Stripe Customer is created server-side via the DB webhook -> Edge Function path; webhook endpoint handles trial-related events.
- ARCH-13 ships: Luxon computes trial_ends_at correctly across DST boundaries.
- The walking skeleton is complete: Login -> OAuth -> Allowlist pass -> Wizard -> Dashboard -> shell, with realtime + offline + theme + error boundaries + Stripe customer all active.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-walking-skeleton/01-05-SUMMARY.md` when done.
</output>
