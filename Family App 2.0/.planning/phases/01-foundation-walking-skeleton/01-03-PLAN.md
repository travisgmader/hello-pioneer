---
phase: 01-foundation-walking-skeleton
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/data/supabase.ts
  - src/auth/allowlist.ts
  - src/routes/login.tsx
  - src/routes/login.module.css
  - src/routes/access-denied.tsx
  - src/routes/access-denied.module.css
  - src/components/GoogleIcon.tsx
  - tests/unit/allowlist.test.ts
autonomous: false
requirements:
  - ARCH-02
  - ARCH-05
user_setup:
  - service: google-oauth
    why: "Google OAuth client is required for sign-in. Closed family app, only family Google accounts are in allowed_emails."
    env_vars: []
    dashboard_config:
      - task: "Create OAuth 2.0 Client ID (Web application) in Google Cloud Console"
        location: "https://console.cloud.google.com/apis/credentials -> Create Credentials -> OAuth client ID"
      - task: "Add Authorized JavaScript origins: http://localhost:5173 and the production Vercel URL"
        location: "Google Cloud Console -> OAuth client edit page"
      - task: "Add Authorized redirect URIs: <SUPABASE_URL>/auth/v1/callback (Supabase handles the OAuth dance, NOT your app URL)"
        location: "Google Cloud Console -> OAuth client edit page"
      - task: "Paste Client ID + Client Secret into Supabase Dashboard -> Authentication -> Providers -> Google -> Enable"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
      - task: "Confirm http://localhost:5173/ and Vercel preview URL are in Supabase -> Authentication -> URL Configuration -> Redirect URLs"
        location: "Supabase Dashboard -> Authentication -> URL Configuration"

must_haves:
  truths:
    - "An allowlisted user can click Sign in with Google on /login, complete OAuth, and land back on the app with a valid Supabase session"
    - "A user whose email is NOT in allowed_emails is signed out immediately and shown the Access Denied screen with their email surfaced"
    - "Sign-in helper uses flowType pkce and detectSessionInUrl true, both required for the 2026 PKCE OAuth flow"
    - "isAllowedEmail returns true for travis.g.mader@gmail.com, false for random@example.com, and case-insensitive"
    - "Login card copy matches UI-SPEC verbatim, title Family Plan, subtitle Sign in to access your family's dashboard, CTA Sign in with Google with loading state Redirecting"
    - "Access Denied copy matches UI-SPEC verbatim, heading This email isn't on the family list, body interpolates the email, CTA Sign out"
  artifacts:
    - path: "src/data/supabase.ts"
      provides: "Configured Supabase client with PKCE + detectSessionInUrl + Database type parameterization"
      contains: "flowType: 'pkce'"
    - path: "src/auth/allowlist.ts"
      provides: "isAllowedEmail() DB-driven allowlist check"
      contains: "from('allowed_emails')"
    - path: "src/routes/login.tsx"
      provides: "Google OAuth login screen matching UI-SPEC copy"
      contains: "Sign in with Google"
    - path: "src/routes/access-denied.tsx"
      provides: "Access Denied screen surfacing the rejected email"
      contains: "isn't on the family list"
  key_links:
    - from: "src/routes/login.tsx"
      to: "supabase.auth.signInWithOAuth"
      via: "signInWithGoogle() helper from src/data/supabase.ts"
      pattern: "signInWithGoogle|signInWithOAuth"
    - from: "src/auth/allowlist.ts"
      to: "allowed_emails table"
      via: "supabase.from('allowed_emails').select('email').eq('email', lowered).maybeSingle()"
      pattern: "from\\('allowed_emails'\\)"
---

<objective>
Wire the Supabase client and the OAuth/allowlist authentication slice. After this plan ships, the dev server at http://localhost:5173/login shows a working Google sign-in card. An allowlisted user clicks the button, completes OAuth, and the session lands in the browser; a non-allowlisted user is signed out and shown the Access Denied screen.

Purpose: This is the first user-visible end-to-end capability of the Walking Skeleton, auth works against a real Supabase project against the real allowed_emails table from Plan 02. Plans 04 and 05 build on this to add the post-auth shell and family creation wizard.

Output: A functioning /login route, a functioning /access-denied route, and the underlying isAllowedEmail() utility, all consuming the schema applied in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-walking-skeleton/01-CONTEXT.md
@.planning/phases/01-foundation-walking-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-walking-skeleton/01-PATTERNS.md
@.planning/phases/01-foundation-walking-skeleton/01-UI-SPEC.md
@.planning/phases/01-foundation-walking-skeleton/01-01-SUMMARY.md
@.planning/phases/01-foundation-walking-skeleton/01-02-SUMMARY.md
@CLAUDE.md
@../family-app/src/pages/Login.jsx
@../family-app/src/pages/Login.module.css
@../family-app/src/lib/supabase.js
@src/lib/env.ts
@src/data/types.ts

<interfaces>
Established by this plan, consumed by Plan 04 (router + RequireAuth loader):

src/data/supabase.ts exports a configured SupabaseClient and a signInWithGoogle() helper.
src/auth/allowlist.ts exports isAllowedEmail(email) returning a Promise of boolean.

Routes mounted by Plan 04 in src/routes/router.ts:
- /login -> src/routes/login.tsx default export
- /access-denied -> src/routes/access-denied.tsx default export, reads email query param via useSearchParams
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.1: Configure Supabase client + allowlist utility</name>
  <files>src/data/supabase.ts, src/auth/allowlist.ts, tests/unit/allowlist.test.ts</files>
  <read_first>../family-app/src/lib/supabase.js, .planning/phases/01-foundation-walking-skeleton/01-PATTERNS.md (sections for src/data/supabase.ts and src/auth/allowlist.ts), .planning/phases/01-foundation-walking-skeleton/01-RESEARCH.md (Pattern 3 + Pattern 4 + Pitfall 1 + Pitfall 9), src/lib/env.ts, src/lib/check.ts, src/data/types.ts</read_first>
  <behavior>
    - supabase exported from src/data/supabase.ts is a SupabaseClient instance parameterized with Database, with auth.persistSession true, auth.autoRefreshToken true, auth.detectSessionInUrl true, auth.flowType pkce
    - signInWithGoogle() calls supabase.auth.signInWithOAuth with provider google and options.redirectTo equal to window.location.origin plus trailing slash (trailing slash REQUIRED for PKCE per Pitfall 9)
    - isAllowedEmail returns true for TRAVIS.G.MADER@GMAIL.COM (case insensitive)
    - isAllowedEmail returns false for not-a-real@example.com
    - isAllowedEmail trims surrounding whitespace before query
    - tests/unit/allowlist.test.ts mocks supabase.from('allowed_emails') chain and asserts lowercase plus trim behavior
  </behavior>
  <action>
src/data/supabase.ts per RESEARCH.md Pattern 3 and PATTERNS.md section for src/data/supabase.ts:

Import createClient from @supabase/supabase-js, import Database from ./types, import env from ../lib/env. Export const supabase = createClient parameterized with Database, passing env.supabaseUrl, env.supabaseAnonKey, and an options object with auth properties persistSession true, autoRefreshToken true, detectSessionInUrl true, flowType pkce. Also export async function signInWithGoogle returning Promise<void> that calls supabase.auth.signInWithOAuth with provider google and options.redirectTo equal to window.location.origin concatenated with a trailing slash. Throw on the returned error if present. Re-export check helper from ../lib/check for downstream convenience.

Do NOT use the isConfigured guard pattern from v1, v2 fails fast at module load via src/lib/env.ts if env vars are missing.

src/auth/allowlist.ts per PATTERNS.md section for src/auth/allowlist.ts and RESEARCH.md Pattern 4:

Import supabase from ../data/supabase. Export async function isAllowedEmail(email: string) returning Promise<boolean> that lowercases plus trims the input, calls supabase.from('allowed_emails').select('email').eq('email', lowered).maybeSingle(), throws on error, returns data is not null. Add JSDoc explaining the DB-driven allowlist per CONTEXT.md D-08 and D-09, noting that the allowed_emails SELECT RLS policy restricts each authenticated user to their own row only, so maybeSingle returns the row if present or null if absent.

tests/unit/allowlist.test.ts, turn the existing RED stub GREEN (if it exists from Plan 01, otherwise create fresh). Use vi.mock to mock ../../src/data/supabase exposing supabase.from as vi.fn returning a chainable thenable. Three tests:
1. isAllowedEmail('travis.g.mader@gmail.com') calls eq with email travis.g.mader@gmail.com and returns true when mock returns data with email key.
2. isAllowedEmail('UPPER@CASE.COM') calls eq with email upper@case.com (lowercased before query).
3. isAllowedEmail('  trim@me.com  ') calls eq with email trim@me.com (trimmed before query).

Do NOT add UI components in this task, Tasks 3.2 and 3.3 own that.
  </action>
  <verify>
    <automated>npm test -- tests/unit/allowlist.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/data/supabase.ts contains the literal string flowType: 'pkce'
    - src/data/supabase.ts contains detectSessionInUrl: true
    - src/data/supabase.ts contains window.location.origin
    - src/data/supabase.ts contains createClient&lt;Database&gt;
    - src/auth/allowlist.ts contains from('allowed_emails')
    - src/auth/allowlist.ts contains .toLowerCase() and .trim()
    - npm test -- tests/unit/allowlist.test.ts exits 0 with 3 tests passing
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The Supabase client is configured for PKCE OAuth and the allowlist utility queries the DB case-insensitively. Routes can now consume signInWithGoogle and isAllowedEmail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.2: Build /login route with Google OAuth card matching UI-SPEC</name>
  <files>src/routes/login.tsx, src/routes/login.module.css, src/components/GoogleIcon.tsx</files>
  <read_first>../family-app/src/pages/Login.jsx, ../family-app/src/pages/Login.module.css, .planning/phases/01-foundation-walking-skeleton/01-PATTERNS.md (sections for src/routes/login.tsx and src/routes/login.module.css), .planning/phases/01-foundation-walking-skeleton/01-UI-SPEC.md (Copywriting Contract + Interaction Contracts), src/data/supabase.ts</read_first>
  <behavior>
    - Visiting /login renders a centered Login card with title "Family Plan", subtitle "Sign in to access your family's dashboard", and a "Sign in with Google" button containing the multi-color Google logo SVG to the left of the label
    - Clicking the button sets loading state, swaps label to "Redirecting…", disables the button (opacity 0.6, cursor not-allowed), and calls signInWithGoogle()
    - If signInWithGoogle throws, an error pill renders below the subtitle with pink-light background and pink-dark text, showing the error.message; clicking the button again clears the error and retries
    - The card geometry matches v1 Login.module.css: 380px max-width, 48px vertical 40px horizontal padding, 12px gap between elements, var(--shadow), var(--radius)
    - googleBtn has min-height 44px for WCAG 2.5.5 touch target
  </behavior>
  <action>
src/routes/login.tsx per PATTERNS.md section for src/routes/login.tsx:

Functional component "Login" as the default export. Use useState&lt;boolean&gt;(false) for loading and useState&lt;string | null&gt;(null) for error. Define handleGoogle as an async arrow function that sets loading true, clears error, awaits signInWithGoogle from ../data/supabase, catches errors by setting error.message and clearing loading. Note that on success the OAuth redirect navigates the browser, so loading does not need explicit clearing on success.

Render a div with class styles.wrapper containing a div with class styles.card. Inside the card: an h1 with class styles.title containing the string Family Plan (UI-SPEC Display 28px). A p with class styles.subtitle containing the string Sign in to access your family's dashboard (UI-SPEC Label 14px). When error is truthy, render a p with class styles.error containing the error message. A button with class styles.googleBtn, type button, onClick handleGoogle, disabled bound to loading, aria-busy bound to loading. The button content: when loading render the text Redirecting (followed by a horizontal ellipsis), otherwise render the GoogleIcon component followed by the text Sign in with Google.

src/components/GoogleIcon.tsx, port the SVG verbatim from v1 Login.jsx lines 39 to 48. The SVG must have viewBox 0 0 24 24, width and height 18, and contain four path elements with the canonical fill colors hex #FFC107, hex #FF3D00, hex #4CAF50, hex #1976D2 (the Google logo color triad plus blue). Export as default function GoogleIcon returning a JSX SVG element with aria-hidden true.

src/routes/login.module.css, port verbatim from ../family-app/src/pages/Login.module.css. Add min-height 44px to the .googleBtn rule (WCAG 2.5.5). All hex colors must be expressed via var(--token), no raw hex values in .module.css. Verify the file contains class selectors .wrapper, .card, .title, .subtitle, .error, .googleBtn (and the :hover and :disabled variants per PATTERNS.md).

Per UI-SPEC Voice and Tone: no exclamation points, no emoji in body text, verb-first CTA. Per UI-SPEC Color Accent rules: the Google button hover uses border-color var(--lavender) and var(--shadow), not solid lavender fill.
  </action>
  <verify>
    <automated>npm test -- tests/unit/router.test.tsx --run 2>&1 | head -20 ; grep -c "Sign in with Google" src/routes/login.tsx | grep -E "^[1-9]"</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/login.tsx contains the literal string Family Plan
    - src/routes/login.tsx contains the literal string Sign in to access your family's dashboard
    - src/routes/login.tsx contains the literal string Sign in with Google
    - src/routes/login.tsx contains the literal string Redirecting
    - src/routes/login.tsx imports signInWithGoogle from ../data/supabase
    - src/routes/login.module.css contains max-width: 380px
    - src/routes/login.module.css contains padding: 48px 40px
    - src/routes/login.module.css contains min-height: 44px (WCAG touch target)
    - src/routes/login.module.css does NOT contain any raw hex color (verify with grep -E "#[0-9a-fA-F]{3,6}" src/routes/login.module.css returns nothing)
    - src/components/GoogleIcon.tsx contains the four fill colors #FFC107, #FF3D00, #4CAF50, #1976D2
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The Login route renders the UI-SPEC card and triggers Google OAuth. The route is registered by Plan 04's router.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3.3: Build /access-denied route surfacing the rejected email</name>
  <files>src/routes/access-denied.tsx, src/routes/access-denied.module.css</files>
  <read_first>../family-app/src/App.jsx (lines 38 to 51, the inline access-denied block), .planning/phases/01-foundation-walking-skeleton/01-PATTERNS.md (section for src/routes/access-denied.tsx), .planning/phases/01-foundation-walking-skeleton/01-UI-SPEC.md (Copywriting Contract), src/routes/login.module.css</read_first>
  <behavior>
    - Visiting /access-denied?email=foo@example.com renders a card with the heading "This email isn't on the family list", a body paragraph interpolating foo@example.com inside a strong tag, and a Sign out button
    - Visiting /access-denied with no email param falls back to the placeholder "your account" in the body
    - Clicking Sign out calls supabase.auth.signOut(); afterwards a manual visit to /login should show no session
  </behavior>
  <action>
src/routes/access-denied.tsx per PATTERNS.md section for src/routes/access-denied.tsx:

Default export AccessDenied. Use useSearchParams from react-router. Read email from params.get('email') with fallback string "your account". Render div class styles.wrapper containing div class styles.card. Inside the card: h1 class styles.title with the literal string "This email isn't on the family list" (UI-SPEC Heading 20px). p class styles.body containing JSX: strong tag wrapping email, then the rest of the sentence: " doesn't have access to this Family Plan. Ask a parent to add you, then sign in again." (UI-SPEC Label 14px). button class styles.signOutBtn type button onClick to an inline arrow calling supabase.auth.signOut(). Import supabase from ../data/supabase.

Per UI-SPEC: heading uses pink-dark destructive color, body uses text default. The card geometry mirrors Login card (same max-width 380px, same padding 48px 40px, same gap 12px, same shadow and radius).

src/routes/access-denied.module.css, reuse .wrapper and .card from Login.module.css structurally (same layout). Add .title rule with color var(--pink-dark) and font-size 20px and font-weight 700. Add .body rule with font-size 14px line-height 1.5 and color var(--text). Add .signOutBtn rule with min-height 44px, border 2px solid var(--border), background var(--card-bg), border-radius var(--radius-sm), padding 12px 20px. Hover variant: border-color var(--pink), background var(--pink-light), color var(--pink-dark). All colors via var(--token), no raw hex.
  </action>
  <verify>
    <automated>grep -c "This email isn't on the family list" src/routes/access-denied.tsx | grep -E "^[1-9]" ; grep -c "useSearchParams" src/routes/access-denied.tsx | grep -E "^[1-9]" ; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/routes/access-denied.tsx contains the literal string "This email isn't on the family list"
    - src/routes/access-denied.tsx contains the literal substring "doesn't have access to this Family Plan"
    - src/routes/access-denied.tsx contains the literal string "Sign out"
    - src/routes/access-denied.tsx imports useSearchParams from react-router (NOT react-router-dom)
    - src/routes/access-denied.tsx imports supabase from ../data/supabase
    - src/routes/access-denied.module.css does NOT contain any raw hex color (grep -E returns nothing)
    - src/routes/access-denied.module.css contains var(--pink-dark) for the heading destructive color
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The Access Denied route renders the UI-SPEC card surfacing the rejected email and offers a sign-out button. The route is registered by Plan 04's router.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3.4: Manual OAuth round-trip smoke test</name>
  <what-built>
    - src/data/supabase.ts configured for PKCE OAuth
    - src/auth/allowlist.ts queries allowed_emails table case-insensitively
    - /login route with Google OAuth button matching UI-SPEC
    - /access-denied route surfacing the rejected email
  </what-built>
  <how-to-verify>
1. Confirm Plan 02 task 2.2 was approved (Google OAuth provider enabled in Supabase, .env.local populated).
2. Run npm run dev. Open http://localhost:5173/login in a browser.
3. Confirm the login card renders: title "Family Plan", subtitle "Sign in to access your family's dashboard", a "Sign in with Google" button with the Google logo.
4. Confirm the card layout matches UI-SPEC: centered, 380px wide, var(--shadow) drop shadow, var(--radius) corners. Confirm the page background uses var(--bg) (Lavender or Midnight depending on your OS color scheme).
5. Click "Sign in with Google". Confirm the button briefly shows "Redirecting" before the OAuth redirect.
6. Sign in with travis.g.mader@gmail.com (allowlisted). After redirect back, you will land on a 404 or blank route (Plan 04 wires the router, not yet built). Open DevTools -> Application -> Local Storage and confirm a key like sb-<ref>-auth-token contains an access_token. The session persists.
7. Sign out via DevTools console: supabase.auth.signOut() (if supabase is exposed) or clear localStorage.
8. Manually navigate to http://localhost:5173/access-denied?email=test@example.com. Confirm the heading reads "This email isn't on the family list" and the body interpolates the email inside a bold strong tag, with the "Ask a parent" copy following.
9. Sign in with a non-allowlisted Google account if you have one handy (optional, Plan 04 actually wires the allowlist gate post-OAuth, so this check is deferred unless you test isAllowedEmail() in the console directly).
10. From DevTools console run: const { isAllowedEmail } = await import('/src/auth/allowlist.ts'); await isAllowedEmail('travis.g.mader@gmail.com'). Expect true. Then await isAllowedEmail('random@noreply.com'). Expect false.

If all checks pass, type approved. If the OAuth redirect URI is wrong (Supabase shows an error about redirect URLs), fix the Supabase Dashboard URL Configuration before approving.
  </how-to-verify>
  <files>n/a (verification-only)</files>
  <action>Halt automated execution and wait for the human to perform the manual OAuth round-trip verification in <how-to-verify>. The human MUST complete: visit /login locally, confirm UI-SPEC card renders, sign in with travis.g.mader@gmail.com, confirm session lands in localStorage, manually navigate /access-denied?email=test@example.com to verify the Access Denied screen, console-test isAllowedEmail for both true and false cases. Then type approved.</action>
  <verify>
    <automated>Wait for human approval. The OAuth dance requires a real browser session that no automated test can perform headlessly without stored credentials.</automated>
  </verify>
  <done>Human typed approved. Auth slice verified end-to-end.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Supabase Auth (Google OAuth) | OAuth callback returns an authorization code; PKCE prevents code interception attacks. |
| browser -> allowed_emails table | Client queries the table directly through PostgREST under RLS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | OAuth code interception | mitigate | flowType pkce in src/data/supabase.ts requires the code_verifier to be present on the same client that initiated the flow. Verified by RESEARCH.md Pitfall 9 and the Supabase Auth Google OAuth docs. |
| T-03-02 | Tampering | Open redirect via redirectTo | mitigate | signInWithGoogle hardcodes window.location.origin + trailing slash. Supabase rejects redirectTo values not in the project's allowed redirect URLs list (configured via human checkpoint Task 2.2). |
| T-03-03 | Information Disclosure | Allowlist enumeration | mitigate | RLS SELECT policy on allowed_emails restricts each user to lower(email) = lower(jwt -> email). Even an authenticated attacker can only see their own row, never the whole list. |
| T-03-04 | Tampering | Session lifetime / token theft via XSS | accept (mitigated by stack) | Supabase persistSession writes to localStorage. Phase 1 has zero user-generated HTML rendering, so XSS surface is zero. Phase 5 (Notes) will be the first time we render user-supplied content and must use React's default escaping. |
| T-03-05 | Spoofing | Allowlist case mismatch (TRAVIS@... vs travis@...) | mitigate | isAllowedEmail lowercases + trims before querying. The allowed_emails bootstrap rows are inserted lowercased. The RLS SELECT policy also lowercases both sides. Triple-defense. |
| T-03-06 | Denial of Service | OAuth retry loop on transient Supabase error | accept | The Login button disables during loading and re-enables on error. A user can manually retry. No client-side rate limiting in Phase 1; Supabase backend rate-limits OAuth attempts. |
| T-03-SC | Tampering | npm packages added in this plan | mitigate | This plan adds no new packages. Plan 01 already cleared the package legitimacy checkpoint. |
</threat_model>

<verification>
1. npm test -- tests/unit/allowlist.test.ts passes 3 tests.
2. npx tsc --noEmit exits 0.
3. npm run dev serves /login and renders the UI-SPEC card.
4. Manual OAuth round-trip with travis.g.mader@gmail.com produces a Supabase session in localStorage.
5. /access-denied?email=foo@bar.com interpolates the email into the body.
6. Human checkpoint Task 3.4 approved.
</verification>

<success_criteria>
- An allowlisted user can sign in with Google end-to-end (OAuth completes, session lands).
- A user can be shown the Access Denied screen with their email surfaced.
- The allowlist check is DB-driven (no hardcoded emails in app code), case-insensitive, and trimmed.
- src/data/supabase.ts is the single point of Supabase client configuration with PKCE + Database type.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-walking-skeleton/01-03-SUMMARY.md` when done.
</output>
