/**
 * Chores placeholder — Phase 3 will replace with the chore management system.
 */
import styles from './placeholder.module.css';

export default function Chores() {
  return (
    <section className={styles.placeholder}>
      <h1 className={styles.title}>Chores</h1>
      <p className={styles.body}>Coming soon — this section ships in Phase 3.</p>
    </section>
  );
}
