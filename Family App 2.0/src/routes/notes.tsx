/**
 * Notes placeholder — Phase 5 will replace with the family notes section.
 */
import styles from './placeholder.module.css';

export default function Notes() {
  return (
    <section className={styles.placeholder}>
      <h1 className={styles.title}>Notes</h1>
      <p className={styles.body}>Coming soon — this section ships in Phase 5.</p>
    </section>
  );
}
